/* ============ units3d.js — 3D model pack (generated by the models-3d workflow) ============ */
var UNIT_MODELS = {}; var BLD_MODELS = {};

/* ---- pack a77db5d6 ---- */
UNIT_MODELS["helo_n"] = {
  len: 14.7,
  build: function (THREE, M, C) {
    var g = new THREE.Group();

    function paintTex(base, light) {
      var cv = document.createElement("canvas");
      cv.width = 512; cv.height = 512;
      var ctx = cv.getContext("2d");
      ctx.fillStyle = base; ctx.fillRect(0, 0, 512, 512);
      var i, p;
      for (i = 0; i < 26; i++) {
        ctx.globalAlpha = 0.04 + Math.random() * 0.045;
        ctx.fillStyle = (i % 3 === 0) ? light : "#000000";
        ctx.fillRect(Math.random() * 512, Math.random() * 512, 45 + Math.random() * 130, 30 + Math.random() * 95);
      }
      ctx.globalAlpha = 1;
      ctx.strokeStyle = "rgba(0,0,0,0.25)"; ctx.lineWidth = 2;
      for (i = 0; i < 10; i++) { p = 16 + Math.random() * 480; ctx.beginPath(); ctx.moveTo(p, 0); ctx.lineTo(p, 512); ctx.stroke(); }
      for (i = 0; i < 6; i++) { p = 16 + Math.random() * 480; ctx.beginPath(); ctx.moveTo(0, p); ctx.lineTo(512, p); ctx.stroke(); }
      ctx.fillStyle = "rgba(0,0,0,0.12)";
      for (i = 0; i < 14; i++) { ctx.fillRect(Math.random() * 512, Math.random() * 512, 2 + Math.random() * 3, 18 + Math.random() * 55); }
      var t = new THREE.CanvasTexture(cv);
      t.wrapS = THREE.RepeatWrapping; t.wrapT = THREE.RepeatWrapping;
      return t;
    }

    var bodyMat = new THREE.MeshStandardMaterial({ map: paintTex("#3e4437", "#79826a"), metalness: 0.25, roughness: 0.62 });
    var darkMat = new THREE.MeshStandardMaterial({ color: 0x1f2320, metalness: 0.35, roughness: 0.6 });
    var glassMat = new THREE.MeshPhysicalMaterial({ color: 0x16222b, metalness: 0.25, roughness: 0.16 });
    var teamMat = new THREE.MeshStandardMaterial({ color: C.team, metalness: 0.2, roughness: 0.6 });

    // ---- narrow tandem fuselage ----
    var fus = new THREE.Mesh(M.loft(THREE, [
      { x: -7.0, w: 0.16, h: 0.22, zc: 0.55 },
      { x: -5.2, w: 0.26, h: 0.32, zc: 0.42 },
      { x: -3.0, w: 0.42, h: 0.50, zc: 0.18 },
      { x: -1.2, w: 0.55, h: 0.72, zc: 0.05, sq: 0.8 },
      { x: 0.8, w: 0.55, h: 0.75, zc: 0.05, sq: 0.8 },
      { x: 2.0, w: 0.48, h: 0.62, zc: -0.02 },
      { x: 3.4, w: 0.44, h: 0.52, zc: -0.10 },
      { x: 4.8, w: 0.36, h: 0.40, zc: -0.22 },
      { x: 5.9, w: 0.14, h: 0.16, zc: -0.35 }
    ], 20), bodyMat);
    g.add(fus);

    // ---- TADS / PNVS nose sensor turret ----
    var tads = new THREE.Mesh(new THREE.CylinderGeometry(0.30, 0.30, 0.46, 14), bodyMat);
    tads.rotation.x = Math.PI / 2;
    tads.position.set(5.85, 0, -0.38);
    g.add(tads);
    var eye1 = new THREE.Mesh(new THREE.BoxGeometry(0.1, 0.22, 0.2), glassMat);
    eye1.position.set(6.12, 0.12, -0.38); g.add(eye1);
    var eye2 = new THREE.Mesh(new THREE.BoxGeometry(0.1, 0.16, 0.14), glassMat);
    eye2.position.set(6.12, -0.16, -0.32); g.add(eye2);
    var pnvs = new THREE.Mesh(new THREE.SphereGeometry(0.16, 10, 8), darkMat);
    pnvs.position.set(6.05, 0, -0.05); g.add(pnvs);

    // ---- stepped flat-plate canopies (gunner low fwd, pilot raised aft) ----
    var wsF = new THREE.Mesh(new THREE.BoxGeometry(0.85, 0.68, 0.04), glassMat);
    wsF.rotation.y = 0.85; wsF.position.set(4.0, 0, 0.42); g.add(wsF);
    var roofF = new THREE.Mesh(new THREE.BoxGeometry(1.15, 0.72, 0.04), glassMat);
    roofF.position.set(3.05, 0, 0.68); g.add(roofF);
    var wsR = new THREE.Mesh(new THREE.BoxGeometry(0.8, 0.72, 0.04), glassMat);
    wsR.rotation.y = 0.85; wsR.position.set(2.45, 0, 0.78); g.add(wsR);
    var roofR = new THREE.Mesh(new THREE.BoxGeometry(1.2, 0.76, 0.04), glassMat);
    roofR.position.set(1.5, 0, 1.02); g.add(roofR);
    var s;
    for (s = -1; s <= 1; s += 2) {
      var sideF = new THREE.Mesh(new THREE.BoxGeometry(1.5, 0.03, 0.5), glassMat);
      sideF.rotation.x = -s * 0.16;
      sideF.position.set(3.15, s * 0.43, 0.38); g.add(sideF);
      var sideR = new THREE.Mesh(new THREE.BoxGeometry(1.35, 0.03, 0.55), glassMat);
      sideR.rotation.x = -s * 0.16;
      sideR.position.set(1.55, s * 0.47, 0.7); g.add(sideR);
    }
    // spine fairing behind rear cockpit
    var spine = new THREE.Mesh(new THREE.BoxGeometry(1.1, 0.66, 0.34), bodyMat);
    spine.position.set(0.35, 0, 0.92); g.add(spine);

    // ---- M230 30mm chin gun ----
    var gunMnt = new THREE.Mesh(new THREE.BoxGeometry(0.5, 0.3, 0.35), darkMat);
    gunMnt.position.set(4.35, 0, -0.78); g.add(gunMnt);
    var cradle = new THREE.Mesh(new THREE.BoxGeometry(0.9, 0.16, 0.16), darkMat);
    cradle.position.set(4.8, 0, -0.92); g.add(cradle);
    var barrelGeo = new THREE.CylinderGeometry(0.045, 0.045, 1.55, 8);
    barrelGeo.rotateZ(Math.PI / 2);
    var barrel = new THREE.Mesh(barrelGeo, darkMat);
    barrel.position.set(5.8, 0, -0.92); g.add(barrel);
    var ammo = new THREE.Mesh(new THREE.BoxGeometry(1.1, 0.7, 0.4), bodyMat);
    ammo.position.set(3.2, 0, -0.55); g.add(ammo);

    // ---- engine nacelles ----
    var nacGeo = new THREE.CylinderGeometry(0.42, 0.42, 2.5, 14);
    nacGeo.rotateZ(Math.PI / 2);
    var capGeo = new THREE.SphereGeometry(0.42, 12, 8);
    var exhGeo = new THREE.CylinderGeometry(0.30, 0.24, 0.5, 10);
    exhGeo.rotateZ(Math.PI / 2);
    for (s = -1; s <= 1; s += 2) {
      var nac = new THREE.Mesh(nacGeo, bodyMat);
      nac.position.set(0.5, s * 0.88, 0.55); g.add(nac);
      var cap = new THREE.Mesh(capGeo, bodyMat);
      cap.scale.set(0.6, 1, 1);
      cap.position.set(1.75, s * 0.88, 0.55); g.add(cap);
      var exh = new THREE.Mesh(exhGeo, darkMat);
      exh.rotation.z = -0.25;
      exh.position.set(-0.95, s * 0.88, 0.62); g.add(exh);
    }

    // ---- stub wings, pylons, 4-round Hellfire racks ----
    var wing = new THREE.Mesh(M.slab(THREE, [
      [0.65, 0.25], [0.28, 2.3], [-0.32, 2.3], [-0.55, 0.25],
      [-0.55, -0.25], [-0.32, -2.3], [0.28, -2.3], [0.65, -0.25]
    ], 0.14), bodyMat);
    wing.position.set(0.45, 0, 0.18);
    g.add(wing);

    function hellfireRack(px, py, pz) {
      var rk = new THREE.Group();
      var pyl = new THREE.Mesh(new THREE.BoxGeometry(0.55, 0.12, 0.42), bodyMat);
      pyl.position.set(0, 0, 0.34); rk.add(pyl);
      var beam = new THREE.Mesh(new THREE.BoxGeometry(1.05, 0.4, 0.1), darkMat);
      rk.add(beam);
      var mg = new THREE.CylinderGeometry(0.09, 0.09, 1.45, 8);
      mg.rotateZ(Math.PI / 2);
      var ng = new THREE.ConeGeometry(0.09, 0.26, 8);
      ng.rotateZ(-Math.PI / 2);
      for (var iy = -1; iy <= 1; iy += 2) {
        for (var iz = 0; iz <= 1; iz++) {
          var m = new THREE.Mesh(mg, darkMat);
          m.position.set(0.1, iy * 0.17, -0.14 - iz * 0.26);
          rk.add(m);
          var n = new THREE.Mesh(ng, darkMat);
          n.position.set(0.96, iy * 0.17, -0.14 - iz * 0.26);
          rk.add(n);
        }
      }
      rk.position.set(px, py, pz);
      return rk;
    }
    for (s = -1; s <= 1; s += 2) {
      g.add(hellfireRack(0.45, s * 1.25, -0.32));
      g.add(hellfireRack(0.35, s * 2.0, -0.32));
    }

    // ---- main rotor (4 blades) + mast + Longbow radome ----
    var mast = new THREE.Mesh(new THREE.CylinderGeometry(0.13, 0.17, 0.6, 10), darkMat);
    mast.rotation.x = Math.PI / 2;
    mast.position.set(0.8, 0, 1.1); g.add(mast);
    var tiltGrp = new THREE.Group();
    tiltGrp.position.set(0.8, 0, 1.4);
    tiltGrp.rotation.y = 0.05;
    var rotor = new THREE.Group();
    rotor.name = "rotor";
    var hub = new THREE.Mesh(new THREE.CylinderGeometry(0.24, 0.2, 0.3, 10), darkMat);
    hub.rotation.x = Math.PI / 2;
    rotor.add(hub);
    var bladeGeo = new THREE.BoxGeometry(7.3 - 0.35, 0.53, 0.07);
    bladeGeo.rotateY(0.02);
    bladeGeo.translate(0.35 + (7.3 - 0.35) / 2, 0, 0);
    for (var b = 0; b < 4; b++) {
      var bl = new THREE.Mesh(bladeGeo, darkMat);
      bl.rotation.z = b * Math.PI / 2;
      rotor.add(bl);
    }
    tiltGrp.add(rotor);
    g.add(tiltGrp);
    var radomeStem = new THREE.Mesh(new THREE.CylinderGeometry(0.10, 0.10, 0.95, 8), darkMat);
    radomeStem.rotation.x = Math.PI / 2;
    radomeStem.position.set(0.8, 0, 1.90); g.add(radomeStem);
    var radome = new THREE.Mesh(new THREE.CylinderGeometry(0.62, 0.66, 0.52, 20), bodyMat);
    radome.rotation.x = Math.PI / 2;
    radome.position.set(0.8, 0, 2.62); g.add(radome);

    // ---- tail: fin, stabilator, LEFT-side scissored tail rotor ----
    var fin = new THREE.Mesh(M.slab(THREE, [
      [-5.9, 0.6], [-6.6, 2.15], [-7.25, 2.15], [-7.2, 0.5]
    ], 0.16, "xz"), bodyMat);
    g.add(fin);
    var stab = new THREE.Mesh(M.slab(THREE, [
      [0.3, 0], [0.22, 1.7], [-0.3, 1.7], [-0.34, 0], [-0.3, -1.7], [0.22, -1.7]
    ], 0.1), bodyMat);
    stab.position.set(-6.85, 0, 0.5);
    g.add(stab);
    var trGear = new THREE.Mesh(new THREE.BoxGeometry(0.5, 0.3, 0.4), bodyMat);
    trGear.position.set(-6.85, 0.18, 1.55); g.add(trGear);
    var tailrotor = new THREE.Group();
    tailrotor.name = "tailrotor";
    tailrotor.position.set(-6.85, 0.42, 1.55);
    var trHub = new THREE.Mesh(new THREE.CylinderGeometry(0.12, 0.12, 0.24, 10), darkMat);
    tailrotor.add(trHub);
    var trBladeGeo = new THREE.BoxGeometry(1.4 - 0.1, 0.05, 0.24);
    trBladeGeo.translate(0.1 + (1.4 - 0.1) / 2, 0, 0);
    var trAngles = [0, 0.96, Math.PI, Math.PI + 0.96];
    for (b = 0; b < 4; b++) {
      var tb = new THREE.Mesh(trBladeGeo, darkMat);
      tb.rotation.y = trAngles[b];
      tailrotor.add(tb);
    }
    g.add(tailrotor);

    // ---- fixed gear: sponson legs, wheels, tailwheel ----
    var strutGeo = new THREE.CylinderGeometry(0.05, 0.06, 0.85, 8);
    strutGeo.rotateX(Math.PI / 2);
    for (s = -1; s <= 1; s += 2) {
      var strut = new THREE.Mesh(strutGeo, darkMat);
      strut.rotation.x = -s * 0.35;
      strut.position.set(3.35, s * 0.85, -0.95); g.add(strut);
      var wheel = new THREE.Mesh(new THREE.CylinderGeometry(0.32, 0.32, 0.2, 14), darkMat);
      wheel.position.set(3.35, s * 1.02, -1.32); g.add(wheel);
    }
    var twStrut = new THREE.Mesh(new THREE.CylinderGeometry(0.04, 0.04, 0.5, 8), darkMat);
    twStrut.rotation.x = Math.PI / 2;
    twStrut.position.set(-5.6, 0, -0.42); g.add(twStrut);
    var twheel = new THREE.Mesh(new THREE.CylinderGeometry(0.17, 0.17, 0.14, 12), darkMat);
    twheel.position.set(-5.6, 0, -0.72); g.add(twheel);

    // ---- small details: antennas, wire cutter, pitot, team band ----
    var antGeo = new THREE.CylinderGeometry(0.012, 0.012, 0.45, 6);
    antGeo.rotateX(Math.PI / 2);
    var ant1 = new THREE.Mesh(antGeo, darkMat);
    ant1.position.set(-1.8, 0, 1.05); g.add(ant1);
    var ant2 = new THREE.Mesh(antGeo, darkMat);
    ant2.rotation.y = 0.4;
    ant2.position.set(-4.3, 0, 0.9); g.add(ant2);
    var cutter = new THREE.Mesh(new THREE.BoxGeometry(0.35, 0.03, 0.4), darkMat);
    cutter.position.set(2.6, 0, 0.95); g.add(cutter);
    var pitotGeo = new THREE.CylinderGeometry(0.015, 0.015, 0.6, 6);
    pitotGeo.rotateZ(Math.PI / 2);
    var pitot = new THREE.Mesh(pitotGeo, darkMat);
    pitot.position.set(6.2, 0.25, 0.05); g.add(pitot);
    var band = new THREE.Mesh(new THREE.BoxGeometry(0.45, 0.64, 0.78), teamMat);
    band.position.set(-4.6, 0, 0.36); g.add(band);

    return g;
  }
};

UNIT_MODELS["helo_p"] = {
  len: 17,
  build: function (THREE, M, C) {
    var g = new THREE.Group();

    function paintTex(base, light) {
      var cv = document.createElement("canvas");
      cv.width = 512; cv.height = 512;
      var ctx = cv.getContext("2d");
      ctx.fillStyle = base; ctx.fillRect(0, 0, 512, 512);
      var i, p;
      for (i = 0; i < 30; i++) {
        ctx.globalAlpha = 0.04 + Math.random() * 0.05;
        ctx.fillStyle = (i % 3 === 0) ? light : "#0a0f0a";
        ctx.fillRect(Math.random() * 512, Math.random() * 512, 55 + Math.random() * 140, 35 + Math.random() * 100);
      }
      ctx.globalAlpha = 1;
      ctx.strokeStyle = "rgba(0,0,0,0.26)"; ctx.lineWidth = 2;
      for (i = 0; i < 9; i++) { p = 16 + Math.random() * 480; ctx.beginPath(); ctx.moveTo(p, 0); ctx.lineTo(p, 512); ctx.stroke(); }
      for (i = 0; i < 6; i++) { p = 16 + Math.random() * 480; ctx.beginPath(); ctx.moveTo(0, p); ctx.lineTo(512, p); ctx.stroke(); }
      ctx.fillStyle = "rgba(20,16,8,0.14)";
      for (i = 0; i < 16; i++) { ctx.fillRect(Math.random() * 512, Math.random() * 512, 2 + Math.random() * 4, 20 + Math.random() * 60); }
      var t = new THREE.CanvasTexture(cv);
      t.wrapS = THREE.RepeatWrapping; t.wrapT = THREE.RepeatWrapping;
      return t;
    }

    var bodyMat = new THREE.MeshStandardMaterial({ map: paintTex("#3e4a35", "#7d8468"), metalness: 0.25, roughness: 0.62 });
    var darkMat = new THREE.MeshStandardMaterial({ color: 0x20241f, metalness: 0.35, roughness: 0.6 });
    var glassMat = new THREE.MeshPhysicalMaterial({ color: 0x17232c, metalness: 0.25, roughness: 0.16 });
    var teamMat = new THREE.MeshStandardMaterial({ color: C.team, metalness: 0.2, roughness: 0.6 });

    // ---- beefy fuselage ----
    var fus = new THREE.Mesh(M.loft(THREE, [
      { x: -8.0, w: 0.20, h: 0.28, zc: 0.60 },
      { x: -6.0, w: 0.30, h: 0.38, zc: 0.45 },
      { x: -3.4, w: 0.52, h: 0.62, zc: 0.15 },
      { x: -1.4, w: 0.74, h: 0.86, zc: 0.00, sq: 0.82 },
      { x: 0.9, w: 0.74, h: 0.92, zc: 0.00, sq: 0.82 },
      { x: 2.4, w: 0.62, h: 0.76, zc: -0.05 },
      { x: 3.9, w: 0.56, h: 0.62, zc: -0.12 },
      { x: 5.4, w: 0.46, h: 0.46, zc: -0.25 },
      { x: 6.6, w: 0.18, h: 0.20, zc: -0.40 }
    ], 20), bodyMat);
    g.add(fus);

    // ---- nose sensors ----
    var ball = new THREE.Mesh(new THREE.SphereGeometry(0.30, 14, 10), darkMat);
    ball.position.set(6.55, 0, -0.62); g.add(ball);
    var eye = new THREE.Mesh(new THREE.BoxGeometry(0.12, 0.2, 0.16), glassMat);
    eye.position.set(6.8, 0, -0.6); g.add(eye);
    var noseBox = new THREE.Mesh(new THREE.BoxGeometry(0.5, 0.55, 0.35), bodyMat);
    noseBox.position.set(6.35, 0, -0.18); g.add(noseBox);

    // ---- stepped armored cockpits ----
    var wsF = new THREE.Mesh(new THREE.BoxGeometry(0.8, 0.8, 0.05), glassMat);
    wsF.rotation.y = 0.95; wsF.position.set(4.75, 0, 0.35); g.add(wsF);
    var roofF = new THREE.Mesh(new THREE.BoxGeometry(1.25, 0.84, 0.05), glassMat);
    roofF.position.set(3.75, 0, 0.62); g.add(roofF);
    var wsR = new THREE.Mesh(new THREE.BoxGeometry(0.85, 0.84, 0.05), glassMat);
    wsR.rotation.y = 0.95; wsR.position.set(3.05, 0, 0.78); g.add(wsR);
    var roofR = new THREE.Mesh(new THREE.BoxGeometry(1.35, 0.9, 0.05), glassMat);
    roofR.position.set(2.0, 0, 1.05); g.add(roofR);
    var s;
    for (s = -1; s <= 1; s += 2) {
      var sideF = new THREE.Mesh(new THREE.BoxGeometry(1.4, 0.04, 0.5), glassMat);
      sideF.rotation.x = -s * 0.12;
      sideF.position.set(3.8, s * 0.56, 0.3); g.add(sideF);
      var sideR = new THREE.Mesh(new THREE.BoxGeometry(1.4, 0.04, 0.55), glassMat);
      sideR.rotation.x = -s * 0.12;
      sideR.position.set(2.05, s * 0.6, 0.72); g.add(sideR);
    }
    var spine = new THREE.Mesh(new THREE.BoxGeometry(1.3, 0.8, 0.4), bodyMat);
    spine.position.set(0.7, 0, 0.98); g.add(spine);

    // ---- twin-barrel chin cannon ----
    var turMnt = new THREE.Mesh(new THREE.CylinderGeometry(0.3, 0.34, 0.4, 12), darkMat);
    turMnt.rotation.x = Math.PI / 2;
    turMnt.position.set(4.9, 0, -0.85); g.add(turMnt);
    var cradle = new THREE.Mesh(new THREE.BoxGeometry(1.0, 0.34, 0.22), darkMat);
    cradle.position.set(5.25, 0, -1.0); g.add(cradle);
    var brlGeo = new THREE.CylinderGeometry(0.05, 0.05, 1.9, 8);
    brlGeo.rotateZ(Math.PI / 2);
    for (s = -1; s <= 1; s += 2) {
      var brl = new THREE.Mesh(brlGeo, darkMat);
      brl.position.set(6.4, s * 0.11, -1.0); g.add(brl);
    }

    // ---- wide engine cowls with intakes and side exhausts ----
    var cowlGeo = new THREE.CylinderGeometry(0.55, 0.52, 3.2, 14);
    cowlGeo.rotateZ(Math.PI / 2);
    var capGeo = new THREE.SphereGeometry(0.55, 12, 8);
    var inkGeo = new THREE.CylinderGeometry(0.34, 0.34, 0.12, 12);
    inkGeo.rotateZ(Math.PI / 2);
    var exhGeo = new THREE.CylinderGeometry(0.28, 0.34, 0.6, 10);
    for (s = -1; s <= 1; s += 2) {
      var cowl = new THREE.Mesh(cowlGeo, bodyMat);
      cowl.position.set(0.7, s * 1.08, 0.62); g.add(cowl);
      var cap = new THREE.Mesh(capGeo, bodyMat);
      cap.scale.set(0.65, 1, 1);
      cap.position.set(2.3, s * 1.08, 0.62); g.add(cap);
      var ink = new THREE.Mesh(inkGeo, darkMat);
      ink.position.set(2.62, s * 1.08, 0.7); g.add(ink);
      var exh = new THREE.Mesh(exhGeo, darkMat);
      exh.rotation.x = -s * 1.25;
      exh.position.set(-1.0, s * 1.25, 0.62); g.add(exh);
    }

    // ---- big stub wings: tip pods, rocket pods inner, ATGM racks outer ----
    var wing = new THREE.Mesh(M.slab(THREE, [
      [0.8, 0.3], [0.35, 2.9], [-0.4, 2.9], [-0.68, 0.3],
      [-0.68, -0.3], [-0.4, -2.9], [0.35, -2.9], [0.8, -0.3]
    ], 0.16), bodyMat);
    wing.position.set(0.6, 0, 0.35);
    g.add(wing);
    var tipGeo = new THREE.CylinderGeometry(0.17, 0.17, 1.1, 10);
    tipGeo.rotateZ(Math.PI / 2);
    var podGeo = new THREE.CylinderGeometry(0.24, 0.24, 1.9, 12);
    podGeo.rotateZ(Math.PI / 2);
    var podCapGeo = new THREE.CylinderGeometry(0.24, 0.18, 0.25, 12);
    podCapGeo.rotateZ(Math.PI / 2);
    var pylGeo = new THREE.BoxGeometry(0.6, 0.13, 0.5);
    for (s = -1; s <= 1; s += 2) {
      var tip = new THREE.Mesh(tipGeo, bodyMat);
      tip.position.set(0.55, s * 2.95, 0.35); g.add(tip);
      var pyl1 = new THREE.Mesh(pylGeo, bodyMat);
      pyl1.position.set(0.6, s * 1.5, 0.05); g.add(pyl1);
      var pod = new THREE.Mesh(podGeo, bodyMat);
      pod.position.set(0.55, s * 1.5, -0.28); g.add(pod);
      var pcap = new THREE.Mesh(podCapGeo, darkMat);
      pcap.position.set(1.6, s * 1.5, -0.28); g.add(pcap);
      var pyl2 = new THREE.Mesh(pylGeo, bodyMat);
      pyl2.position.set(0.5, s * 2.4, 0.05); g.add(pyl2);
      var beam = new THREE.Mesh(new THREE.BoxGeometry(1.1, 0.42, 0.1), darkMat);
      beam.position.set(0.5, s * 2.4, -0.22); g.add(beam);
      var tubeGeo = new THREE.CylinderGeometry(0.075, 0.075, 1.7, 8);
      tubeGeo.rotateZ(Math.PI / 2);
      for (var iy = -1; iy <= 1; iy += 2) {
        for (var iz = 0; iz <= 1; iz++) {
          var tube = new THREE.Mesh(tubeGeo, darkMat);
          tube.position.set(0.55, s * 2.4 + iy * 0.14, -0.36 - iz * 0.22);
          g.add(tube);
        }
      }
    }

    // ---- main rotor (5 blades) + mast radar ball (N model) ----
    var mast = new THREE.Mesh(new THREE.CylinderGeometry(0.15, 0.2, 0.6, 10), darkMat);
    mast.rotation.x = Math.PI / 2;
    mast.position.set(0.9, 0, 1.35); g.add(mast);
    var tiltGrp = new THREE.Group();
    tiltGrp.position.set(0.9, 0, 1.62);
    tiltGrp.rotation.y = 0.05;
    var rotor = new THREE.Group();
    rotor.name = "rotor";
    var hub = new THREE.Mesh(new THREE.CylinderGeometry(0.28, 0.24, 0.34, 10), darkMat);
    hub.rotation.x = Math.PI / 2;
    rotor.add(hub);
    var bladeGeo = new THREE.BoxGeometry(8.6 - 0.4, 0.58, 0.08);
    bladeGeo.rotateY(0.02);
    bladeGeo.translate(0.4 + (8.6 - 0.4) / 2, 0, 0);
    for (var b = 0; b < 5; b++) {
      var bl = new THREE.Mesh(bladeGeo, darkMat);
      bl.rotation.z = b * Math.PI * 2 / 5;
      rotor.add(bl);
    }
    tiltGrp.add(rotor);
    g.add(tiltGrp);
    var radome = new THREE.Mesh(new THREE.SphereGeometry(0.48, 16, 12), bodyMat);
    radome.scale.set(1, 1, 0.75);
    radome.position.set(0.9, 0, 2.2); g.add(radome);

    // ---- tail: swept fin, stabilator, X-scissor tail rotor (right side) ----
    var fin = new THREE.Mesh(M.slab(THREE, [
      [-6.6, 0.7], [-7.6, 2.5], [-8.35, 2.5], [-8.25, 0.5]
    ], 0.2, "xz"), bodyMat);
    g.add(fin);
    var stab = new THREE.Mesh(M.slab(THREE, [
      [0.35, 0], [0.26, 1.9], [-0.35, 1.9], [-0.4, 0], [-0.35, -1.9], [0.26, -1.9]
    ], 0.1), bodyMat);
    stab.position.set(-7.0, 0, 0.62);
    g.add(stab);
    var trGear = new THREE.Mesh(new THREE.BoxGeometry(0.55, 0.34, 0.5), bodyMat);
    trGear.position.set(-7.9, -0.2, 1.9); g.add(trGear);
    var tailrotor = new THREE.Group();
    tailrotor.name = "tailrotor";
    tailrotor.position.set(-7.9, -0.48, 1.9);
    var trHub = new THREE.Mesh(new THREE.CylinderGeometry(0.14, 0.14, 0.28, 10), darkMat);
    tailrotor.add(trHub);
    var trBladeGeo = new THREE.BoxGeometry(1.92 - 0.12, 0.06, 0.26);
    trBladeGeo.translate(0.12 + (1.92 - 0.12) / 2, 0, 0);
    var trAngles = [0, 1.22, Math.PI, Math.PI + 1.22];
    for (b = 0; b < 4; b++) {
      var tb = new THREE.Mesh(trBladeGeo, darkMat);
      tb.rotation.y = trAngles[b];
      tailrotor.add(tb);
    }
    g.add(tailrotor);

    // ---- fixed tailwheel gear ----
    var strutGeo = new THREE.CylinderGeometry(0.06, 0.07, 1.0, 8);
    strutGeo.rotateX(Math.PI / 2);
    for (s = -1; s <= 1; s += 2) {
      var strut = new THREE.Mesh(strutGeo, darkMat);
      strut.rotation.x = -s * 0.35;
      strut.position.set(4.0, s * 1.0, -1.05); g.add(strut);
      var wheel = new THREE.Mesh(new THREE.CylinderGeometry(0.38, 0.38, 0.24, 14), darkMat);
      wheel.position.set(4.0, s * 1.22, -1.5); g.add(wheel);
    }
    var twStrut = new THREE.Mesh(new THREE.CylinderGeometry(0.05, 0.05, 0.55, 8), darkMat);
    twStrut.rotation.x = Math.PI / 2;
    twStrut.position.set(-5.2, 0, -0.55); g.add(twStrut);
    var twheel = new THREE.Mesh(new THREE.CylinderGeometry(0.2, 0.2, 0.16, 12), darkMat);
    twheel.position.set(-5.2, 0, -0.88); g.add(twheel);

    // ---- details: antennas, pitot, team band ----
    var antGeo = new THREE.CylinderGeometry(0.014, 0.014, 0.5, 6);
    antGeo.rotateX(Math.PI / 2);
    var ant1 = new THREE.Mesh(antGeo, darkMat);
    ant1.position.set(-2.5, 0, 1.0); g.add(ant1);
    var ant2 = new THREE.Mesh(antGeo, darkMat);
    ant2.rotation.y = -0.4;
    ant2.position.set(-5.5, 0, 0.95); g.add(ant2);
    var pitotGeo = new THREE.CylinderGeometry(0.016, 0.016, 0.7, 6);
    pitotGeo.rotateZ(Math.PI / 2);
    var pitot = new THREE.Mesh(pitotGeo, darkMat);
    pitot.position.set(7.0, 0.3, 0.0); g.add(pitot);
    var band = new THREE.Mesh(new THREE.BoxGeometry(0.5, 0.72, 0.9), teamMat);
    band.position.set(-5.3, 0, 0.4); g.add(band);

    return g;
  }
};

UNIT_MODELS["helo_c"] = {
  len: 14.2,
  build: function (THREE, M, C) {
    var g = new THREE.Group();

    function paintTex(base, light) {
      var cv = document.createElement("canvas");
      cv.width = 512; cv.height = 512;
      var ctx = cv.getContext("2d");
      ctx.fillStyle = base; ctx.fillRect(0, 0, 512, 512);
      var i, p;
      for (i = 0; i < 24; i++) {
        ctx.globalAlpha = 0.035 + Math.random() * 0.045;
        ctx.fillStyle = (i % 3 === 0) ? light : "#050807";
        ctx.fillRect(Math.random() * 512, Math.random() * 512, 60 + Math.random() * 120, 40 + Math.random() * 90);
      }
      ctx.globalAlpha = 1;
      ctx.strokeStyle = "rgba(0,0,0,0.24)"; ctx.lineWidth = 2;
      for (i = 0; i < 11; i++) { p = 16 + Math.random() * 480; ctx.beginPath(); ctx.moveTo(p, 0); ctx.lineTo(p, 512); ctx.stroke(); }
      for (i = 0; i < 6; i++) { p = 16 + Math.random() * 480; ctx.beginPath(); ctx.moveTo(0, p); ctx.lineTo(512, p); ctx.stroke(); }
      ctx.fillStyle = "rgba(0,0,0,0.12)";
      for (i = 0; i < 12; i++) { ctx.fillRect(Math.random() * 512, Math.random() * 512, 2 + Math.random() * 3, 16 + Math.random() * 50); }
      var t = new THREE.CanvasTexture(cv);
      t.wrapS = THREE.RepeatWrapping; t.wrapT = THREE.RepeatWrapping;
      return t;
    }

    var bodyMat = new THREE.MeshStandardMaterial({ map: paintTex("#474e46", "#828879"), metalness: 0.28, roughness: 0.58 });
    var darkMat = new THREE.MeshStandardMaterial({ color: 0x1e2220, metalness: 0.35, roughness: 0.6 });
    var glassMat = new THREE.MeshPhysicalMaterial({ color: 0x151f27, metalness: 0.25, roughness: 0.15 });
    var teamMat = new THREE.MeshStandardMaterial({ color: C.team, metalness: 0.2, roughness: 0.6 });

    // ---- slim faceted fuselage (hard sq flanks) ----
    var fus = new THREE.Mesh(M.loft(THREE, [
      { x: -6.8, w: 0.14, h: 0.20, zc: 0.50 },
      { x: -5.0, w: 0.24, h: 0.30, zc: 0.38 },
      { x: -2.8, w: 0.40, h: 0.50, zc: 0.12, sq: 0.85 },
      { x: -1.0, w: 0.50, h: 0.70, zc: 0.00, sq: 0.88 },
      { x: 0.9, w: 0.50, h: 0.72, zc: 0.00, sq: 0.88 },
      { x: 2.1, w: 0.42, h: 0.60, zc: -0.03, sq: 0.85 },
      { x: 3.4, w: 0.36, h: 0.48, zc: -0.10, sq: 0.85 },
      { x: 4.6, w: 0.26, h: 0.34, zc: -0.20 },
      { x: 5.6, w: 0.08, h: 0.12, zc: -0.30 }
    ], 16), bodyMat);
    g.add(fus);

    // ---- narrow nose sensor ball ----
    var ball = new THREE.Mesh(new THREE.SphereGeometry(0.20, 14, 10), darkMat);
    ball.position.set(5.62, 0, -0.5); g.add(ball);
    var eye = new THREE.Mesh(new THREE.BoxGeometry(0.08, 0.14, 0.1), glassMat);
    eye.position.set(5.79, 0, -0.5); g.add(eye);
    var chinFair = new THREE.Mesh(new THREE.BoxGeometry(0.55, 0.34, 0.24), bodyMat);
    chinFair.position.set(5.25, 0, -0.42); g.add(chinFair);

    // ---- sharply raked stepped canopies ----
    var wsF = new THREE.Mesh(new THREE.BoxGeometry(0.95, 0.6, 0.04), glassMat);
    wsF.rotation.y = 0.78; wsF.position.set(3.85, 0, 0.35); g.add(wsF);
    var roofF = new THREE.Mesh(new THREE.BoxGeometry(1.05, 0.64, 0.04), glassMat);
    roofF.position.set(2.95, 0, 0.62); g.add(roofF);
    var wsR = new THREE.Mesh(new THREE.BoxGeometry(0.85, 0.66, 0.04), glassMat);
    wsR.rotation.y = 0.78; wsR.position.set(2.35, 0, 0.72); g.add(wsR);
    var roofR = new THREE.Mesh(new THREE.BoxGeometry(1.15, 0.68, 0.04), glassMat);
    roofR.position.set(1.45, 0, 0.96); g.add(roofR);
    var s;
    for (s = -1; s <= 1; s += 2) {
      var sideF = new THREE.Mesh(new THREE.BoxGeometry(1.35, 0.03, 0.44), glassMat);
      sideF.rotation.x = -s * 0.28;
      sideF.position.set(3.05, s * 0.38, 0.36); g.add(sideF);
      var sideR = new THREE.Mesh(new THREE.BoxGeometry(1.25, 0.03, 0.48), glassMat);
      sideR.rotation.x = -s * 0.28;
      sideR.position.set(1.5, s * 0.42, 0.68); g.add(sideR);
    }
    var spine = new THREE.Mesh(new THREE.BoxGeometry(1.0, 0.6, 0.3), bodyMat);
    spine.position.set(0.35, 0, 0.85); g.add(spine);

    // ---- 23mm chin cannon ----
    var turMnt = new THREE.Mesh(new THREE.CylinderGeometry(0.22, 0.26, 0.32, 12), darkMat);
    turMnt.rotation.x = Math.PI / 2;
    turMnt.position.set(4.35, 0, -0.72); g.add(turMnt);
    var brlGeo = new THREE.CylinderGeometry(0.04, 0.04, 1.5, 8);
    brlGeo.rotateZ(Math.PI / 2);
    var brl = new THREE.Mesh(brlGeo, darkMat);
    brl.position.set(5.2, 0, -0.82); g.add(brl);

    // ---- slim engine cowls with upward exhausts ----
    var cowlGeo = new THREE.CylinderGeometry(0.38, 0.36, 2.2, 12);
    cowlGeo.rotateZ(Math.PI / 2);
    var capGeo = new THREE.SphereGeometry(0.38, 10, 8);
    var exhGeo = new THREE.CylinderGeometry(0.18, 0.22, 0.4, 10);
    for (s = -1; s <= 1; s += 2) {
      var cowl = new THREE.Mesh(cowlGeo, bodyMat);
      cowl.position.set(0.3, s * 0.78, 0.52); g.add(cowl);
      var cap = new THREE.Mesh(capGeo, bodyMat);
      cap.scale.set(0.6, 1, 1);
      cap.position.set(1.4, s * 0.78, 0.52); g.add(cap);
      var exh = new THREE.Mesh(exhGeo, darkMat);
      exh.rotation.x = -s * 0.5;
      exh.position.set(-0.75, s * 0.85, 0.78); g.add(exh);
    }

    // ---- stub wings with 4-tube HJ-10 racks ----
    var wing = new THREE.Mesh(M.slab(THREE, [
      [0.55, 0.25], [0.24, 2.15], [-0.28, 2.15], [-0.48, 0.25],
      [-0.48, -0.25], [-0.28, -2.15], [0.24, -2.15], [0.55, -0.25]
    ], 0.12), bodyMat);
    wing.position.set(0.35, 0, 0.12);
    g.add(wing);

    function atgmRack(px, py, pz) {
      var rk = new THREE.Group();
      var pyl = new THREE.Mesh(new THREE.BoxGeometry(0.5, 0.11, 0.36), bodyMat);
      pyl.position.set(0, 0, 0.3); rk.add(pyl);
      var beam = new THREE.Mesh(new THREE.BoxGeometry(0.95, 0.36, 0.09), darkMat);
      rk.add(beam);
      var mg = new THREE.CylinderGeometry(0.08, 0.08, 1.5, 8);
      mg.rotateZ(Math.PI / 2);
      var ng = new THREE.SphereGeometry(0.08, 8, 6);
      for (var iy = -1; iy <= 1; iy += 2) {
        for (var iz = 0; iz <= 1; iz++) {
          var m = new THREE.Mesh(mg, darkMat);
          m.position.set(0.05, iy * 0.15, -0.13 - iz * 0.24);
          rk.add(m);
          var n = new THREE.Mesh(ng, darkMat);
          n.position.set(0.8, iy * 0.15, -0.13 - iz * 0.24);
          rk.add(n);
        }
      }
      rk.position.set(px, py, pz);
      return rk;
    }
    for (s = -1; s <= 1; s += 2) {
      g.add(atgmRack(0.4, s * 1.1, -0.3));
      g.add(atgmRack(0.3, s * 1.8, -0.3));
    }

    // ---- main rotor (5 blades) ----
    var mast = new THREE.Mesh(new THREE.CylinderGeometry(0.12, 0.16, 0.55, 10), darkMat);
    mast.rotation.x = Math.PI / 2;
    mast.position.set(0.6, 0, 1.0); g.add(mast);
    var tiltGrp = new THREE.Group();
    tiltGrp.position.set(0.6, 0, 1.27);
    tiltGrp.rotation.y = 0.05;
    var rotor = new THREE.Group();
    rotor.name = "rotor";
    var hub = new THREE.Mesh(new THREE.CylinderGeometry(0.22, 0.18, 0.28, 10), darkMat);
    hub.rotation.x = Math.PI / 2;
    rotor.add(hub);
    var bladeGeo = new THREE.BoxGeometry(6.5 - 0.32, 0.46, 0.06);
    bladeGeo.rotateY(0.02);
    bladeGeo.translate(0.32 + (6.5 - 0.32) / 2, 0, 0);
    for (var b = 0; b < 5; b++) {
      var bl = new THREE.Mesh(bladeGeo, darkMat);
      bl.rotation.z = b * Math.PI * 2 / 5;
      rotor.add(bl);
    }
    tiltGrp.add(rotor);
    g.add(tiltGrp);

    // ---- tail: fin, tailplane, 4-blade tail rotor left ----
    var fin = new THREE.Mesh(M.slab(THREE, [
      [-5.8, 0.55], [-6.5, 1.9], [-7.05, 1.9], [-7.0, 0.45]
    ], 0.14, "xz"), bodyMat);
    g.add(fin);
    var stab = new THREE.Mesh(M.slab(THREE, [
      [0.26, 0], [0.2, 1.4], [-0.26, 1.4], [-0.3, 0], [-0.26, -1.4], [0.2, -1.4]
    ], 0.08), bodyMat);
    stab.position.set(-6.3, 0, 0.5);
    g.add(stab);
    var trGear = new THREE.Mesh(new THREE.BoxGeometry(0.42, 0.26, 0.36), bodyMat);
    trGear.position.set(-6.6, 0.16, 1.35); g.add(trGear);
    var tailrotor = new THREE.Group();
    tailrotor.name = "tailrotor";
    tailrotor.position.set(-6.6, 0.38, 1.35);
    var trHub = new THREE.Mesh(new THREE.CylinderGeometry(0.1, 0.1, 0.2, 10), darkMat);
    tailrotor.add(trHub);
    var trBladeGeo = new THREE.BoxGeometry(1.0 - 0.08, 0.045, 0.2);
    trBladeGeo.translate(0.08 + (1.0 - 0.08) / 2, 0, 0);
    var trAngles = [0, 1.05, Math.PI, Math.PI + 1.05];
    for (b = 0; b < 4; b++) {
      var tb = new THREE.Mesh(trBladeGeo, darkMat);
      tb.rotation.y = trAngles[b];
      tailrotor.add(tb);
    }
    g.add(tailrotor);

    // ---- gear: faired legs, wheels, tailwheel ----
    var strutGeo = new THREE.CylinderGeometry(0.045, 0.055, 0.8, 8);
    strutGeo.rotateX(Math.PI / 2);
    for (s = -1; s <= 1; s += 2) {
      var strut = new THREE.Mesh(strutGeo, darkMat);
      strut.rotation.x = -s * 0.32;
      strut.position.set(3.2, s * 0.75, -0.9); g.add(strut);
      var wheel = new THREE.Mesh(new THREE.CylinderGeometry(0.28, 0.28, 0.18, 14), darkMat);
      wheel.position.set(3.2, s * 0.9, -1.25); g.add(wheel);
    }
    var twStrut = new THREE.Mesh(new THREE.CylinderGeometry(0.035, 0.035, 0.45, 8), darkMat);
    twStrut.rotation.x = Math.PI / 2;
    twStrut.position.set(-5.3, 0, -0.35); g.add(twStrut);
    var twheel = new THREE.Mesh(new THREE.CylinderGeometry(0.15, 0.15, 0.12, 12), darkMat);
    twheel.position.set(-5.3, 0, -0.62); g.add(twheel);

    // ---- details ----
    var antGeo = new THREE.CylinderGeometry(0.012, 0.012, 0.4, 6);
    antGeo.rotateX(Math.PI / 2);
    var ant1 = new THREE.Mesh(antGeo, darkMat);
    ant1.position.set(-2.0, 0, 0.85); g.add(ant1);
    var ant2 = new THREE.Mesh(antGeo, darkMat);
    ant2.rotation.y = 0.35;
    ant2.position.set(-4.4, 0, 0.78); g.add(ant2);
    var pitotGeo = new THREE.CylinderGeometry(0.014, 0.014, 0.55, 6);
    pitotGeo.rotateZ(Math.PI / 2);
    var pitot = new THREE.Mesh(pitotGeo, darkMat);
    pitot.position.set(5.75, 0.2, 0.02); g.add(pitot);
    var band = new THREE.Mesh(new THREE.BoxGeometry(0.4, 0.56, 0.7), teamMat);
    band.position.set(-4.4, 0, 0.32); g.add(band);

    return g;
  }
};

UNIT_MODELS["trans_n"] = {
  len: 15.3,
  build: function (THREE, M, C) {
    var g = new THREE.Group();

    function paintTex(base, light) {
      var cv = document.createElement("canvas");
      cv.width = 512; cv.height = 512;
      var ctx = cv.getContext("2d");
      ctx.fillStyle = base; ctx.fillRect(0, 0, 512, 512);
      var i, p;
      for (i = 0; i < 28; i++) {
        ctx.globalAlpha = 0.04 + Math.random() * 0.05;
        ctx.fillStyle = (i % 3 === 0) ? light : "#000000";
        ctx.fillRect(Math.random() * 512, Math.random() * 512, 50 + Math.random() * 140, 35 + Math.random() * 100);
      }
      ctx.globalAlpha = 1;
      ctx.strokeStyle = "rgba(0,0,0,0.24)"; ctx.lineWidth = 2;
      for (i = 0; i < 10; i++) { p = 16 + Math.random() * 480; ctx.beginPath(); ctx.moveTo(p, 0); ctx.lineTo(p, 512); ctx.stroke(); }
      for (i = 0; i < 7; i++) { p = 16 + Math.random() * 480; ctx.beginPath(); ctx.moveTo(0, p); ctx.lineTo(512, p); ctx.stroke(); }
      ctx.fillStyle = "rgba(15,12,5,0.13)";
      for (i = 0; i < 15; i++) { ctx.fillRect(Math.random() * 512, Math.random() * 512, 2 + Math.random() * 4, 20 + Math.random() * 60); }
      var t = new THREE.CanvasTexture(cv);
      t.wrapS = THREE.RepeatWrapping; t.wrapT = THREE.RepeatWrapping;
      return t;
    }

    var bodyMat = new THREE.MeshStandardMaterial({ map: paintTex("#3d4335", "#767d63"), metalness: 0.25, roughness: 0.62 });
    var darkMat = new THREE.MeshStandardMaterial({ color: 0x1f2320, metalness: 0.35, roughness: 0.6 });
    var glassMat = new THREE.MeshPhysicalMaterial({ color: 0x161f27, metalness: 0.25, roughness: 0.16 });
    var teamMat = new THREE.MeshStandardMaterial({ color: C.team, metalness: 0.2, roughness: 0.6 });

    // ---- wide low cabin, drooped tail boom ----
    var fus = new THREE.Mesh(M.loft(THREE, [
      { x: -7.2, w: 0.18, h: 0.28, zc: 0.05 },
      { x: -5.4, w: 0.30, h: 0.42, zc: 0.18 },
      { x: -3.2, w: 0.55, h: 0.60, zc: 0.32 },
      { x: -1.8, w: 1.05, h: 0.80, zc: 0.30, sq: 0.85 },
      { x: 1.6, w: 1.10, h: 0.85, zc: 0.30, sq: 0.85 },
      { x: 3.2, w: 0.95, h: 0.80, zc: 0.25, sq: 0.8 },
      { x: 4.6, w: 0.70, h: 0.55, zc: 0.05 },
      { x: 5.7, w: 0.30, h: 0.25, zc: -0.10 }
    ], 20), bodyMat);
    g.add(fus);

    // ---- cockpit glass: raked windscreen halves + door windows ----
    var s;
    for (s = -1; s <= 1; s += 2) {
      var ws = new THREE.Mesh(new THREE.BoxGeometry(1.0, 0.62, 0.05), glassMat);
      ws.rotation.y = 0.72;
      ws.rotation.z = s * 0.28;
      ws.position.set(4.35, s * 0.34, 0.72); g.add(ws);
      var dwin = new THREE.Mesh(new THREE.BoxGeometry(0.85, 0.05, 0.6), glassMat);
      dwin.rotation.x = -s * 0.1;
      dwin.position.set(3.6, s * 0.9, 0.55); g.add(dwin);
    }

    // ---- two sliding cabin doors with windows ----
    for (s = -1; s <= 1; s += 2) {
      var door = new THREE.Mesh(new THREE.BoxGeometry(1.8, 0.06, 1.3), bodyMat);
      door.position.set(1.2, s * 1.1, 0.35); g.add(door);
      var win = new THREE.Mesh(new THREE.BoxGeometry(0.8, 0.05, 0.55), glassMat);
      win.position.set(1.35, s * 1.13, 0.55); g.add(win);
      var rail = new THREE.Mesh(new THREE.BoxGeometry(2.6, 0.04, 0.06), darkMat);
      rail.position.set(0.6, s * 1.12, 1.06); g.add(rail);
    }

    // ---- engine deck: two turboshafts flanking rotor, outboard exhausts ----
    var engGeo = new THREE.CylinderGeometry(0.4, 0.4, 2.1, 12);
    engGeo.rotateZ(Math.PI / 2);
    var inkGeo = new THREE.SphereGeometry(0.4, 10, 8);
    var exhGeo = new THREE.CylinderGeometry(0.26, 0.3, 0.5, 10);
    exhGeo.rotateZ(Math.PI / 2);
    for (s = -1; s <= 1; s += 2) {
      var eng = new THREE.Mesh(engGeo, bodyMat);
      eng.position.set(-1.0, s * 0.75, 1.28); g.add(eng);
      var ink = new THREE.Mesh(inkGeo, bodyMat);
      ink.scale.set(0.6, 1, 1);
      ink.position.set(0.05, s * 0.75, 1.28); g.add(ink);
      var exh = new THREE.Mesh(exhGeo, darkMat);
      exh.rotation.z = 0.2;
      exh.rotation.x = -s * 0.5;
      exh.position.set(-2.15, s * 0.85, 1.3); g.add(exh);
    }
    var deck = new THREE.Mesh(new THREE.BoxGeometry(3.4, 1.1, 0.35), bodyMat);
    deck.position.set(-0.7, 0, 1.18); g.add(deck);

    // ---- main rotor (4 blades) ----
    var mast = new THREE.Mesh(new THREE.CylinderGeometry(0.14, 0.18, 0.5, 10), darkMat);
    mast.rotation.x = Math.PI / 2;
    mast.position.set(0.7, 0, 1.4); g.add(mast);
    var rotor = new THREE.Group();
    rotor.name = "rotor";
    rotor.position.set(0.7, 0, 1.62);
    var hub = new THREE.Mesh(new THREE.CylinderGeometry(0.26, 0.22, 0.3, 10), darkMat);
    hub.rotation.x = Math.PI / 2;
    rotor.add(hub);
    var bladeGeo = new THREE.BoxGeometry(8.2 - 0.4, 0.53, 0.08);
    bladeGeo.rotateY(0.02);
    bladeGeo.translate(0.4 + (8.2 - 0.4) / 2, 0, 0);
    for (var b = 0; b < 4; b++) {
      var bl = new THREE.Mesh(bladeGeo, darkMat);
      bl.rotation.z = b * Math.PI / 2;
      rotor.add(bl);
    }
    g.add(rotor);

    // ---- tail: swept fin, big low stabilator, canted 4-blade tail rotor ----
    var fin = new THREE.Mesh(M.slab(THREE, [
      [-6.3, 0.2], [-7.1, 2.2], [-7.85, 2.3], [-7.65, -0.05]
    ], 0.24, "xz"), bodyMat);
    g.add(fin);
    var stab = new THREE.Mesh(M.slab(THREE, [
      [0.42, 0], [0.3, 2.2], [-0.42, 2.2], [-0.5, 0], [-0.42, -2.2], [0.3, -2.2]
    ], 0.1), bodyMat);
    stab.position.set(-7.15, 0, 0.3);
    g.add(stab);
    var cantGrp = new THREE.Group();
    cantGrp.position.set(-7.35, 0.3, 1.75);
    cantGrp.rotation.x = 0.34;
    var tailrotor = new THREE.Group();
    tailrotor.name = "tailrotor";
    var trHub = new THREE.Mesh(new THREE.CylinderGeometry(0.13, 0.13, 0.26, 10), darkMat);
    tailrotor.add(trHub);
    var trBladeGeo = new THREE.BoxGeometry(1.68 - 0.1, 0.05, 0.24);
    trBladeGeo.translate(0.1 + (1.68 - 0.1) / 2, 0, 0);
    for (b = 0; b < 4; b++) {
      var tb = new THREE.Mesh(trBladeGeo, darkMat);
      tb.rotation.y = b * Math.PI / 2;
      tailrotor.add(tb);
    }
    cantGrp.add(tailrotor);
    g.add(cantGrp);

    // ---- gear: main struts + wheels, tailwheel far aft ----
    var strutGeo = new THREE.CylinderGeometry(0.06, 0.07, 0.75, 8);
    strutGeo.rotateX(Math.PI / 2);
    for (s = -1; s <= 1; s += 2) {
      var strut = new THREE.Mesh(strutGeo, darkMat);
      strut.rotation.x = -s * 0.3;
      strut.position.set(3.3, s * 1.05, -0.6); g.add(strut);
      var wheel = new THREE.Mesh(new THREE.CylinderGeometry(0.33, 0.33, 0.22, 14), darkMat);
      wheel.position.set(3.3, s * 1.2, -0.95); g.add(wheel);
    }
    var twStrut = new THREE.Mesh(new THREE.CylinderGeometry(0.05, 0.05, 0.55, 8), darkMat);
    twStrut.rotation.x = Math.PI / 2;
    twStrut.position.set(-5.7, 0, -0.15); g.add(twStrut);
    var twheel = new THREE.Mesh(new THREE.CylinderGeometry(0.2, 0.2, 0.16, 12), darkMat);
    twheel.position.set(-5.7, 0, -0.48); g.add(twheel);

    // ---- details: FLIR ball, antennas, team band ----
    var flir = new THREE.Mesh(new THREE.SphereGeometry(0.18, 12, 8), darkMat);
    flir.position.set(4.9, 0.4, -0.45); g.add(flir);
    var antGeo = new THREE.CylinderGeometry(0.012, 0.012, 0.45, 6);
    antGeo.rotateX(Math.PI / 2);
    var ant1 = new THREE.Mesh(antGeo, darkMat);
    ant1.position.set(-2.6, 0, 1.15); g.add(ant1);
    var ant2 = new THREE.Mesh(antGeo, darkMat);
    ant2.rotation.y = -0.35;
    ant2.position.set(-4.6, 0, 0.85); g.add(ant2);
    var ant3 = new THREE.Mesh(antGeo, darkMat);
    ant3.rotation.y = Math.PI;
    ant3.position.set(2.6, 0, -0.6); g.add(ant3);
    var band = new THREE.Mesh(new THREE.BoxGeometry(0.5, 0.66, 0.92), teamMat);
    band.position.set(-4.6, 0, 0.28); g.add(band);

    return g;
  }
};

UNIT_MODELS["trans_p"] = {
  len: 18.4,
  build: function (THREE, M, C) {
    var g = new THREE.Group();

    function paintTex(base, light) {
      var cv = document.createElement("canvas");
      cv.width = 512; cv.height = 512;
      var ctx = cv.getContext("2d");
      ctx.fillStyle = base; ctx.fillRect(0, 0, 512, 512);
      var i, p;
      for (i = 0; i < 30; i++) {
        ctx.globalAlpha = 0.045 + Math.random() * 0.05;
        ctx.fillStyle = (i % 3 === 0) ? light : "#0c0f08";
        ctx.fillRect(Math.random() * 512, Math.random() * 512, 60 + Math.random() * 150, 40 + Math.random() * 110);
      }
      ctx.globalAlpha = 1;
      ctx.strokeStyle = "rgba(0,0,0,0.25)"; ctx.lineWidth = 2;
      for (i = 0; i < 9; i++) { p = 16 + Math.random() * 480; ctx.beginPath(); ctx.moveTo(p, 0); ctx.lineTo(p, 512); ctx.stroke(); }
      for (i = 0; i < 6; i++) { p = 16 + Math.random() * 480; ctx.beginPath(); ctx.moveTo(0, p); ctx.lineTo(512, p); ctx.stroke(); }
      ctx.fillStyle = "rgba(25,20,8,0.15)";
      for (i = 0; i < 18; i++) { ctx.fillRect(Math.random() * 512, Math.random() * 512, 2 + Math.random() * 4, 22 + Math.random() * 70); }
      var t = new THREE.CanvasTexture(cv);
      t.wrapS = THREE.RepeatWrapping; t.wrapT = THREE.RepeatWrapping;
      return t;
    }

    var bodyMat = new THREE.MeshStandardMaterial({ map: paintTex("#3e4a35", "#8a8a6a"), metalness: 0.25, roughness: 0.65 });
    var darkMat = new THREE.MeshStandardMaterial({ color: 0x22261f, metalness: 0.35, roughness: 0.6 });
    var glassMat = new THREE.MeshPhysicalMaterial({ color: 0x18242c, metalness: 0.25, roughness: 0.16 });
    var teamMat = new THREE.MeshStandardMaterial({ color: C.team, metalness: 0.2, roughness: 0.6 });

    // ---- fat round-nose fuselage with clamshell taper ----
    var fus = new THREE.Mesh(M.loft(THREE, [
      { x: -9.0, w: 0.20, h: 0.30, zc: 1.10 },
      { x: -7.0, w: 0.30, h: 0.40, zc: 0.90 },
      { x: -4.6, w: 0.50, h: 0.55, zc: 0.60 },
      { x: -2.6, w: 0.90, h: 0.85, zc: 0.20 },
      { x: -0.8, w: 1.25, h: 1.15, zc: 0.00, sq: 0.7 },
      { x: 2.2, w: 1.25, h: 1.15, zc: 0.00, sq: 0.7 },
      { x: 4.2, w: 1.15, h: 1.05, zc: 0.00 },
      { x: 5.6, w: 0.90, h: 0.85, zc: -0.02 },
      { x: 6.6, w: 0.45, h: 0.50, zc: -0.05 }
    ], 22), bodyMat);
    g.add(fus);

    // ---- fully glazed round nose ----
    var dome = new THREE.Mesh(new THREE.SphereGeometry(0.62, 14, 10), glassMat);
    dome.scale.set(0.72, 1.0, 0.9);
    dome.position.set(6.6, 0, 0.02); g.add(dome);
    var s;
    for (s = -1; s <= 1; s += 2) {
      var cockWin = new THREE.Mesh(new THREE.BoxGeometry(1.0, 0.05, 0.6), glassMat);
      cockWin.rotation.x = -s * 0.15;
      cockWin.position.set(5.5, s * 0.92, 0.35); g.add(cockWin);
    }

    // ---- round cabin windows (porthole discs) ----
    var winGeo = new THREE.CylinderGeometry(0.17, 0.17, 0.04, 12);
    var wx = [-2.1, -1.0, 0.1, 1.2, 2.6];
    for (s = -1; s <= 1; s += 2) {
      for (var wi = 0; wi < wx.length; wi++) {
        var pw = new THREE.Mesh(winGeo, glassMat);
        pw.position.set(wx[wi], s * 1.24, 0.32); g.add(pw);
      }
    }
    // front-left sliding door
    var door = new THREE.Mesh(new THREE.BoxGeometry(1.3, 0.06, 1.5), bodyMat);
    door.position.set(3.6, 1.16, -0.1); g.add(door);
    var doorWin = new THREE.Mesh(winGeo, glassMat);
    doorWin.position.set(3.8, 1.2, 0.32); g.add(doorWin);

    // ---- clamshell rear doors ----
    for (s = -1; s <= 1; s += 2) {
      var shell = new THREE.Mesh(new THREE.BoxGeometry(2.0, 0.85, 0.07), bodyMat);
      shell.rotation.y = 0.3;
      shell.rotation.z = -s * 0.14;
      shell.position.set(-3.4, s * 0.48, -0.32); g.add(shell);
    }

    // ---- engine cowl + intakes with PZU dust filters ----
    var cowl = new THREE.Mesh(new THREE.CylinderGeometry(0.72, 0.72, 4.4, 14), bodyMat);
    cowl.rotation.z = Math.PI / 2;
    cowl.scale.set(1, 1, 0.8);
    cowl.position.set(1.3, 0, 1.45); g.add(cowl);
    var gbox = new THREE.Mesh(new THREE.BoxGeometry(1.4, 1.1, 0.6), bodyMat);
    gbox.position.set(0.4, 0, 1.75); g.add(gbox);
    var inkGeo = new THREE.CylinderGeometry(0.26, 0.26, 0.9, 12);
    inkGeo.rotateZ(Math.PI / 2);
    var pzuGeo = new THREE.SphereGeometry(0.2, 10, 8);
    for (s = -1; s <= 1; s += 2) {
      var ink = new THREE.Mesh(inkGeo, bodyMat);
      ink.position.set(3.4, s * 0.4, 1.5); g.add(ink);
      var pzu = new THREE.Mesh(pzuGeo, darkMat);
      pzu.position.set(3.95, s * 0.4, 1.5); g.add(pzu);
      var exh = new THREE.Mesh(new THREE.CylinderGeometry(0.24, 0.3, 0.5, 10), darkMat);
      exh.rotation.x = -s * 1.35;
      exh.position.set(0.4, s * 0.72, 1.6); g.add(exh);
    }

    // ---- external fuel tanks + AMTSh weapon outriggers ----
    var tankGeo = new THREE.CylinderGeometry(0.36, 0.36, 4.6, 12);
    tankGeo.rotateZ(Math.PI / 2);
    var podGeo = new THREE.CylinderGeometry(0.22, 0.22, 1.8, 10);
    podGeo.rotateZ(Math.PI / 2);
    var podCapGeo = new THREE.CylinderGeometry(0.22, 0.16, 0.22, 10);
    podCapGeo.rotateZ(Math.PI / 2);
    for (s = -1; s <= 1; s += 2) {
      var tank = new THREE.Mesh(tankGeo, bodyMat);
      tank.position.set(0.4, s * 1.45, -0.78); g.add(tank);
      var rig = new THREE.Mesh(new THREE.BoxGeometry(0.12, 0.7, 0.12), darkMat);
      rig.position.set(0.9, s * 1.75, -0.15); g.add(rig);
      var pod = new THREE.Mesh(podGeo, bodyMat);
      pod.position.set(0.9, s * 2.05, -0.35); g.add(pod);
      var pcap = new THREE.Mesh(podCapGeo, darkMat);
      pcap.position.set(1.9, s * 2.05, -0.35); g.add(pcap);
    }

    // ---- main rotor (5 blades, big) ----
    var mast = new THREE.Mesh(new THREE.CylinderGeometry(0.16, 0.22, 0.5, 10), darkMat);
    mast.rotation.x = Math.PI / 2;
    mast.position.set(0.8, 0, 2.05); g.add(mast);
    var rotor = new THREE.Group();
    rotor.name = "rotor";
    rotor.position.set(0.8, 0, 2.3);
    var hub = new THREE.Mesh(new THREE.CylinderGeometry(0.3, 0.26, 0.34, 10), darkMat);
    hub.rotation.x = Math.PI / 2;
    rotor.add(hub);
    var bladeGeo = new THREE.BoxGeometry(10.65 - 0.5, 0.62, 0.09);
    bladeGeo.rotateY(0.025);
    bladeGeo.translate(0.5 + (10.65 - 0.5) / 2, 0, 0);
    for (var b = 0; b < 5; b++) {
      var bl = new THREE.Mesh(bladeGeo, darkMat);
      bl.rotation.z = b * Math.PI * 2 / 5;
      rotor.add(bl);
    }
    g.add(rotor);

    // ---- big fin, tailplane, 3-blade tail rotor left ----
    var fin = new THREE.Mesh(M.slab(THREE, [
      [-7.4, 0.7], [-8.6, 2.6], [-9.35, 2.7], [-9.2, 0.6]
    ], 0.22, "xz"), bodyMat);
    g.add(fin);
    var stab = new THREE.Mesh(M.slab(THREE, [
      [0.4, 0], [0.3, 1.9], [-0.4, 1.9], [-0.46, 0], [-0.4, -1.9], [0.3, -1.9]
    ], 0.1), bodyMat);
    stab.position.set(-7.6, 0, 0.85);
    g.add(stab);
    var trGear = new THREE.Mesh(new THREE.BoxGeometry(0.5, 0.34, 0.5), bodyMat);
    trGear.position.set(-8.95, 0.22, 2.25); g.add(trGear);
    var tailrotor = new THREE.Group();
    tailrotor.name = "tailrotor";
    tailrotor.position.set(-8.95, 0.5, 2.25);
    var trHub = new THREE.Mesh(new THREE.CylinderGeometry(0.14, 0.14, 0.28, 10), darkMat);
    tailrotor.add(trHub);
    var trBladeGeo = new THREE.BoxGeometry(1.95 - 0.12, 0.06, 0.3);
    trBladeGeo.translate(0.12 + (1.95 - 0.12) / 2, 0, 0);
    for (b = 0; b < 3; b++) {
      var tb = new THREE.Mesh(trBladeGeo, darkMat);
      tb.rotation.y = b * Math.PI * 2 / 3;
      tailrotor.add(tb);
    }
    g.add(tailrotor);

    // ---- fixed tricycle gear ----
    var mStrutGeo = new THREE.CylinderGeometry(0.07, 0.08, 1.1, 8);
    mStrutGeo.rotateX(Math.PI / 2);
    for (s = -1; s <= 1; s += 2) {
      var strut = new THREE.Mesh(mStrutGeo, darkMat);
      strut.rotation.x = -s * 0.5;
      strut.position.set(-0.2, s * 1.55, -1.0); g.add(strut);
      var brace = new THREE.Mesh(new THREE.CylinderGeometry(0.05, 0.05, 1.0, 8), darkMat);
      brace.rotation.x = Math.PI / 2 - s * 1.1;
      brace.position.set(-0.2, s * 1.1, -1.15); g.add(brace);
      var wheel = new THREE.Mesh(new THREE.CylinderGeometry(0.42, 0.42, 0.28, 14), darkMat);
      wheel.position.set(-0.2, s * 1.85, -1.45); g.add(wheel);
    }
    var nStrut = new THREE.Mesh(new THREE.CylinderGeometry(0.06, 0.06, 0.7, 8), darkMat);
    nStrut.rotation.x = Math.PI / 2;
    nStrut.position.set(4.8, 0, -1.0); g.add(nStrut);
    for (s = -1; s <= 1; s += 2) {
      var nwheel = new THREE.Mesh(new THREE.CylinderGeometry(0.25, 0.25, 0.16, 12), darkMat);
      nwheel.position.set(4.8, s * 0.28, -1.32); g.add(nwheel);
    }

    // ---- details: antenna masts, team band ----
    var antGeo = new THREE.CylinderGeometry(0.015, 0.015, 0.55, 6);
    antGeo.rotateX(Math.PI / 2);
    var ant1 = new THREE.Mesh(antGeo, darkMat);
    ant1.position.set(-3.6, 0, 1.0); g.add(ant1);
    var ant2 = new THREE.Mesh(antGeo, darkMat);
    ant2.rotation.y = -0.3;
    ant2.position.set(-5.8, 0, 1.15); g.add(ant2);
    var ant3 = new THREE.Mesh(antGeo, darkMat);
    ant3.position.set(4.6, 0, 1.15); g.add(ant3);
    var band = new THREE.Mesh(new THREE.BoxGeometry(0.55, 0.68, 0.95), teamMat);
    band.position.set(-5.6, 0, 0.75); g.add(band);

    return g;
  }
};

UNIT_MODELS["trans_c"] = {
  len: 15,
  build: function (THREE, M, C) {
    var g = new THREE.Group();

    function paintTex(base, light) {
      var cv = document.createElement("canvas");
      cv.width = 512; cv.height = 512;
      var ctx = cv.getContext("2d");
      ctx.fillStyle = base; ctx.fillRect(0, 0, 512, 512);
      var i, p;
      for (i = 0; i < 26; i++) {
        ctx.globalAlpha = 0.04 + Math.random() * 0.045;
        ctx.fillStyle = (i % 3 === 0) ? light : "#060806";
        ctx.fillRect(Math.random() * 512, Math.random() * 512, 55 + Math.random() * 130, 38 + Math.random() * 95);
      }
      ctx.globalAlpha = 1;
      ctx.strokeStyle = "rgba(0,0,0,0.24)"; ctx.lineWidth = 2;
      for (i = 0; i < 10; i++) { p = 16 + Math.random() * 480; ctx.beginPath(); ctx.moveTo(p, 0); ctx.lineTo(p, 512); ctx.stroke(); }
      for (i = 0; i < 6; i++) { p = 16 + Math.random() * 480; ctx.beginPath(); ctx.moveTo(0, p); ctx.lineTo(512, p); ctx.stroke(); }
      ctx.fillStyle = "rgba(0,0,0,0.12)";
      for (i = 0; i < 14; i++) { ctx.fillRect(Math.random() * 512, Math.random() * 512, 2 + Math.random() * 3, 18 + Math.random() * 55); }
      var t = new THREE.CanvasTexture(cv);
      t.wrapS = THREE.RepeatWrapping; t.wrapT = THREE.RepeatWrapping;
      return t;
    }

    var bodyMat = new THREE.MeshStandardMaterial({ map: paintTex("#4b514a", "#878c80"), metalness: 0.28, roughness: 0.6 });
    var darkMat = new THREE.MeshStandardMaterial({ color: 0x1f2321, metalness: 0.35, roughness: 0.6 });
    var glassMat = new THREE.MeshPhysicalMaterial({ color: 0x151f26, metalness: 0.25, roughness: 0.16 });
    var teamMat = new THREE.MeshStandardMaterial({ color: C.team, metalness: 0.2, roughness: 0.6 });

    // ---- Black-Hawk-like fuselage with DEEPER cabin ----
    var fus = new THREE.Mesh(M.loft(THREE, [
      { x: -7.1, w: 0.17, h: 0.30, zc: 0.10 },
      { x: -5.2, w: 0.30, h: 0.45, zc: 0.25 },
      { x: -3.0, w: 0.60, h: 0.65, zc: 0.38 },
      { x: -1.6, w: 1.05, h: 0.95, zc: 0.25, sq: 0.85 },
      { x: 1.7, w: 1.10, h: 1.00, zc: 0.25, sq: 0.85 },
      { x: 3.3, w: 0.95, h: 0.85, zc: 0.20, sq: 0.8 },
      { x: 4.6, w: 0.68, h: 0.55, zc: 0.00 },
      { x: 5.6, w: 0.28, h: 0.24, zc: -0.12 }
    ], 20), bodyMat);
    g.add(fus);

    // ---- cockpit glass ----
    var s;
    for (s = -1; s <= 1; s += 2) {
      var ws = new THREE.Mesh(new THREE.BoxGeometry(1.0, 0.6, 0.05), glassMat);
      ws.rotation.y = 0.75;
      ws.rotation.z = s * 0.26;
      ws.position.set(4.3, s * 0.33, 0.72); g.add(ws);
      var dwin = new THREE.Mesh(new THREE.BoxGeometry(0.8, 0.05, 0.55), glassMat);
      dwin.rotation.x = -s * 0.1;
      dwin.position.set(3.55, s * 0.9, 0.5); g.add(dwin);
    }

    // ---- deep sliding cabin doors ----
    for (s = -1; s <= 1; s += 2) {
      var door = new THREE.Mesh(new THREE.BoxGeometry(1.85, 0.06, 1.5), bodyMat);
      door.position.set(1.15, s * 1.1, 0.3); g.add(door);
      var win = new THREE.Mesh(new THREE.BoxGeometry(0.85, 0.05, 0.6), glassMat);
      win.position.set(1.3, s * 1.13, 0.55); g.add(win);
      var rail = new THREE.Mesh(new THREE.BoxGeometry(2.7, 0.04, 0.06), darkMat);
      rail.position.set(0.55, s * 1.12, 1.16); g.add(rail);
    }

    // ---- engine deck with squared IR-suppressor exhausts ----
    var deck = new THREE.Mesh(new THREE.BoxGeometry(3.6, 1.15, 0.4), bodyMat);
    deck.position.set(-0.6, 0, 1.32); g.add(deck);
    var engGeo = new THREE.CylinderGeometry(0.38, 0.38, 2.0, 12);
    engGeo.rotateZ(Math.PI / 2);
    var inkGeo = new THREE.SphereGeometry(0.38, 10, 8);
    for (s = -1; s <= 1; s += 2) {
      var eng = new THREE.Mesh(engGeo, bodyMat);
      eng.position.set(-0.9, s * 0.78, 1.42); g.add(eng);
      var ink = new THREE.Mesh(inkGeo, bodyMat);
      ink.scale.set(0.6, 1, 1);
      ink.position.set(0.1, s * 0.78, 1.42); g.add(ink);
      var exh = new THREE.Mesh(new THREE.BoxGeometry(0.7, 0.34, 0.34), darkMat);
      exh.rotation.z = 0.18;
      exh.position.set(-2.15, s * 0.82, 1.42); g.add(exh);
    }

    // ---- main rotor (5 blades - Z-20 signature) ----
    var mast = new THREE.Mesh(new THREE.CylinderGeometry(0.14, 0.18, 0.5, 10), darkMat);
    mast.rotation.x = Math.PI / 2;
    mast.position.set(0.6, 0, 1.55); g.add(mast);
    var rotor = new THREE.Group();
    rotor.name = "rotor";
    rotor.position.set(0.6, 0, 1.78);
    var hub = new THREE.Mesh(new THREE.CylinderGeometry(0.26, 0.22, 0.3, 10), darkMat);
    hub.rotation.x = Math.PI / 2;
    rotor.add(hub);
    var bladeGeo = new THREE.BoxGeometry(8.0 - 0.4, 0.5, 0.08);
    bladeGeo.rotateY(0.02);
    bladeGeo.translate(0.4 + (8.0 - 0.4) / 2, 0, 0);
    for (var b = 0; b < 5; b++) {
      var bl = new THREE.Mesh(bladeGeo, darkMat);
      bl.rotation.z = b * Math.PI * 2 / 5;
      rotor.add(bl);
    }
    g.add(rotor);

    // ---- tail: fin, stabilator, canted 4-blade tail rotor ----
    var fin = new THREE.Mesh(M.slab(THREE, [
      [-6.2, 0.25], [-7.0, 2.15], [-7.75, 2.25], [-7.55, 0.0]
    ], 0.24, "xz"), bodyMat);
    g.add(fin);
    var stab = new THREE.Mesh(M.slab(THREE, [
      [0.4, 0], [0.28, 2.1], [-0.4, 2.1], [-0.48, 0], [-0.4, -2.1], [0.28, -2.1]
    ], 0.1), bodyMat);
    stab.position.set(-7.05, 0, 0.35);
    g.add(stab);
    var cantGrp = new THREE.Group();
    cantGrp.position.set(-7.25, 0.3, 1.72);
    cantGrp.rotation.x = 0.3;
    var tailrotor = new THREE.Group();
    tailrotor.name = "tailrotor";
    var trHub = new THREE.Mesh(new THREE.CylinderGeometry(0.13, 0.13, 0.26, 10), darkMat);
    tailrotor.add(trHub);
    var trBladeGeo = new THREE.BoxGeometry(1.7 - 0.1, 0.05, 0.24);
    trBladeGeo.translate(0.1 + (1.7 - 0.1) / 2, 0, 0);
    for (b = 0; b < 4; b++) {
      var tb = new THREE.Mesh(trBladeGeo, darkMat);
      tb.rotation.y = b * Math.PI / 2;
      tailrotor.add(tb);
    }
    cantGrp.add(tailrotor);
    g.add(cantGrp);

    // ---- gear ----
    var strutGeo = new THREE.CylinderGeometry(0.06, 0.07, 0.8, 8);
    strutGeo.rotateX(Math.PI / 2);
    for (s = -1; s <= 1; s += 2) {
      var strut = new THREE.Mesh(strutGeo, darkMat);
      strut.rotation.x = -s * 0.3;
      strut.position.set(3.25, s * 1.05, -0.7); g.add(strut);
      var wheel = new THREE.Mesh(new THREE.CylinderGeometry(0.32, 0.32, 0.22, 14), darkMat);
      wheel.position.set(3.25, s * 1.2, -1.05); g.add(wheel);
    }
    var twStrut = new THREE.Mesh(new THREE.CylinderGeometry(0.05, 0.05, 0.55, 8), darkMat);
    twStrut.rotation.x = Math.PI / 2;
    twStrut.position.set(-5.5, 0, -0.2); g.add(twStrut);
    var twheel = new THREE.Mesh(new THREE.CylinderGeometry(0.2, 0.2, 0.16, 12), darkMat);
    twheel.position.set(-5.5, 0, -0.52); g.add(twheel);

    // ---- details: FLIR, satcom hump, antennas, team band ----
    var flir = new THREE.Mesh(new THREE.SphereGeometry(0.17, 12, 8), darkMat);
    flir.position.set(4.85, -0.35, -0.4); g.add(flir);
    var hump = new THREE.Mesh(new THREE.SphereGeometry(0.22, 10, 8), bodyMat);
    hump.scale.set(1.4, 1, 0.6);
    hump.position.set(-3.3, 0, 1.05); g.add(hump);
    var antGeo = new THREE.CylinderGeometry(0.012, 0.012, 0.45, 6);
    antGeo.rotateX(Math.PI / 2);
    var ant1 = new THREE.Mesh(antGeo, darkMat);
    ant1.position.set(-2.4, 0, 1.25); g.add(ant1);
    var ant2 = new THREE.Mesh(antGeo, darkMat);
    ant2.rotation.y = -0.35;
    ant2.position.set(-4.5, 0, 0.95); g.add(ant2);
    var band = new THREE.Mesh(new THREE.BoxGeometry(0.5, 0.66, 0.95), teamMat);
    band.position.set(-4.5, 0, 0.32); g.add(band);

    return g;
  }
};

/* ---- pack a25b19db ---- */
// ============================================================================
// JETS - procedural Three.js models. +X forward, +Z up, +Y left. Metres.
// Aircraft origin: fuselage centre. No landing gear (rendered airborne).
// ============================================================================

// ---------------------------------------------------------------------------
// MiG-29S Fulcrum-C. len 17.3, span 11.4. Twin widely spaced nacelles under a
// flat lifting centre body, big LERX, twin outward-canted fins, bubble canopy,
// wingtip R-73s + underwing R-77s.
// ---------------------------------------------------------------------------
UNIT_MODELS["fighter_p"] = {
  len: 17.3,
  build: function (THREE, M, C) {
    var r = new THREE.Group();
    var seed = 2901;
    function rnd() { seed = (seed * 16807) % 2147483647; return seed / 2147483647; }
    function paintTex(base, camo) {
      var cv = document.createElement("canvas");
      cv.width = 512; cv.height = 512;
      var g = cv.getContext("2d"), i, p;
      g.fillStyle = base; g.fillRect(0, 0, 512, 512);
      for (i = 0; i < 12; i++) {
        g.globalAlpha = 0.4; g.fillStyle = camo[i % camo.length];
        g.beginPath();
        g.ellipse(rnd() * 512, rnd() * 512, 60 + rnd() * 100, 35 + rnd() * 60, rnd() * 3.14, 0, 6.283);
        g.fill();
      }
      for (i = 0; i < 40; i++) {
        g.globalAlpha = 0.04 + rnd() * 0.04;
        g.fillStyle = rnd() < 0.5 ? "#000000" : "#ffffff";
        g.fillRect(rnd() * 512, rnd() * 512, 30 + rnd() * 100, 18 + rnd() * 70);
      }
      g.globalAlpha = 0.3; g.strokeStyle = "#171b1e"; g.lineWidth = 1.2;
      p = 0; while (p < 512) { p += 30 + rnd() * 55; g.beginPath(); g.moveTo(p, 0); g.lineTo(p, 512); g.stroke(); }
      p = 0; while (p < 512) { p += 36 + rnd() * 64; g.beginPath(); g.moveTo(0, p); g.lineTo(512, p); g.stroke(); }
      g.globalAlpha = 0.09; g.fillStyle = "#0a0d0d";
      for (i = 0; i < 24; i++) { g.fillRect(rnd() * 512, rnd() * 512, 2 + rnd() * 3, 20 + rnd() * 80); }
      var t = new THREE.CanvasTexture(cv);
      t.wrapS = t.wrapT = THREE.RepeatWrapping;
      return t;
    }
    var skin = new THREE.MeshStandardMaterial({ map: paintTex("#7b838b", ["#68707a", "#8e969e"]), metalness: 0.3, roughness: 0.6, side: THREE.DoubleSide });
    var dark = new THREE.MeshStandardMaterial({ color: 0x1e2124, metalness: 0.5, roughness: 0.55, side: THREE.DoubleSide });
    var steel = new THREE.MeshStandardMaterial({ color: 0x8a8f94, metalness: 0.85, roughness: 0.3, side: THREE.DoubleSide });
    var glass = new THREE.MeshPhysicalMaterial({ color: 0x2a4a58, metalness: 0.5, roughness: 0.15, clearcoat: 1.0 });
    var team = new THREE.MeshStandardMaterial({ color: C.team, metalness: 0.3, roughness: 0.6 });
    var store = new THREE.MeshStandardMaterial({ color: 0xcdd2d6, metalness: 0.3, roughness: 0.55 });
    var lampL = new THREE.MeshStandardMaterial({ color: 0xff3333, emissive: 0xdd2222, emissiveIntensity: 0.9 });
    var lampR = new THREE.MeshStandardMaterial({ color: 0x33ff55, emissive: 0x22cc44, emissiveIntensity: 0.9 });
    function put(parent, geo, mat, x, y, z, rx, ry, rz) {
      var m = new THREE.Mesh(geo, mat);
      m.position.set(x, y, z);
      m.rotation.set(rx || 0, ry || 0, rz || 0);
      parent.add(m);
      return m;
    }
    function mir(p) { var o = [], k; for (k = 0; k < p.length; k++) o.push([p[k][0], -p[k][1]]); return o; }
    function aam(parent, len, dia, x, y, z) {
      var mg = new THREE.Group();
      var body = new THREE.Mesh(new THREE.CylinderGeometry(dia / 2, dia / 2, len * 0.8, 10), store);
      body.rotation.set(0, 0, -Math.PI / 2); mg.add(body);
      var nose = new THREE.Mesh(new THREE.ConeGeometry(dia / 2, len * 0.2, 10), store);
      nose.rotation.set(0, 0, -Math.PI / 2); nose.position.set(len * 0.5, 0, 0); mg.add(nose);
      var f1 = new THREE.Mesh(new THREE.BoxGeometry(len * 0.16, dia * 3.6, 0.02), store);
      f1.position.set(-len * 0.33, 0, 0); mg.add(f1);
      var f2 = new THREE.Mesh(new THREE.BoxGeometry(len * 0.16, 0.02, dia * 3.6), store);
      f2.position.set(-len * 0.33, 0, 0); mg.add(f2);
      var f3 = new THREE.Mesh(new THREE.BoxGeometry(len * 0.12, dia * 2.6, 0.02), store);
      f3.position.set(len * 0.14, 0, 0); mg.add(f3);
      mg.position.set(x, y, z);
      parent.add(mg);
      return mg;
    }
    // flat lifting centre body (nose to tail beaver between the nacelles)
    put(r, M.loft(THREE, [
      { x: -8.4, w: 0.6, h: 0.16, zc: 0.55 },
      { x: -6.0, w: 1.35, h: 0.34, zc: 0.5, sq: 0.85 },
      { x: -2.5, w: 1.65, h: 0.5, zc: 0.45, sq: 0.85 },
      { x: 0.8, w: 1.6, h: 0.58, zc: 0.4, sq: 0.8 },
      { x: 3.0, w: 1.15, h: 0.6, zc: 0.38, sq: 0.75 },
      { x: 4.6, w: 0.8, h: 0.62, zc: 0.36 },
      { x: 6.1, w: 0.55, h: 0.55, zc: 0.34 },
      { x: 7.5, w: 0.32, h: 0.34, zc: 0.3 },
      { x: 8.3, w: 0.06, h: 0.06, zc: 0.28 }
    ], 22), skin, 0, 0, 0);
    // tail sting between the nozzles
    put(r, new THREE.ConeGeometry(0.18, 0.6, 12), dark, -8.6, 0, 0.55, 0, 0, Math.PI / 2);
    // nose pitot probe
    put(r, new THREE.CylinderGeometry(0.02, 0.02, 0.7, 6), steel, 8.45, 0, 0.28, 0, 0, -Math.PI / 2);
    // widely spaced underslung engine nacelles
    var si, s;
    for (si = 0; si < 2; si++) {
      s = si === 0 ? 1 : -1;
      var nac = new THREE.Group();
      nac.position.set(0, 1.12 * s, 0);
      nac.add((function () {
        var m = new THREE.Mesh(M.loft(THREE, [
          { x: -7.9, w: 0.38, h: 0.38, zc: -0.15 },
          { x: -6.0, w: 0.46, h: 0.46, zc: -0.18, sq: 0.6 },
          { x: -2.5, w: 0.55, h: 0.52, zc: -0.22, sq: 0.7 },
          { x: 0.8, w: 0.52, h: 0.5, zc: -0.28, sq: 0.65 },
          { x: 2.6, w: 0.46, h: 0.52, zc: -0.32, sq: 0.55 },
          { x: 3.5, w: 0.4, h: 0.5, zc: -0.34, sq: 0.5 }
        ], 18), skin);
        m.position.set(0, 0, 0);
        return m;
      })());
      // angled intake mouth
      put(nac, new THREE.BoxGeometry(0.1, 0.75, 0.92), dark, 3.45, 0, -0.32, 0, 0.18, 0);
      // nozzle + exhaust
      put(nac, new THREE.CylinderGeometry(0.42, 0.34, 0.9, 16), steel, -8.3, 0, -0.15, 0, 0, -Math.PI / 2);
      put(nac, new THREE.CylinderGeometry(0.3, 0.3, 0.06, 16), dark, -8.72, 0, -0.15, 0, 0, -Math.PI / 2);
      r.add(nac);
      // outward-canted twin fin (group carries the cant)
      var fg = new THREE.Group();
      fg.position.set(0, 1.45 * s, 0);
      fg.rotation.set(-0.1 * s, 0, 0);
      var fin = new THREE.Mesh(M.slab(THREE, [[-4.6, 0.3], [-6.5, 3.2], [-7.7, 3.2], [-8.45, 0.3]], 0.09, "xz"), skin);
      fg.add(fin);
      put(fg, new THREE.BoxGeometry(0.9, 0.13, 0.55), team, -7.0, 0, 2.65, 0, 0, 0);
      r.add(fg);
    }
    // main wings, ~40 deg LE sweep, span 11.4
    var wingPts = [[2.3, 1.2], [-0.9, 5.65], [-2.15, 5.65], [-2.7, 1.2]];
    put(r, M.slab(THREE, wingPts, 0.12), skin, 0, 0, 0.1);
    put(r, M.slab(THREE, mir(wingPts), 0.12), skin, 0, 0, 0.1);
    // big LERX shelves, gently pitched up toward the nose
    var lerxPts = [[6.3, 0.45], [4.2, 1.05], [2.3, 1.6], [2.3, 0.75], [5.0, 0.5]];
    put(r, M.slab(THREE, lerxPts, 0.1), skin, 0, 0, 0.55, 0, -0.06, 0);
    put(r, M.slab(THREE, mir(lerxPts), 0.1), skin, 0, 0, 0.55, 0, -0.06, 0);
    // stabilators
    var stabPts = [[-4.9, 1.3], [-6.7, 3.85], [-7.6, 3.85], [-7.15, 1.3]];
    put(r, M.slab(THREE, stabPts, 0.1), skin, 0, 0, 0.4);
    put(r, M.slab(THREE, mir(stabPts), 0.1), skin, 0, 0, 0.4);
    // bubble canopy + frames + IRST ball
    var cn = put(r, new THREE.SphereGeometry(0.5, 20, 14), glass, 5.55, 0, 0.95);
    cn.scale.set(1.9, 0.72, 0.85);
    put(r, new THREE.BoxGeometry(0.06, 0.95, 0.5), dark, 5.0, 0, 1.05);
    put(r, new THREE.BoxGeometry(0.06, 0.8, 0.45), dark, 6.35, 0, 0.95);
    put(r, new THREE.SphereGeometry(0.12, 10, 8), dark, 6.7, -0.15, 0.78);
    // GSh-301 gun fairing on the left LERX
    put(r, new THREE.BoxGeometry(0.7, 0.12, 0.1), dark, 4.9, 0.72, 0.5);
    // spine blade antennas
    put(r, new THREE.BoxGeometry(0.28, 0.03, 0.2), dark, 2.5, 0, 1.02);
    put(r, new THREE.BoxGeometry(0.28, 0.03, 0.2), dark, -3.0, 0, 0.98);
    // wingtip rails + R-73s, underwing pylons + R-77s
    for (si = 0; si < 2; si++) {
      s = si === 0 ? 1 : -1;
      put(r, new THREE.BoxGeometry(1.6, 0.06, 0.1), skin, -0.8, 5.66 * s, 0.2);
      aam(r, 2.9, 0.17, -0.8, 5.66 * s, 0.05);
      put(r, new THREE.BoxGeometry(0.9, 0.09, 0.28), skin, -0.4, 3.0 * s, -0.05);
      aam(r, 3.6, 0.2, -0.4, 3.0 * s, -0.3);
      put(r, new THREE.BoxGeometry(0.9, 0.09, 0.28), skin, -0.8, 4.3 * s, -0.05);
      aam(r, 3.6, 0.2, -0.8, 4.3 * s, -0.3);
      // wing roundels
      put(r, new THREE.CylinderGeometry(0.32, 0.32, 0.03, 16), team, -1.2, 3.4 * s, 0.22, Math.PI / 2, 0, 0);
    }
    // nav lights (left red / right green)
    put(r, new THREE.SphereGeometry(0.05, 8, 6), lampL, -0.75, 5.62, 0.16);
    put(r, new THREE.SphereGeometry(0.05, 8, 6), lampR, -0.75, -5.62, 0.16);
    return r;
  }
};

// ---------------------------------------------------------------------------
// J-10C Vigorous Dragon. len 16.9, span 9.75. Tailless delta + canards, single
// tall fin, chin intake with splitter and DSI bump, bubble canopy, PL-15s.
// ---------------------------------------------------------------------------
UNIT_MODELS["fighter_c"] = {
  len: 16.9,
  build: function (THREE, M, C) {
    var r = new THREE.Group();
    var seed = 1013;
    function rnd() { seed = (seed * 16807) % 2147483647; return seed / 2147483647; }
    function paintTex(base, camo) {
      var cv = document.createElement("canvas");
      cv.width = 512; cv.height = 512;
      var g = cv.getContext("2d"), i, p;
      g.fillStyle = base; g.fillRect(0, 0, 512, 512);
      for (i = 0; i < 12; i++) {
        g.globalAlpha = 0.4; g.fillStyle = camo[i % camo.length];
        g.beginPath();
        g.ellipse(rnd() * 512, rnd() * 512, 60 + rnd() * 100, 35 + rnd() * 60, rnd() * 3.14, 0, 6.283);
        g.fill();
      }
      for (i = 0; i < 40; i++) {
        g.globalAlpha = 0.04 + rnd() * 0.04;
        g.fillStyle = rnd() < 0.5 ? "#000000" : "#ffffff";
        g.fillRect(rnd() * 512, rnd() * 512, 30 + rnd() * 100, 18 + rnd() * 70);
      }
      g.globalAlpha = 0.3; g.strokeStyle = "#171b1e"; g.lineWidth = 1.2;
      p = 0; while (p < 512) { p += 30 + rnd() * 55; g.beginPath(); g.moveTo(p, 0); g.lineTo(p, 512); g.stroke(); }
      p = 0; while (p < 512) { p += 36 + rnd() * 64; g.beginPath(); g.moveTo(0, p); g.lineTo(512, p); g.stroke(); }
      g.globalAlpha = 0.09; g.fillStyle = "#0a0d0d";
      for (i = 0; i < 24; i++) { g.fillRect(rnd() * 512, rnd() * 512, 2 + rnd() * 3, 20 + rnd() * 80); }
      var t = new THREE.CanvasTexture(cv);
      t.wrapS = t.wrapT = THREE.RepeatWrapping;
      return t;
    }
    var skin = new THREE.MeshStandardMaterial({ map: paintTex("#7f868d", ["#6d747c", "#929aa1"]), metalness: 0.3, roughness: 0.6, side: THREE.DoubleSide });
    var dark = new THREE.MeshStandardMaterial({ color: 0x1e2124, metalness: 0.5, roughness: 0.55, side: THREE.DoubleSide });
    var steel = new THREE.MeshStandardMaterial({ color: 0x8a8f94, metalness: 0.85, roughness: 0.3, side: THREE.DoubleSide });
    var glass = new THREE.MeshPhysicalMaterial({ color: 0x2a4a58, metalness: 0.5, roughness: 0.15, clearcoat: 1.0 });
    var team = new THREE.MeshStandardMaterial({ color: C.team, metalness: 0.3, roughness: 0.6 });
    var store = new THREE.MeshStandardMaterial({ color: 0xcdd2d6, metalness: 0.3, roughness: 0.55 });
    var lampL = new THREE.MeshStandardMaterial({ color: 0xff3333, emissive: 0xdd2222, emissiveIntensity: 0.9 });
    var lampR = new THREE.MeshStandardMaterial({ color: 0x33ff55, emissive: 0x22cc44, emissiveIntensity: 0.9 });
    function put(parent, geo, mat, x, y, z, rx, ry, rz) {
      var m = new THREE.Mesh(geo, mat);
      m.position.set(x, y, z);
      m.rotation.set(rx || 0, ry || 0, rz || 0);
      parent.add(m);
      return m;
    }
    function mir(p) { var o = [], k; for (k = 0; k < p.length; k++) o.push([p[k][0], -p[k][1]]); return o; }
    function aam(parent, len, dia, x, y, z) {
      var mg = new THREE.Group();
      var body = new THREE.Mesh(new THREE.CylinderGeometry(dia / 2, dia / 2, len * 0.8, 10), store);
      body.rotation.set(0, 0, -Math.PI / 2); mg.add(body);
      var nose = new THREE.Mesh(new THREE.ConeGeometry(dia / 2, len * 0.2, 10), store);
      nose.rotation.set(0, 0, -Math.PI / 2); nose.position.set(len * 0.5, 0, 0); mg.add(nose);
      var f1 = new THREE.Mesh(new THREE.BoxGeometry(len * 0.14, dia * 3.2, 0.02), store);
      f1.position.set(-len * 0.34, 0, 0); mg.add(f1);
      var f2 = new THREE.Mesh(new THREE.BoxGeometry(len * 0.14, 0.02, dia * 3.2), store);
      f2.position.set(-len * 0.34, 0, 0); mg.add(f2);
      mg.position.set(x, y, z);
      parent.add(mg);
      return mg;
    }
    // fuselage
    put(r, M.loft(THREE, [
      { x: -8.35, w: 0.46, h: 0.46, zc: 0.05 },
      { x: -6.5, w: 0.68, h: 0.6, zc: 0.08, sq: 0.7 },
      { x: -3.5, w: 0.88, h: 0.78, zc: 0.05, sq: 0.8 },
      { x: 0.5, w: 0.95, h: 0.88, zc: 0, sq: 0.8 },
      { x: 3.2, w: 0.8, h: 0.82, zc: 0.02, sq: 0.7 },
      { x: 5.0, w: 0.6, h: 0.66, zc: 0.08 },
      { x: 6.6, w: 0.42, h: 0.48, zc: 0.1 },
      { x: 8.2, w: 0.05, h: 0.05, zc: 0.12 }
    ], 22), skin, 0, 0, 0);
    // nozzle with afterburner petals
    put(r, new THREE.CylinderGeometry(0.46, 0.36, 0.7, 18), steel, -8.35, 0, 0.05, 0, 0, -Math.PI / 2);
    put(r, new THREE.CylinderGeometry(0.32, 0.32, 0.06, 18), dark, -8.68, 0, 0.05, 0, 0, -Math.PI / 2);
    // nose pitot probe
    put(r, new THREE.CylinderGeometry(0.022, 0.022, 0.6, 6), steel, 8.45, 0, 0.12, 0, 0, -Math.PI / 2);
    // chin intake duct with splitter plate + DSI bump
    put(r, M.loft(THREE, [
      { x: 1.2, w: 0.5, h: 0.3, zc: -0.8 },
      { x: 3.4, w: 0.54, h: 0.34, zc: -0.86, sq: 0.6 },
      { x: 4.8, w: 0.5, h: 0.32, zc: -0.88, sq: 0.55 }
    ], 16), skin, 0, 0, 0);
    put(r, new THREE.BoxGeometry(0.08, 0.9, 0.56), dark, 4.78, 0, -0.88);
    put(r, new THREE.BoxGeometry(0.7, 0.75, 0.04), dark, 4.3, 0, -0.56);
    var bump = put(r, new THREE.SphereGeometry(0.22, 12, 10), skin, 4.7, 0, -0.62);
    bump.scale.set(1.5, 1.1, 0.7);
    // delta wing, ~50 deg LE sweep
    var wingPts = [[2.8, 0.9], [-2.6, 4.85], [-3.5, 4.85], [-4.4, 0.9]];
    put(r, M.slab(THREE, wingPts, 0.13), skin, 0, 0, -0.15);
    put(r, M.slab(THREE, mir(wingPts), 0.13), skin, 0, 0, -0.15);
    // canards with dihedral
    var canPts = [[5.2, 0.7], [4.2, 2.0], [3.6, 2.0], [3.7, 0.7]];
    put(r, M.slab(THREE, canPts, 0.09), skin, 0, 0, 0.3, 0.15, 0, 0);
    put(r, M.slab(THREE, mir(canPts), 0.09), skin, 0, 0, 0.3, -0.15, 0, 0);
    // single tall fin + tip ECM pod + team stripe
    put(r, M.slab(THREE, [[-3.2, 0.6], [-5.6, 3.7], [-6.7, 3.7], [-7.4, 0.6]], 0.09, "xz"), skin, 0, 0, 0);
    put(r, new THREE.CylinderGeometry(0.08, 0.08, 1.0, 10), skin, -6.1, 0, 3.7, 0, 0, -Math.PI / 2);
    put(r, new THREE.BoxGeometry(0.8, 0.13, 0.7), team, -6.3, 0, 2.95);
    // canted ventral strakes
    var vsPts = [[-4.6, -0.6], [-5.1, -1.25], [-6.2, -1.25], [-6.5, -0.6]];
    put(r, M.slab(THREE, vsPts, 0.07, "xz"), skin, 0, 0.4, 0, 0.35, 0, 0);
    put(r, M.slab(THREE, vsPts, 0.07, "xz"), skin, 0, -0.4, 0, -0.35, 0, 0);
    // bubble canopy + frames
    var cn = put(r, new THREE.SphereGeometry(0.5, 20, 14), glass, 5.4, 0, 0.8);
    cn.scale.set(1.9, 0.8, 0.85);
    put(r, new THREE.BoxGeometry(0.06, 0.9, 0.5), dark, 4.8, 0, 0.9);
    put(r, new THREE.BoxGeometry(0.06, 0.75, 0.45), dark, 6.2, 0, 0.8);
    // spine blade antennas + AoA vanes
    put(r, new THREE.BoxGeometry(0.28, 0.03, 0.2), dark, 2.0, 0, 0.95);
    put(r, new THREE.BoxGeometry(0.28, 0.03, 0.18), dark, -2.5, 0, 0.92);
    put(r, new THREE.CylinderGeometry(0.015, 0.015, 0.3, 6), steel, 7.3, 0.3, 0.15);
    // PL-15s on underwing pylons
    var si, s;
    for (si = 0; si < 2; si++) {
      s = si === 0 ? 1 : -1;
      put(r, new THREE.BoxGeometry(0.9, 0.09, 0.3), skin, -0.8, 1.9 * s, -0.35);
      aam(r, 4.0, 0.2, -0.8, 1.9 * s, -0.55);
      put(r, new THREE.BoxGeometry(0.9, 0.09, 0.3), skin, -1.4, 3.1 * s, -0.35);
      aam(r, 4.0, 0.2, -1.4, 3.1 * s, -0.55);
      // wing roundels
      put(r, new THREE.CylinderGeometry(0.32, 0.32, 0.03, 16), team, -2.4, 3.2 * s, -0.03, Math.PI / 2, 0, 0);
    }
    // nav lights
    put(r, new THREE.SphereGeometry(0.05, 8, 6), lampL, -3.0, 4.8, -0.1);
    put(r, new THREE.SphereGeometry(0.05, 8, 6), lampR, -3.0, -4.8, -0.1);
    return r;
  }
};

// ---------------------------------------------------------------------------
// A-10C Thunderbolt II. len 16.3, span 17.5. Straight wide wings with drooped
// tips, two TF34 pods high on the rear fuselage, twin tail fins, GAU-8 muzzle
// at the nose tip, underwing gear pods, 6 Mk-82s.
// ---------------------------------------------------------------------------
UNIT_MODELS["bomber_n"] = {
  len: 16.3,
  build: function (THREE, M, C) {
    var r = new THREE.Group();
    var seed = 1077;
    function rnd() { seed = (seed * 16807) % 2147483647; return seed / 2147483647; }
    function paintTex(base, camo) {
      var cv = document.createElement("canvas");
      cv.width = 512; cv.height = 512;
      var g = cv.getContext("2d"), i, p;
      g.fillStyle = base; g.fillRect(0, 0, 512, 512);
      for (i = 0; i < 12; i++) {
        g.globalAlpha = 0.4; g.fillStyle = camo[i % camo.length];
        g.beginPath();
        g.ellipse(rnd() * 512, rnd() * 512, 60 + rnd() * 100, 35 + rnd() * 60, rnd() * 3.14, 0, 6.283);
        g.fill();
      }
      for (i = 0; i < 40; i++) {
        g.globalAlpha = 0.04 + rnd() * 0.04;
        g.fillStyle = rnd() < 0.5 ? "#000000" : "#ffffff";
        g.fillRect(rnd() * 512, rnd() * 512, 30 + rnd() * 100, 18 + rnd() * 70);
      }
      g.globalAlpha = 0.3; g.strokeStyle = "#171b1e"; g.lineWidth = 1.2;
      p = 0; while (p < 512) { p += 30 + rnd() * 55; g.beginPath(); g.moveTo(p, 0); g.lineTo(p, 512); g.stroke(); }
      p = 0; while (p < 512) { p += 36 + rnd() * 64; g.beginPath(); g.moveTo(0, p); g.lineTo(512, p); g.stroke(); }
      g.globalAlpha = 0.09; g.fillStyle = "#0a0d0d";
      for (i = 0; i < 24; i++) { g.fillRect(rnd() * 512, rnd() * 512, 2 + rnd() * 3, 20 + rnd() * 80); }
      var t = new THREE.CanvasTexture(cv);
      t.wrapS = t.wrapT = THREE.RepeatWrapping;
      return t;
    }
    var skin = new THREE.MeshStandardMaterial({ map: paintTex("#757c81", ["#616970", "#838b91"]), metalness: 0.25, roughness: 0.65, side: THREE.DoubleSide });
    var dark = new THREE.MeshStandardMaterial({ color: 0x1e2124, metalness: 0.5, roughness: 0.55, side: THREE.DoubleSide });
    var steel = new THREE.MeshStandardMaterial({ color: 0x8a8f94, metalness: 0.85, roughness: 0.3, side: THREE.DoubleSide });
    var glass = new THREE.MeshPhysicalMaterial({ color: 0x2a4a58, metalness: 0.5, roughness: 0.15, clearcoat: 1.0 });
    var team = new THREE.MeshStandardMaterial({ color: C.team, metalness: 0.3, roughness: 0.6 });
    var olive = new THREE.MeshStandardMaterial({ color: 0x4a5240, metalness: 0.25, roughness: 0.6 });
    var lampL = new THREE.MeshStandardMaterial({ color: 0xff3333, emissive: 0xdd2222, emissiveIntensity: 0.9 });
    var lampR = new THREE.MeshStandardMaterial({ color: 0x33ff55, emissive: 0x22cc44, emissiveIntensity: 0.9 });
    function put(parent, geo, mat, x, y, z, rx, ry, rz) {
      var m = new THREE.Mesh(geo, mat);
      m.position.set(x, y, z);
      m.rotation.set(rx || 0, ry || 0, rz || 0);
      parent.add(m);
      return m;
    }
    function mir(p) { var o = [], k; for (k = 0; k < p.length; k++) o.push([p[k][0], -p[k][1]]); return o; }
    function bomb(parent, x, y, z) {
      var b = new THREE.Group();
      var bod = new THREE.Mesh(new THREE.CylinderGeometry(0.14, 0.14, 1.3, 10), olive);
      bod.rotation.set(0, 0, -Math.PI / 2); b.add(bod);
      var no = new THREE.Mesh(new THREE.SphereGeometry(0.14, 10, 8), olive);
      no.position.set(0.65, 0, 0); no.scale.set(2.2, 1, 1); b.add(no);
      var tl = new THREE.Mesh(new THREE.CylinderGeometry(0.1, 0.14, 0.6, 10), olive);
      tl.rotation.set(0, 0, Math.PI / 2); tl.position.set(-0.9, 0, 0); b.add(tl);
      var f1 = new THREE.Mesh(new THREE.BoxGeometry(0.35, 0.5, 0.02), olive);
      f1.position.set(-1.1, 0, 0); b.add(f1);
      var f2 = new THREE.Mesh(new THREE.BoxGeometry(0.35, 0.02, 0.5), olive);
      f2.position.set(-1.1, 0, 0); b.add(f2);
      b.position.set(x, y, z);
      parent.add(b);
      return b;
    }
    // deep slab-sided fuselage
    put(r, M.loft(THREE, [
      { x: -8.1, w: 0.22, h: 0.28, zc: 0.3 },
      { x: -6.6, w: 0.45, h: 0.52, zc: 0.22 },
      { x: -4.0, w: 0.68, h: 0.72, zc: 0.1, sq: 0.7 },
      { x: -0.5, w: 0.85, h: 0.88, zc: 0, sq: 0.8 },
      { x: 3.0, w: 0.85, h: 0.9, zc: 0, sq: 0.75 },
      { x: 5.3, w: 0.78, h: 0.85, zc: 0.02, sq: 0.7 },
      { x: 6.9, w: 0.55, h: 0.62, zc: 0 },
      { x: 8.1, w: 0.16, h: 0.2, zc: -0.05 }
    ], 22), skin, 0, 0, 0);
    // GAU-8: rotary housing + protruding muzzle at the nose tip
    put(r, new THREE.CylinderGeometry(0.17, 0.17, 0.6, 12), dark, 7.75, 0, -0.12, 0, 0, -Math.PI / 2);
    put(r, new THREE.CylinderGeometry(0.09, 0.09, 0.7, 10), dark, 8.15, 0, -0.12, 0, 0, -Math.PI / 2);
    put(r, new THREE.CylinderGeometry(0.12, 0.12, 0.08, 10), steel, 8.45, 0, -0.12, 0, 0, -Math.PI / 2);
    // bubble canopy high over the nose + windscreen
    var cn = put(r, new THREE.SphereGeometry(0.6, 20, 14), glass, 5.75, 0, 1.05);
    cn.scale.set(1.4, 0.75, 0.8);
    put(r, new THREE.BoxGeometry(0.06, 1.0, 0.55), dark, 5.0, 0, 1.1);
    put(r, new THREE.BoxGeometry(0.7, 0.85, 0.06), dark, 6.65, 0, 0.85, 0, -0.75, 0);
    // straight constant-chord wing, low mounted
    put(r, M.slab(THREE, [[0.9, -7.4], [0.9, 7.4], [-1.8, 7.4], [-1.8, -7.4]], 0.16), skin, 0, 0, -0.55);
    // drooped wingtips (local coords, hinged at the tip station)
    var si, s;
    var tipPts = [[0.8, 0], [0.75, 1.35], [-1.4, 1.35], [-1.5, 0]];
    for (si = 0; si < 2; si++) {
      s = si === 0 ? 1 : -1;
      var tp = new THREE.Group();
      tp.position.set(0, 7.38 * s, -0.55);
      tp.rotation.set(-0.4 * s, 0, 0);
      var tm = new THREE.Mesh(M.slab(THREE, si === 0 ? tipPts : mir(tipPts), 0.12), skin);
      tp.add(tm);
      r.add(tp);
    }
    // TF34 pods high on the rear fuselage
    for (si = 0; si < 2; si++) {
      s = si === 0 ? 1 : -1;
      var pod = new THREE.Group();
      pod.position.set(-4.4, 1.6 * s, 1.4);
      var nc = new THREE.Mesh(new THREE.CylinderGeometry(0.63, 0.63, 2.4, 18), skin);
      nc.rotation.set(0, 0, -Math.PI / 2); pod.add(nc);
      var lip = new THREE.Mesh(new THREE.CylinderGeometry(0.56, 0.56, 0.1, 18), dark);
      lip.rotation.set(0, 0, -Math.PI / 2); lip.position.set(1.22, 0, 0); pod.add(lip);
      var spin = new THREE.Mesh(new THREE.ConeGeometry(0.18, 0.3, 12), steel);
      spin.rotation.set(0, 0, -Math.PI / 2); spin.position.set(1.35, 0, 0); pod.add(spin);
      var ex = new THREE.Mesh(new THREE.CylinderGeometry(0.55, 0.3, 0.9, 16), steel);
      ex.rotation.set(0, 0, -Math.PI / 2); ex.position.set(-1.6, 0, 0); pod.add(ex);
      r.add(pod);
      // slanted pylon strut into the fuselage shoulder
      put(r, new THREE.BoxGeometry(1.4, 0.16, 0.9), skin, -4.3, 1.25 * s, 1.0, -0.6 * s, 0, 0);
    }
    // tailplane + twin endplate fins with team bands
    put(r, M.slab(THREE, [[-6.3, -2.9], [-6.3, 2.9], [-8.25, 2.9], [-8.25, -2.9]], 0.14), skin, 0, 0, 0.35);
    for (si = 0; si < 2; si++) {
      s = si === 0 ? 1 : -1;
      put(r, M.slab(THREE, [[-6.35, 0.4], [-6.5, 2.3], [-8.3, 2.3], [-8.3, 0.4]], 0.1, "xz"), skin, 0, 2.85 * s, 0);
      put(r, new THREE.BoxGeometry(0.7, 0.13, 1.0), team, -7.35, 2.85 * s, 1.35);
    }
    // underwing main-gear pods with half-exposed wheels
    for (si = 0; si < 2; si++) {
      s = si === 0 ? 1 : -1;
      put(r, new THREE.CylinderGeometry(0.34, 0.34, 1.6, 14), skin, 0.6, 2.5 * s, -0.85, 0, 0, -Math.PI / 2);
      var cap = put(r, new THREE.SphereGeometry(0.34, 12, 10), skin, 1.4, 2.5 * s, -0.85);
      cap.scale.set(1.3, 1, 1);
      put(r, new THREE.CylinderGeometry(0.28, 0.28, 0.2, 14), dark, 0.5, 2.5 * s, -1.0);
    }
    // six Mk-82s on underwing pylons
    for (si = 0; si < 2; si++) {
      s = si === 0 ? 1 : -1;
      var ys = [3.4, 4.6, 5.8], yi;
      for (yi = 0; yi < 3; yi++) {
        put(r, new THREE.BoxGeometry(0.7, 0.1, 0.3), skin, -0.3, ys[yi] * s, -0.78);
        bomb(r, -0.3, ys[yi] * s, -1.05);
      }
    }
    // antennas + AoA probe
    put(r, new THREE.BoxGeometry(0.28, 0.03, 0.2), dark, 2.0, 0, 0.95);
    put(r, new THREE.BoxGeometry(0.28, 0.03, 0.2), dark, -3.0, 0, 0.9);
    put(r, new THREE.BoxGeometry(0.2, 0.03, 0.16), dark, 6.5, 0, -0.85);
    put(r, new THREE.CylinderGeometry(0.015, 0.015, 0.5, 6), steel, 7.6, -0.4, 0.15, 0, 0, -Math.PI / 2);
    // nav lights on the main wingtips
    put(r, new THREE.SphereGeometry(0.05, 8, 6), lampL, 0.7, 7.3, -0.5);
    put(r, new THREE.SphereGeometry(0.05, 8, 6), lampR, 0.7, -7.3, -0.5);
    return r;
  }
};

// ---------------------------------------------------------------------------
// Su-25SM3 Frogfoot. len 15.5, span 14.4. Straight tapered wings with wingtip
// airbrake fairings, engines faired along the fuselage sides, single fin,
// armoured cockpit tub, rocket pods + bombs.
// ---------------------------------------------------------------------------
UNIT_MODELS["bomber_p"] = {
  len: 15.5,
  build: function (THREE, M, C) {
    var r = new THREE.Group();
    var seed = 2555;
    function rnd() { seed = (seed * 16807) % 2147483647; return seed / 2147483647; }
    function paintTex(base, camo) {
      var cv = document.createElement("canvas");
      cv.width = 512; cv.height = 512;
      var g = cv.getContext("2d"), i, p;
      g.fillStyle = base; g.fillRect(0, 0, 512, 512);
      for (i = 0; i < 12; i++) {
        g.globalAlpha = 0.4; g.fillStyle = camo[i % camo.length];
        g.beginPath();
        g.ellipse(rnd() * 512, rnd() * 512, 60 + rnd() * 100, 35 + rnd() * 60, rnd() * 3.14, 0, 6.283);
        g.fill();
      }
      for (i = 0; i < 40; i++) {
        g.globalAlpha = 0.04 + rnd() * 0.04;
        g.fillStyle = rnd() < 0.5 ? "#000000" : "#ffffff";
        g.fillRect(rnd() * 512, rnd() * 512, 30 + rnd() * 100, 18 + rnd() * 70);
      }
      g.globalAlpha = 0.3; g.strokeStyle = "#171b1e"; g.lineWidth = 1.2;
      p = 0; while (p < 512) { p += 30 + rnd() * 55; g.beginPath(); g.moveTo(p, 0); g.lineTo(p, 512); g.stroke(); }
      p = 0; while (p < 512) { p += 36 + rnd() * 64; g.beginPath(); g.moveTo(0, p); g.lineTo(512, p); g.stroke(); }
      g.globalAlpha = 0.09; g.fillStyle = "#0a0d0d";
      for (i = 0; i < 24; i++) { g.fillRect(rnd() * 512, rnd() * 512, 2 + rnd() * 3, 20 + rnd() * 80); }
      var t = new THREE.CanvasTexture(cv);
      t.wrapS = t.wrapT = THREE.RepeatWrapping;
      return t;
    }
    var skin = new THREE.MeshStandardMaterial({ map: paintTex("#7d838a", ["#646b73", "#90969d"]), metalness: 0.25, roughness: 0.65, side: THREE.DoubleSide });
    var dark = new THREE.MeshStandardMaterial({ color: 0x1e2124, metalness: 0.5, roughness: 0.55, side: THREE.DoubleSide });
    var steel = new THREE.MeshStandardMaterial({ color: 0x8a8f94, metalness: 0.85, roughness: 0.3, side: THREE.DoubleSide });
    var glass = new THREE.MeshPhysicalMaterial({ color: 0x2a4a58, metalness: 0.5, roughness: 0.15, clearcoat: 1.0 });
    var team = new THREE.MeshStandardMaterial({ color: C.team, metalness: 0.3, roughness: 0.6 });
    var olive = new THREE.MeshStandardMaterial({ color: 0x4a5240, metalness: 0.25, roughness: 0.6 });
    var lampL = new THREE.MeshStandardMaterial({ color: 0xff3333, emissive: 0xdd2222, emissiveIntensity: 0.9 });
    var lampR = new THREE.MeshStandardMaterial({ color: 0x33ff55, emissive: 0x22cc44, emissiveIntensity: 0.9 });
    function put(parent, geo, mat, x, y, z, rx, ry, rz) {
      var m = new THREE.Mesh(geo, mat);
      m.position.set(x, y, z);
      m.rotation.set(rx || 0, ry || 0, rz || 0);
      parent.add(m);
      return m;
    }
    function mir(p) { var o = [], k; for (k = 0; k < p.length; k++) o.push([p[k][0], -p[k][1]]); return o; }
    // fuselage
    put(r, M.loft(THREE, [
      { x: -7.6, w: 0.24, h: 0.26, zc: 0.18 },
      { x: -5.5, w: 0.5, h: 0.5, zc: 0.12 },
      { x: -2.0, w: 0.66, h: 0.62, zc: 0.05, sq: 0.75 },
      { x: 1.5, w: 0.7, h: 0.66, zc: 0.05, sq: 0.75 },
      { x: 3.6, w: 0.66, h: 0.66, zc: 0.08, sq: 0.6 },
      { x: 5.4, w: 0.55, h: 0.6, zc: 0.12 },
      { x: 6.6, w: 0.35, h: 0.4, zc: 0.1 },
      { x: 7.6, w: 0.06, h: 0.07, zc: 0.05 }
    ], 22), skin, 0, 0, 0);
    // Klen laser window nose cap + brake-chute cone at the tail
    put(r, new THREE.CylinderGeometry(0.16, 0.16, 0.06, 12), dark, 7.62, 0, 0.06, 0, 0, -Math.PI / 2);
    put(r, new THREE.ConeGeometry(0.2, 0.6, 12), dark, -7.85, 0, 0.18, 0, 0, Math.PI / 2);
    // engines faired along the fuselage sides
    var si, s;
    for (si = 0; si < 2; si++) {
      s = si === 0 ? 1 : -1;
      var nac = new THREE.Group();
      nac.position.set(0, 1.0 * s, 0);
      nac.add((function () {
        var m = new THREE.Mesh(M.loft(THREE, [
          { x: -6.2, w: 0.36, h: 0.36, zc: -0.05 },
          { x: -4.0, w: 0.44, h: 0.44, zc: -0.05, sq: 0.6 },
          { x: -0.5, w: 0.48, h: 0.46, zc: -0.05, sq: 0.65 },
          { x: 2.0, w: 0.44, h: 0.44, zc: -0.05, sq: 0.6 },
          { x: 3.3, w: 0.38, h: 0.42, zc: -0.05 }
        ], 16), skin);
        return m;
      })());
      put(nac, new THREE.CylinderGeometry(0.3, 0.3, 0.08, 14), dark, 3.32, 0, -0.05, 0, 0, -Math.PI / 2);
      put(nac, new THREE.CylinderGeometry(0.34, 0.3, 0.7, 14), steel, -6.5, 0, -0.05, 0, 0, -Math.PI / 2);
      put(nac, new THREE.CylinderGeometry(0.26, 0.26, 0.06, 14), dark, -6.82, 0, -0.05, 0, 0, -Math.PI / 2);
      r.add(nac);
    }
    // armoured cockpit tub (angular titanium bathtub) + flat-plate canopy
    put(r, new THREE.BoxGeometry(2.0, 1.34, 0.7), skin, 5.0, 0, 0.55);
    var cn = put(r, new THREE.SphereGeometry(0.45, 18, 12), glass, 5.0, 0, 1.05);
    cn.scale.set(1.5, 0.8, 0.7);
    put(r, new THREE.BoxGeometry(0.7, 0.7, 0.06), dark, 5.9, 0, 0.95, 0, -0.85, 0);
    put(r, new THREE.BoxGeometry(0.06, 0.8, 0.4), dark, 4.35, 0, 1.05);
    // twin-barrel GSh-30-2 under the nose (offset to port)
    put(r, new THREE.BoxGeometry(0.8, 0.28, 0.18), dark, 6.2, 0.25, -0.35);
    put(r, new THREE.CylinderGeometry(0.035, 0.035, 0.7, 8), dark, 6.75, 0.19, -0.33, 0, 0, -Math.PI / 2);
    put(r, new THREE.CylinderGeometry(0.035, 0.035, 0.7, 8), dark, 6.75, 0.31, -0.33, 0, 0, -Math.PI / 2);
    // straight tapered shoulder wing with slight anhedral
    var wingPts = [[1.9, 0.85], [-0.4, 7.05], [-1.7, 7.05], [-1.6, 0.85]];
    put(r, M.slab(THREE, wingPts, 0.14), skin, 0, 0, 0.55, -0.04, 0, 0);
    put(r, M.slab(THREE, mir(wingPts), 0.14), skin, 0, 0, 0.55, 0.04, 0, 0);
    // wingtip airbrake fairing pods
    for (si = 0; si < 2; si++) {
      s = si === 0 ? 1 : -1;
      var tipz = 0.55 - 0.04 * 7.05;
      put(r, new THREE.BoxGeometry(2.3, 0.34, 0.3), skin, -0.5, 7.1 * s, tipz);
      var capf = put(r, new THREE.SphereGeometry(0.17, 10, 8), skin, 0.65, 7.1 * s, tipz);
      capf.scale.set(2.0, 1.0, 0.88);
      put(r, new THREE.BoxGeometry(0.06, 0.3, 0.28), dark, -1.66, 7.1 * s, tipz);
      // nav light on each pod nose
      put(r, new THREE.SphereGeometry(0.05, 8, 6), si === 0 ? lampL : lampR, 0.95, 7.1 * s, tipz);
    }
    // single fin with team stripe + tailplane
    put(r, M.slab(THREE, [[-4.7, 0.4], [-6.1, 2.85], [-7.05, 2.85], [-7.5, 0.4]], 0.1, "xz"), skin, 0, 0, 0);
    put(r, new THREE.BoxGeometry(0.75, 0.13, 0.6), team, -6.6, 0, 2.45);
    var stabPts = [[-5.7, 0.3], [-6.5, 2.55], [-7.4, 2.55], [-7.1, 0.3]];
    put(r, M.slab(THREE, stabPts, 0.1), skin, 0, 0, 0.55);
    put(r, M.slab(THREE, mir(stabPts), 0.1), skin, 0, 0, 0.55);
    // SM3 satnav hump + spine antennas + twin nose probes
    put(r, new THREE.BoxGeometry(0.8, 0.4, 0.18), skin, 3.8, 0, 0.82);
    put(r, new THREE.BoxGeometry(0.28, 0.03, 0.2), dark, 1.5, 0, 0.8);
    put(r, new THREE.BoxGeometry(0.28, 0.03, 0.18), dark, -3.5, 0, 0.72);
    put(r, new THREE.CylinderGeometry(0.02, 0.02, 0.7, 6), steel, 7.9, 0, 0.12, 0, 0, -Math.PI / 2);
    put(r, new THREE.CylinderGeometry(0.014, 0.014, 0.5, 6), steel, 7.7, 0.18, 0.22, 0, 0, -Math.PI / 2);
    // stores: FAB-250 inboard, B-8 rocket pods mid + outboard
    for (si = 0; si < 2; si++) {
      s = si === 0 ? 1 : -1;
      // inboard bomb
      put(r, new THREE.BoxGeometry(0.8, 0.1, 0.4), skin, 0.2, 2.3 * s, 0.26);
      var b = new THREE.Group();
      b.position.set(0.2, 2.3 * s, -0.08);
      var bod = new THREE.Mesh(new THREE.CylinderGeometry(0.18, 0.18, 1.5, 10), olive);
      bod.rotation.set(0, 0, -Math.PI / 2); b.add(bod);
      var no = new THREE.Mesh(new THREE.SphereGeometry(0.18, 10, 8), olive);
      no.position.set(0.75, 0, 0); no.scale.set(2.0, 1, 1); b.add(no);
      var tl = new THREE.Mesh(new THREE.CylinderGeometry(0.12, 0.18, 0.6, 10), olive);
      tl.rotation.set(0, 0, Math.PI / 2); tl.position.set(-1.0, 0, 0); b.add(tl);
      var bf1 = new THREE.Mesh(new THREE.BoxGeometry(0.4, 0.6, 0.02), olive);
      bf1.position.set(-1.2, 0, 0); b.add(bf1);
      var bf2 = new THREE.Mesh(new THREE.BoxGeometry(0.4, 0.02, 0.6), olive);
      bf2.position.set(-1.2, 0, 0); b.add(bf2);
      r.add(b);
      // rocket pods
      var ys = [3.7, 5.1], yi;
      for (yi = 0; yi < 2; yi++) {
        var wz = 0.55 - 0.04 * ys[yi];
        put(r, new THREE.BoxGeometry(0.7, 0.1, 0.32), skin, -0.2, ys[yi] * s, wz - 0.22);
        put(r, new THREE.CylinderGeometry(0.19, 0.19, 1.7, 12), skin, -0.2, ys[yi] * s, wz - 0.5, 0, 0, -Math.PI / 2);
        put(r, new THREE.CylinderGeometry(0.17, 0.17, 0.06, 12), dark, 0.68, ys[yi] * s, wz - 0.5, 0, 0, -Math.PI / 2);
        put(r, new THREE.ConeGeometry(0.19, 0.3, 12), skin, -1.2, ys[yi] * s, wz - 0.5, 0, 0, Math.PI / 2);
      }
      // wing roundels
      put(r, new THREE.CylinderGeometry(0.32, 0.32, 0.03, 16), team, 0.1, 4.4 * s, 0.47 - 0.04 * 4.4, Math.PI / 2, 0, 0);
    }
    return r;
  }
};

// ---------------------------------------------------------------------------
// JH-7A Flying Leopard. len 22.3, span 12.7. Long slim fuselage, high swept
// shoulder wings with anhedral, tall single fin, tandem twin canopy, ventral
// fin, YJ-83K anti-ship missiles + PL-5s.
// ---------------------------------------------------------------------------
UNIT_MODELS["bomber_c"] = {
  len: 22.3,
  build: function (THREE, M, C) {
    var r = new THREE.Group();
    var seed = 787;
    function rnd() { seed = (seed * 16807) % 2147483647; return seed / 2147483647; }
    function paintTex(base, camo) {
      var cv = document.createElement("canvas");
      cv.width = 512; cv.height = 512;
      var g = cv.getContext("2d"), i, p;
      g.fillStyle = base; g.fillRect(0, 0, 512, 512);
      for (i = 0; i < 12; i++) {
        g.globalAlpha = 0.4; g.fillStyle = camo[i % camo.length];
        g.beginPath();
        g.ellipse(rnd() * 512, rnd() * 512, 60 + rnd() * 100, 35 + rnd() * 60, rnd() * 3.14, 0, 6.283);
        g.fill();
      }
      for (i = 0; i < 40; i++) {
        g.globalAlpha = 0.04 + rnd() * 0.04;
        g.fillStyle = rnd() < 0.5 ? "#000000" : "#ffffff";
        g.fillRect(rnd() * 512, rnd() * 512, 30 + rnd() * 100, 18 + rnd() * 70);
      }
      g.globalAlpha = 0.3; g.strokeStyle = "#171b1e"; g.lineWidth = 1.2;
      p = 0; while (p < 512) { p += 30 + rnd() * 55; g.beginPath(); g.moveTo(p, 0); g.lineTo(p, 512); g.stroke(); }
      p = 0; while (p < 512) { p += 36 + rnd() * 64; g.beginPath(); g.moveTo(0, p); g.lineTo(512, p); g.stroke(); }
      g.globalAlpha = 0.09; g.fillStyle = "#0a0d0d";
      for (i = 0; i < 24; i++) { g.fillRect(rnd() * 512, rnd() * 512, 2 + rnd() * 3, 20 + rnd() * 80); }
      var t = new THREE.CanvasTexture(cv);
      t.wrapS = t.wrapT = THREE.RepeatWrapping;
      return t;
    }
    var skin = new THREE.MeshStandardMaterial({ map: paintTex("#70808a", ["#5c6c76", "#82929c"]), metalness: 0.3, roughness: 0.6, side: THREE.DoubleSide });
    var dark = new THREE.MeshStandardMaterial({ color: 0x1e2124, metalness: 0.5, roughness: 0.55, side: THREE.DoubleSide });
    var steel = new THREE.MeshStandardMaterial({ color: 0x8a8f94, metalness: 0.85, roughness: 0.3, side: THREE.DoubleSide });
    var glass = new THREE.MeshPhysicalMaterial({ color: 0x2a4a58, metalness: 0.5, roughness: 0.15, clearcoat: 1.0 });
    var team = new THREE.MeshStandardMaterial({ color: C.team, metalness: 0.3, roughness: 0.6 });
    var store = new THREE.MeshStandardMaterial({ color: 0xb8bcc0, metalness: 0.3, roughness: 0.55 });
    var lampL = new THREE.MeshStandardMaterial({ color: 0xff3333, emissive: 0xdd2222, emissiveIntensity: 0.9 });
    var lampR = new THREE.MeshStandardMaterial({ color: 0x33ff55, emissive: 0x22cc44, emissiveIntensity: 0.9 });
    function put(parent, geo, mat, x, y, z, rx, ry, rz) {
      var m = new THREE.Mesh(geo, mat);
      m.position.set(x, y, z);
      m.rotation.set(rx || 0, ry || 0, rz || 0);
      parent.add(m);
      return m;
    }
    function mir(p) { var o = [], k; for (k = 0; k < p.length; k++) o.push([p[k][0], -p[k][1]]); return o; }
    // long slim fuselage
    put(r, M.loft(THREE, [
      { x: -10.9, w: 0.4, h: 0.42, zc: 0.15 },
      { x: -8.5, w: 0.62, h: 0.68, zc: 0.1 },
      { x: -5.0, w: 0.85, h: 0.88, zc: 0.02, sq: 0.75 },
      { x: -1.0, w: 0.95, h: 0.98, zc: 0, sq: 0.78 },
      { x: 3.0, w: 0.95, h: 0.98, zc: 0.02, sq: 0.72 },
      { x: 5.8, w: 0.8, h: 0.9, zc: 0.08, sq: 0.6 },
      { x: 7.6, w: 0.62, h: 0.72, zc: 0.14 },
      { x: 9.4, w: 0.42, h: 0.48, zc: 0.15 },
      { x: 11.05, w: 0.05, h: 0.05, zc: 0.18 }
    ], 24), skin, 0, 0, 0);
    // twin Spey nozzles side by side
    var si, s;
    for (si = 0; si < 2; si++) {
      s = si === 0 ? 1 : -1;
      put(r, new THREE.CylinderGeometry(0.44, 0.36, 0.9, 16), steel, -10.75, 0.45 * s, 0.1, 0, 0, -Math.PI / 2);
      put(r, new THREE.CylinderGeometry(0.32, 0.32, 0.06, 16), dark, -11.17, 0.45 * s, 0.1, 0, 0, -Math.PI / 2);
    }
    // nose pitot probe
    put(r, new THREE.CylinderGeometry(0.022, 0.022, 0.6, 6), steel, 11.3, 0, 0.18, 0, 0, -Math.PI / 2);
    // side intakes with splitter plates
    for (si = 0; si < 2; si++) {
      s = si === 0 ? 1 : -1;
      put(r, new THREE.BoxGeometry(2.6, 0.6, 1.15), skin, 5.0, 1.1 * s, 0.15);
      put(r, new THREE.BoxGeometry(0.1, 0.55, 1.05), dark, 6.25, 1.1 * s, 0.15);
      put(r, new THREE.BoxGeometry(1.0, 0.06, 1.05), dark, 6.3, 0.72 * s, 0.15);
    }
    // high shoulder wings, ~47 deg sweep, 5 deg anhedral
    var wingPts = [[3.0, 0.95], [-2.4, 6.3], [-3.6, 6.3], [-1.8, 0.95]];
    put(r, M.slab(THREE, wingPts, 0.15), skin, 0, 0, 0.75, -0.09, 0, 0);
    put(r, M.slab(THREE, mir(wingPts), 0.15), skin, 0, 0, 0.75, 0.09, 0, 0);
    // tall swept fin + tip fairing + team stripe
    put(r, M.slab(THREE, [[-5.4, 0.9], [-8.0, 4.5], [-9.3, 4.5], [-10.2, 0.9]], 0.11, "xz"), skin, 0, 0, 0);
    put(r, new THREE.CylinderGeometry(0.09, 0.09, 1.1, 10), skin, -8.6, 0, 4.5, 0, 0, -Math.PI / 2);
    put(r, new THREE.BoxGeometry(0.9, 0.15, 0.8), team, -8.7, 0, 3.55);
    // ventral fin under the tail
    put(r, M.slab(THREE, [[-7.6, -0.5], [-8.3, -1.55], [-9.4, -1.55], [-9.7, -0.5]], 0.08, "xz"), skin, 0, 0, 0);
    // all-moving tailplane
    var stabPts = [[-8.2, 0.85], [-9.8, 3.1], [-10.7, 3.1], [-9.9, 0.85]];
    put(r, M.slab(THREE, stabPts, 0.1), skin, 0, 0, 0.15);
    put(r, M.slab(THREE, mir(stabPts), 0.1), skin, 0, 0, 0.15);
    // tandem twin canopy: windscreen, two bubbles, frame bows, dorsal fairing
    var c1 = put(r, new THREE.SphereGeometry(0.42, 18, 12), glass, 7.95, 0, 1.0);
    c1.scale.set(1.5, 0.85, 0.9);
    var c2 = put(r, new THREE.SphereGeometry(0.42, 18, 12), glass, 6.55, 0, 1.1);
    c2.scale.set(1.5, 0.85, 0.9);
    put(r, new THREE.BoxGeometry(0.6, 0.75, 0.06), dark, 8.6, 0, 0.85, 0, -0.7, 0);
    put(r, new THREE.BoxGeometry(0.06, 0.8, 0.5), dark, 7.25, 0, 1.05);
    put(r, new THREE.BoxGeometry(0.06, 0.75, 0.5), dark, 5.85, 0, 1.1);
    // dorsal spine behind the cockpits
    put(r, new THREE.BoxGeometry(4.5, 0.55, 0.25), skin, 3.0, 0, 1.0);
    // spine antennas
    put(r, new THREE.BoxGeometry(0.3, 0.03, 0.22), dark, 1.0, 0, 1.15);
    put(r, new THREE.BoxGeometry(0.3, 0.03, 0.2), dark, -4.0, 0, 0.95);
    // stores: YJ-83K inboard, PL-5 on outer rails
    for (si = 0; si < 2; si++) {
      s = si === 0 ? 1 : -1;
      // YJ-83K sea-skimmer
      put(r, new THREE.BoxGeometry(1.0, 0.12, 0.5), skin, -1.3, 3.3 * s, 0.22);
      var mg = new THREE.Group();
      mg.position.set(-1.0, 3.3 * s, -0.12);
      var body = new THREE.Mesh(new THREE.CylinderGeometry(0.18, 0.18, 3.6, 12), store);
      body.rotation.set(0, 0, -Math.PI / 2); mg.add(body);
      var nose = new THREE.Mesh(new THREE.ConeGeometry(0.18, 0.7, 12), store);
      nose.rotation.set(0, 0, -Math.PI / 2); nose.position.set(2.15, 0, 0); mg.add(nose);
      var mw1 = new THREE.Mesh(new THREE.BoxGeometry(0.7, 1.0, 0.025), store);
      mw1.position.set(-0.3, 0, 0); mg.add(mw1);
      var mf1 = new THREE.Mesh(new THREE.BoxGeometry(0.45, 0.8, 0.025), store);
      mf1.position.set(-1.55, 0, 0); mg.add(mf1);
      var mf2 = new THREE.Mesh(new THREE.BoxGeometry(0.45, 0.025, 0.8), store);
      mf2.position.set(-1.55, 0, 0); mg.add(mf2);
      r.add(mg);
      // PL-5 on outer wing rail
      put(r, new THREE.BoxGeometry(1.3, 0.06, 0.14), skin, -2.6, 5.2 * s, 0.24);
      var pl = new THREE.Group();
      pl.position.set(-2.4, 5.2 * s, 0.08);
      var pb = new THREE.Mesh(new THREE.CylinderGeometry(0.065, 0.065, 2.3, 8), store);
      pb.rotation.set(0, 0, -Math.PI / 2); pl.add(pb);
      var pn = new THREE.Mesh(new THREE.ConeGeometry(0.065, 0.5, 8), store);
      pn.rotation.set(0, 0, -Math.PI / 2); pn.position.set(1.4, 0, 0); pl.add(pn);
      var pf1 = new THREE.Mesh(new THREE.BoxGeometry(0.35, 0.45, 0.02), store);
      pf1.position.set(-0.95, 0, 0); pl.add(pf1);
      var pf2 = new THREE.Mesh(new THREE.BoxGeometry(0.35, 0.02, 0.45), store);
      pf2.position.set(-0.95, 0, 0); pl.add(pf2);
      r.add(pl);
      // wing roundels
      put(r, new THREE.CylinderGeometry(0.34, 0.34, 0.03, 16), team, -1.6, 4.0 * s, 0.47, Math.PI / 2, 0, 0);
    }
    // nav lights
    put(r, new THREE.SphereGeometry(0.05, 8, 6), lampL, -2.9, 6.25, 0.2);
    put(r, new THREE.SphereGeometry(0.05, 8, 6), lampR, -2.9, -6.25, 0.2);
    return r;
  }
};

/* ---- pack aace94df ---- */
UNIT_MODELS["repair"] = {
  len: 8.5,
  build: function (THREE, M, C) {
    var g = new THREE.Group();
    var seed = 48271009;
    function R() { seed = (seed * 16807 + 17) % 2147483647; return seed / 2147483647; }
    function paintHull(base) {
      var cv = document.createElement("canvas"); cv.width = 512; cv.height = 512;
      var q = cv.getContext("2d");
      q.fillStyle = base; q.fillRect(0, 0, 512, 512);
      var i, p;
      for (i = 0; i < 36; i++) {
        q.fillStyle = R() < 0.5 ? "rgba(232,238,214,0.05)" : "rgba(8,12,4,0.06)";
        q.fillRect(R() * 512, R() * 512, 36 + R() * 120, 28 + R() * 96);
      }
      q.strokeStyle = "rgba(6,8,4,0.30)"; q.lineWidth = 2;
      for (i = 0; i < 7; i++) { p = 20 + R() * 472; q.beginPath(); q.moveTo(p, 0); q.lineTo(p, 512); q.stroke(); }
      for (i = 0; i < 5; i++) { p = 20 + R() * 472; q.beginPath(); q.moveTo(0, p); q.lineTo(512, p); q.stroke(); }
      q.fillStyle = "rgba(0,0,0,0.22)";
      for (i = 0; i < 70; i++) q.fillRect(R() * 508, R() * 508, 2, 2);
      q.fillStyle = "rgba(18,14,6,0.09)";
      for (i = 0; i < 24; i++) q.fillRect(R() * 512, R() * 380, 3 + R() * 5, 50 + R() * 130);
      var t = new THREE.CanvasTexture(cv); t.wrapS = t.wrapT = THREE.RepeatWrapping; return t;
    }
    var hullMat = new THREE.MeshStandardMaterial({ map: paintHull("#414b36"), metalness: 0.28, roughness: 0.62 });
    var darkMat = new THREE.MeshStandardMaterial({ color: 0x23251e, metalness: 0.15, roughness: 0.9 });
    var steelMat = new THREE.MeshStandardMaterial({ color: 0x8d9094, metalness: 0.85, roughness: 0.3 });
    var teamMat = new THREE.MeshStandardMaterial({ color: C.team, metalness: 0.2, roughness: 0.55 });
    function B(sx, sy, sz, mat, x, y, z) { var m = new THREE.Mesh(new THREE.BoxGeometry(sx, sy, sz), mat); m.position.set(x, y, z); g.add(m); return m; }
    function Cy(r, h, seg, mat, x, y, z, axis) {
      var m = new THREE.Mesh(new THREE.CylinderGeometry(r, r, h, seg), mat);
      if (axis === "z") m.rotation.x = Math.PI / 2; else if (axis === "x") m.rotation.z = Math.PI / 2;
      m.position.set(x, y, z); g.add(m); return m;
    }
    // hull with sloped glacis
    var hull = new THREE.Mesh(M.loft(THREE, [
      { x: -3.8, w: 1.3, h: 0.3, zc: 1.28, sq: 0.8 },
      { x: -2.9, w: 1.52, h: 0.5, zc: 1.34, sq: 0.85 },
      { x: 1.9, w: 1.52, h: 0.5, zc: 1.34, sq: 0.85 },
      { x: 3.1, w: 1.45, h: 0.38, zc: 1.26, sq: 0.8 },
      { x: 3.85, w: 1.05, h: 0.1, zc: 1.06 }
    ], 20), hullMat);
    g.add(hull);
    // tracks: dark runs + visible road wheels
    var s, i;
    for (s = -1; s <= 1; s += 2) {
      var ty = s * 1.42;
      B(5.7, 0.72, 0.34, darkMat, -0.15, ty, 0.17);
      B(6.8, 0.72, 0.26, darkMat, -0.1, ty, 0.95);
      B(1.2, 0.72, 0.28, darkMat, 3.05, ty, 0.55).rotation.y = -0.81;
      B(1.05, 0.72, 0.28, darkMat, -3.15, ty, 0.55).rotation.y = 0.95;
      for (i = 0; i < 6; i++) Cy(0.33, 0.78, 14, darkMat, -2.35 + i * 0.94, ty, 0.36);
      Cy(0.29, 0.62, 12, darkMat, 3.45, ty, 0.72);
      Cy(0.29, 0.62, 12, darkMat, -3.5, ty, 0.72);
      B(7.0, 0.82, 0.06, hullMat, -0.15, ty, 1.14);
    }
    // superstructure (crew cab, offset right - crane stows on the left)
    B(2.5, 1.7, 0.8, hullMat, 0.9, -0.55, 2.26);
    B(0.9, 1.7, 0.12, hullMat, 2.25, -0.55, 2.38).rotation.y = -0.5;
    Cy(0.4, 0.24, 16, hullMat, 0.6, -0.9, 2.78);
    Cy(0.34, 0.05, 16, darkMat, 0.6, -0.9, 2.92);
    B(0.12, 0.3, 0.1, darkMat, 1.05, -0.9, 2.72);
    Cy(0.32, 0.07, 14, hullMat, 1.6, 0.85, 1.9);
    // dozer blade at bow
    B(0.14, 3.3, 0.82, hullMat, 4.32, 0, 0.6).rotation.y = -0.22;
    B(0.12, 3.3, 0.24, hullMat, 4.2, 0, 1.04).rotation.y = -0.62;
    B(0.07, 3.3, 0.14, steelMat, 4.4, 0, 0.16);
    B(1.35, 0.2, 0.24, hullMat, 3.4, 1.05, 0.82).rotation.y = 0.25;
    B(1.35, 0.2, 0.24, hullMat, 3.4, -1.05, 0.82).rotation.y = 0.25;
    // A-frame crane folded flat along the left side, apex to the rear
    var bm1 = B(5.77, 0.16, 0.16, hullMat, -0.42, 1.16, 2.02); bm1.rotation.z = 0.075;
    var bm2 = B(5.76, 0.16, 0.16, hullMat, -0.42, 0.7, 2.02); bm2.rotation.z = -0.07;
    B(0.12, 0.62, 0.12, hullMat, -1.5, 0.93, 2.02);
    B(0.25, 0.22, 0.3, hullMat, 2.5, 1.38, 1.96);
    B(0.25, 0.22, 0.3, hullMat, 2.5, 0.5, 1.96);
    B(0.5, 0.36, 0.24, hullMat, -3.45, 0.92, 2.02);
    Cy(0.13, 0.12, 10, steelMat, -3.6, 0.92, 1.92);
    Cy(0.02, 0.42, 5, darkMat, -3.6, 0.92, 1.66, "z");
    var hk = new THREE.Mesh(new THREE.TorusGeometry(0.09, 0.028, 6, 10, 4.4), steelMat);
    hk.rotation.x = Math.PI / 2; hk.position.set(-3.6, 0.92, 1.4); g.add(hk);
    // rear deck winch + cable drum
    B(0.85, 1.1, 0.42, hullMat, -2.6, -0.5, 2.06);
    Cy(0.19, 0.72, 10, steelMat, -2.6, -0.5, 2.28);
    // stowage bins on fenders, spare road wheel, tow cable
    B(1.7, 0.62, 0.4, hullMat, 0.8, -1.44, 1.4);
    B(1.1, 0.62, 0.36, hullMat, -1.2, -1.44, 1.38);
    B(1.4, 0.62, 0.38, hullMat, -2.2, 1.44, 1.39);
    B(0.9, 0.5, 0.3, hullMat, 2.2, 1.44, 1.35);
    Cy(0.34, 0.2, 14, darkMat, -2.0, 0.75, 1.96, "z");
    Cy(0.035, 3.8, 6, steelMat, 0.3, -1.58, 1.55, "x");
    Cy(0.24, 1.3, 10, darkMat, -3.3, 0.6, 2.0, "y");
    // lights, antennas, team panels
    Cy(0.09, 0.1, 10, steelMat, 3.5, 0.9, 1.7, "x");
    Cy(0.09, 0.1, 10, steelMat, 3.5, -0.9, 1.7, "x");
    Cy(0.015, 1.5, 5, darkMat, -0.32, -1.32, 3.4, "z");
    Cy(0.015, 1.2, 5, darkMat, -3.6, -1.2, 2.4, "z");
    B(0.55, 0.03, 0.38, teamMat, 0.9, -1.42, 2.3);
    B(0.55, 0.03, 0.38, teamMat, 1.6, 1.54, 1.55);
    return g;
  }
};
UNIT_MODELS["supply_n"] = {
  len: 10.2,
  build: function (THREE, M, C) {
    var g = new THREE.Group();
    var seed = 90210077;
    function R() { seed = (seed * 16807 + 29) % 2147483647; return seed / 2147483647; }
    function paintBody(base) {
      var cv = document.createElement("canvas"); cv.width = 512; cv.height = 512;
      var q = cv.getContext("2d");
      q.fillStyle = base; q.fillRect(0, 0, 512, 512);
      var i, p;
      for (i = 0; i < 30; i++) {
        q.fillStyle = R() < 0.5 ? "rgba(255,246,220,0.05)" : "rgba(40,30,12,0.06)";
        q.fillRect(R() * 512, R() * 512, 40 + R() * 130, 30 + R() * 100);
      }
      q.strokeStyle = "rgba(30,24,10,0.26)"; q.lineWidth = 2;
      for (i = 0; i < 6; i++) { p = 24 + R() * 464; q.beginPath(); q.moveTo(p, 0); q.lineTo(p, 512); q.stroke(); }
      for (i = 0; i < 4; i++) { p = 24 + R() * 464; q.beginPath(); q.moveTo(0, p); q.lineTo(512, p); q.stroke(); }
      q.fillStyle = "rgba(50,38,18,0.10)";
      for (i = 0; i < 20; i++) q.fillRect(R() * 512, R() * 400, 3 + R() * 6, 40 + R() * 110);
      q.fillStyle = "rgba(0,0,0,0.2)";
      for (i = 0; i < 50; i++) q.fillRect(R() * 508, R() * 508, 2, 2);
      var t = new THREE.CanvasTexture(cv); t.wrapS = t.wrapT = THREE.RepeatWrapping; return t;
    }
    function grilleTex() {
      var cv = document.createElement("canvas"); cv.width = 128; cv.height = 128;
      var q = cv.getContext("2d");
      q.fillStyle = "#1a1d18"; q.fillRect(0, 0, 128, 128);
      for (var y = 10; y < 120; y += 14) {
        q.fillStyle = "#3c423a"; q.fillRect(8, y, 112, 2);
        q.fillStyle = "#0a0c09"; q.fillRect(8, y + 2, 112, 6);
      }
      return new THREE.CanvasTexture(cv);
    }
    var bodyMat = new THREE.MeshStandardMaterial({ map: paintBody("#ab9663"), metalness: 0.25, roughness: 0.62 });
    var darkMat = new THREE.MeshStandardMaterial({ color: 0x24251f, metalness: 0.1, roughness: 0.92 });
    var steelMat = new THREE.MeshStandardMaterial({ color: 0x8d9094, metalness: 0.85, roughness: 0.32 });
    var glassMat = new THREE.MeshStandardMaterial({ color: 0x18262e, metalness: 0.5, roughness: 0.14 });
    var cargoMat = new THREE.MeshStandardMaterial({ color: 0x897952, metalness: 0.1, roughness: 0.85 });
    var drumMat = new THREE.MeshStandardMaterial({ color: 0x3f4434, metalness: 0.35, roughness: 0.5 });
    var grilleMat = new THREE.MeshStandardMaterial({ map: grilleTex(), metalness: 0.4, roughness: 0.6 });
    var teamMat = new THREE.MeshStandardMaterial({ color: C.team, metalness: 0.2, roughness: 0.55 });
    function B(sx, sy, sz, mat, x, y, z) { var m = new THREE.Mesh(new THREE.BoxGeometry(sx, sy, sz), mat); m.position.set(x, y, z); g.add(m); return m; }
    function Cy(r, h, seg, mat, x, y, z, axis) {
      var m = new THREE.Mesh(new THREE.CylinderGeometry(r, r, h, seg), mat);
      if (axis === "z") m.rotation.x = Math.PI / 2; else if (axis === "x") m.rotation.z = Math.PI / 2;
      m.position.set(x, y, z); g.add(m); return m;
    }
    // chassis
    B(9.8, 1.05, 0.28, darkMat, 0, 0, 0.98);
    B(0.3, 2.3, 0.45, bodyMat, 5.05, 0, 0.95);
    B(1.5, 0.5, 0.65, bodyMat, 0.3, -1.05, 0.72);
    B(1.1, 0.45, 0.55, bodyMat, 0.4, 1.05, 0.7);
    // flat-faced 2-man cab
    B(1.6, 2.3, 1.15, bodyMat, 4.25, 0, 1.72);
    B(1.3, 2.2, 0.75, bodyMat, 4.1, 0, 2.67);
    B(1.45, 2.3, 0.08, bodyMat, 4.08, 0, 3.08);
    B(0.06, 2.05, 0.6, glassMat, 4.73, 0, 2.68).rotation.y = -0.12;
    B(1.0, 0.03, 0.5, glassMat, 4.0, 1.16, 2.62);
    B(1.0, 0.03, 0.5, glassMat, 4.0, -1.16, 2.62);
    B(0.05, 1.6, 0.7, grilleMat, 5.06, 0, 1.75);
    B(0.12, 0.28, 0.18, steelMat, 5.05, 0.95, 2.2);
    B(0.12, 0.28, 0.18, steelMat, 5.05, -0.95, 2.2);
    var s;
    for (s = -1; s <= 1; s += 2) {
      Cy(0.02, 0.55, 5, darkMat, 4.85, s * 1.28, 2.8, "z");
      B(0.05, 0.2, 0.32, darkMat, 4.85, s * 1.3, 3.08);
    }
    // behind-cab kit: exhaust stack, air cleaner, vertical spare
    Cy(0.07, 1.5, 8, steelMat, 3.35, -0.95, 2.55, "z");
    B(0.2, 0.16, 0.12, darkMat, 3.35, -0.95, 3.32);
    Cy(0.26, 0.9, 12, darkMat, 3.32, 0.6, 2.5, "y");
    Cy(0.62, 0.4, 16, darkMat, 3.18, -0.25, 2.35, "x");
    Cy(0.2, 0.44, 10, steelMat, 3.18, -0.25, 2.35, "x");
    // cargo deck with rails and headboard
    B(0.08, 2.44, 1.0, bodyMat, 2.95, 0, 2.1);
    B(8.1, 2.44, 0.14, bodyMat, -1.05, 0, 1.55);
    B(8.1, 0.07, 0.28, bodyMat, -1.05, 1.185, 1.78);
    B(8.1, 0.07, 0.28, bodyMat, -1.05, -1.185, 1.78);
    var i;
    for (i = 0; i < 5; i++) {
      B(0.07, 0.07, 0.5, bodyMat, 2.4 - i * 1.7, 1.185, 1.85);
      B(0.07, 0.07, 0.5, bodyMat, 2.4 - i * 1.7, -1.185, 1.85);
    }
    // strapped pallets
    B(1.7, 2.0, 1.05, cargoMat, 1.9, 0, 2.15);
    B(0.07, 2.06, 1.11, darkMat, 2.3, 0, 2.15);
    B(0.07, 2.06, 1.11, darkMat, 1.5, 0, 2.15);
    B(1.4, 1.9, 0.85, cargoMat, 0.25, 0, 2.05);
    B(0.07, 1.96, 0.91, darkMat, 0.25, 0, 2.05);
    B(1.1, 0.88, 0.7, cargoMat, -1.25, 0.52, 1.97);
    B(1.1, 0.88, 0.7, cargoMat, -1.25, -0.52, 1.97);
    B(0.07, 2.0, 0.76, darkMat, -1.25, 0, 1.97);
    Cy(0.22, 2.1, 10, cargoMat, -2.15, 0, 1.82, "y");
    // fuel drums aft, with rib rings
    var dx = [-3.0, -3.7, -4.4], dyy = [0.55, -0.55], a, b;
    for (a = 0; a < 3; a++) for (b = 0; b < 2; b++) {
      Cy(0.29, 0.92, 14, drumMat, dx[a], dyy[b], 2.08, "z");
      Cy(0.3, 0.05, 14, drumMat, dx[a], dyy[b], 1.85, "z");
      Cy(0.3, 0.05, 14, drumMat, dx[a], dyy[b], 2.3, "z");
    }
    // small material-handling crane folded at rear right
    Cy(0.17, 0.55, 10, bodyMat, -4.75, -0.8, 1.9, "z");
    B(1.6, 0.16, 0.16, bodyMat, -4.0, -0.8, 2.22);
    B(1.1, 0.12, 0.12, bodyMat, -3.9, -0.8, 2.05);
    // 8 big single tires + hubs
    var ax = [3.5, 2.0, -2.3, -3.8];
    for (a = 0; a < 4; a++) for (s = -1; s <= 1; s += 2) {
      Cy(0.66, 0.46, 18, darkMat, ax[a], s * 0.99, 0.66);
      Cy(0.2, 0.5, 10, steelMat, ax[a], s * 1.0, 0.66);
    }
    B(0.04, 0.5, 0.45, darkMat, -4.5, 0.95, 0.35);
    B(0.04, 0.5, 0.45, darkMat, -4.5, -0.95, 0.35);
    B(0.1, 0.25, 0.12, darkMat, -5.08, 0.9, 1.3);
    B(0.1, 0.25, 0.12, darkMat, -5.08, -0.9, 1.3);
    Cy(0.014, 1.4, 5, darkMat, 3.5, 1.05, 3.7, "z");
    // team ID on cab doors
    B(0.5, 0.03, 0.4, teamMat, 4.3, 1.17, 1.95);
    B(0.5, 0.03, 0.4, teamMat, 4.3, -1.17, 1.95);
    return g;
  }
};
UNIT_MODELS["supply_p"] = {
  len: 9.3,
  build: function (THREE, M, C) {
    var g = new THREE.Group();
    var seed = 36251441;
    function R() { seed = (seed * 16807 + 43) % 2147483647; return seed / 2147483647; }
    function paintBody(base) {
      var cv = document.createElement("canvas"); cv.width = 512; cv.height = 512;
      var q = cv.getContext("2d");
      q.fillStyle = base; q.fillRect(0, 0, 512, 512);
      var i, p;
      for (i = 0; i < 32; i++) {
        q.fillStyle = R() < 0.5 ? "rgba(226,234,206,0.05)" : "rgba(6,10,4,0.06)";
        q.fillRect(R() * 512, R() * 512, 36 + R() * 120, 28 + R() * 96);
      }
      q.strokeStyle = "rgba(6,8,4,0.28)"; q.lineWidth = 2;
      for (i = 0; i < 6; i++) { p = 24 + R() * 464; q.beginPath(); q.moveTo(p, 0); q.lineTo(p, 512); q.stroke(); }
      for (i = 0; i < 4; i++) { p = 24 + R() * 464; q.beginPath(); q.moveTo(0, p); q.lineTo(512, p); q.stroke(); }
      q.fillStyle = "rgba(14,12,6,0.10)";
      for (i = 0; i < 22; i++) q.fillRect(R() * 512, R() * 400, 3 + R() * 5, 40 + R() * 120);
      var t = new THREE.CanvasTexture(cv); t.wrapS = t.wrapT = THREE.RepeatWrapping; return t;
    }
    function tarpTex(base) {
      var cv = document.createElement("canvas"); cv.width = 256; cv.height = 256;
      var q = cv.getContext("2d");
      q.fillStyle = base; q.fillRect(0, 0, 256, 256);
      var i;
      q.fillStyle = "rgba(0,0,0,0.05)";
      for (i = 0; i < 256; i += 3) q.fillRect(0, i, 256, 1);
      q.fillStyle = "rgba(0,0,0,0.07)";
      for (i = 8; i < 256; i += 32) q.fillRect(i, 0, 6, 256);
      q.fillStyle = "rgba(255,255,235,0.05)";
      for (i = 22; i < 256; i += 32) q.fillRect(i, 0, 8, 256);
      q.fillStyle = "rgba(20,16,8,0.08)";
      for (i = 0; i < 14; i++) q.fillRect(R() * 256, R() * 200, 10 + R() * 30, 20 + R() * 50);
      var t = new THREE.CanvasTexture(cv); t.wrapS = t.wrapT = THREE.RepeatWrapping; return t;
    }
    function grilleTex() {
      var cv = document.createElement("canvas"); cv.width = 128; cv.height = 128;
      var q = cv.getContext("2d");
      q.fillStyle = "#161a14"; q.fillRect(0, 0, 128, 128);
      for (var x = 8; x < 120; x += 12) {
        q.fillStyle = "#38402f"; q.fillRect(x, 8, 2, 112);
        q.fillStyle = "#080a06"; q.fillRect(x + 2, 8, 6, 112);
      }
      return new THREE.CanvasTexture(cv);
    }
    var bodyMat = new THREE.MeshStandardMaterial({ map: paintBody("#3e4a35"), metalness: 0.28, roughness: 0.6 });
    var tarpMat = new THREE.MeshStandardMaterial({ map: tarpTex("#565843"), metalness: 0.05, roughness: 0.92 });
    var darkMat = new THREE.MeshStandardMaterial({ color: 0x24251f, metalness: 0.1, roughness: 0.92 });
    var steelMat = new THREE.MeshStandardMaterial({ color: 0x8d9094, metalness: 0.85, roughness: 0.32 });
    var glassMat = new THREE.MeshStandardMaterial({ color: 0x18262e, metalness: 0.5, roughness: 0.14 });
    var grilleMat = new THREE.MeshStandardMaterial({ map: grilleTex(), metalness: 0.4, roughness: 0.6 });
    var teamMat = new THREE.MeshStandardMaterial({ color: C.team, metalness: 0.2, roughness: 0.55 });
    function B(sx, sy, sz, mat, x, y, z) { var m = new THREE.Mesh(new THREE.BoxGeometry(sx, sy, sz), mat); m.position.set(x, y, z); g.add(m); return m; }
    function Cy(r, h, seg, mat, x, y, z, axis) {
      var m = new THREE.Mesh(new THREE.CylinderGeometry(r, r, h, seg), mat);
      if (axis === "z") m.rotation.x = Math.PI / 2; else if (axis === "x") m.rotation.z = Math.PI / 2;
      m.position.set(x, y, z); g.add(m); return m;
    }
    // chassis, tanks, bumper
    B(9.0, 1.0, 0.26, darkMat, -0.1, 0, 0.95);
    B(1.3, 0.42, 0.55, bodyMat, 0.9, 1.1, 0.75);
    B(1.1, 0.42, 0.5, bodyMat, 0.9, -1.1, 0.73);
    B(0.28, 2.4, 0.4, darkMat, 4.72, 0, 0.9);
    Cy(0.08, 0.08, 10, steelMat, 4.88, 0.85, 0.98, "x");
    Cy(0.08, 0.08, 10, steelMat, 4.88, -0.85, 0.98, "x");
    // KamAZ cab-over with ribbed grille
    B(1.65, 2.3, 1.1, bodyMat, 3.82, 0, 1.75);
    B(1.55, 2.26, 0.72, bodyMat, 3.78, 0, 2.64);
    B(1.5, 2.1, 0.1, bodyMat, 3.75, 0, 3.05);
    B(0.06, 2.0, 0.56, glassMat, 4.58, 0, 2.62).rotation.y = -0.1;
    B(0.9, 0.03, 0.48, glassMat, 3.7, 1.16, 2.6);
    B(0.9, 0.03, 0.48, glassMat, 3.7, -1.16, 2.6);
    B(0.05, 1.9, 0.55, grilleMat, 4.66, 0, 1.72);
    B(1.2, 0.3, 0.08, bodyMat, 3.6, 1.22, 1.3);
    B(1.2, 0.3, 0.08, bodyMat, 3.6, -1.22, 1.3);
    B(0.45, 0.32, 0.06, darkMat, 3.95, 1.12, 0.72);
    B(0.45, 0.32, 0.06, darkMat, 3.95, -1.12, 0.72);
    var s;
    for (s = -1; s <= 1; s += 2) {
      Cy(0.02, 0.5, 5, darkMat, 4.5, s * 1.26, 2.85, "z");
      B(0.05, 0.18, 0.3, darkMat, 4.5, s * 1.28, 3.12);
    }
    // snorkel + spare wheel behind cab
    Cy(0.085, 1.9, 8, bodyMat, 3.1, 1.18, 2.15, "z");
    B(0.3, 0.17, 0.14, bodyMat, 3.22, 1.18, 3.12);
    Cy(0.58, 0.38, 16, darkMat, 2.6, 0, 2.0, "x");
    Cy(0.18, 0.42, 10, steelMat, 2.6, 0, 2.0, "x");
    // dropside bed
    B(6.8, 2.5, 0.12, bodyMat, -1.1, 0, 1.44);
    B(6.8, 0.06, 0.55, bodyMat, -1.1, 1.22, 1.78);
    B(6.8, 0.06, 0.55, bodyMat, -1.1, -1.22, 1.78);
    B(0.06, 2.5, 0.55, bodyMat, -4.53, 0, 1.78);
    // ribbed canvas tarp (loft + raised hoops)
    var tarp = new THREE.Mesh(M.loft(THREE, [
      { x: 2.25, w: 1.24, h: 0.62, zc: 2.42, sq: 0.6 },
      { x: -4.45, w: 1.24, h: 0.62, zc: 2.42, sq: 0.6 }
    ], 18), tarpMat);
    g.add(tarp);
    var i, xi;
    for (i = 0; i < 8; i++) {
      xi = 1.85 - i * 0.85;
      g.add(new THREE.Mesh(M.loft(THREE, [
        { x: xi + 0.07, w: 1.245, h: 0.625, zc: 2.42, sq: 0.6 },
        { x: xi, w: 1.29, h: 0.67, zc: 2.42, sq: 0.6 },
        { x: xi - 0.07, w: 1.245, h: 0.625, zc: 2.42, sq: 0.6 }
      ], 18), tarpMat));
    }
    B(0.06, 2.42, 1.22, tarpMat, 2.24, 0, 2.42);
    B(0.06, 2.42, 1.22, tarpMat, -4.44, 0, 2.42);
    // 8 wheels + hubs, mudflaps
    var ax = [3.55, 2.05, -2.15, -3.6], a;
    for (a = 0; a < 4; a++) for (s = -1; s <= 1; s += 2) {
      Cy(0.58, 0.4, 18, darkMat, ax[a], s * 1.03, 0.58);
      Cy(0.18, 0.44, 10, steelMat, ax[a], s * 1.04, 0.58);
    }
    B(0.04, 0.45, 0.4, darkMat, -4.25, 0.95, 0.3);
    B(0.04, 0.45, 0.4, darkMat, -4.25, -0.95, 0.3);
    Cy(0.014, 1.3, 5, darkMat, 3.3, -1.0, 3.7, "z");
    // team ID on cab doors
    B(0.5, 0.03, 0.38, teamMat, 3.85, 1.17, 1.95);
    B(0.5, 0.03, 0.38, teamMat, 3.85, -1.17, 1.95);
    return g;
  }
};
UNIT_MODELS["supply_c"] = {
  len: 9.5,
  build: function (THREE, M, C) {
    var g = new THREE.Group();
    var seed = 77003199;
    function R() { seed = (seed * 16807 + 51) % 2147483647; return seed / 2147483647; }
    function paintBody(base) {
      var cv = document.createElement("canvas"); cv.width = 512; cv.height = 512;
      var q = cv.getContext("2d");
      q.fillStyle = base; q.fillRect(0, 0, 512, 512);
      var i, p;
      for (i = 0; i < 32; i++) {
        q.fillStyle = R() < 0.5 ? "rgba(230,238,212,0.05)" : "rgba(8,12,6,0.06)";
        q.fillRect(R() * 512, R() * 512, 36 + R() * 120, 28 + R() * 96);
      }
      q.strokeStyle = "rgba(8,10,6,0.28)"; q.lineWidth = 2;
      for (i = 0; i < 6; i++) { p = 24 + R() * 464; q.beginPath(); q.moveTo(p, 0); q.lineTo(p, 512); q.stroke(); }
      for (i = 0; i < 4; i++) { p = 24 + R() * 464; q.beginPath(); q.moveTo(0, p); q.lineTo(512, p); q.stroke(); }
      q.fillStyle = "rgba(14,12,6,0.10)";
      for (i = 0; i < 22; i++) q.fillRect(R() * 512, R() * 400, 3 + R() * 5, 40 + R() * 120);
      var t = new THREE.CanvasTexture(cv); t.wrapS = t.wrapT = THREE.RepeatWrapping; return t;
    }
    function tarpTex(base) {
      var cv = document.createElement("canvas"); cv.width = 256; cv.height = 256;
      var q = cv.getContext("2d");
      q.fillStyle = base; q.fillRect(0, 0, 256, 256);
      var i;
      q.fillStyle = "rgba(0,0,0,0.05)";
      for (i = 0; i < 256; i += 3) q.fillRect(0, i, 256, 1);
      q.fillStyle = "rgba(0,0,0,0.07)";
      for (i = 10; i < 256; i += 34) q.fillRect(i, 0, 6, 256);
      q.fillStyle = "rgba(255,255,235,0.05)";
      for (i = 24; i < 256; i += 34) q.fillRect(i, 0, 8, 256);
      q.fillStyle = "rgba(20,16,8,0.08)";
      for (i = 0; i < 14; i++) q.fillRect(R() * 256, R() * 200, 10 + R() * 30, 20 + R() * 50);
      var t = new THREE.CanvasTexture(cv); t.wrapS = t.wrapT = THREE.RepeatWrapping; return t;
    }
    function grilleTex() {
      var cv = document.createElement("canvas"); cv.width = 128; cv.height = 128;
      var q = cv.getContext("2d");
      q.fillStyle = "#171a15"; q.fillRect(0, 0, 128, 128);
      for (var y = 12; y < 116; y += 16) {
        q.fillStyle = "#3a4234"; q.fillRect(6, y, 116, 3);
        q.fillStyle = "#090b07"; q.fillRect(6, y + 3, 116, 7);
      }
      return new THREE.CanvasTexture(cv);
    }
    var bodyMat = new THREE.MeshStandardMaterial({ map: paintBody("#4b5541"), metalness: 0.28, roughness: 0.6 });
    var tarpMat = new THREE.MeshStandardMaterial({ map: tarpTex("#5d6048"), metalness: 0.05, roughness: 0.92 });
    var darkMat = new THREE.MeshStandardMaterial({ color: 0x24261f, metalness: 0.1, roughness: 0.92 });
    var steelMat = new THREE.MeshStandardMaterial({ color: 0x8d9094, metalness: 0.85, roughness: 0.32 });
    var glassMat = new THREE.MeshStandardMaterial({ color: 0x18262e, metalness: 0.5, roughness: 0.14 });
    var drumMat = new THREE.MeshStandardMaterial({ color: 0x424836, metalness: 0.35, roughness: 0.5 });
    var grilleMat = new THREE.MeshStandardMaterial({ map: grilleTex(), metalness: 0.4, roughness: 0.6 });
    var teamMat = new THREE.MeshStandardMaterial({ color: C.team, metalness: 0.2, roughness: 0.55 });
    function B(sx, sy, sz, mat, x, y, z) { var m = new THREE.Mesh(new THREE.BoxGeometry(sx, sy, sz), mat); m.position.set(x, y, z); g.add(m); return m; }
    function Cy(r, h, seg, mat, x, y, z, axis) {
      var m = new THREE.Mesh(new THREE.CylinderGeometry(r, r, h, seg), mat);
      if (axis === "z") m.rotation.x = Math.PI / 2; else if (axis === "x") m.rotation.z = Math.PI / 2;
      m.position.set(x, y, z); g.add(m); return m;
    }
    // chassis + bumper with tow pins
    B(9.2, 1.0, 0.26, darkMat, -0.05, 0, 0.95);
    B(0.3, 2.42, 0.42, bodyMat, 4.78, 0, 0.88);
    Cy(0.06, 0.2, 8, steelMat, 4.95, 0.7, 0.88, "x");
    Cy(0.06, 0.2, 8, steelMat, 4.95, -0.7, 0.88, "x");
    B(0.1, 0.3, 0.16, steelMat, 4.9, 0.95, 1.14);
    B(0.1, 0.3, 0.16, steelMat, 4.9, -0.95, 1.14);
    // squared SX cab with wide grille and roof markers
    B(1.7, 2.32, 1.1, bodyMat, 3.9, 0, 1.75);
    B(1.6, 2.28, 0.74, bodyMat, 3.86, 0, 2.65);
    B(1.6, 2.2, 0.1, bodyMat, 3.84, 0, 3.07);
    B(0.06, 2.05, 0.58, glassMat, 4.68, 0, 2.64).rotation.y = -0.14;
    B(0.95, 0.03, 0.48, glassMat, 3.8, 1.17, 2.62);
    B(0.95, 0.03, 0.48, glassMat, 3.8, -1.17, 2.62);
    B(0.05, 2.1, 0.5, grilleMat, 4.76, 0, 1.72);
    B(0.09, 0.24, 0.14, steelMat, 4.76, 0.9, 1.28);
    B(0.09, 0.24, 0.14, steelMat, 4.76, -0.9, 1.28);
    var i;
    for (i = -1; i <= 1; i++) B(0.1, 0.16, 0.07, darkMat, 4.6, i * 0.5, 3.15);
    B(1.5, 0.06, 0.12, bodyMat, 3.84, 0, 3.18);
    var s;
    for (s = -1; s <= 1; s += 2) {
      Cy(0.02, 0.6, 5, darkMat, 4.62, s * 1.3, 2.85, "z");
      B(0.05, 0.2, 0.32, darkMat, 4.62, s * 1.32, 3.16);
      B(0.45, 0.34, 0.06, darkMat, 4.0, s * 1.14, 0.72);
    }
    // bed: tarp forward, drums aft
    B(7.25, 2.5, 0.12, bodyMat, -0.97, 0, 1.44);
    B(7.25, 0.06, 0.6, bodyMat, -0.97, 1.22, 1.8);
    B(7.25, 0.06, 0.6, bodyMat, -0.97, -1.22, 1.8);
    B(0.06, 2.5, 0.6, bodyMat, -4.58, 0, 1.8);
    B(0.08, 2.5, 1.05, bodyMat, 2.62, 0, 2.0);
    var tarp = new THREE.Mesh(M.loft(THREE, [
      { x: 2.58, w: 1.24, h: 0.6, zc: 2.45, sq: 0.6 },
      { x: -0.68, w: 1.24, h: 0.6, zc: 2.45, sq: 0.6 }
    ], 18), tarpMat);
    g.add(tarp);
    var rx = [2.15, 1.4, 0.65, -0.1];
    for (i = 0; i < 4; i++) {
      g.add(new THREE.Mesh(M.loft(THREE, [
        { x: rx[i] + 0.07, w: 1.245, h: 0.605, zc: 2.45, sq: 0.6 },
        { x: rx[i], w: 1.29, h: 0.65, zc: 2.45, sq: 0.6 },
        { x: rx[i] - 0.07, w: 1.245, h: 0.605, zc: 2.45, sq: 0.6 }
      ], 18), tarpMat));
    }
    B(0.06, 2.42, 1.18, tarpMat, 2.56, 0, 2.44);
    B(0.06, 2.42, 1.18, tarpMat, -0.66, 0, 2.44);
    // drums aft with retaining bar
    var dx = [-1.5, -2.35, -3.2], dyy = [0.6, -0.6], a, b;
    for (a = 0; a < 3; a++) for (b = 0; b < 2; b++) {
      Cy(0.3, 0.95, 14, drumMat, dx[a], dyy[b], 1.98, "z");
      Cy(0.31, 0.05, 14, drumMat, dx[a], dyy[b], 1.75, "z");
      Cy(0.31, 0.05, 14, drumMat, dx[a], dyy[b], 2.2, "z");
    }
    B(0.08, 2.44, 0.1, bodyMat, -1.02, 0, 2.2);
    B(0.08, 2.44, 0.1, bodyMat, -3.68, 0, 2.2);
    // spare under rear frame
    Cy(0.55, 0.32, 14, darkMat, -4.35, 0, 0.6, "z");
    // 8 wheels + hubs
    var ax = [3.6, 2.1, -2.2, -3.65];
    for (a = 0; a < 4; a++) for (s = -1; s <= 1; s += 2) {
      Cy(0.58, 0.4, 18, darkMat, ax[a], s * 1.03, 0.58);
      Cy(0.18, 0.44, 10, steelMat, ax[a], s * 1.04, 0.58);
    }
    B(0.04, 0.45, 0.4, darkMat, -4.3, 0.95, 0.3);
    B(0.04, 0.45, 0.4, darkMat, -4.3, -0.95, 0.3);
    Cy(0.014, 1.4, 5, darkMat, 3.4, 1.0, 3.8, "z");
    // team ID on cab doors
    B(0.5, 0.03, 0.38, teamMat, 3.95, 1.18, 1.95);
    B(0.5, 0.03, 0.38, teamMat, 3.95, -1.18, 1.95);
    return g;
  }
};
UNIT_MODELS["harvester"] = {
  len: 10.5,
  build: function (THREE, M, C) {
    var g = new THREE.Group();
    var seed = 55511023;
    function R() { seed = (seed * 16807 + 61) % 2147483647; return seed / 2147483647; }
    function paintBody(base) {
      var cv = document.createElement("canvas"); cv.width = 512; cv.height = 512;
      var q = cv.getContext("2d");
      q.fillStyle = base; q.fillRect(0, 0, 512, 512);
      var i, p;
      for (i = 0; i < 26; i++) {
        q.fillStyle = R() < 0.5 ? "rgba(255,240,200,0.05)" : "rgba(30,24,8,0.07)";
        q.fillRect(R() * 512, R() * 512, 40 + R() * 130, 30 + R() * 100);
      }
      q.strokeStyle = "rgba(20,16,6,0.3)"; q.lineWidth = 2;
      for (i = 0; i < 6; i++) { p = 24 + R() * 464; q.beginPath(); q.moveTo(p, 0); q.lineTo(p, 512); q.stroke(); }
      for (i = 0; i < 4; i++) { p = 24 + R() * 464; q.beginPath(); q.moveTo(0, p); q.lineTo(512, p); q.stroke(); }
      // heavy wear: scrapes and rust streaks
      q.fillStyle = "rgba(15,12,8,0.16)";
      for (i = 0; i < 30; i++) q.fillRect(R() * 512, R() * 512, 20 + R() * 70, 2 + R() * 3);
      q.fillStyle = "rgba(96,52,18,0.2)";
      for (i = 0; i < 26; i++) q.fillRect(R() * 512, R() * 420, 3 + R() * 4, 30 + R() * 110);
      q.fillStyle = "rgba(70,40,14,0.3)";
      for (i = 0; i < 40; i++) q.fillRect(R() * 508, R() * 508, 3, 3);
      var t = new THREE.CanvasTexture(cv); t.wrapS = t.wrapT = THREE.RepeatWrapping; return t;
    }
    function oreTex() {
      var cv = document.createElement("canvas"); cv.width = 256; cv.height = 256;
      var q = cv.getContext("2d");
      q.fillStyle = "#231e16"; q.fillRect(0, 0, 256, 256);
      var i, x, y, r;
      for (i = 0; i < 420; i++) {
        var v = 24 + Math.floor(R() * 50);
        q.fillStyle = "rgba(" + v + "," + Math.floor(v * 0.9) + "," + Math.floor(v * 0.7) + ",0.8)";
        q.fillRect(R() * 252, R() * 252, 2 + R() * 5, 2 + R() * 4);
      }
      for (i = 0; i < 46; i++) {
        x = 8 + R() * 240; y = 8 + R() * 240; r = 2 + R() * 5;
        var gr = q.createRadialGradient(x, y, 0, x, y, r);
        gr.addColorStop(0, "rgba(255,208,60,0.95)");
        gr.addColorStop(0.6, "rgba(210,150,30,0.55)");
        gr.addColorStop(1, "rgba(120,80,10,0)");
        q.fillStyle = gr;
        q.beginPath(); q.arc(x, y, r, 0, Math.PI * 2); q.fill();
        if (R() < 0.4) { q.fillStyle = "rgba(255,255,230,0.9)"; q.fillRect(x - 1, y - 1, 2, 2); }
      }
      var t = new THREE.CanvasTexture(cv); t.wrapS = t.wrapT = THREE.RepeatWrapping; return t;
    }
    function hazTex() {
      var cv = document.createElement("canvas"); cv.width = 128; cv.height = 128;
      var q = cv.getContext("2d");
      q.fillStyle = "#b99a20"; q.fillRect(0, 0, 128, 128);
      q.fillStyle = "#1a1a18";
      q.save(); q.translate(64, 64); q.rotate(-Math.PI / 4);
      for (var i = -3; i <= 3; i++) q.fillRect(-140, i * 40 - 10, 280, 20);
      q.restore();
      var t = new THREE.CanvasTexture(cv); t.wrapS = t.wrapT = THREE.RepeatWrapping; return t;
    }
    var bodyMat = new THREE.MeshStandardMaterial({ map: paintBody("#a5822c"), metalness: 0.3, roughness: 0.62 });
    var greyMat = new THREE.MeshStandardMaterial({ color: 0x54585a, metalness: 0.35, roughness: 0.68 });
    var darkMat = new THREE.MeshStandardMaterial({ color: 0x232420, metalness: 0.1, roughness: 0.92 });
    var oreMat = new THREE.MeshStandardMaterial({ map: oreTex(), metalness: 0.35, roughness: 0.55 });
    var glassMat = new THREE.MeshStandardMaterial({ color: 0x18262e, metalness: 0.5, roughness: 0.14 });
    var hazMat = new THREE.MeshStandardMaterial({ map: hazTex(), metalness: 0.2, roughness: 0.6 });
    var teamMat = new THREE.MeshStandardMaterial({ color: C.team, metalness: 0.2, roughness: 0.55 });
    function B(sx, sy, sz, mat, x, y, z) { var m = new THREE.Mesh(new THREE.BoxGeometry(sx, sy, sz), mat); m.position.set(x, y, z); g.add(m); return m; }
    function Cy(r, h, seg, mat, x, y, z, axis) {
      var m = new THREE.Mesh(new THREE.CylinderGeometry(r, r, h, seg), mat);
      if (axis === "z") m.rotation.x = Math.PI / 2; else if (axis === "x") m.rotation.z = Math.PI / 2;
      m.position.set(x, y, z); g.add(m); return m;
    }
    // frame, bumper, radiator face with louvres
    B(9.4, 1.7, 0.55, greyMat, 0, 0, 1.15);
    B(0.4, 3.6, 0.5, greyMat, 5.02, 0, 0.75);
    B(0.05, 2.4, 0.34, hazMat, 5.24, 0, 0.75);
    B(0.9, 3.2, 1.5, bodyMat, 4.7, 0, 1.7);
    var i;
    for (i = 0; i < 5; i++) B(0.05, 2.0, 0.09, darkMat, 5.17, 0, 1.2 + i * 0.24);
    Cy(0.09, 0.1, 10, greyMat, 5.17, 1.3, 2.1, "x");
    Cy(0.09, 0.1, 10, greyMat, 5.17, -1.3, 2.1, "x");
    // high front deck with rails and access ladder
    B(3.0, 3.7, 0.14, greyMat, 3.3, 0, 2.42);
    var p, py = [-1.6, -0.55, 0.55, 1.6];
    for (i = 0; i < 4; i++) Cy(0.025, 0.5, 5, greyMat, 4.78, py[i], 2.74, "z");
    Cy(0.02, 3.3, 5, greyMat, 4.78, 0, 2.99, "x");
    Cy(0.025, 0.5, 5, greyMat, 3.0, -1.78, 2.74, "z");
    Cy(0.025, 0.5, 5, greyMat, 4.3, -1.78, 2.74, "z");
    Cy(0.02, 1.5, 5, greyMat, 3.65, -1.78, 2.99, "x");
    Cy(0.02, 2.1, 5, greyMat, 5.14, 1.35, 1.35, "z");
    Cy(0.02, 2.1, 5, greyMat, 5.14, 1.65, 1.35, "z");
    for (i = 0; i < 5; i++) B(0.05, 0.32, 0.05, greyMat, 5.14, 1.5, 0.5 + i * 0.42);
    // armoured cab (left) with slit windows
    B(1.6, 1.55, 1.25, bodyMat, 3.4, 1.0, 3.12);
    B(1.8, 1.75, 0.1, bodyMat, 3.4, 1.0, 3.8);
    B(0.04, 1.1, 0.26, glassMat, 4.22, 1.0, 3.5);
    B(0.85, 0.03, 0.24, glassMat, 3.4, 1.79, 3.5);
    B(0.85, 0.03, 0.24, glassMat, 3.35, 0.21, 3.5);
    Cy(0.014, 1.2, 5, darkMat, 2.75, 1.55, 4.4, "z");
    // twin exhausts + air cleaner on the right deck
    Cy(0.1, 1.3, 8, darkMat, 3.0, -0.85, 3.1, "z");
    Cy(0.1, 1.3, 8, darkMat, 3.0, -1.15, 3.1, "z");
    Cy(0.28, 0.9, 12, greyMat, 3.9, -1.05, 2.85, "y");
    B(0.5, 0.5, 0.35, greyMat, 2.4, -1.2, 2.7);
    // ore body: floor, flared sides, tall front wall, canopy over cab
    B(7.0, 3.8, 0.2, bodyMat, -1.75, 0, 2.2);
    B(7.0, 0.16, 1.35, bodyMat, -1.75, 1.92, 2.95);
    B(7.0, 0.16, 1.35, bodyMat, -1.75, -1.92, 2.95);
    B(0.2, 3.8, 1.6, bodyMat, 1.7, 0, 3.05);
    B(2.9, 3.8, 0.16, bodyMat, 3.1, 0, 3.98).rotation.y = 0.1;
    for (i = 0; i < 5; i++) {
      B(0.12, 0.1, 1.35, bodyMat, -4.6 + i * 1.45, 2.02, 2.95);
      B(0.12, 0.1, 1.35, bodyMat, -4.6 + i * 1.45, -2.02, 2.95);
    }
    for (i = 0; i < 3; i++) B(0.16, 3.8, 0.22, greyMat, -3.9 + i * 2.1, 0, 2.02);
    B(0.24, 3.8, 0.2, bodyMat, -5.32, 0, 2.24);
    // ore heap with gold glints
    var s1 = new THREE.Mesh(new THREE.SphereGeometry(1, 18, 12), oreMat);
    s1.scale.set(3.1, 1.8, 0.8); s1.position.set(-2.0, 0, 3.15); g.add(s1);
    var s2 = new THREE.Mesh(new THREE.SphereGeometry(1, 16, 10), oreMat);
    s2.scale.set(2.0, 1.4, 0.65); s2.position.set(-0.4, 0.3, 3.3); g.add(s2);
    var s3 = new THREE.Mesh(new THREE.SphereGeometry(1, 14, 10), oreMat);
    s3.scale.set(1.3, 1.0, 0.5); s3.position.set(-3.9, -0.4, 3.3); g.add(s3);
    // 6 fat wheels: 2 front singles + 4 rear duals
    var s;
    for (s = -1; s <= 1; s += 2) {
      Cy(0.95, 0.75, 20, darkMat, 3.3, s * 1.6, 0.95);
      Cy(0.34, 0.79, 12, greyMat, 3.3, s * 1.61, 0.95);
      Cy(0.95, 0.52, 20, darkMat, -2.6, s * 1.28, 0.95);
      Cy(0.95, 0.52, 20, darkMat, -2.6, s * 1.82, 0.95);
      Cy(0.34, 0.56, 12, greyMat, -2.6, s * 1.83, 0.95);
    }
    // rear crossmember with hazard plate + taillights
    B(0.3, 2.6, 0.5, greyMat, -4.62, 0, 0.95);
    B(0.05, 2.0, 0.3, hazMat, -4.8, 0, 0.95);
    B(0.1, 0.22, 0.12, darkMat, -4.75, 1.1, 1.25);
    B(0.1, 0.22, 0.12, darkMat, -4.75, -1.1, 1.25);
    // team ID panels
    B(0.05, 0.6, 0.35, teamMat, 1.57, 1.35, 3.5);
    B(0.05, 0.6, 0.35, teamMat, 1.57, -1.35, 3.5);
    B(0.6, 0.03, 0.28, teamMat, 3.4, 1.8, 3.05);
    return g;
  }
};
UNIT_MODELS["mcv"] = {
  len: 12,
  build: function (THREE, M, C) {
    var g = new THREE.Group();
    var seed = 13750699;
    function R() { seed = (seed * 16807 + 71) % 2147483647; return seed / 2147483647; }
    function paintBody(base) {
      var cv = document.createElement("canvas"); cv.width = 512; cv.height = 512;
      var q = cv.getContext("2d");
      q.fillStyle = base; q.fillRect(0, 0, 512, 512);
      var i, p;
      for (i = 0; i < 30; i++) {
        q.fillStyle = R() < 0.5 ? "rgba(235,240,232,0.05)" : "rgba(8,10,8,0.06)";
        q.fillRect(R() * 512, R() * 512, 40 + R() * 120, 30 + R() * 96);
      }
      q.strokeStyle = "rgba(10,12,10,0.28)"; q.lineWidth = 2;
      for (i = 0; i < 7; i++) { p = 20 + R() * 472; q.beginPath(); q.moveTo(p, 0); q.lineTo(p, 512); q.stroke(); }
      for (i = 0; i < 5; i++) { p = 20 + R() * 472; q.beginPath(); q.moveTo(0, p); q.lineTo(512, p); q.stroke(); }
      q.fillStyle = "rgba(0,0,0,0.22)";
      for (i = 0; i < 60; i++) q.fillRect(R() * 508, R() * 508, 2, 2);
      q.fillStyle = "rgba(16,14,8,0.09)";
      for (i = 0; i < 20; i++) q.fillRect(R() * 512, R() * 400, 3 + R() * 5, 40 + R() * 120);
      var t = new THREE.CanvasTexture(cv); t.wrapS = t.wrapT = THREE.RepeatWrapping; return t;
    }
    function contTex(base) {
      var cv = document.createElement("canvas"); cv.width = 256; cv.height = 256;
      var q = cv.getContext("2d");
      q.fillStyle = base; q.fillRect(0, 0, 256, 256);
      var i;
      for (i = 0; i < 256; i += 16) {
        q.fillStyle = "rgba(255,255,255,0.13)"; q.fillRect(i, 0, 3, 256);
        q.fillStyle = "rgba(0,0,0,0.17)"; q.fillRect(i + 8, 0, 3, 256);
      }
      q.fillStyle = "rgba(0,0,0,0.15)"; q.fillRect(0, 120, 256, 3);
      q.fillStyle = "rgba(20,16,10,0.1)";
      for (i = 0; i < 12; i++) q.fillRect(R() * 256, R() * 200, 8 + R() * 26, 16 + R() * 46);
      var t = new THREE.CanvasTexture(cv); t.wrapS = t.wrapT = THREE.RepeatWrapping; return t;
    }
    function hazTex() {
      var cv = document.createElement("canvas"); cv.width = 128; cv.height = 128;
      var q = cv.getContext("2d");
      q.fillStyle = "#b99a20"; q.fillRect(0, 0, 128, 128);
      q.fillStyle = "#1a1a18";
      q.save(); q.translate(64, 64); q.rotate(-Math.PI / 4);
      for (var i = -3; i <= 3; i++) q.fillRect(-140, i * 40 - 10, 280, 20);
      q.restore();
      var t = new THREE.CanvasTexture(cv); t.wrapS = t.wrapT = THREE.RepeatWrapping; return t;
    }
    var bodyMat = new THREE.MeshStandardMaterial({ map: paintBody("#5d6259"), metalness: 0.3, roughness: 0.6 });
    var contMat = new THREE.MeshStandardMaterial({ map: contTex("#6a6f64"), metalness: 0.3, roughness: 0.62 });
    var craneMat = new THREE.MeshStandardMaterial({ color: 0xb3902f, metalness: 0.35, roughness: 0.55 });
    var darkMat = new THREE.MeshStandardMaterial({ color: 0x242624, metalness: 0.15, roughness: 0.9 });
    var glassMat = new THREE.MeshStandardMaterial({ color: 0x18262e, metalness: 0.5, roughness: 0.14 });
    var hazMat = new THREE.MeshStandardMaterial({ map: hazTex(), metalness: 0.2, roughness: 0.6 });
    var beaconMat = new THREE.MeshStandardMaterial({ color: 0xcc5511, emissive: 0x882200, emissiveIntensity: 0.6, roughness: 0.4, metalness: 0.1 });
    var teamMat = new THREE.MeshStandardMaterial({ color: C.team, metalness: 0.2, roughness: 0.55 });
    function B(sx, sy, sz, mat, x, y, z) { var m = new THREE.Mesh(new THREE.BoxGeometry(sx, sy, sz), mat); m.position.set(x, y, z); g.add(m); return m; }
    function Cy(r, h, seg, mat, x, y, z, axis) {
      var m = new THREE.Mesh(new THREE.CylinderGeometry(r, r, h, seg), mat);
      if (axis === "z") m.rotation.x = Math.PI / 2; else if (axis === "x") m.rotation.z = Math.PI / 2;
      m.position.set(x, y, z); g.add(m); return m;
    }
    // heavy frame, crossmembers, full deck
    B(11.2, 2.4, 0.6, darkMat, -0.1, 0, 1.05);
    var i;
    for (i = 0; i < 3; i++) B(0.3, 3.6, 0.4, darkMat, -3.5 + i * 3.2, 0, 1.55);
    B(11.4, 4.3, 0.22, bodyMat, -0.1, 0, 1.85);
    // wheel skirts
    var s;
    for (s = -1; s <= 1; s += 2) {
      B(3.1, 0.12, 0.65, bodyMat, 0.9, s * 2.05, 1.35);
      B(0.8, 0.12, 0.65, bodyMat, -5.25, s * 2.05, 1.35);
      B(0.7, 0.12, 0.65, bodyMat, 5.35, s * 2.05, 1.35);
    }
    // full-width industrial cab
    B(1.7, 4.0, 1.5, bodyMat, 4.85, 0, 2.72);
    B(1.8, 4.1, 0.12, bodyMat, 4.85, 0, 3.53);
    B(0.06, 3.6, 0.78, glassMat, 5.66, 0, 3.02).rotation.y = -0.18;
    B(1.2, 0.04, 0.6, glassMat, 4.75, 2.01, 3.0);
    B(1.2, 0.04, 0.6, glassMat, 4.75, -2.01, 3.0);
    B(0.06, 2.4, 0.7, darkMat, 5.72, 0, 1.6);
    B(0.15, 2.0, 0.15, darkMat, 5.6, 0, 3.64);
    Cy(0.09, 0.14, 8, beaconMat, 5.45, 1.7, 3.68, "z");
    Cy(0.09, 0.14, 8, beaconMat, 5.45, -1.7, 3.68, "z");
    Cy(0.014, 1.5, 5, darkMat, 4.15, 1.85, 4.3, "z");
    Cy(0.014, 1.2, 5, darkMat, 4.15, -1.85, 4.15, "z");
    for (s = -1; s <= 1; s += 2) {
      Cy(0.02, 0.55, 5, darkMat, 5.5, s * 2.15, 3.15, "z");
      B(0.05, 0.2, 0.35, darkMat, 5.5, s * 2.18, 3.45);
    }
    // bumpers with hazard chevrons
    B(0.35, 4.3, 0.5, bodyMat, 5.8, 0, 0.9);
    B(0.05, 3.4, 0.36, hazMat, 5.99, 0, 0.9);
    B(0.3, 4.2, 0.45, bodyMat, -5.75, 0, 0.9);
    B(0.05, 3.0, 0.32, hazMat, -5.92, 0, 0.9);
    // folded crane spine: pedestal behind cab, boom lying rearward over the deck
    B(1.6, 1.8, 0.22, craneMat, 3.3, 0, 2.07);
    Cy(0.55, 1.2, 16, craneMat, 3.3, 0, 2.78, "z");
    Cy(0.57, 0.18, 16, hazMat, 3.3, 0, 2.4, "z");
    B(0.9, 0.8, 0.6, craneMat, 3.3, 0, 3.55);
    B(7.6, 0.55, 0.55, craneMat, -0.7, 0, 3.8);
    B(2.6, 0.42, 0.42, craneMat, -4.4, 0, 3.77);
    B(0.4, 0.55, 0.65, craneMat, -5.6, 0, 3.7);
    Cy(0.17, 0.24, 10, darkMat, -5.7, 0, 3.5, "y");
    Cy(0.018, 0.7, 5, darkMat, -5.7, 0, 3.1, "z");
    B(0.18, 0.24, 0.32, darkMat, -5.7, 0, 2.68);
    var hk = new THREE.Mesh(new THREE.TorusGeometry(0.1, 0.03, 6, 10, 4.4), darkMat);
    hk.rotation.x = Math.PI / 2; hk.position.set(-5.7, 0, 2.42); g.add(hk);
    // hydraulic ram from pedestal to boom
    var ramO = new THREE.CylinderGeometry(0.09, 0.09, 1.4, 8); ramO.rotateZ(Math.PI / 2);
    var ram1 = new THREE.Mesh(ramO, craneMat); ram1.position.set(2.15, 0, 2.9); ram1.rotation.y = 0.59; g.add(ram1);
    var ramI = new THREE.CylinderGeometry(0.055, 0.055, 1.2, 8); ramI.rotateZ(Math.PI / 2);
    var ram2 = new THREE.Mesh(ramI, darkMat); ram2.position.set(1.5, 0, 3.33); ram2.rotation.y = 0.59; g.add(ram2);
    // container modules flanking the spine + rear generator set
    B(3.4, 1.55, 1.5, contMat, 0.3, 1.25, 2.72);
    B(2.0, 1.55, 1.35, contMat, 1.3, -1.25, 2.64);
    B(1.6, 1.55, 1.1, contMat, -1.3, -1.25, 2.52);
    B(1.8, 3.4, 1.2, contMat, -3.0, 0, 2.57);
    for (i = 0; i < 4; i++) B(0.24, 3.42, 0.08, darkMat, -3.55 + i * 0.38, 0, 3.18);
    // cable reel at rear right
    Cy(0.42, 0.06, 14, darkMat, -5.1, -1.32, 2.42, "y");
    Cy(0.42, 0.06, 14, darkMat, -5.1, -0.88, 2.42, "y");
    Cy(0.2, 0.4, 10, craneMat, -5.1, -1.1, 2.42, "y");
    // deck handrails
    for (s = -1; s <= 1; s += 2) {
      Cy(0.018, 9.4, 5, darkMat, -0.9, s * 2.09, 2.9, "x");
      var px = [3.6, 1.4, -0.8, -3.0, -5.2];
      for (i = 0; i < 5; i++) Cy(0.022, 0.95, 5, darkMat, px[i], s * 2.09, 2.43, "z");
    }
    // 10 wheels + hubs
    var ax = [4.6, 3.2, -1.4, -2.8, -4.2], a;
    for (a = 0; a < 5; a++) for (s = -1; s <= 1; s += 2) {
      Cy(0.78, 0.55, 18, darkMat, ax[a], s * 1.85, 0.78);
      Cy(0.26, 0.6, 12, bodyMat, ax[a], s * 1.87, 0.78);
    }
    // team ID: cab stripes + front panel
    B(1.5, 0.04, 0.25, teamMat, 4.85, 2.03, 2.35);
    B(1.5, 0.04, 0.25, teamMat, 4.85, -2.03, 2.35);
    B(0.04, 1.2, 0.3, teamMat, 5.73, 0, 2.25);
    return g;
  }
};

/* ---- pack a0fb4699 ---- */
UNIT_MODELS["inf_rifle"] = {
  len: 0.7,
  build: function (THREE, M, C) {
    var g = new THREE.Group();
    function camo(base, b1, b2) {
      var cv = document.createElement("canvas"); cv.width = 64; cv.height = 64;
      var x = cv.getContext("2d"); x.fillStyle = base; x.fillRect(0, 0, 64, 64);
      var s = 7; function rn() { s = (s * 16807) % 2147483647; return (s % 1000) / 1000; }
      x.globalAlpha = 0.33;
      for (var i = 0; i < 26; i++) {
        x.fillStyle = (i % 2) ? b1 : b2;
        x.beginPath(); x.ellipse(rn() * 64, rn() * 64, 3 + rn() * 7, 2 + rn() * 4, rn() * 3.1, 0, 6.29); x.fill();
      }
      x.globalAlpha = 1;
      var t = new THREE.CanvasTexture(cv); t.wrapS = t.wrapT = THREE.RepeatWrapping; return t;
    }
    var fat = new THREE.MeshStandardMaterial({ color: 0xffffff, map: camo("#4a5240", "#3b4434", "#5a6148"), roughness: 0.85, metalness: 0.05 });
    var gear = new THREE.MeshStandardMaterial({ color: 0x33362c, roughness: 0.9, metalness: 0.05 });
    var skin = new THREE.MeshStandardMaterial({ color: 0xb08a68, roughness: 0.9, metalness: 0.0 });
    var gun = new THREE.MeshStandardMaterial({ color: 0x24272b, roughness: 0.5, metalness: 0.4 });
    var team = new THREE.MeshStandardMaterial({ color: C.team, roughness: 0.7, metalness: 0.1 });
    function box(a, b, c, mat) { return new THREE.Mesh(new THREE.BoxGeometry(a, b, c), mat); }
    function limb(len, rA, rB, mat) {
      var geo = new THREE.CylinderGeometry(rA, rB, len, 7);
      geo.rotateX(Math.PI / 2); geo.translate(0, 0, -len / 2);
      return new THREE.Mesh(geo, mat);
    }
    var pel = box(0.21, 0.3, 0.2, fat); pel.position.set(0, 0, 0.93); g.add(pel);
    var tor = box(0.22, 0.31, 0.46, fat); tor.position.set(0, 0, 1.25); g.add(tor);
    var vest = box(0.28, 0.36, 0.4, gear); vest.position.set(0, 0, 1.28); g.add(vest);
    var p1 = box(0.07, 0.1, 0.12, gear); p1.position.set(0.16, 0.08, 1.16); g.add(p1);
    var p2 = box(0.07, 0.1, 0.12, gear); p2.position.set(0.16, -0.08, 1.16); g.add(p2);
    var p3 = box(0.08, 0.14, 0.1, gear); p3.position.set(-0.15, 0.06, 0.95); g.add(p3);
    function leg(side, swing) {
      var th = limb(0.46, 0.075, 0.06, fat); th.position.set(0, side * 0.1, 0.95);
      th.rotation.y = swing; th.rotation.x = side * 0.05;
      var ca = limb(0.42, 0.058, 0.048, fat); ca.position.set(0, 0, -0.46); ca.rotation.y = -swing + 0.05; th.add(ca);
      var bt = box(0.27, 0.11, 0.1, gear); bt.position.set(0.06, 0, -0.43); ca.add(bt);
      /* named so render3d can drive a walk cycle: these figures are built
         posed mid-stride and used to slide along the ground unmoving */
      th.name = side > 0 ? "leg.L" : "leg.R";
      th.userData.stride = swing;
      th.userData.shin = ca;
      th.userData.shinBase = -swing + 0.05;
      g.add(th);
    }
    leg(1, -0.05); leg(-1, 0.06);
    function arm(side, ry, rx, fy) {
      var ua = limb(0.3, 0.055, 0.048, fat); ua.position.set(0, side * 0.235, 1.47);
      ua.rotation.y = ry; ua.rotation.x = rx;
      var fa = limb(0.28, 0.047, 0.04, fat); fa.position.set(0, 0, -0.3); fa.rotation.y = fy; ua.add(fa);
      var hn = new THREE.Mesh(new THREE.SphereGeometry(0.045, 6, 5), skin); hn.position.set(0, 0, -0.29); fa.add(hn);
      g.add(ua); return ua;
    }
    arm(-1, -0.55, 0.35, -1.5);
    arm(1, -0.9, -0.35, -1.0);
    var hd = new THREE.Mesh(new THREE.SphereGeometry(0.105, 10, 8), skin); hd.position.set(0.02, 0, 1.62); g.add(hd);
    var hm = new THREE.Mesh(new THREE.SphereGeometry(0.13, 10, 8), fat); hm.scale.set(1, 1, 0.75); hm.position.set(0.01, 0, 1.67); g.add(hm);
    var bgeo = new THREE.CylinderGeometry(0.135, 0.135, 0.035, 10, 1, true); bgeo.rotateX(Math.PI / 2);
    var band = new THREE.Mesh(bgeo, team); band.position.set(0.01, 0, 1.64); g.add(band);
    var rif = new THREE.Group();
    function rbox(a, b, c, x, y, z) { var m = box(a, b, c, gun); m.position.set(x, y, z); rif.add(m); return m; }
    rbox(0.5, 0.045, 0.075, 0.02, 0, 0);
    rbox(0.16, 0.04, 0.07, -0.31, 0, 0.005);
    var mag = rbox(0.05, 0.035, 0.15, 0.08, 0, -0.1); mag.rotation.y = 0.35;
    rbox(0.1, 0.03, 0.03, 0.1, 0, 0.055);
    rbox(0.04, 0.03, 0.08, -0.08, 0, -0.07);
    var bge = new THREE.CylinderGeometry(0.016, 0.016, 0.34, 7); bge.rotateZ(-Math.PI / 2);
    var brl = new THREE.Mesh(bge, gun); brl.position.set(0.44, 0, 0.015); rif.add(brl);
    rif.position.set(0.17, 0.02, 1.28); rif.rotation.set(0, -0.5, 0.45);
    g.add(rif);
    var age = new THREE.CylinderGeometry(0.006, 0.006, 0.5, 5); age.rotateX(Math.PI / 2);
    var ant = new THREE.Mesh(age, gun); ant.position.set(-0.16, 0.1, 1.65); ant.rotation.x = 0.15; g.add(ant);
    return g;
  }
};

UNIT_MODELS["inf_mg"] = {
  len: 2.6,
  build: function (THREE, M, C) {
    var g = new THREE.Group();
    function camo(base, b1, b2) {
      var cv = document.createElement("canvas"); cv.width = 64; cv.height = 64;
      var x = cv.getContext("2d"); x.fillStyle = base; x.fillRect(0, 0, 64, 64);
      var s = 11; function rn() { s = (s * 16807) % 2147483647; return (s % 1000) / 1000; }
      x.globalAlpha = 0.33;
      for (var i = 0; i < 26; i++) {
        x.fillStyle = (i % 2) ? b1 : b2;
        x.beginPath(); x.ellipse(rn() * 64, rn() * 64, 3 + rn() * 7, 2 + rn() * 4, rn() * 3.1, 0, 6.29); x.fill();
      }
      x.globalAlpha = 1;
      var t = new THREE.CanvasTexture(cv); t.wrapS = t.wrapT = THREE.RepeatWrapping; return t;
    }
    var fat = new THREE.MeshStandardMaterial({ color: 0xffffff, map: camo("#47503e", "#3a4232", "#576046"), roughness: 0.85, metalness: 0.05 });
    var gear = new THREE.MeshStandardMaterial({ color: 0x33362c, roughness: 0.9, metalness: 0.05 });
    var skin = new THREE.MeshStandardMaterial({ color: 0xb08a68, roughness: 0.9, metalness: 0.0 });
    var gun = new THREE.MeshStandardMaterial({ color: 0x24272b, roughness: 0.5, metalness: 0.4 });
    var team = new THREE.MeshStandardMaterial({ color: C.team, roughness: 0.7, metalness: 0.1 });
    function box(a, b, c, mat) { return new THREE.Mesh(new THREE.BoxGeometry(a, b, c), mat); }
    var hips = box(0.36, 0.34, 0.17, fat); hips.position.set(-0.2, 0, 0.12); g.add(hips);
    var chest = box(0.46, 0.36, 0.19, fat); chest.position.set(0.2, 0, 0.16); chest.rotation.y = -0.18; g.add(chest);
    var pack = box(0.26, 0.24, 0.14, gear); pack.position.set(-0.02, 0, 0.26); g.add(pack);
    function pleg(side) {
      var geo = new THREE.CylinderGeometry(0.05, 0.07, 0.85, 7); geo.rotateZ(Math.PI / 2);
      var m = new THREE.Mesh(geo, fat); m.position.set(-0.78, side * 0.16, 0.1); m.rotation.z = -side * 0.12; g.add(m);
      var bt = box(0.12, 0.1, 0.22, gear); bt.position.set(-1.22, side * 0.24, 0.1); bt.rotation.y = 0.3; g.add(bt);
    }
    pleg(1); pleg(-1);
    function parm(side) {
      var geo = new THREE.CylinderGeometry(0.045, 0.055, 0.32, 7); geo.rotateZ(Math.PI / 2);
      var m = new THREE.Mesh(geo, fat); m.position.set(0.5, side * 0.19, 0.17); m.rotation.z = -side * 0.28; g.add(m);
      var hn = new THREE.Mesh(new THREE.SphereGeometry(0.045, 6, 5), skin); hn.position.set(0.68, side * 0.09, 0.2); g.add(hn);
    }
    parm(1); parm(-1);
    var hd = new THREE.Mesh(new THREE.SphereGeometry(0.105, 10, 8), skin); hd.position.set(0.52, 0, 0.3); g.add(hd);
    var hm = new THREE.Mesh(new THREE.SphereGeometry(0.13, 10, 8), fat); hm.scale.set(1, 1, 0.75); hm.position.set(0.53, 0, 0.35); g.add(hm);
    var bgeo = new THREE.CylinderGeometry(0.135, 0.135, 0.03, 10, 1, true); bgeo.rotateX(Math.PI / 2);
    var band = new THREE.Mesh(bgeo, team); band.position.set(0.53, 0, 0.325); g.add(band);
    var rec = box(0.55, 0.09, 0.12, gun); rec.position.set(0.75, 0, 0.24); g.add(rec);
    var stk = box(0.16, 0.05, 0.09, gun); stk.position.set(0.42, 0, 0.23); g.add(stk);
    var sgt = box(0.1, 0.03, 0.04, gun); sgt.position.set(0.72, 0, 0.32); g.add(sgt);
    var bge = new THREE.CylinderGeometry(0.02, 0.024, 0.6, 8); bge.rotateZ(-Math.PI / 2);
    var brl = new THREE.Mesh(bge, gun); brl.position.set(1.32, 0, 0.26); g.add(brl);
    var fge = new THREE.CylinderGeometry(0.034, 0.022, 0.09, 8); fge.rotateZ(-Math.PI / 2);
    var fh = new THREE.Mesh(fge, gun); fh.position.set(1.66, 0, 0.26); g.add(fh);
    function bpod(side) {
      var geo = new THREE.CylinderGeometry(0.011, 0.011, 0.3, 5);
      geo.rotateX(Math.PI / 2); geo.translate(0, 0, -0.15);
      var m = new THREE.Mesh(geo, gun); m.position.set(1.28, side * 0.03, 0.25); m.rotation.x = side * 0.55; g.add(m);
    }
    bpod(1); bpod(-1);
    var abx = box(0.26, 0.15, 0.17, gear); abx.position.set(0.82, 0.32, 0.09); g.add(abx);
    var lid = box(0.27, 0.16, 0.015, gun); lid.position.set(0.82, 0.32, 0.18); g.add(lid);
    var blt = box(0.05, 0.2, 0.03, gun); blt.position.set(0.8, 0.16, 0.21); blt.rotation.x = 0.4; g.add(blt);
    return g;
  }
};

UNIT_MODELS["inf_at"] = {
  len: 1.3,
  build: function (THREE, M, C) {
    var g = new THREE.Group();
    function camo(base, b1, b2) {
      var cv = document.createElement("canvas"); cv.width = 64; cv.height = 64;
      var x = cv.getContext("2d"); x.fillStyle = base; x.fillRect(0, 0, 64, 64);
      var s = 23; function rn() { s = (s * 16807) % 2147483647; return (s % 1000) / 1000; }
      x.globalAlpha = 0.33;
      for (var i = 0; i < 26; i++) {
        x.fillStyle = (i % 2) ? b1 : b2;
        x.beginPath(); x.ellipse(rn() * 64, rn() * 64, 3 + rn() * 7, 2 + rn() * 4, rn() * 3.1, 0, 6.29); x.fill();
      }
      x.globalAlpha = 1;
      var t = new THREE.CanvasTexture(cv); t.wrapS = t.wrapT = THREE.RepeatWrapping; return t;
    }
    var fat = new THREE.MeshStandardMaterial({ color: 0xffffff, map: camo("#4c5442", "#3d4535", "#5c634a"), roughness: 0.85, metalness: 0.05 });
    var gear = new THREE.MeshStandardMaterial({ color: 0x33362c, roughness: 0.9, metalness: 0.05 });
    var skin = new THREE.MeshStandardMaterial({ color: 0xa9835f, roughness: 0.9, metalness: 0.0 });
    var gun = new THREE.MeshStandardMaterial({ color: 0x2a2e28, roughness: 0.55, metalness: 0.35 });
    var team = new THREE.MeshStandardMaterial({ color: C.team, roughness: 0.7, metalness: 0.1 });
    function box(a, b, c, mat) { return new THREE.Mesh(new THREE.BoxGeometry(a, b, c), mat); }
    function limb(len, rA, rB, mat) {
      var geo = new THREE.CylinderGeometry(rA, rB, len, 7);
      geo.rotateX(Math.PI / 2); geo.translate(0, 0, -len / 2);
      return new THREE.Mesh(geo, mat);
    }
    var pel = box(0.21, 0.3, 0.19, fat); pel.position.set(-0.02, 0, 0.55); g.add(pel);
    var tor = box(0.22, 0.31, 0.44, fat); tor.position.set(0.02, 0, 0.88); g.add(tor);
    var vest = box(0.28, 0.36, 0.38, gear); vest.position.set(0.03, 0, 0.9); g.add(vest);
    var p1 = box(0.07, 0.1, 0.12, gear); p1.position.set(0.18, 0.08, 0.8); g.add(p1);
    var thL = limb(0.44, 0.075, 0.06, fat); thL.position.set(0, 0.1, 0.53); thL.rotation.y = -1.35; g.add(thL);
    var caL = limb(0.42, 0.058, 0.048, fat); caL.position.set(0, 0, -0.44); caL.rotation.y = 1.3; thL.add(caL);
    var btL = box(0.27, 0.11, 0.1, gear); btL.position.set(0.06, 0, -0.4); caL.add(btL);
    var thR = limb(0.44, 0.075, 0.06, fat); thR.position.set(0, -0.1, 0.53); thR.rotation.y = 0.2; g.add(thR);
    var caR = limb(0.4, 0.058, 0.048, fat); caR.position.set(0, 0, -0.44); caR.rotation.y = 1.35; thR.add(caR);
    var btR = box(0.13, 0.1, 0.16, gear); btR.position.set(0.02, 0, -0.42); caR.add(btR);
    function arm(side, ry, rx, fy) {
      var ua = limb(0.3, 0.055, 0.048, fat); ua.position.set(0.02, side * 0.235, 1.1);
      ua.rotation.y = ry; ua.rotation.x = rx;
      var fa = limb(0.28, 0.047, 0.04, fat); fa.position.set(0, 0, -0.3); fa.rotation.y = fy; ua.add(fa);
      var hn = new THREE.Mesh(new THREE.SphereGeometry(0.045, 6, 5), skin); hn.position.set(0, 0, -0.29); fa.add(hn);
      g.add(ua);
    }
    arm(-1, -0.8, 0.25, -1.6);
    arm(1, -1.1, -0.55, -1.2);
    var hd = new THREE.Mesh(new THREE.SphereGeometry(0.105, 10, 8), skin); hd.position.set(0.06, 0, 1.24); g.add(hd);
    var hm = new THREE.Mesh(new THREE.SphereGeometry(0.13, 10, 8), fat); hm.scale.set(1, 1, 0.75); hm.position.set(0.05, 0, 1.29); g.add(hm);
    var bgeo = new THREE.CylinderGeometry(0.135, 0.135, 0.035, 10, 1, true); bgeo.rotateX(Math.PI / 2);
    var band = new THREE.Mesh(bgeo, team); band.position.set(0.05, 0, 1.26); g.add(band);
    var tube = new THREE.Group();
    var tge = new THREE.CylinderGeometry(0.062, 0.062, 1.15, 10); tge.rotateZ(-Math.PI / 2);
    tube.add(new THREE.Mesh(tge, gun));
    var mge = new THREE.CylinderGeometry(0.082, 0.064, 0.14, 10); mge.rotateZ(-Math.PI / 2);
    var mzl = new THREE.Mesh(mge, gun); mzl.position.set(0.63, 0, 0); tube.add(mzl);
    var vge = new THREE.CylinderGeometry(0.062, 0.09, 0.16, 10); vge.rotateZ(-Math.PI / 2);
    var ven = new THREE.Mesh(vge, gun); ven.position.set(-0.63, 0, 0); tube.add(ven);
    var gr1 = box(0.05, 0.04, 0.12, gun); gr1.position.set(0.08, 0, -0.11); tube.add(gr1);
    var gr2 = box(0.05, 0.04, 0.1, gun); gr2.position.set(0.28, 0, -0.1); tube.add(gr2);
    var sig = box(0.1, 0.04, 0.06, gun); sig.position.set(0.2, 0.08, 0.05); tube.add(sig);
    tube.position.set(0.15, -0.16, 1.22); tube.rotation.y = -0.15;
    g.add(tube);
    return g;
  }
};

UNIT_MODELS["inf_aa"] = {
  len: 1.1,
  build: function (THREE, M, C) {
    var g = new THREE.Group();
    function camo(base, b1, b2) {
      var cv = document.createElement("canvas"); cv.width = 64; cv.height = 64;
      var x = cv.getContext("2d"); x.fillStyle = base; x.fillRect(0, 0, 64, 64);
      var s = 31; function rn() { s = (s * 16807) % 2147483647; return (s % 1000) / 1000; }
      x.globalAlpha = 0.33;
      for (var i = 0; i < 26; i++) {
        x.fillStyle = (i % 2) ? b1 : b2;
        x.beginPath(); x.ellipse(rn() * 64, rn() * 64, 3 + rn() * 7, 2 + rn() * 4, rn() * 3.1, 0, 6.29); x.fill();
      }
      x.globalAlpha = 1;
      var t = new THREE.CanvasTexture(cv); t.wrapS = t.wrapT = THREE.RepeatWrapping; return t;
    }
    var fat = new THREE.MeshStandardMaterial({ color: 0xffffff, map: camo("#485140", "#394131", "#586047"), roughness: 0.85, metalness: 0.05 });
    var gear = new THREE.MeshStandardMaterial({ color: 0x33362c, roughness: 0.9, metalness: 0.05 });
    var skin = new THREE.MeshStandardMaterial({ color: 0xb08a68, roughness: 0.9, metalness: 0.0 });
    var gun = new THREE.MeshStandardMaterial({ color: 0x3d4438, roughness: 0.55, metalness: 0.3 });
    var team = new THREE.MeshStandardMaterial({ color: C.team, roughness: 0.7, metalness: 0.1 });
    function box(a, b, c, mat) { return new THREE.Mesh(new THREE.BoxGeometry(a, b, c), mat); }
    function limb(len, rA, rB, mat) {
      var geo = new THREE.CylinderGeometry(rA, rB, len, 7);
      geo.rotateX(Math.PI / 2); geo.translate(0, 0, -len / 2);
      return new THREE.Mesh(geo, mat);
    }
    var pel = box(0.21, 0.3, 0.2, fat); pel.position.set(0, 0, 0.93); g.add(pel);
    var tor = box(0.22, 0.31, 0.46, fat); tor.position.set(0, 0, 1.25); g.add(tor);
    var vest = box(0.28, 0.36, 0.4, gear); vest.position.set(0, 0, 1.28); g.add(vest);
    var p1 = box(0.07, 0.1, 0.12, gear); p1.position.set(0.16, 0.09, 1.16); g.add(p1);
    var p3 = box(0.08, 0.14, 0.1, gear); p3.position.set(-0.15, -0.06, 0.95); g.add(p3);
    function leg(side, swing) {
      var th = limb(0.46, 0.075, 0.06, fat); th.position.set(0, side * 0.1, 0.95);
      th.rotation.y = swing; th.rotation.x = side * 0.07;
      var ca = limb(0.42, 0.058, 0.048, fat); ca.position.set(0, 0, -0.46); ca.rotation.y = -swing + 0.05; th.add(ca);
      var bt = box(0.27, 0.11, 0.1, gear); bt.position.set(0.06, 0, -0.43); ca.add(bt);
      /* named so render3d can drive a walk cycle: these figures are built
         posed mid-stride and used to slide along the ground unmoving */
      th.name = side > 0 ? "leg.L" : "leg.R";
      th.userData.stride = swing;
      th.userData.shin = ca;
      th.userData.shinBase = -swing + 0.05;
      g.add(th);
    }
    leg(1, 0.1); leg(-1, -0.12);
    function arm(side, ry, rx, fy) {
      var ua = limb(0.3, 0.055, 0.048, fat); ua.position.set(0, side * 0.235, 1.47);
      ua.rotation.y = ry; ua.rotation.x = rx;
      var fa = limb(0.28, 0.047, 0.04, fat); fa.position.set(0, 0, -0.3); fa.rotation.y = fy; ua.add(fa);
      var hn = new THREE.Mesh(new THREE.SphereGeometry(0.045, 6, 5), skin); hn.position.set(0, 0, -0.29); fa.add(hn);
      g.add(ua);
    }
    arm(-1, -1.3, 0.2, -1.5);
    arm(1, -1.7, -0.4, -0.9);
    var hd = new THREE.Mesh(new THREE.SphereGeometry(0.105, 10, 8), skin); hd.position.set(0.02, 0, 1.62); g.add(hd);
    var hm = new THREE.Mesh(new THREE.SphereGeometry(0.13, 10, 8), fat); hm.scale.set(1, 1, 0.75); hm.position.set(0.01, 0, 1.67); g.add(hm);
    var bgeo = new THREE.CylinderGeometry(0.135, 0.135, 0.035, 10, 1, true); bgeo.rotateX(Math.PI / 2);
    var band = new THREE.Mesh(bgeo, team); band.position.set(0.01, 0, 1.64); g.add(band);
    var tube = new THREE.Group();
    var tge = new THREE.CylinderGeometry(0.05, 0.05, 1.5, 10); tge.rotateZ(-Math.PI / 2);
    tube.add(new THREE.Mesh(tge, gun));
    var fge = new THREE.CylinderGeometry(0.066, 0.052, 0.14, 10); fge.rotateZ(-Math.PI / 2);
    var fc = new THREE.Mesh(fge, gun); fc.position.set(0.71, 0, 0); tube.add(fc);
    var rge = new THREE.CylinderGeometry(0.052, 0.062, 0.1, 10); rge.rotateZ(-Math.PI / 2);
    var rc = new THREE.Mesh(rge, gun); rc.position.set(-0.72, 0, 0); tube.add(rc);
    var grip = box(0.18, 0.05, 0.15, gun); grip.position.set(0.05, 0, -0.12); tube.add(grip);
    var skr = box(0.12, 0.06, 0.09, gun); skr.position.set(0.42, 0, -0.08); tube.add(skr);
    var tring = new THREE.CylinderGeometry(0.056, 0.056, 0.07, 10); tring.rotateZ(-Math.PI / 2);
    var tr = new THREE.Mesh(tring, team); tr.position.set(0.55, 0, 0); tube.add(tr);
    tube.position.set(0.06, -0.18, 1.55); tube.rotation.y = -Math.PI / 4;
    g.add(tube);
    return g;
  }
};

UNIT_MODELS["inf_mortar"] = {
  len: 1.3,
  build: function (THREE, M, C) {
    var g = new THREE.Group();
    function camo(base, b1, b2) {
      var cv = document.createElement("canvas"); cv.width = 64; cv.height = 64;
      var x = cv.getContext("2d"); x.fillStyle = base; x.fillRect(0, 0, 64, 64);
      var s = 43; function rn() { s = (s * 16807) % 2147483647; return (s % 1000) / 1000; }
      x.globalAlpha = 0.33;
      for (var i = 0; i < 26; i++) {
        x.fillStyle = (i % 2) ? b1 : b2;
        x.beginPath(); x.ellipse(rn() * 64, rn() * 64, 3 + rn() * 7, 2 + rn() * 4, rn() * 3.1, 0, 6.29); x.fill();
      }
      x.globalAlpha = 1;
      var t = new THREE.CanvasTexture(cv); t.wrapS = t.wrapT = THREE.RepeatWrapping; return t;
    }
    var fat = new THREE.MeshStandardMaterial({ color: 0xffffff, map: camo("#4a5240", "#3c4434", "#596048"), roughness: 0.85, metalness: 0.05 });
    var gear = new THREE.MeshStandardMaterial({ color: 0x33362c, roughness: 0.9, metalness: 0.05 });
    var skin = new THREE.MeshStandardMaterial({ color: 0xb08a68, roughness: 0.9, metalness: 0.0 });
    var gun = new THREE.MeshStandardMaterial({ color: 0x2b2f2a, roughness: 0.5, metalness: 0.4 });
    var team = new THREE.MeshStandardMaterial({ color: C.team, roughness: 0.7, metalness: 0.1 });
    function box(a, b, c, mat) { return new THREE.Mesh(new THREE.BoxGeometry(a, b, c), mat); }
    function limb(len, rA, rB, mat) {
      var geo = new THREE.CylinderGeometry(rA, rB, len, 7);
      geo.rotateX(Math.PI / 2); geo.translate(0, 0, -len / 2);
      return new THREE.Mesh(geo, mat);
    }
    var sol = new THREE.Group(); sol.position.set(-0.1, 0.24, 0); g.add(sol);
    var pel = box(0.21, 0.3, 0.19, fat); pel.position.set(-0.02, 0, 0.55); sol.add(pel);
    var tor = box(0.22, 0.31, 0.44, fat); tor.position.set(0.02, 0, 0.88); sol.add(tor);
    var vest = box(0.28, 0.36, 0.38, gear); vest.position.set(0.03, 0, 0.9); sol.add(vest);
    var thL = limb(0.44, 0.075, 0.06, fat); thL.position.set(0, 0.1, 0.53); thL.rotation.y = -1.35; sol.add(thL);
    var caL = limb(0.42, 0.058, 0.048, fat); caL.position.set(0, 0, -0.44); caL.rotation.y = 1.3; thL.add(caL);
    var btL = box(0.27, 0.11, 0.1, gear); btL.position.set(0.06, 0, -0.4); caL.add(btL);
    var thR = limb(0.44, 0.075, 0.06, fat); thR.position.set(0, -0.1, 0.53); thR.rotation.y = 0.2; sol.add(thR);
    var caR = limb(0.4, 0.058, 0.048, fat); caR.position.set(0, 0, -0.44); caR.rotation.y = 1.35; thR.add(caR);
    var btR = box(0.13, 0.1, 0.16, gear); btR.position.set(0.02, 0, -0.42); caR.add(btR);
    function arm(side, ry, rx, fy) {
      var ua = limb(0.3, 0.055, 0.048, fat); ua.position.set(0.02, side * 0.235, 1.1);
      ua.rotation.y = ry; ua.rotation.x = rx;
      var fa = limb(0.28, 0.047, 0.04, fat); fa.position.set(0, 0, -0.3); fa.rotation.y = fy; ua.add(fa);
      var hn = new THREE.Mesh(new THREE.SphereGeometry(0.045, 6, 5), skin); hn.position.set(0, 0, -0.29); fa.add(hn);
      sol.add(ua); return fa;
    }
    var faR = arm(-1, -2.1, -0.35, -0.3);
    arm(1, -1.0, -0.3, -1.2);
    var rge = new THREE.CylinderGeometry(0.032, 0.032, 0.24, 8); rge.rotateX(Math.PI / 2);
    var rnd = new THREE.Mesh(rge, gun); rnd.position.set(0, 0, -0.38); faR.add(rnd);
    var nge = new THREE.ConeGeometry(0.032, 0.08, 8); nge.rotateX(Math.PI / 2);
    var nose = new THREE.Mesh(nge, gun); nose.position.set(0, 0, -0.54); faR.add(nose);
    var hd = new THREE.Mesh(new THREE.SphereGeometry(0.105, 10, 8), skin); hd.position.set(0.06, 0, 1.24); sol.add(hd);
    var hm = new THREE.Mesh(new THREE.SphereGeometry(0.13, 10, 8), fat); hm.scale.set(1, 1, 0.75); hm.position.set(0.05, 0, 1.29); sol.add(hm);
    var bgeo = new THREE.CylinderGeometry(0.135, 0.135, 0.035, 10, 1, true); bgeo.rotateX(Math.PI / 2);
    var band = new THREE.Mesh(bgeo, team); band.position.set(0.05, 0, 1.26); sol.add(band);
    var bpge = new THREE.CylinderGeometry(0.26, 0.3, 0.06, 12); bpge.rotateX(Math.PI / 2);
    var bp = new THREE.Mesh(bpge, gun); bp.position.set(0.28, -0.28, 0.03); g.add(bp);
    var mt = new THREE.Group(); mt.position.set(0.28, -0.28, 0.05); mt.rotation.y = -1.25; g.add(mt);
    var tge = new THREE.CylinderGeometry(0.045, 0.05, 1.15, 10); tge.rotateZ(-Math.PI / 2); tge.translate(0.575, 0, 0);
    mt.add(new THREE.Mesh(tge, gun));
    var mrge = new THREE.CylinderGeometry(0.055, 0.05, 0.07, 10); mrge.rotateZ(-Math.PI / 2);
    var mring = new THREE.Mesh(mrge, gun); mring.position.set(1.1, 0, 0); mt.add(mring);
    function bleg(side) {
      var geo = new THREE.CylinderGeometry(0.013, 0.013, 0.55, 6);
      geo.rotateX(Math.PI / 2); geo.translate(0, 0, -0.275);
      var m = new THREE.Mesh(geo, gun); m.position.set(0.44, -0.28 + side * 0.02, 0.47);
      m.rotation.x = side * 0.42; m.rotation.y = -0.12; g.add(m);
    }
    bleg(1); bleg(-1);
    var sge = new THREE.CylinderGeometry(0.02, 0.02, 0.3, 6); sge.rotateX(Math.PI / 2);
    var scr = new THREE.Mesh(sge, gun); scr.position.set(0.46, -0.28, 0.4); g.add(scr);
    var crate = box(0.5, 0.3, 0.2, gear); crate.position.set(-0.32, -0.5, 0.1); crate.rotation.z = 0.25; g.add(crate);
    var r2ge = new THREE.CylinderGeometry(0.032, 0.032, 0.3, 8); r2ge.rotateZ(-Math.PI / 2);
    var r2 = new THREE.Mesh(r2ge, gun); r2.position.set(-0.3, -0.45, 0.23); r2.rotation.z = 0.25; g.add(r2);
    var r3 = new THREE.Mesh(r2ge, gun); r3.position.set(-0.34, -0.53, 0.23); r3.rotation.z = 0.25; g.add(r3);
    return g;
  }
};

UNIT_MODELS["inf_sniper"] = {
  len: 2.8,
  build: function (THREE, M, C) {
    var g = new THREE.Group();
    function camo(base, b1, b2) {
      var cv = document.createElement("canvas"); cv.width = 64; cv.height = 64;
      var x = cv.getContext("2d"); x.fillStyle = base; x.fillRect(0, 0, 64, 64);
      var s = 57; function rn() { s = (s * 16807) % 2147483647; return (s % 1000) / 1000; }
      x.globalAlpha = 0.38;
      for (var i = 0; i < 30; i++) {
        x.fillStyle = (i % 2) ? b1 : b2;
        x.beginPath(); x.ellipse(rn() * 64, rn() * 64, 3 + rn() * 8, 2 + rn() * 5, rn() * 3.1, 0, 6.29); x.fill();
      }
      x.globalAlpha = 1;
      var t = new THREE.CanvasTexture(cv); t.wrapS = t.wrapT = THREE.RepeatWrapping; return t;
    }
    var fat = new THREE.MeshStandardMaterial({ color: 0xffffff, map: camo("#454f3a", "#37402d", "#535c41"), roughness: 0.9, metalness: 0.03 });
    var gear = new THREE.MeshStandardMaterial({ color: 0x31352a, roughness: 0.92, metalness: 0.03 });
    var skin = new THREE.MeshStandardMaterial({ color: 0xb08a68, roughness: 0.9, metalness: 0.0 });
    var gun = new THREE.MeshStandardMaterial({ color: 0x23261f, roughness: 0.55, metalness: 0.35 });
    var team = new THREE.MeshStandardMaterial({ color: C.team, roughness: 0.7, metalness: 0.1 });
    var gh1 = new THREE.MeshStandardMaterial({ color: 0x46503c, roughness: 0.95, metalness: 0.0 });
    var gh2 = new THREE.MeshStandardMaterial({ color: 0x39422f, roughness: 0.95, metalness: 0.0 });
    function box(a, b, c, mat) { return new THREE.Mesh(new THREE.BoxGeometry(a, b, c), mat); }
    var hips = box(0.36, 0.34, 0.17, fat); hips.position.set(-0.2, 0, 0.12); g.add(hips);
    var chest = box(0.46, 0.36, 0.19, fat); chest.position.set(0.2, 0, 0.16); chest.rotation.y = -0.16; g.add(chest);
    function pleg(side) {
      var geo = new THREE.CylinderGeometry(0.05, 0.07, 0.85, 7); geo.rotateZ(Math.PI / 2);
      var m = new THREE.Mesh(geo, fat); m.position.set(-0.78, side * 0.15, 0.1); m.rotation.z = -side * 0.1; g.add(m);
      var bt = box(0.12, 0.1, 0.22, gear); bt.position.set(-1.22, side * 0.22, 0.1); bt.rotation.y = 0.3; g.add(bt);
    }
    pleg(1); pleg(-1);
    function parm(side) {
      var geo = new THREE.CylinderGeometry(0.045, 0.055, 0.3, 7); geo.rotateZ(Math.PI / 2);
      var m = new THREE.Mesh(geo, fat); m.position.set(0.48, side * 0.17, 0.16); m.rotation.z = -side * 0.3; g.add(m);
      var hn = new THREE.Mesh(new THREE.SphereGeometry(0.045, 6, 5), skin); hn.position.set(0.64, side * 0.07, 0.19); g.add(hn);
    }
    parm(1); parm(-1);
    var hd = new THREE.Mesh(new THREE.SphereGeometry(0.105, 10, 8), skin); hd.position.set(0.5, 0, 0.29); g.add(hd);
    var hm = new THREE.Mesh(new THREE.SphereGeometry(0.13, 10, 8), fat); hm.scale.set(1, 1, 0.75); hm.position.set(0.51, 0, 0.34); g.add(hm);
    var bgeo = new THREE.CylinderGeometry(0.135, 0.135, 0.03, 10, 1, true); bgeo.rotateX(Math.PI / 2);
    var band = new THREE.Mesh(bgeo, team); band.position.set(0.51, 0, 0.315); g.add(band);
    var lumps = [
      [0.28, 0.1, 0.3, 0.1], [0.08, -0.11, 0.29, 0.09], [-0.14, 0.06, 0.26, 0.11],
      [0.42, -0.07, 0.33, 0.08], [0.55, 0.03, 0.42, 0.07], [-0.38, -0.09, 0.22, 0.09], [-0.05, 0.13, 0.24, 0.08]
    ];
    for (var i = 0; i < lumps.length; i++) {
      var L = lumps[i];
      var m = new THREE.Mesh(new THREE.SphereGeometry(L[3], 7, 5), (i % 2) ? gh1 : gh2);
      m.scale.set(1, 1, 0.6); m.position.set(L[0], L[1], L[2]); g.add(m);
    }
    var rec = box(0.5, 0.05, 0.07, gun); rec.position.set(0.7, 0, 0.22); g.add(rec);
    var stk = box(0.3, 0.05, 0.09, gun); stk.position.set(0.34, 0, 0.2); g.add(stk);
    var bge = new THREE.CylinderGeometry(0.012, 0.016, 0.78, 8); bge.rotateZ(-Math.PI / 2);
    var brl = new THREE.Mesh(bge, gun); brl.position.set(1.32, 0, 0.23); g.add(brl);
    var sup = new THREE.CylinderGeometry(0.022, 0.022, 0.2, 8); sup.rotateZ(-Math.PI / 2);
    var sp = new THREE.Mesh(sup, gun); sp.position.set(1.78, 0, 0.23); g.add(sp);
    var scg = new THREE.CylinderGeometry(0.028, 0.028, 0.24, 8); scg.rotateZ(-Math.PI / 2);
    var sc = new THREE.Mesh(scg, gun); sc.position.set(0.66, 0, 0.3); g.add(sc);
    function bpod(side) {
      var geo = new THREE.CylinderGeometry(0.01, 0.01, 0.26, 5);
      geo.rotateX(Math.PI / 2); geo.translate(0, 0, -0.13);
      var m = new THREE.Mesh(geo, gun); m.position.set(1.5, side * 0.02, 0.23); m.rotation.x = side * 0.5; g.add(m);
    }
    bpod(1); bpod(-1);
    return g;
  }
};

UNIT_MODELS["inf_engineer"] = {
  len: 0.7,
  build: function (THREE, M, C) {
    var g = new THREE.Group();
    function camo(base, b1, b2) {
      var cv = document.createElement("canvas"); cv.width = 64; cv.height = 64;
      var x = cv.getContext("2d"); x.fillStyle = base; x.fillRect(0, 0, 64, 64);
      var s = 67; function rn() { s = (s * 16807) % 2147483647; return (s % 1000) / 1000; }
      x.globalAlpha = 0.33;
      for (var i = 0; i < 26; i++) {
        x.fillStyle = (i % 2) ? b1 : b2;
        x.beginPath(); x.ellipse(rn() * 64, rn() * 64, 3 + rn() * 7, 2 + rn() * 4, rn() * 3.1, 0, 6.29); x.fill();
      }
      x.globalAlpha = 1;
      var t = new THREE.CanvasTexture(cv); t.wrapS = t.wrapT = THREE.RepeatWrapping; return t;
    }
    var fat = new THREE.MeshStandardMaterial({ color: 0xffffff, map: camo("#4d5343", "#3e4435", "#5d634c"), roughness: 0.85, metalness: 0.05 });
    var gear = new THREE.MeshStandardMaterial({ color: 0x33362c, roughness: 0.9, metalness: 0.05 });
    var skin = new THREE.MeshStandardMaterial({ color: 0xb5906c, roughness: 0.9, metalness: 0.0 });
    var yel = new THREE.MeshStandardMaterial({ color: 0xe0a81c, roughness: 0.5, metalness: 0.1 });
    var tbx = new THREE.MeshStandardMaterial({ color: 0x94402f, roughness: 0.5, metalness: 0.35 });
    var team = new THREE.MeshStandardMaterial({ color: C.team, roughness: 0.7, metalness: 0.1 });
    function box(a, b, c, mat) { return new THREE.Mesh(new THREE.BoxGeometry(a, b, c), mat); }
    function limb(len, rA, rB, mat) {
      var geo = new THREE.CylinderGeometry(rA, rB, len, 7);
      geo.rotateX(Math.PI / 2); geo.translate(0, 0, -len / 2);
      return new THREE.Mesh(geo, mat);
    }
    var pel = box(0.21, 0.3, 0.2, fat); pel.position.set(0, 0, 0.93); g.add(pel);
    var tor = box(0.22, 0.31, 0.46, fat); tor.position.set(0, 0, 1.25); g.add(tor);
    var vest = box(0.28, 0.36, 0.4, gear); vest.position.set(0, 0, 1.28); g.add(vest);
    var hv = box(0.02, 0.28, 0.06, tbx); hv.position.set(0.15, 0, 1.36); g.add(hv);
    var p3 = box(0.08, 0.14, 0.1, gear); p3.position.set(-0.15, 0.06, 0.95); g.add(p3);
    function leg(side, swing) {
      var th = limb(0.46, 0.075, 0.06, fat); th.position.set(0, side * 0.1, 0.95);
      th.rotation.y = swing; th.rotation.x = side * 0.06;
      var ca = limb(0.42, 0.058, 0.048, fat); ca.position.set(0, 0, -0.46); ca.rotation.y = -swing + 0.05; th.add(ca);
      var bt = box(0.27, 0.11, 0.1, gear); bt.position.set(0.06, 0, -0.43); ca.add(bt);
      /* named so render3d can drive a walk cycle: these figures are built
         posed mid-stride and used to slide along the ground unmoving */
      th.name = side > 0 ? "leg.L" : "leg.R";
      th.userData.stride = swing;
      th.userData.shin = ca;
      th.userData.shinBase = -swing + 0.05;
      g.add(th);
    }
    leg(1, -0.04); leg(-1, 0.05);
    function arm(side, ry, rx, fy) {
      var ua = limb(0.3, 0.055, 0.048, fat); ua.position.set(0, side * 0.235, 1.47);
      ua.rotation.y = ry; ua.rotation.x = rx;
      var fa = limb(0.28, 0.047, 0.04, fat); fa.position.set(0, 0, -0.3); fa.rotation.y = fy; ua.add(fa);
      var hn = new THREE.Mesh(new THREE.SphereGeometry(0.045, 6, 5), skin); hn.position.set(0, 0, -0.29); fa.add(hn);
      g.add(ua);
    }
    arm(-1, -0.02, 0.12, -0.08);
    arm(1, -0.4, -0.2, -1.3);
    var hd = new THREE.Mesh(new THREE.SphereGeometry(0.105, 10, 8), skin); hd.position.set(0.02, 0, 1.62); g.add(hd);
    var hh = new THREE.Mesh(new THREE.SphereGeometry(0.125, 10, 8), yel); hh.scale.set(1, 1, 0.7); hh.position.set(0.01, 0, 1.68); g.add(hh);
    var brge = new THREE.CylinderGeometry(0.165, 0.165, 0.015, 12); brge.rotateX(Math.PI / 2);
    var brim = new THREE.Mesh(brge, yel); brim.position.set(0.02, 0, 1.63); g.add(brim);
    var tp = box(0.03, 0.06, 0.08, team); tp.position.set(0.01, 0.27, 1.44); g.add(tp);
    var tb = box(0.36, 0.15, 0.17, tbx); tb.position.set(0.02, -0.31, 0.72); g.add(tb);
    var th2 = box(0.14, 0.025, 0.03, gear); th2.position.set(0.02, -0.31, 0.82); g.add(th2);
    var latch = box(0.02, 0.16, 0.02, gear); latch.position.set(0.2, -0.31, 0.74); g.add(latch);
    return g;
  }
};

UNIT_MODELS["inf_medic"] = {
  len: 0.7,
  build: function (THREE, M, C) {
    var g = new THREE.Group();
    function camo(base, b1, b2) {
      var cv = document.createElement("canvas"); cv.width = 64; cv.height = 64;
      var x = cv.getContext("2d"); x.fillStyle = base; x.fillRect(0, 0, 64, 64);
      var s = 83; function rn() { s = (s * 16807) % 2147483647; return (s % 1000) / 1000; }
      x.globalAlpha = 0.33;
      for (var i = 0; i < 26; i++) {
        x.fillStyle = (i % 2) ? b1 : b2;
        x.beginPath(); x.ellipse(rn() * 64, rn() * 64, 3 + rn() * 7, 2 + rn() * 4, rn() * 3.1, 0, 6.29); x.fill();
      }
      x.globalAlpha = 1;
      var t = new THREE.CanvasTexture(cv); t.wrapS = t.wrapT = THREE.RepeatWrapping; return t;
    }
    function crossTex() {
      var cv = document.createElement("canvas"); cv.width = 32; cv.height = 32;
      var x = cv.getContext("2d");
      x.fillStyle = "#f2f2ec"; x.fillRect(0, 0, 32, 32);
      x.fillStyle = "#c1272d"; x.fillRect(13, 5, 6, 22); x.fillRect(5, 13, 22, 6);
      return new THREE.CanvasTexture(cv);
    }
    var fat = new THREE.MeshStandardMaterial({ color: 0xffffff, map: camo("#495341", "#3a4432", "#596349"), roughness: 0.85, metalness: 0.05 });
    var gear = new THREE.MeshStandardMaterial({ color: 0x33362c, roughness: 0.9, metalness: 0.05 });
    var skin = new THREE.MeshStandardMaterial({ color: 0xa9835f, roughness: 0.9, metalness: 0.0 });
    var pk = new THREE.MeshStandardMaterial({ color: 0x565b4c, roughness: 0.85, metalness: 0.05 });
    var crs = new THREE.MeshStandardMaterial({ color: 0xffffff, map: crossTex(), roughness: 0.7, metalness: 0.0 });
    var team = new THREE.MeshStandardMaterial({ color: C.team, roughness: 0.7, metalness: 0.1 });
    function box(a, b, c, mat) { return new THREE.Mesh(new THREE.BoxGeometry(a, b, c), mat); }
    function limb(len, rA, rB, mat) {
      var geo = new THREE.CylinderGeometry(rA, rB, len, 7);
      geo.rotateX(Math.PI / 2); geo.translate(0, 0, -len / 2);
      return new THREE.Mesh(geo, mat);
    }
    var pel = box(0.21, 0.3, 0.2, fat); pel.position.set(0, 0, 0.93); g.add(pel);
    var tor = box(0.22, 0.31, 0.46, fat); tor.position.set(0, 0, 1.25); g.add(tor);
    var vest = box(0.28, 0.36, 0.4, gear); vest.position.set(0, 0, 1.28); g.add(vest);
    var p1 = box(0.07, 0.12, 0.1, crs); p1.position.set(0.16, -0.1, 0.95); g.add(p1);
    function leg(side, swing) {
      var th = limb(0.46, 0.075, 0.06, fat); th.position.set(0, side * 0.1, 0.95);
      th.rotation.y = swing; th.rotation.x = side * 0.05;
      var ca = limb(0.42, 0.058, 0.048, fat); ca.position.set(0, 0, -0.46); ca.rotation.y = -swing + 0.05; th.add(ca);
      var bt = box(0.27, 0.11, 0.1, gear); bt.position.set(0.06, 0, -0.43); ca.add(bt);
      /* named so render3d can drive a walk cycle: these figures are built
         posed mid-stride and used to slide along the ground unmoving */
      th.name = side > 0 ? "leg.L" : "leg.R";
      th.userData.stride = swing;
      th.userData.shin = ca;
      th.userData.shinBase = -swing + 0.05;
      g.add(th);
    }
    leg(1, 0.05); leg(-1, -0.06);
    function arm(side, ry, rx, fy) {
      var ua = limb(0.3, 0.055, 0.048, fat); ua.position.set(0, side * 0.235, 1.47);
      ua.rotation.y = ry; ua.rotation.x = rx;
      var fa = limb(0.28, 0.047, 0.04, fat); fa.position.set(0, 0, -0.3); fa.rotation.y = fy; ua.add(fa);
      var hn = new THREE.Mesh(new THREE.SphereGeometry(0.045, 6, 5), skin); hn.position.set(0, 0, -0.29); fa.add(hn);
      g.add(ua);
    }
    arm(-1, -0.15, 0.15, -0.5);
    arm(1, -0.1, -0.15, -0.45);
    var hd = new THREE.Mesh(new THREE.SphereGeometry(0.105, 10, 8), skin); hd.position.set(0.02, 0, 1.62); g.add(hd);
    var hm = new THREE.Mesh(new THREE.SphereGeometry(0.13, 10, 8), fat); hm.scale.set(1, 1, 0.75); hm.position.set(0.01, 0, 1.67); g.add(hm);
    var bgeo = new THREE.CylinderGeometry(0.135, 0.135, 0.035, 10, 1, true); bgeo.rotateX(Math.PI / 2);
    var band = new THREE.Mesh(bgeo, team); band.position.set(0.01, 0, 1.64); g.add(band);
    var patch = new THREE.Mesh(new THREE.PlaneGeometry(0.09, 0.09), crs);
    patch.position.set(0.0, 0.297, 1.42); patch.rotation.x = -Math.PI / 2; g.add(patch);
    var bpk = box(0.16, 0.3, 0.36, pk); bpk.position.set(-0.24, 0, 1.27); g.add(bpk);
    var bcross = new THREE.Mesh(new THREE.PlaneGeometry(0.16, 0.16), crs);
    bcross.position.set(-0.325, 0, 1.29); bcross.rotation.y = -Math.PI / 2; g.add(bcross);
    var strap1 = box(0.03, 0.04, 0.4, gear); strap1.position.set(-0.13, 0.12, 1.3); g.add(strap1);
    var strap2 = box(0.03, 0.04, 0.4, gear); strap2.position.set(-0.13, -0.12, 1.3); g.add(strap2);
    return g;
  }
};

BLD_MODELS["def_atpost"] = {
  build: function (THREE, M, C) {
    var g = new THREE.Group();
    function ptex(base, seed) {
      var cv = document.createElement("canvas"); cv.width = 128; cv.height = 128;
      var x = cv.getContext("2d"); x.fillStyle = base; x.fillRect(0, 0, 128, 128);
      var s = seed; function rn() { s = (s * 16807) % 2147483647; return (s % 1000) / 1000; }
      for (var i = 0; i < 14; i++) {
        x.globalAlpha = 0.05 + rn() * 0.03; x.fillStyle = (i % 2) ? "#ffffff" : "#101410";
        x.fillRect(rn() * 128, rn() * 128, 14 + rn() * 34, 10 + rn() * 26);
      }
      x.globalAlpha = 0.5; x.strokeStyle = "#22251f"; x.lineWidth = 1;
      x.beginPath();
      for (var j = 1; j < 4; j++) { x.moveTo(j * 32, 0); x.lineTo(j * 32, 128); x.moveTo(0, j * 32); x.lineTo(128, j * 32); }
      x.stroke();
      x.globalAlpha = 0.1; x.fillStyle = "#0a0c0a";
      for (var k = 0; k < 8; k++) { x.fillRect(rn() * 128, rn() * 100, 2 + rn() * 3, 20 + rn() * 26); }
      x.globalAlpha = 1;
      var t = new THREE.CanvasTexture(cv); t.wrapS = t.wrapT = THREE.RepeatWrapping; return t;
    }
    var metal = new THREE.MeshStandardMaterial({ color: 0xffffff, map: ptex("#49523f", 13), roughness: 0.6, metalness: 0.25 });
    var dark = new THREE.MeshStandardMaterial({ color: 0x2b2e29, roughness: 0.55, metalness: 0.35 });
    var conc = new THREE.MeshStandardMaterial({ color: 0xffffff, map: ptex("#8b8b83", 29), roughness: 0.95, metalness: 0.0 });
    var wood = new THREE.MeshStandardMaterial({ color: 0x6f5f42, roughness: 0.85, metalness: 0.0 });
    var team = new THREE.MeshStandardMaterial({ color: C.team, roughness: 0.7, metalness: 0.1 });
    function box(a, b, c, mat) { return new THREE.Mesh(new THREE.BoxGeometry(a, b, c), mat); }
    function cylZ(rA, rB, h, mat, seg) {
      var geo = new THREE.CylinderGeometry(rA, rB, h, seg || 12); geo.rotateX(Math.PI / 2);
      return new THREE.Mesh(geo, mat);
    }
    function cylX(rA, rB, len, mat, seg) {
      var geo = new THREE.CylinderGeometry(rA, rB, len, seg || 10); geo.rotateZ(-Math.PI / 2);
      return new THREE.Mesh(geo, mat);
    }
    var base = cylZ(2.4, 2.5, 0.2, conc, 20); base.position.set(0, 0, 0.1); g.add(base);
    var cr1 = box(0.85, 0.45, 0.35, wood); cr1.position.set(-1.5, 1.45, 0.38); cr1.rotation.z = 0.3; g.add(cr1);
    var cr2 = box(0.85, 0.45, 0.35, wood); cr2.position.set(-1.45, 1.5, 0.73); cr2.rotation.z = 0.45; g.add(cr2);
    var tur = new THREE.Group(); tur.name = "turret"; tur.position.set(0, 0, 0.2); g.add(tur);
    var car = box(1.3, 0.85, 0.4, metal); car.position.set(-0.1, 0, 0.5); tur.add(car);
    function wheel(side) {
      var w = new THREE.Mesh(new THREE.CylinderGeometry(0.42, 0.42, 0.22, 14), dark);
      w.position.set(0.15, side * 0.62, 0.42); tur.add(w);
      var hub = new THREE.Mesh(new THREE.CylinderGeometry(0.17, 0.17, 0.24, 10), metal);
      hub.position.set(0.15, side * 0.62, 0.42); tur.add(hub);
    }
    wheel(1); wheel(-1);
    function trail(side) {
      var geo = new THREE.BoxGeometry(2.7, 0.17, 0.15); geo.translate(-1.35, 0, 0);
      var t = new THREE.Mesh(geo, metal); t.position.set(-0.6, side * 0.14, 0.35);
      t.rotation.z = -side * 0.3; tur.add(t);
      var sp = box(0.12, 0.3, 0.35, metal); sp.position.set(-2.55, 0, -0.06); t.add(sp);
    }
    trail(1); trail(-1);
    var ev = new THREE.Group(); ev.position.set(0.1, 0, 0.78); ev.rotation.y = -0.02; tur.add(ev);
    var brch = box(0.65, 0.26, 0.3, metal); brch.position.set(-0.05, 0, 0); ev.add(brch);
    var brl = cylX(0.05, 0.1, 5.5, dark); brl.position.set(3.05, 0, 0); ev.add(brl);
    var slv = cylX(0.062, 0.062, 0.35, dark); slv.position.set(5.6, 0, 0); ev.add(slv);
    var rcl = cylX(0.065, 0.065, 1.3, metal); rcl.position.set(1.1, 0, -0.16); ev.add(rcl);
    function plate(y, w, h, z) {
      var p = box(0.05, w, h, metal); p.position.set(0.55, y, z); p.rotation.y = -0.35; tur.add(p); return p;
    }
    var pr = plate(-0.58, 1.05, 1.15, 0.95);
    plate(0.58, 1.05, 1.15, 0.95);
    plate(0, 0.55, 0.55, 0.52);
    var tp = box(0.02, 0.4, 0.14, team); tp.position.set(0.035, 0, 0.25); pr.add(tp);
    var seat = box(0.28, 0.28, 0.05, dark); seat.position.set(-0.55, 0.45, 0.62); tur.add(seat);
    var spost = cylZ(0.03, 0.03, 0.25, dark, 6); spost.position.set(-0.55, 0.45, 0.5); tur.add(spost);
    var hwge = new THREE.TorusGeometry(0.09, 0.015, 6, 12);
    var hw1 = new THREE.Mesh(hwge, dark); hw1.position.set(-0.35, 0.5, 0.85); hw1.rotation.x = Math.PI / 2; tur.add(hw1);
    var hw2 = new THREE.Mesh(hwge, dark); hw2.position.set(-0.35, 0.5, 0.65); hw2.rotation.x = Math.PI / 2; tur.add(hw2);
    var opt = box(0.15, 0.12, 0.18, dark); opt.position.set(0.2, 0.35, 0.95); tur.add(opt);
    return g;
  }
};

BLD_MODELS["def_flak"] = {
  build: function (THREE, M, C) {
    var g = new THREE.Group();
    function ptex(base, seed) {
      var cv = document.createElement("canvas"); cv.width = 128; cv.height = 128;
      var x = cv.getContext("2d"); x.fillStyle = base; x.fillRect(0, 0, 128, 128);
      var s = seed; function rn() { s = (s * 16807) % 2147483647; return (s % 1000) / 1000; }
      for (var i = 0; i < 14; i++) {
        x.globalAlpha = 0.05 + rn() * 0.03; x.fillStyle = (i % 2) ? "#ffffff" : "#101410";
        x.fillRect(rn() * 128, rn() * 128, 14 + rn() * 34, 10 + rn() * 26);
      }
      x.globalAlpha = 0.5; x.strokeStyle = "#22251f"; x.lineWidth = 1;
      x.beginPath();
      for (var j = 1; j < 4; j++) { x.moveTo(j * 32, 0); x.lineTo(j * 32, 128); x.moveTo(0, j * 32); x.lineTo(128, j * 32); }
      x.stroke();
      x.globalAlpha = 0.1; x.fillStyle = "#0a0c0a";
      for (var k = 0; k < 8; k++) { x.fillRect(rn() * 128, rn() * 100, 2 + rn() * 3, 20 + rn() * 26); }
      x.globalAlpha = 1;
      var t = new THREE.CanvasTexture(cv); t.wrapS = t.wrapT = THREE.RepeatWrapping; return t;
    }
    var metal = new THREE.MeshStandardMaterial({ color: 0xffffff, map: ptex("#4e5744", 37), roughness: 0.6, metalness: 0.25 });
    var dark = new THREE.MeshStandardMaterial({ color: 0x2b2e29, roughness: 0.5, metalness: 0.4 });
    var steel = new THREE.MeshStandardMaterial({ color: 0x565a56, roughness: 0.3, metalness: 0.85 });
    var conc = new THREE.MeshStandardMaterial({ color: 0xffffff, map: ptex("#8b8b83", 53), roughness: 0.95, metalness: 0.0 });
    var team = new THREE.MeshStandardMaterial({ color: C.team, roughness: 0.7, metalness: 0.1 });
    function box(a, b, c, mat) { return new THREE.Mesh(new THREE.BoxGeometry(a, b, c), mat); }
    function cylZ(rA, rB, h, mat, seg) {
      var geo = new THREE.CylinderGeometry(rA, rB, h, seg || 12); geo.rotateX(Math.PI / 2);
      return new THREE.Mesh(geo, mat);
    }
    function cylX(rA, rB, len, mat, seg) {
      var geo = new THREE.CylinderGeometry(rA, rB, len, seg || 10); geo.rotateZ(-Math.PI / 2);
      return new THREE.Mesh(geo, mat);
    }
    var base = cylZ(2.1, 2.2, 0.18, conc, 20); base.position.set(0, 0, 0.09); g.add(base);
    var tur = new THREE.Group(); tur.name = "turret"; tur.position.set(0, 0, 0.18); g.add(tur);
    var ped = cylZ(0.42, 0.5, 0.6, metal, 12); ped.position.set(0, 0, 0.3); tur.add(ped);
    var plat = cylZ(1.05, 1.05, 0.06, metal, 16); plat.position.set(0, 0, 0.62); tur.add(plat);
    var tpn = box(0.03, 0.3, 0.14, team); tpn.position.set(0.46, 0, 0.35); tur.add(tpn);
    function tru(side) {
      var p = box(0.9, 0.06, 0.55, metal); p.position.set(0, side * 0.32, 0.95); tur.add(p);
    }
    tru(1); tru(-1);
    var cradle = box(0.8, 0.56, 0.24, metal); cradle.position.set(0, 0, 1.02); tur.add(cradle);
    var ev = new THREE.Group(); ev.position.set(0.1, 0, 1.05); ev.rotation.y = -0.26; tur.add(ev);
    function barrel(side) {
      var brl = cylX(0.032, 0.042, 2.9, dark); brl.position.set(0.95, side * 0.14, 0.05); ev.add(brl);
      var slv = cylX(0.055, 0.06, 0.9, metal); slv.position.set(0.0, side * 0.14, 0.05); ev.add(slv);
      var fge = new THREE.CylinderGeometry(0.075, 0.045, 0.22, 10); fge.rotateZ(-Math.PI / 2);
      var fh = new THREE.Mesh(fge, dark); fh.position.set(2.51, side * 0.14, 0.05); ev.add(fh);
      var clip = box(0.16, 0.1, 0.5, dark); clip.position.set(-0.38, side * 0.14, 0.35); ev.add(clip);
    }
    barrel(1); barrel(-1);
    var rgu = box(0.6, 0.44, 0.16, metal); rgu.position.set(-0.25, 0, -0.06); ev.add(rgu);
    function seatArm(side) {
      var beam = box(0.7, 0.08, 0.08, metal); beam.position.set(-0.6, side * 0.35, 0.55); beam.rotation.z = side * 0.35; tur.add(beam);
      var seat = box(0.32, 0.3, 0.05, dark); seat.position.set(-0.85, side * 0.5, 0.78); tur.add(seat);
      var back = box(0.06, 0.3, 0.3, dark); back.position.set(-1.02, side * 0.5, 0.98); tur.add(back);
      var foot = box(0.3, 0.2, 0.03, dark); foot.position.set(-0.55, side * 0.5, 0.45); tur.add(foot);
      var hwge = new THREE.TorusGeometry(0.11, 0.018, 6, 12);
      var hw = new THREE.Mesh(hwge, steel); hw.position.set(-0.55, side * 0.62, 0.95); hw.rotation.x = Math.PI / 2; tur.add(hw);
    }
    seatArm(1); seatArm(-1);
    var sight = box(0.2, 0.08, 0.25, dark); sight.position.set(0.2, 0.4, 1.35); tur.add(sight);
    return g;
  }
};

BLD_MODELS["def_sam"] = {
  build: function (THREE, M, C) {
    var g = new THREE.Group();
    function ptex(base, seed) {
      var cv = document.createElement("canvas"); cv.width = 128; cv.height = 128;
      var x = cv.getContext("2d"); x.fillStyle = base; x.fillRect(0, 0, 128, 128);
      var s = seed; function rn() { s = (s * 16807) % 2147483647; return (s % 1000) / 1000; }
      for (var i = 0; i < 14; i++) {
        x.globalAlpha = 0.05 + rn() * 0.03; x.fillStyle = (i % 2) ? "#ffffff" : "#101410";
        x.fillRect(rn() * 128, rn() * 128, 14 + rn() * 34, 10 + rn() * 26);
      }
      x.globalAlpha = 0.5; x.strokeStyle = "#22251f"; x.lineWidth = 1;
      x.beginPath();
      for (var j = 1; j < 4; j++) { x.moveTo(j * 32, 0); x.lineTo(j * 32, 128); x.moveTo(0, j * 32); x.lineTo(128, j * 32); }
      x.stroke();
      x.globalAlpha = 0.1; x.fillStyle = "#0a0c0a";
      for (var k = 0; k < 8; k++) { x.fillRect(rn() * 128, rn() * 100, 2 + rn() * 3, 20 + rn() * 26); }
      x.globalAlpha = 1;
      var t = new THREE.CanvasTexture(cv); t.wrapS = t.wrapT = THREE.RepeatWrapping; return t;
    }
    function faceTex() {
      var cv = document.createElement("canvas"); cv.width = 64; cv.height = 64;
      var x = cv.getContext("2d");
      x.fillStyle = "#33372f"; x.fillRect(0, 0, 64, 64);
      x.fillStyle = "#22251f"; x.beginPath(); x.arc(32, 32, 24, 0, 6.29); x.fill();
      x.strokeStyle = "#585f52"; x.lineWidth = 3; x.beginPath(); x.arc(32, 32, 26, 0, 6.29); x.stroke();
      x.fillStyle = "#585f52";
      x.fillRect(4, 4, 4, 4); x.fillRect(56, 4, 4, 4); x.fillRect(4, 56, 4, 4); x.fillRect(56, 56, 4, 4);
      return new THREE.CanvasTexture(cv);
    }
    var metal = new THREE.MeshStandardMaterial({ color: 0xffffff, map: ptex("#4d5544", 71), roughness: 0.6, metalness: 0.25 });
    var dark = new THREE.MeshStandardMaterial({ color: 0x2f332c, roughness: 0.55, metalness: 0.35 });
    var face = new THREE.MeshStandardMaterial({ color: 0xffffff, map: faceTex(), roughness: 0.7, metalness: 0.15 });
    var conc = new THREE.MeshStandardMaterial({ color: 0xffffff, map: ptex("#8b8b83", 91), roughness: 0.95, metalness: 0.0 });
    var team = new THREE.MeshStandardMaterial({ color: C.team, roughness: 0.7, metalness: 0.1 });
    function box(a, b, c, mat) { return new THREE.Mesh(new THREE.BoxGeometry(a, b, c), mat); }
    function cylZ(rA, rB, h, mat, seg) {
      var geo = new THREE.CylinderGeometry(rA, rB, h, seg || 12); geo.rotateX(Math.PI / 2);
      return new THREE.Mesh(geo, mat);
    }
    var base = cylZ(2.7, 2.8, 0.2, conc, 20); base.position.set(0, 0, 0.1); g.add(base);
    var tur = new THREE.Group(); tur.name = "turret"; tur.position.set(0, 0, 0.2); g.add(tur);
    var tt = cylZ(1.35, 1.45, 0.35, metal, 16); tt.position.set(0, 0, 0.175); tur.add(tt);
    var deck = cylZ(1.45, 1.45, 0.05, dark, 16); deck.position.set(0, 0, 0.375); tur.add(deck);
    var ecs = box(0.5, 0.4, 0.45, metal); ecs.position.set(0.55, 0.85, 0.63); tur.add(ecs);
    var jbox = box(0.25, 0.3, 0.3, dark); jbox.position.set(0.1, -1.05, 0.55); tur.add(jbox);
    function trun(side) {
      var t = box(0.35, 0.25, 0.4, metal); t.position.set(-0.7, side * 0.62, 0.55); tur.add(t);
    }
    trun(1); trun(-1);
    var tilt = new THREE.Group(); tilt.position.set(-0.7, 0, 0.62); tilt.rotation.y = -0.61; tur.add(tilt);
    var frame = box(4.6, 1.35, 0.12, metal); frame.position.set(2.3, 0, -0.08); tilt.add(frame);
    var axle = new THREE.CylinderGeometry(0.09, 0.09, 1.5, 10);
    var ax = new THREE.Mesh(axle, dark); ax.position.set(0, 0, 0); tilt.add(ax);
    function can(y, z) {
      var geo = new THREE.BoxGeometry(5.0, 1.02, 1.02); geo.translate(2.5, 0, 0);
      var c = new THREE.Mesh(geo, metal); c.position.set(0, y, z); tilt.add(c);
      var f = new THREE.Mesh(new THREE.PlaneGeometry(0.94, 0.94), face);
      f.position.set(5.02, y, z); f.rotation.y = Math.PI / 2; tilt.add(f);
    }
    can(0.55, 0.55); can(-0.55, 0.55); can(0.55, 1.6); can(-0.55, 1.6);
    var strap1 = box(0.06, 2.25, 0.06, dark); strap1.position.set(4.55, 0, 1.07); tilt.add(strap1);
    var strap2 = box(0.06, 2.25, 0.06, dark); strap2.position.set(1.2, 0, 1.07); tilt.add(strap2);
    var tstripe = box(1.0, 0.03, 0.18, team); tstripe.position.set(4.0, -1.07, 0.55); tilt.add(tstripe);
    var hy = new THREE.Group(); hy.position.set(0.45, 0, 0.42); hy.rotation.y = -0.95; tur.add(hy);
    var ho = cylZ(0.07, 0.07, 0.8, dark, 8); ho.position.set(0, 0, 0.4); hy.add(ho);
    var hr = cylZ(0.045, 0.045, 0.7, dark, 8); hr.position.set(0, 0, 0.95); hy.add(hr);
    function post(a) {
      var p = cylZ(0.02, 0.02, 0.45, dark, 6);
      p.position.set(1.3 * Math.cos(a), 1.3 * Math.sin(a), 0.62); tur.add(p);
    }
    post(0.6 * Math.PI); post(Math.PI); post(1.4 * Math.PI);
    var rge = new THREE.TorusGeometry(1.3, 0.02, 5, 14, 0.8 * Math.PI);
    var rail = new THREE.Mesh(rge, dark); rail.position.set(0, 0, 0.84); rail.rotation.z = 0.6 * Math.PI; tur.add(rail);
    return g;
  }
};

BLD_MODELS["def_coastal"] = {
  build: function (THREE, M, C) {
    var g = new THREE.Group();
    function ptex(base, seed) {
      var cv = document.createElement("canvas"); cv.width = 128; cv.height = 128;
      var x = cv.getContext("2d"); x.fillStyle = base; x.fillRect(0, 0, 128, 128);
      var s = seed; function rn() { s = (s * 16807) % 2147483647; return (s % 1000) / 1000; }
      for (var i = 0; i < 14; i++) {
        x.globalAlpha = 0.05 + rn() * 0.03; x.fillStyle = (i % 2) ? "#ffffff" : "#101410";
        x.fillRect(rn() * 128, rn() * 128, 14 + rn() * 34, 10 + rn() * 26);
      }
      x.globalAlpha = 0.5; x.strokeStyle = "#22251f"; x.lineWidth = 1;
      x.beginPath();
      for (var j = 1; j < 4; j++) { x.moveTo(j * 32, 0); x.lineTo(j * 32, 128); x.moveTo(0, j * 32); x.lineTo(128, j * 32); }
      x.stroke();
      x.globalAlpha = 0.1; x.fillStyle = "#0a0c0a";
      for (var k = 0; k < 8; k++) { x.fillRect(rn() * 128, rn() * 100, 2 + rn() * 3, 20 + rn() * 26); }
      x.globalAlpha = 1;
      var t = new THREE.CanvasTexture(cv); t.wrapS = t.wrapT = THREE.RepeatWrapping; return t;
    }
    var metal = new THREE.MeshStandardMaterial({ color: 0xffffff, map: ptex("#6d7378", 101), roughness: 0.55, metalness: 0.3 });
    var dark = new THREE.MeshStandardMaterial({ color: 0x3c4146, roughness: 0.5, metalness: 0.4 });
    var conc = new THREE.MeshStandardMaterial({ color: 0xffffff, map: ptex("#8b8b83", 113), roughness: 0.95, metalness: 0.0 });
    var team = new THREE.MeshStandardMaterial({ color: C.team, roughness: 0.7, metalness: 0.1 });
    function box(a, b, c, mat) { return new THREE.Mesh(new THREE.BoxGeometry(a, b, c), mat); }
    function cylZ(rA, rB, h, mat, seg) {
      var geo = new THREE.CylinderGeometry(rA, rB, h, seg || 12); geo.rotateX(Math.PI / 2);
      return new THREE.Mesh(geo, mat);
    }
    function cylX(rA, rB, len, mat, seg) {
      var geo = new THREE.CylinderGeometry(rA, rB, len, seg || 12); geo.rotateZ(-Math.PI / 2);
      return new THREE.Mesh(geo, mat);
    }
    var base = cylZ(2.1, 2.2, 0.3, conc, 20); base.position.set(0, 0, 0.15); g.add(base);
    var collar = cylZ(1.4, 1.5, 0.35, conc, 18); collar.position.set(0, 0, 0.47); g.add(collar);
    var tur = new THREE.Group(); tur.name = "turret"; tur.position.set(0, 0, 0.3); g.add(tur);
    var ped = cylZ(0.88, 0.95, 0.9, metal, 14); ped.position.set(0, 0, 0.45); tur.add(ped);
    var man = new THREE.Mesh(new THREE.SphereGeometry(1.25, 16, 12), metal);
    man.scale.set(0.95, 1, 0.9); man.position.set(0.25, 0, 1.35); tur.add(man);
    var ev = new THREE.Group(); ev.position.set(0.25, 0, 1.35); ev.rotation.y = -0.04; tur.add(ev);
    var root = cylX(0.23, 0.26, 0.9, dark); root.position.set(1.15, 0, 0); ev.add(root);
    var jkt = cylX(0.17, 0.2, 2.0, metal); jkt.position.set(2.5, 0, 0); ev.add(jkt);
    var brl = cylX(0.085, 0.16, 5.2, dark); brl.position.set(4.1, 0, 0); ev.add(brl);
    var mzc = cylX(0.1, 0.1, 0.25, dark); mzc.position.set(6.6, 0, 0); ev.add(mzc);
    var slit = box(0.1, 0.35, 0.09, dark); slit.position.set(1.2, 0.55, 1.75); tur.add(slit);
    var lge = new THREE.TorusGeometry(0.08, 0.02, 5, 10);
    var lug1 = new THREE.Mesh(lge, dark); lug1.position.set(0.35, 0.5, 2.42); lug1.rotation.x = Math.PI / 2; tur.add(lug1);
    var lug2 = new THREE.Mesh(lge, dark); lug2.position.set(0.35, -0.5, 2.42); lug2.rotation.x = Math.PI / 2; tur.add(lug2);
    var tp = box(0.04, 0.14, 0.5, team); tp.position.set(0.9, 0, 0.5); tur.add(tp);
    var hbox = box(0.4, 0.5, 0.25, dark); hbox.position.set(-0.9, 0, 1.0); tur.add(hbox);
    return g;
  }
};

BLD_MODELS["def_arty"] = {
  build: function (THREE, M, C) {
    var g = new THREE.Group();
    function ptex(base, seed) {
      var cv = document.createElement("canvas"); cv.width = 128; cv.height = 128;
      var x = cv.getContext("2d"); x.fillStyle = base; x.fillRect(0, 0, 128, 128);
      var s = seed; function rn() { s = (s * 16807) % 2147483647; return (s % 1000) / 1000; }
      for (var i = 0; i < 14; i++) {
        x.globalAlpha = 0.05 + rn() * 0.03; x.fillStyle = (i % 2) ? "#ffffff" : "#101410";
        x.fillRect(rn() * 128, rn() * 128, 14 + rn() * 34, 10 + rn() * 26);
      }
      x.globalAlpha = 0.5; x.strokeStyle = "#22251f"; x.lineWidth = 1;
      x.beginPath();
      for (var j = 1; j < 4; j++) { x.moveTo(j * 32, 0); x.lineTo(j * 32, 128); x.moveTo(0, j * 32); x.lineTo(128, j * 32); }
      x.stroke();
      x.globalAlpha = 0.1; x.fillStyle = "#0a0c0a";
      for (var k = 0; k < 8; k++) { x.fillRect(rn() * 128, rn() * 100, 2 + rn() * 3, 20 + rn() * 26); }
      x.globalAlpha = 1;
      var t = new THREE.CanvasTexture(cv); t.wrapS = t.wrapT = THREE.RepeatWrapping; return t;
    }
    var metal = new THREE.MeshStandardMaterial({ color: 0xffffff, map: ptex("#4a5240", 131), roughness: 0.6, metalness: 0.25 });
    var dark = new THREE.MeshStandardMaterial({ color: 0x2b2e29, roughness: 0.5, metalness: 0.4 });
    var conc = new THREE.MeshStandardMaterial({ color: 0xffffff, map: ptex("#8b8b83", 151), roughness: 0.95, metalness: 0.0 });
    var bag = new THREE.MeshStandardMaterial({ color: 0x7a7052, roughness: 0.95, metalness: 0.0 });
    var team = new THREE.MeshStandardMaterial({ color: C.team, roughness: 0.7, metalness: 0.1 });
    function box(a, b, c, mat) { return new THREE.Mesh(new THREE.BoxGeometry(a, b, c), mat); }
    function cylZ(rA, rB, h, mat, seg) {
      var geo = new THREE.CylinderGeometry(rA, rB, h, seg || 12); geo.rotateX(Math.PI / 2);
      return new THREE.Mesh(geo, mat);
    }
    function cylX(rA, rB, len, mat, seg) {
      var geo = new THREE.CylinderGeometry(rA, rB, len, seg || 10); geo.rotateZ(-Math.PI / 2);
      return new THREE.Mesh(geo, mat);
    }
    var base = cylZ(2.7, 2.8, 0.15, conc, 20); base.position.set(0, 0, 0.075); g.add(base);
    for (var i = 0; i < 6; i++) {
      var a = 0.5 + i * 0.42;
      var sb = box(0.52, 0.3, 0.2, bag);
      sb.position.set(2.45 * Math.cos(a), 2.45 * Math.sin(a), 0.25);
      sb.rotation.z = a + Math.PI / 2; g.add(sb);
    }
    var pal = box(0.8, 0.5, 0.12, dark); pal.position.set(0.9, -1.9, 0.21); g.add(pal);
    for (var k2 = 0; k2 < 3; k2++) {
      var sh = cylX(0.055, 0.078, 0.62, metal, 8); sh.position.set(0.85, -2.05 + k2 * 0.17, 0.34); g.add(sh);
      var ng = new THREE.ConeGeometry(0.05, 0.2, 8);
      ng.rotateZ(-Math.PI / 2);
      var nc = new THREE.Mesh(ng, dark); nc.position.set(1.25, -2.05 + k2 * 0.17, 0.34); g.add(nc);
    }
    var tur = new THREE.Group(); tur.name = "turret"; tur.position.set(0, 0, 0.15); g.add(tur);
    var car = box(1.7, 0.95, 0.45, metal); car.position.set(0, 0, 0.62); tur.add(car);
    function wheel(side) {
      var w = new THREE.Mesh(new THREE.CylinderGeometry(0.5, 0.5, 0.26, 14), dark);
      w.position.set(0.25, side * 0.72, 0.5); tur.add(w);
      var hub = new THREE.Mesh(new THREE.CylinderGeometry(0.2, 0.2, 0.28, 10), metal);
      hub.position.set(0.25, side * 0.72, 0.5); tur.add(hub);
    }
    wheel(1); wheel(-1);
    function trail(side) {
      var geo = new THREE.BoxGeometry(3.6, 0.2, 0.17); geo.translate(-1.8, 0, 0);
      var t = new THREE.Mesh(geo, metal); t.position.set(-0.7, side * 0.18, 0.4);
      t.rotation.z = -side * 0.33; tur.add(t);
      var sp = box(0.14, 0.4, 0.45, metal); sp.position.set(-3.45, 0, -0.12); t.add(sp);
      var fl = box(0.35, 0.3, 0.06, metal); fl.position.set(-3.25, 0, -0.24); t.add(fl);
    }
    trail(1); trail(-1);
    var cradle = box(1.3, 0.44, 0.3, metal); cradle.position.set(0.15, 0, 0.95); tur.add(cradle);
    function equil(side) {
      var e = cylZ(0.05, 0.05, 0.55, dark, 8); e.position.set(0.42, side * 0.3, 0.9);
      e.rotation.y = 0.5; tur.add(e);
    }
    equil(1); equil(-1);
    var ev = new THREE.Group(); ev.position.set(-0.1, 0, 1.1); ev.rotation.y = -0.35; tur.add(ev);
    var brch = box(0.6, 0.34, 0.36, metal); brch.position.set(-0.1, 0, 0); ev.add(brch);
    var brl = cylX(0.062, 0.105, 6.6, metal); brl.position.set(3.6, 0, 0); ev.add(brl);
    var bf1 = cylX(0.15, 0.15, 0.09, dark); bf1.position.set(6.78, 0, 0); ev.add(bf1);
    var bf2 = cylX(0.15, 0.15, 0.09, dark); bf2.position.set(6.98, 0, 0); ev.add(bf2);
    var bf3 = cylX(0.15, 0.15, 0.09, dark); bf3.position.set(7.18, 0, 0); ev.add(bf3);
    var core = cylX(0.05, 0.05, 0.62, dark); core.position.set(6.95, 0, 0); ev.add(core);
    var tring = cylX(0.115, 0.115, 0.09, team); tring.position.set(6.4, 0, 0); ev.add(tring);
    var rc1 = cylX(0.06, 0.06, 1.7, dark); rc1.position.set(0.9, 0.13, 0.19); ev.add(rc1);
    var rc2 = cylX(0.06, 0.06, 1.7, dark); rc2.position.set(0.9, -0.13, 0.19); ev.add(rc2);
    var hwge = new THREE.TorusGeometry(0.1, 0.016, 6, 12);
    var hw1 = new THREE.Mesh(hwge, dark); hw1.position.set(-0.3, 0.55, 0.8); hw1.rotation.x = Math.PI / 2; tur.add(hw1);
    var seat = box(0.28, 0.26, 0.05, dark); seat.position.set(-0.6, 0.5, 0.6); tur.add(seat);
    return g;
  }
};

/* ---- pack abb58670 ---- */
UNIT_MODELS["atgmv_n"] = {
  len: 6.95,
  build: function (THREE, M, C) {
    var g = new THREE.Group();
    var seed = 11;
    function rnd() { seed = (seed * 48271) % 2147483647; return seed / 2147483647; }
    function paint(base) {
      var cv = document.createElement("canvas");
      cv.width = 256; cv.height = 256;
      var p = cv.getContext("2d");
      p.fillStyle = base; p.fillRect(0, 0, 256, 256);
      var i;
      for (i = 0; i < 24; i++) {
        p.globalAlpha = 0.04 + rnd() * 0.04;
        p.fillStyle = rnd() < 0.5 ? "#c8cdb8" : "#181c12";
        p.fillRect(rnd() * 256, rnd() * 256, 22 + rnd() * 66, 14 + rnd() * 48);
      }
      p.globalAlpha = 1;
      p.strokeStyle = "rgba(8,10,6,0.32)";
      p.lineWidth = 1;
      var vx = [20, 54, 92, 128, 164, 200, 238], hz = [36, 88, 148, 208];
      for (i = 0; i < vx.length; i++) { p.beginPath(); p.moveTo(vx[i], 0); p.lineTo(vx[i] + rnd() * 5, 256); p.stroke(); }
      for (i = 0; i < hz.length; i++) { p.beginPath(); p.moveTo(0, hz[i]); p.lineTo(256, hz[i] + rnd() * 5); p.stroke(); }
      p.fillStyle = "rgba(0,0,0,0.30)";
      for (i = 0; i < 46; i++) p.fillRect(rnd() * 256, rnd() * 256, 2, 2);
      for (i = 0; i < 13; i++) {
        p.fillStyle = "rgba(14,11,5,0.06)";
        p.fillRect(rnd() * 256, rnd() * 170, 2 + rnd() * 3, 26 + rnd() * 64);
      }
      var t = new THREE.CanvasTexture(cv);
      t.wrapS = THREE.RepeatWrapping; t.wrapT = THREE.RepeatWrapping;
      return t;
    }
    var bodyMat = new THREE.MeshStandardMaterial({ map: paint("#4a5240"), metalness: 0.25, roughness: 0.62 });
    var trimMat = new THREE.MeshStandardMaterial({ color: 0x424a39, metalness: 0.25, roughness: 0.66 });
    var darkMat = new THREE.MeshStandardMaterial({ color: 0x20231e, metalness: 0.15, roughness: 0.9 });
    var steelMat = new THREE.MeshStandardMaterial({ color: 0x33363a, metalness: 0.85, roughness: 0.3 });
    var lensMat = new THREE.MeshStandardMaterial({ color: 0x10151c, metalness: 0.6, roughness: 0.25 });
    var teamMat = new THREE.MeshStandardMaterial({ color: C.team, metalness: 0.3, roughness: 0.55 });
    function bx(sx, sy, sz, x, y, z, mat, parent) {
      var m = new THREE.Mesh(new THREE.BoxGeometry(sx, sy, sz), mat);
      m.position.set(x, y, z);
      (parent || g).add(m);
      return m;
    }
    function cyl(r1, r2, h, x, y, z, mat, parent, axis, seg) {
      var geo = new THREE.CylinderGeometry(r1, r2, h, seg || 14);
      if (axis === "x") geo.rotateZ(Math.PI / 2);
      else if (axis === "z") geo.rotateX(Math.PI / 2);
      var m = new THREE.Mesh(geo, mat);
      m.position.set(x, y, z);
      (parent || g).add(m);
      return m;
    }
    // Faceted 8x8 hull, sharply sloped bow. Roof 2.30 m, belly 0.60 m.
    var hull = new THREE.Mesh(M.loft(THREE, [
      { x: -3.45, w: 1.1, h: 0.68, zc: 1.46, sq: 0.85 },
      { x: -3.0, w: 1.22, h: 0.8, zc: 1.5, sq: 0.85 },
      { x: -1.9, w: 1.25, h: 0.85, zc: 1.45, sq: 0.88 },
      { x: 1.3, w: 1.25, h: 0.85, zc: 1.45, sq: 0.88 },
      { x: 2.3, w: 1.2, h: 0.72, zc: 1.55, sq: 0.85 },
      { x: 3.0, w: 1.0, h: 0.45, zc: 1.5, sq: 0.82 },
      { x: 3.45, w: 0.55, h: 0.16, zc: 1.28, sq: 0.8 }
    ], 12), bodyMat);
    g.add(hull);
    // Rear ramp with hinge bar
    bx(0.06, 2.15, 1.3, -3.47, 0, 1.47, trimMat);
    bx(0.06, 1.9, 0.1, -3.45, 0, 0.86, darkMat);
    // Eight wheels: tire, hub, cap
    var wxs = [2.45, 1.45, -1.0, -2.15];
    var tireGeo = new THREE.CylinderGeometry(0.55, 0.55, 0.34, 18);
    var hubGeo = new THREE.CylinderGeometry(0.3, 0.3, 0.36, 12);
    var capGeo = new THREE.CylinderGeometry(0.11, 0.11, 0.38, 8);
    var i, s;
    for (i = 0; i < 4; i++) {
      for (s = -1; s <= 1; s += 2) {
        var tire = new THREE.Mesh(tireGeo, darkMat);
        tire.position.set(wxs[i], s * 1.19, 0.55);
        g.add(tire);
        var hub = new THREE.Mesh(hubGeo, trimMat);
        hub.position.set(wxs[i], s * 1.19, 0.55);
        g.add(hub);
        var cap = new THREE.Mesh(capGeo, steelMat);
        cap.position.set(wxs[i], s * 1.19, 0.55);
        g.add(cap);
      }
    }
    // Fender strips, stowage racks, id panels
    for (s = -1; s <= 1; s += 2) {
      bx(5.9, 0.2, 0.06, -0.2, s * 1.26, 1.68, trimMat);
      bx(0.9, 0.14, 0.42, 0.4, s * 1.28, 1.98, trimMat);
      bx(0.7, 0.14, 0.38, -0.7, s * 1.28, 1.96, darkMat);
      bx(0.8, 0.14, 0.4, -1.8, s * 1.28, 1.98, trimMat);
      bx(0.7, 0.03, 0.3, 2.35, s * 1.22, 1.78, teamMat);
    }
    // Bow: bumper, headlights, smoke tubes
    bx(0.1, 1.9, 0.24, 3.42, 0, 1.22, trimMat);
    for (s = -1; s <= 1; s += 2) {
      bx(0.14, 0.24, 0.14, 3.18, s * 0.78, 1.62, trimMat);
      bx(0.03, 0.18, 0.1, 3.26, s * 0.78, 1.62, lensMat);
      for (i = 0; i < 3; i++) {
        var st = cyl(0.04, 0.04, 0.3, 2.9, s * (0.92 + i * 0.1), 2.02, steelMat, g, "x", 8);
        st.rotation.z = s * (0.3 + i * 0.25);
        st.rotation.y = -0.5;
      }
    }
    // Roof furniture: driver hatch, commander hatch ring, periscopes, exhaust
    bx(0.55, 0.5, 0.06, 1.9, 0.6, 2.32, trimMat);
    cyl(0.32, 0.32, 0.06, 0.6, -0.5, 2.32, trimMat, g, "z", 16);
    bx(0.12, 0.3, 0.08, 2.25, 0.6, 2.33, darkMat);
    bx(0.12, 0.3, 0.08, 0.15, -0.5, 2.33, darkMat);
    bx(0.95, 0.26, 0.2, 0.5, -1.16, 1.95, darkMat);
    // Whip antennas aft
    var a1 = cyl(0.012, 0.012, 1.3, -3.1, 0.95, 2.95, steelMat, g, "z", 6);
    a1.rotation.x = 0.12;
    var a2 = cyl(0.012, 0.012, 1.3, -3.1, -0.95, 2.95, steelMat, g, "z", 6);
    a2.rotation.x = -0.12;
    // Elevated twin-TOW launcher (M1134 signature) --------------------
    var turret = new THREE.Group();
    turret.name = "turret";
    turret.position.set(-1.15, 0, 2.3);
    g.add(turret);
    cyl(0.5, 0.55, 0.14, 0, 0, 0.07, trimMat, turret, "z", 18);
    bx(0.34, 0.3, 0.62, 0, 0, 0.44, bodyMat, turret);
    var elev = new THREE.Group();
    elev.position.set(0, 0, 0.78);
    elev.rotation.y = -0.1;
    turret.add(elev);
    bx(0.26, 1.16, 0.2, 0, 0, 0, trimMat, elev);
    for (s = -1; s <= 1; s += 2) {
      bx(1.3, 0.44, 0.5, 0.12, s * 0.44, 0.02, bodyMat, elev);
      bx(0.05, 0.4, 0.46, 0.79, s * 0.44, 0.02, darkMat, elev);
      bx(0.05, 0.4, 0.46, -0.55, s * 0.44, 0.02, trimMat, elev);
      bx(0.2, 0.03, 0.2, 0.2, s * 0.67, 0.05, teamMat, elev);
    }
    bx(0.4, 0.34, 0.34, 0.05, 0, 0.42, trimMat, elev);
    bx(0.04, 0.28, 0.24, 0.27, 0, 0.42, lensMat, elev);
    return g;
  }
};
UNIT_MODELS["atgmv_p"] = {
  len: 6.7,
  build: function (THREE, M, C) {
    var g = new THREE.Group();
    var seed = 23;
    function rnd() { seed = (seed * 48271) % 2147483647; return seed / 2147483647; }
    function paint(base) {
      var cv = document.createElement("canvas");
      cv.width = 256; cv.height = 256;
      var p = cv.getContext("2d");
      p.fillStyle = base; p.fillRect(0, 0, 256, 256);
      var i;
      for (i = 0; i < 24; i++) {
        p.globalAlpha = 0.04 + rnd() * 0.04;
        p.fillStyle = rnd() < 0.5 ? "#b9c2a4" : "#151a10";
        p.fillRect(rnd() * 256, rnd() * 256, 22 + rnd() * 66, 14 + rnd() * 48);
      }
      p.globalAlpha = 1;
      p.strokeStyle = "rgba(8,10,6,0.32)";
      p.lineWidth = 1;
      var vx = [26, 62, 100, 138, 176, 214], hz = [42, 96, 152, 212];
      for (i = 0; i < vx.length; i++) { p.beginPath(); p.moveTo(vx[i], 0); p.lineTo(vx[i] + rnd() * 5, 256); p.stroke(); }
      for (i = 0; i < hz.length; i++) { p.beginPath(); p.moveTo(0, hz[i]); p.lineTo(256, hz[i] + rnd() * 5); p.stroke(); }
      p.fillStyle = "rgba(0,0,0,0.30)";
      for (i = 0; i < 40; i++) p.fillRect(rnd() * 256, rnd() * 256, 2, 2);
      for (i = 0; i < 14; i++) {
        p.fillStyle = "rgba(14,11,5,0.06)";
        p.fillRect(rnd() * 256, rnd() * 170, 2 + rnd() * 3, 26 + rnd() * 64);
      }
      var t = new THREE.CanvasTexture(cv);
      t.wrapS = THREE.RepeatWrapping; t.wrapT = THREE.RepeatWrapping;
      return t;
    }
    var bodyMat = new THREE.MeshStandardMaterial({ map: paint("#3e4a35"), metalness: 0.25, roughness: 0.62 });
    var trimMat = new THREE.MeshStandardMaterial({ color: 0x37412e, metalness: 0.25, roughness: 0.66 });
    var darkMat = new THREE.MeshStandardMaterial({ color: 0x1f221c, metalness: 0.15, roughness: 0.9 });
    var steelMat = new THREE.MeshStandardMaterial({ color: 0x33363a, metalness: 0.85, roughness: 0.3 });
    var lensMat = new THREE.MeshStandardMaterial({ color: 0x10151c, metalness: 0.6, roughness: 0.25 });
    var teamMat = new THREE.MeshStandardMaterial({ color: C.team, metalness: 0.3, roughness: 0.55 });
    function bx(sx, sy, sz, x, y, z, mat, parent) {
      var m = new THREE.Mesh(new THREE.BoxGeometry(sx, sy, sz), mat);
      m.position.set(x, y, z);
      (parent || g).add(m);
      return m;
    }
    function cyl(r1, r2, h, x, y, z, mat, parent, axis, seg) {
      var geo = new THREE.CylinderGeometry(r1, r2, h, seg || 14);
      if (axis === "x") geo.rotateZ(Math.PI / 2);
      else if (axis === "z") geo.rotateX(Math.PI / 2);
      var m = new THREE.Mesh(geo, mat);
      m.position.set(x, y, z);
      (parent || g).add(m);
      return m;
    }
    function track(sy, tl, th, tw, n, wr, span) {
      var t = new THREE.Group();
      var pts = [
        [tl * 0.5, th * 0.55],
        [tl * 0.5 - 0.5, th],
        [-tl * 0.5 + 0.5, th],
        [-tl * 0.5, th * 0.55],
        [-tl * 0.5 + 0.55, 0.01],
        [tl * 0.5 - 0.55, 0.01]
      ];
      var geo = M.slab(THREE, pts, tw, "xz");
      geo.computeBoundingBox();
      geo.translate(0, -(geo.boundingBox.max.y + geo.boundingBox.min.y) / 2, 0);
      t.add(new THREE.Mesh(geo, darkMat));
      var wg = new THREE.CylinderGeometry(wr, wr, tw + 0.1, 16);
      var hg = new THREE.CylinderGeometry(wr * 0.55, wr * 0.55, tw + 0.16, 12);
      for (var i = 0; i < n; i++) {
        var wx = span * 0.5 - span * i / (n - 1);
        var w = new THREE.Mesh(wg, darkMat);
        w.position.set(wx, 0, wr + 0.01);
        t.add(w);
        var h = new THREE.Mesh(hg, trimMat);
        h.position.set(wx, 0, wr + 0.01);
        t.add(h);
      }
      var sg = new THREE.CylinderGeometry(wr * 0.75, wr * 0.75, tw + 0.06, 12);
      var s1 = new THREE.Mesh(sg, darkMat);
      s1.position.set(tl * 0.5 - 0.26, 0, th * 0.58);
      t.add(s1);
      var s2 = new THREE.Mesh(sg, darkMat);
      s2.position.set(-tl * 0.5 + 0.26, 0, th * 0.58);
      t.add(s2);
      t.position.y = sy;
      g.add(t);
      return t;
    }
    // BMP-3 hull: long flat bow, low silhouette. Roof 1.74 m.
    var hull = new THREE.Mesh(M.loft(THREE, [
      { x: -3.35, w: 1.3, h: 0.5, zc: 1.2, sq: 0.88 },
      { x: -2.9, w: 1.42, h: 0.58, zc: 1.14, sq: 0.9 },
      { x: -1.6, w: 1.42, h: 0.62, zc: 1.12, sq: 0.9 },
      { x: 1.2, w: 1.42, h: 0.62, zc: 1.12, sq: 0.9 },
      { x: 2.4, w: 1.38, h: 0.46, zc: 1.26, sq: 0.88 },
      { x: 3.35, w: 1.1, h: 0.1, zc: 1.34, sq: 0.85 }
    ], 12), bodyMat);
    g.add(hull);
    bx(0.06, 2.3, 0.95, -3.36, 0, 1.2, trimMat);
    track(1.32, 6.2, 0.95, 0.44, 6, 0.31, 4.7);
    track(-1.32, 6.2, 0.95, 0.44, 6, 0.31, 4.7);
    var i, s;
    // Glacis rib, headlights, splash board
    var rib = bx(0.07, 2.2, 0.1, 2.55, 0, 1.56, trimMat);
    rib.rotation.y = 0.35;
    for (s = -1; s <= 1; s += 2) {
      bx(0.14, 0.2, 0.14, 3.05, s * 0.9, 1.52, trimMat);
      bx(0.03, 0.14, 0.1, 3.13, s * 0.9, 1.52, lensMat);
    }
    // Roof: crew hatches, engine deck grilles aft, periscopes
    for (s = -1; s <= 1; s += 2) {
      cyl(0.29, 0.29, 0.05, 1.7, s * 0.55, 1.76, trimMat, g, "z", 16);
    }
    bx(0.12, 0.28, 0.07, 2.15, 0.55, 1.77, darkMat);
    bx(1.3, 1.5, 0.04, -2.55, 0, 1.74, darkMat);
    bx(0.9, 1.1, 0.04, -1.55, 0, 1.75, trimMat);
    // Fender ends and mudflaps
    for (s = -1; s <= 1; s += 2) {
      bx(0.4, 0.44, 0.05, 3.1, s * 1.32, 1.02, trimMat);
      bx(0.3, 0.44, 0.05, -3.2, s * 1.32, 1.02, trimMat);
      bx(0.55, 0.03, 0.4, 2.5, s * 1.36, 1.28, teamMat);
    }
    // Whip antenna
    var ant = cyl(0.012, 0.012, 1.25, -3.0, 0.85, 2.35, steelMat, g, "z", 6);
    ant.rotation.x = 0.14;
    // Khrizantema launcher: twin 9M123 canisters + folding radar mast ----
    var turret = new THREE.Group();
    turret.name = "turret";
    turret.position.set(-0.8, 0, 1.74);
    g.add(turret);
    cyl(0.72, 0.78, 0.16, 0, 0, 0.08, trimMat, turret, "z", 20);
    bx(0.8, 0.66, 0.4, 0, 0, 0.36, bodyMat, turret);
    bx(0.3, 0.28, 0.3, 0.48, -0.24, 0.42, trimMat, turret);
    bx(0.03, 0.2, 0.18, 0.64, -0.24, 0.44, lensMat, turret);
    var arm = new THREE.Group();
    arm.position.set(0.1, 0, 0.62);
    arm.rotation.y = -0.16;
    turret.add(arm);
    bx(0.9, 0.72, 0.12, -0.1, 0, -0.05, trimMat, arm);
    for (s = -1; s <= 1; s += 2) {
      cyl(0.17, 0.17, 2.05, 0.15, s * 0.27, 0.12, bodyMat, arm, "x", 14);
      cyl(0.185, 0.185, 0.07, 0.75, s * 0.27, 0.12, darkMat, arm, "x", 14);
      cyl(0.185, 0.185, 0.07, -0.45, s * 0.27, 0.12, darkMat, arm, "x", 14);
      cyl(0.175, 0.175, 0.05, 1.18, s * 0.27, 0.12, darkMat, arm, "x", 14);
      cyl(0.175, 0.175, 0.05, -0.88, s * 0.27, 0.12, trimMat, arm, "x", 14);
    }
    bx(0.24, 0.03, 0.16, 0.4, 0.455, 0.12, teamMat, arm);
    bx(0.24, 0.03, 0.16, 0.4, -0.455, 0.12, teamMat, arm);
    // Mast-mounted radar dish, raised
    var mast = new THREE.Group();
    mast.position.set(-0.62, 0, 0.55);
    turret.add(mast);
    cyl(0.055, 0.07, 1.1, 0, 0, 0.55, steelMat, mast, "z", 10);
    cyl(0.038, 0.038, 0.55, 0, 0, 1.3, steelMat, mast, "z", 8);
    var dgeo = new THREE.CylinderGeometry(0.34, 0.44, 0.1, 18);
    dgeo.rotateZ(Math.PI / 2);
    var dishm = new THREE.Mesh(dgeo, trimMat);
    dishm.position.set(0.06, 0, 1.62);
    dishm.scale.set(1, 1, 1.25);
    mast.add(dishm);
    cyl(0.018, 0.018, 0.38, 0.28, 0, 1.62, steelMat, mast, "x", 6);
    bx(0.08, 0.08, 0.08, 0.48, 0, 1.62, darkMat, mast);
    return g;
  }
};
UNIT_MODELS["atgmv_c"] = {
  len: 6.7,
  build: function (THREE, M, C) {
    var g = new THREE.Group();
    var seed = 37;
    function rnd() { seed = (seed * 48271) % 2147483647; return seed / 2147483647; }
    function paint(base) {
      var cv = document.createElement("canvas");
      cv.width = 256; cv.height = 256;
      var p = cv.getContext("2d");
      p.fillStyle = base; p.fillRect(0, 0, 256, 256);
      var i;
      for (i = 0; i < 24; i++) {
        p.globalAlpha = 0.04 + rnd() * 0.04;
        p.fillStyle = rnd() < 0.5 ? "#c2cbb0" : "#171c12";
        p.fillRect(rnd() * 256, rnd() * 256, 22 + rnd() * 66, 14 + rnd() * 48);
      }
      p.globalAlpha = 1;
      p.strokeStyle = "rgba(8,10,6,0.32)";
      p.lineWidth = 1;
      var vx = [24, 60, 98, 134, 172, 208, 242], hz = [38, 90, 146, 206];
      for (i = 0; i < vx.length; i++) { p.beginPath(); p.moveTo(vx[i], 0); p.lineTo(vx[i] + rnd() * 5, 256); p.stroke(); }
      for (i = 0; i < hz.length; i++) { p.beginPath(); p.moveTo(0, hz[i]); p.lineTo(256, hz[i] + rnd() * 5); p.stroke(); }
      p.fillStyle = "rgba(0,0,0,0.30)";
      for (i = 0; i < 42; i++) p.fillRect(rnd() * 256, rnd() * 256, 2, 2);
      for (i = 0; i < 12; i++) {
        p.fillStyle = "rgba(14,11,5,0.06)";
        p.fillRect(rnd() * 256, rnd() * 170, 2 + rnd() * 3, 26 + rnd() * 64);
      }
      var t = new THREE.CanvasTexture(cv);
      t.wrapS = THREE.RepeatWrapping; t.wrapT = THREE.RepeatWrapping;
      return t;
    }
    var bodyMat = new THREE.MeshStandardMaterial({ map: paint("#4e5844"), metalness: 0.25, roughness: 0.62 });
    var trimMat = new THREE.MeshStandardMaterial({ color: 0x454f3c, metalness: 0.25, roughness: 0.66 });
    var darkMat = new THREE.MeshStandardMaterial({ color: 0x20231d, metalness: 0.15, roughness: 0.9 });
    var steelMat = new THREE.MeshStandardMaterial({ color: 0x33363a, metalness: 0.85, roughness: 0.3 });
    var lensMat = new THREE.MeshStandardMaterial({ color: 0x10151c, metalness: 0.6, roughness: 0.25 });
    var teamMat = new THREE.MeshStandardMaterial({ color: C.team, metalness: 0.3, roughness: 0.55 });
    function bx(sx, sy, sz, x, y, z, mat, parent) {
      var m = new THREE.Mesh(new THREE.BoxGeometry(sx, sy, sz), mat);
      m.position.set(x, y, z);
      (parent || g).add(m);
      return m;
    }
    function cyl(r1, r2, h, x, y, z, mat, parent, axis, seg) {
      var geo = new THREE.CylinderGeometry(r1, r2, h, seg || 14);
      if (axis === "x") geo.rotateZ(Math.PI / 2);
      else if (axis === "z") geo.rotateX(Math.PI / 2);
      var m = new THREE.Mesh(geo, mat);
      m.position.set(x, y, z);
      (parent || g).add(m);
      return m;
    }
    function track(sy, tl, th, tw, n, wr, span) {
      var t = new THREE.Group();
      var pts = [
        [tl * 0.5, th * 0.55],
        [tl * 0.5 - 0.5, th],
        [-tl * 0.5 + 0.5, th],
        [-tl * 0.5, th * 0.55],
        [-tl * 0.5 + 0.55, 0.01],
        [tl * 0.5 - 0.55, 0.01]
      ];
      var geo = M.slab(THREE, pts, tw, "xz");
      geo.computeBoundingBox();
      geo.translate(0, -(geo.boundingBox.max.y + geo.boundingBox.min.y) / 2, 0);
      t.add(new THREE.Mesh(geo, darkMat));
      var wg = new THREE.CylinderGeometry(wr, wr, tw + 0.1, 16);
      var hg = new THREE.CylinderGeometry(wr * 0.55, wr * 0.55, tw + 0.16, 12);
      for (var i = 0; i < n; i++) {
        var wx = span * 0.5 - span * i / (n - 1);
        var w = new THREE.Mesh(wg, darkMat);
        w.position.set(wx, 0, wr + 0.01);
        t.add(w);
        var h = new THREE.Mesh(hg, trimMat);
        h.position.set(wx, 0, wr + 0.01);
        t.add(h);
      }
      var sg = new THREE.CylinderGeometry(wr * 0.75, wr * 0.75, tw + 0.06, 12);
      var s1 = new THREE.Mesh(sg, darkMat);
      s1.position.set(tl * 0.5 - 0.26, 0, th * 0.58);
      t.add(s1);
      var s2 = new THREE.Mesh(sg, darkMat);
      s2.position.set(-tl * 0.5 + 0.26, 0, th * 0.58);
      t.add(s2);
      t.position.y = sy;
      g.add(t);
      return t;
    }
    // ZBD-04A style hull. Roof 1.76 m.
    var hull = new THREE.Mesh(M.loft(THREE, [
      { x: -3.35, w: 1.3, h: 0.52, zc: 1.2, sq: 0.88 },
      { x: -2.1, w: 1.38, h: 0.62, zc: 1.14, sq: 0.9 },
      { x: 1.2, w: 1.38, h: 0.62, zc: 1.14, sq: 0.9 },
      { x: 2.4, w: 1.34, h: 0.46, zc: 1.28, sq: 0.88 },
      { x: 3.35, w: 1.08, h: 0.1, zc: 1.4, sq: 0.85 }
    ], 12), bodyMat);
    g.add(hull);
    bx(0.06, 2.25, 0.98, -3.36, 0, 1.2, trimMat);
    track(1.34, 6.2, 0.95, 0.44, 6, 0.31, 4.7);
    track(-1.34, 6.2, 0.95, 0.44, 6, 0.31, 4.7);
    var i, s;
    // Driver hatch front-left, engine intake grille front-right
    bx(0.55, 0.45, 0.06, 2.15, 0.62, 1.72, trimMat);
    bx(0.12, 0.42, 0.07, 1.82, 0.62, 1.73, darkMat);
    bx(0.9, 0.9, 0.05, 2.0, -0.68, 1.7, darkMat);
    for (i = 0; i < 4; i++) bx(0.04, 0.86, 0.07, 1.7 + i * 0.2, -0.68, 1.71, trimMat);
    // Headlights, mudflaps, id panels
    for (s = -1; s <= 1; s += 2) {
      bx(0.14, 0.2, 0.14, 3.08, s * 0.85, 1.56, trimMat);
      bx(0.03, 0.14, 0.1, 3.16, s * 0.85, 1.56, lensMat);
      bx(0.4, 0.42, 0.05, 3.05, s * 1.34, 1.02, trimMat);
      bx(0.3, 0.42, 0.05, -3.15, s * 1.34, 1.02, trimMat);
      bx(0.55, 0.03, 0.38, 2.45, s * 1.31, 1.3, teamMat);
    }
    var ant = cyl(0.012, 0.012, 1.25, -3.05, -0.85, 2.35, steelMat, g, "z", 6);
    ant.rotation.x = -0.14;
    // AFT-10: elevating 4x2 missile cell bank ---------------------------
    var turret = new THREE.Group();
    turret.name = "turret";
    turret.position.set(-0.55, 0, 1.78);
    g.add(turret);
    cyl(0.88, 0.95, 0.2, 0, 0, 0.1, trimMat, turret, "z", 20);
    for (s = -1; s <= 1; s += 2) {
      bx(0.75, 0.1, 0.55, 0, s * 0.99, 0.42, bodyMat, turret);
    }
    var bank = new THREE.Group();
    bank.position.set(0.05, 0, 0.6);
    bank.rotation.y = -0.3;
    turret.add(bank);
    bx(1.9, 2.0, 1.06, -0.25, 0, 0, bodyMat, bank);
    // 8 launch-cell covers, 4 across x 2 high, proud of the front face
    var r, c;
    for (r = 0; r < 2; r++) {
      for (c = 0; c < 4; c++) {
        bx(0.05, 0.42, 0.44, 0.72, -0.75 + c * 0.5, -0.27 + r * 0.54, darkMat, bank);
      }
    }
    // Frame strips between cells
    bx(0.06, 2.02, 0.08, 0.71, 0, 0, trimMat, bank);
    for (c = 0; c < 3; c++) bx(0.06, 0.06, 1.08, 0.71, -0.5 + c * 0.5, 0, trimMat, bank);
    bx(0.05, 1.9, 0.95, -1.22, 0, 0, trimMat, bank);
    for (s = -1; s <= 1; s += 2) {
      bx(1.86, 0.06, 0.18, -0.25, s * 1.04, -0.4, trimMat, bank);
      bx(0.5, 0.03, 0.3, 0.15, s * 1.04, 0.15, teamMat, bank);
    }
    // EO sight mast, front right of the turntable
    var mastb = new THREE.Group();
    mastb.position.set(0.7, -0.72, 0.1);
    turret.add(mastb);
    cyl(0.05, 0.06, 0.7, 0, 0, 0.35, steelMat, mastb, "z", 10);
    bx(0.24, 0.2, 0.26, 0, 0, 0.82, trimMat, mastb);
    bx(0.03, 0.16, 0.16, 0.13, 0, 0.82, lensMat, mastb);
    return g;
  }
};
UNIT_MODELS["spaag_n"] = {
  len: 7.7,
  build: function (THREE, M, C) {
    var g = new THREE.Group();
    var seed = 51;
    function rnd() { seed = (seed * 48271) % 2147483647; return seed / 2147483647; }
    function paint(base) {
      var cv = document.createElement("canvas");
      cv.width = 256; cv.height = 256;
      var p = cv.getContext("2d");
      p.fillStyle = base; p.fillRect(0, 0, 256, 256);
      var i;
      for (i = 0; i < 26; i++) {
        p.globalAlpha = 0.04 + rnd() * 0.04;
        p.fillStyle = rnd() < 0.5 ? "#c8cdb8" : "#181c12";
        p.fillRect(rnd() * 256, rnd() * 256, 22 + rnd() * 66, 14 + rnd() * 48);
      }
      p.globalAlpha = 1;
      p.strokeStyle = "rgba(8,10,6,0.32)";
      p.lineWidth = 1;
      var vx = [22, 58, 94, 130, 168, 202, 238], hz = [40, 92, 150, 210];
      for (i = 0; i < vx.length; i++) { p.beginPath(); p.moveTo(vx[i], 0); p.lineTo(vx[i] + rnd() * 5, 256); p.stroke(); }
      for (i = 0; i < hz.length; i++) { p.beginPath(); p.moveTo(0, hz[i]); p.lineTo(256, hz[i] + rnd() * 5); p.stroke(); }
      p.fillStyle = "rgba(0,0,0,0.30)";
      for (i = 0; i < 46; i++) p.fillRect(rnd() * 256, rnd() * 256, 2, 2);
      for (i = 0; i < 14; i++) {
        p.fillStyle = "rgba(14,11,5,0.06)";
        p.fillRect(rnd() * 256, rnd() * 170, 2 + rnd() * 3, 26 + rnd() * 64);
      }
      var t = new THREE.CanvasTexture(cv);
      t.wrapS = THREE.RepeatWrapping; t.wrapT = THREE.RepeatWrapping;
      return t;
    }
    var bodyMat = new THREE.MeshStandardMaterial({ map: paint("#4a5240"), metalness: 0.25, roughness: 0.62 });
    var trimMat = new THREE.MeshStandardMaterial({ color: 0x424a39, metalness: 0.25, roughness: 0.66 });
    var darkMat = new THREE.MeshStandardMaterial({ color: 0x20231e, metalness: 0.15, roughness: 0.9 });
    var steelMat = new THREE.MeshStandardMaterial({ color: 0x33363a, metalness: 0.85, roughness: 0.3 });
    var lensMat = new THREE.MeshStandardMaterial({ color: 0x10151c, metalness: 0.6, roughness: 0.25 });
    var teamMat = new THREE.MeshStandardMaterial({ color: C.team, metalness: 0.3, roughness: 0.55 });
    function bx(sx, sy, sz, x, y, z, mat, parent) {
      var m = new THREE.Mesh(new THREE.BoxGeometry(sx, sy, sz), mat);
      m.position.set(x, y, z);
      (parent || g).add(m);
      return m;
    }
    function cyl(r1, r2, h, x, y, z, mat, parent, axis, seg) {
      var geo = new THREE.CylinderGeometry(r1, r2, h, seg || 14);
      if (axis === "x") geo.rotateZ(Math.PI / 2);
      else if (axis === "z") geo.rotateX(Math.PI / 2);
      var m = new THREE.Mesh(geo, mat);
      m.position.set(x, y, z);
      (parent || g).add(m);
      return m;
    }
    function track(sy, tl, th, tw, n, wr, span) {
      var t = new THREE.Group();
      var pts = [
        [tl * 0.5, th * 0.55],
        [tl * 0.5 - 0.5, th],
        [-tl * 0.5 + 0.5, th],
        [-tl * 0.5, th * 0.55],
        [-tl * 0.5 + 0.55, 0.01],
        [tl * 0.5 - 0.55, 0.01]
      ];
      var geo = M.slab(THREE, pts, tw, "xz");
      geo.computeBoundingBox();
      geo.translate(0, -(geo.boundingBox.max.y + geo.boundingBox.min.y) / 2, 0);
      t.add(new THREE.Mesh(geo, darkMat));
      var wg = new THREE.CylinderGeometry(wr, wr, tw + 0.1, 16);
      var hg = new THREE.CylinderGeometry(wr * 0.55, wr * 0.55, tw + 0.16, 12);
      for (var i = 0; i < n; i++) {
        var wx = span * 0.5 - span * i / (n - 1);
        var w = new THREE.Mesh(wg, darkMat);
        w.position.set(wx, 0, wr + 0.01);
        t.add(w);
        var h = new THREE.Mesh(hg, trimMat);
        h.position.set(wx, 0, wr + 0.01);
        t.add(h);
      }
      var sg = new THREE.CylinderGeometry(wr * 0.75, wr * 0.75, tw + 0.06, 12);
      var s1 = new THREE.Mesh(sg, darkMat);
      s1.position.set(tl * 0.5 - 0.26, 0, th * 0.58);
      t.add(s1);
      var s2 = new THREE.Mesh(sg, darkMat);
      s2.position.set(-tl * 0.5 + 0.26, 0, th * 0.58);
      t.add(s2);
      t.position.y = sy;
      g.add(t);
      return t;
    }
    // Leopard 1 hull. Roof 1.72 m.
    var hull = new THREE.Mesh(M.loft(THREE, [
      { x: -3.5, w: 1.42, h: 0.5, zc: 1.2, sq: 0.9 },
      { x: -2.4, w: 1.58, h: 0.6, zc: 1.12, sq: 0.9 },
      { x: 1.5, w: 1.58, h: 0.6, zc: 1.12, sq: 0.9 },
      { x: 2.6, w: 1.52, h: 0.46, zc: 1.26, sq: 0.88 },
      { x: 3.5, w: 1.28, h: 0.12, zc: 1.36, sq: 0.85 }
    ], 12), bodyMat);
    g.add(hull);
    bx(0.06, 2.55, 0.92, -3.51, 0, 1.2, trimMat);
    track(1.56, 6.6, 1.0, 0.5, 7, 0.32, 5.0);
    track(-1.56, 6.6, 1.0, 0.5, 7, 0.32, 5.0);
    var i, s;
    // Side skirts over the track run
    for (s = -1; s <= 1; s += 2) {
      bx(6.3, 0.05, 0.38, 0, s * 1.72, 1.12, bodyMat);
      bx(0.45, 0.5, 0.05, 3.15, s * 1.56, 1.06, trimMat);
    }
    // Glacis: spare track links, tow hooks, headlights
    for (i = 0; i < 3; i++) {
      var lk = bx(0.34, 0.5, 0.09, 2.85, -0.55 + i * 0.55, 1.62, darkMat);
      lk.rotation.y = 0.24;
    }
    for (s = -1; s <= 1; s += 2) {
      bx(0.18, 0.14, 0.2, 3.35, s * 1.0, 1.32, darkMat);
      bx(0.14, 0.22, 0.14, 3.1, s * 1.25, 1.6, trimMat);
      bx(0.03, 0.16, 0.1, 3.18, s * 1.25, 1.6, lensMat);
    }
    // Engine deck grilles
    bx(1.5, 2.2, 0.04, -2.5, 0, 1.71, darkMat);
    for (i = 0; i < 5; i++) bx(0.05, 2.1, 0.06, -3.05 + i * 0.28, 0, 1.72, trimMat);
    var ant = cyl(0.012, 0.012, 1.35, -3.3, 0.95, 2.35, steelMat, g, "z", 6);
    ant.rotation.x = 0.12;
    // Gepard turret: wide-set 35 mm guns, twin radars ------------------
    var turret = new THREE.Group();
    turret.name = "turret";
    turret.position.set(0.15, 0, 1.7);
    g.add(turret);
    var tur = new THREE.Mesh(M.loft(THREE, [
      { x: -1.35, w: 0.78, h: 0.44, zc: 0.52, sq: 0.8 },
      { x: -0.7, w: 1.0, h: 0.58, zc: 0.58, sq: 0.82 },
      { x: 0.5, w: 0.95, h: 0.55, zc: 0.55, sq: 0.82 },
      { x: 1.2, w: 0.55, h: 0.34, zc: 0.48, sq: 0.8 }
    ], 12), bodyMat);
    turret.add(tur);
    bx(0.2, 0.9, 0.58, 1.22, 0, 0.48, trimMat, turret);
    bx(0.16, 1.4, 0.8, -1.4, 0, 0.55, trimMat, turret);
    // Twin 35 mm KDA guns far apart on the turret sides
    for (s = -1; s <= 1; s += 2) {
      bx(1.0, 0.34, 0.5, 0.3, s * 1.18, 0.45, bodyMat, turret);
      cyl(0.2, 0.2, 0.38, 0, s * 1.18, 0.45, trimMat, turret, null, 14);
      cyl(0.055, 0.062, 2.85, 2.2, s * 1.18, 0.45, steelMat, turret, "x", 10);
      cyl(0.085, 0.085, 0.5, 1.05, s * 1.18, 0.45, steelMat, turret, "x", 10);
      cyl(0.09, 0.075, 0.32, 3.55, s * 1.18, 0.45, darkMat, turret, "x", 10);
      cyl(0.04, 0.04, 0.7, 1.1, s * 1.18, 0.26, steelMat, turret, "x", 8);
      // Smoke dischargers on the turret flank
      for (i = 0; i < 3; i++) {
        var st = cyl(0.042, 0.042, 0.32, -0.85, s * (0.95 + i * 0.1), 0.78, steelMat, turret, "x", 8);
        st.rotation.z = s * (0.35 + i * 0.22);
        st.rotation.y = -0.45;
      }
      bx(0.6, 0.03, 0.26, -0.35, s * 1.02, 0.62, teamMat, turret);
    }
    // Tracking radar dish, turret front top
    bx(0.3, 0.35, 0.4, 0.85, 0, 1.02, trimMat, turret);
    var tdg = new THREE.CylinderGeometry(0.34, 0.43, 0.1, 18);
    tdg.rotateZ(Math.PI / 2);
    var tdish = new THREE.Mesh(tdg, trimMat);
    tdish.position.set(1.08, 0, 1.12);
    turret.add(tdish);
    cyl(0.016, 0.016, 0.38, 1.3, 0, 1.12, steelMat, turret, "x", 6);
    bx(0.07, 0.07, 0.07, 1.5, 0, 1.12, darkMat, turret);
    // Round search radar dish folded flat at the turret rear
    bx(0.5, 0.3, 0.45, -1.35, 0, 1.02, trimMat, turret);
    var sdg = new THREE.CylinderGeometry(0.8, 0.66, 0.09, 20);
    sdg.rotateX(Math.PI / 2);
    var sdish = new THREE.Mesh(sdg, trimMat);
    sdish.position.set(-1.5, 0, 1.32);
    sdish.rotation.y = -0.32;
    turret.add(sdish);
    cyl(0.1, 0.1, 0.18, -1.5, 0, 1.42, darkMat, turret, "z", 10);
    // Crew hatches and periscope
    for (s = -1; s <= 1; s += 2) cyl(0.27, 0.27, 0.06, -0.45, s * 0.45, 1.18, trimMat, turret, "z", 16);
    bx(0.24, 0.3, 0.22, 0.15, 0.55, 1.16, trimMat, turret);
    bx(0.03, 0.22, 0.14, 0.28, 0.55, 1.18, lensMat, turret);
    return g;
  }
};
UNIT_MODELS["spaag_p"] = {
  len: 7.9,
  build: function (THREE, M, C) {
    var g = new THREE.Group();
    var seed = 67;
    function rnd() { seed = (seed * 48271) % 2147483647; return seed / 2147483647; }
    function paint(base) {
      var cv = document.createElement("canvas");
      cv.width = 256; cv.height = 256;
      var p = cv.getContext("2d");
      p.fillStyle = base; p.fillRect(0, 0, 256, 256);
      var i;
      for (i = 0; i < 24; i++) {
        p.globalAlpha = 0.04 + rnd() * 0.04;
        p.fillStyle = rnd() < 0.5 ? "#b9c2a4" : "#151a10";
        p.fillRect(rnd() * 256, rnd() * 256, 22 + rnd() * 66, 14 + rnd() * 48);
      }
      p.globalAlpha = 1;
      p.strokeStyle = "rgba(8,10,6,0.32)";
      p.lineWidth = 1;
      var vx = [28, 64, 100, 138, 176, 212], hz = [44, 98, 154, 214];
      for (i = 0; i < vx.length; i++) { p.beginPath(); p.moveTo(vx[i], 0); p.lineTo(vx[i] + rnd() * 5, 256); p.stroke(); }
      for (i = 0; i < hz.length; i++) { p.beginPath(); p.moveTo(0, hz[i]); p.lineTo(256, hz[i] + rnd() * 5); p.stroke(); }
      p.fillStyle = "rgba(0,0,0,0.30)";
      for (i = 0; i < 40; i++) p.fillRect(rnd() * 256, rnd() * 256, 2, 2);
      for (i = 0; i < 14; i++) {
        p.fillStyle = "rgba(14,11,5,0.06)";
        p.fillRect(rnd() * 256, rnd() * 170, 2 + rnd() * 3, 26 + rnd() * 64);
      }
      var t = new THREE.CanvasTexture(cv);
      t.wrapS = THREE.RepeatWrapping; t.wrapT = THREE.RepeatWrapping;
      return t;
    }
    var bodyMat = new THREE.MeshStandardMaterial({ map: paint("#3e4a35"), metalness: 0.25, roughness: 0.62 });
    var trimMat = new THREE.MeshStandardMaterial({ color: 0x37412e, metalness: 0.25, roughness: 0.66 });
    var darkMat = new THREE.MeshStandardMaterial({ color: 0x1f221c, metalness: 0.15, roughness: 0.9 });
    var steelMat = new THREE.MeshStandardMaterial({ color: 0x33363a, metalness: 0.85, roughness: 0.3 });
    var lensMat = new THREE.MeshStandardMaterial({ color: 0x10151c, metalness: 0.6, roughness: 0.25 });
    var teamMat = new THREE.MeshStandardMaterial({ color: C.team, metalness: 0.3, roughness: 0.55 });
    function bx(sx, sy, sz, x, y, z, mat, parent) {
      var m = new THREE.Mesh(new THREE.BoxGeometry(sx, sy, sz), mat);
      m.position.set(x, y, z);
      (parent || g).add(m);
      return m;
    }
    function cyl(r1, r2, h, x, y, z, mat, parent, axis, seg) {
      var geo = new THREE.CylinderGeometry(r1, r2, h, seg || 14);
      if (axis === "x") geo.rotateZ(Math.PI / 2);
      else if (axis === "z") geo.rotateX(Math.PI / 2);
      var m = new THREE.Mesh(geo, mat);
      m.position.set(x, y, z);
      (parent || g).add(m);
      return m;
    }
    function track(sy, tl, th, tw, n, wr, span) {
      var t = new THREE.Group();
      var pts = [
        [tl * 0.5, th * 0.55],
        [tl * 0.5 - 0.5, th],
        [-tl * 0.5 + 0.5, th],
        [-tl * 0.5, th * 0.55],
        [-tl * 0.5 + 0.55, 0.01],
        [tl * 0.5 - 0.55, 0.01]
      ];
      var geo = M.slab(THREE, pts, tw, "xz");
      geo.computeBoundingBox();
      geo.translate(0, -(geo.boundingBox.max.y + geo.boundingBox.min.y) / 2, 0);
      t.add(new THREE.Mesh(geo, darkMat));
      var wg = new THREE.CylinderGeometry(wr, wr, tw + 0.1, 16);
      var hg = new THREE.CylinderGeometry(wr * 0.55, wr * 0.55, tw + 0.16, 12);
      for (var i = 0; i < n; i++) {
        var wx = span * 0.5 - span * i / (n - 1);
        var w = new THREE.Mesh(wg, darkMat);
        w.position.set(wx, 0, wr + 0.01);
        t.add(w);
        var h = new THREE.Mesh(hg, trimMat);
        h.position.set(wx, 0, wr + 0.01);
        t.add(h);
      }
      var sg = new THREE.CylinderGeometry(wr * 0.75, wr * 0.75, tw + 0.06, 12);
      var s1 = new THREE.Mesh(sg, darkMat);
      s1.position.set(tl * 0.5 - 0.26, 0, th * 0.58);
      t.add(s1);
      var s2 = new THREE.Mesh(sg, darkMat);
      s2.position.set(-tl * 0.5 + 0.26, 0, th * 0.58);
      t.add(s2);
      t.position.y = sy;
      g.add(t);
      return t;
    }
    // GM-352 chassis. Roof 1.72 m.
    var hull = new THREE.Mesh(M.loft(THREE, [
      { x: -3.9, w: 1.32, h: 0.5, zc: 1.2, sq: 0.88 },
      { x: -2.6, w: 1.46, h: 0.6, zc: 1.12, sq: 0.9 },
      { x: 1.6, w: 1.46, h: 0.6, zc: 1.12, sq: 0.9 },
      { x: 2.9, w: 1.4, h: 0.46, zc: 1.26, sq: 0.88 },
      { x: 3.9, w: 1.02, h: 0.12, zc: 1.36, sq: 0.85 }
    ], 12), bodyMat);
    g.add(hull);
    bx(0.06, 2.3, 0.92, -3.91, 0, 1.2, trimMat);
    track(1.46, 7.2, 1.0, 0.48, 6, 0.36, 5.5);
    track(-1.46, 7.2, 1.0, 0.48, 6, 0.36, 5.5);
    var i, s;
    // Bow splash rib, headlights, mudflaps
    var rib = bx(0.07, 1.9, 0.1, 3.1, 0, 1.56, trimMat);
    rib.rotation.y = 0.3;
    for (s = -1; s <= 1; s += 2) {
      bx(0.14, 0.2, 0.14, 3.5, s * 0.75, 1.5, trimMat);
      bx(0.03, 0.14, 0.1, 3.58, s * 0.75, 1.5, lensMat);
      bx(0.42, 0.46, 0.05, 3.6, s * 1.46, 1.06, trimMat);
      bx(0.32, 0.46, 0.05, -3.7, s * 1.46, 1.06, trimMat);
      bx(0.6, 0.03, 0.36, 2.9, s * 1.4, 1.28, teamMat);
    }
    // Driver hatch and engine deck
    bx(0.55, 0.45, 0.06, 2.5, 0.6, 1.7, trimMat);
    bx(1.4, 1.9, 0.04, -2.9, 0, 1.71, darkMat);
    for (i = 0; i < 4; i++) bx(0.05, 1.8, 0.06, -3.4 + i * 0.32, 0, 1.72, trimMat);
    var ant = cyl(0.012, 0.012, 1.3, -3.6, 0.85, 2.35, steelMat, g, "z", 6);
    ant.rotation.x = 0.13;
    // Tunguska turret: gun pods, 4+4 missile tubes, twin radars --------
    var turret = new THREE.Group();
    turret.name = "turret";
    turret.position.set(-0.15, 0, 1.72);
    g.add(turret);
    cyl(0.95, 1.05, 0.12, 0, 0, 0.06, trimMat, turret, "z", 20);
    bx(2.2, 2.0, 0.95, 0, 0, 0.55, bodyMat, turret);
    var fp = bx(0.08, 1.95, 1.0, 1.15, 0, 0.55, bodyMat, turret);
    fp.rotation.y = -0.4;
    bx(1.1, 1.3, 0.3, -0.45, 0, 1.15, bodyMat, turret);
    // Twin-barrel 2A38 gun pods, one low on each side
    var k;
    for (s = -1; s <= 1; s += 2) {
      bx(1.4, 0.26, 0.38, 0.35, s * 1.1, 0.22, bodyMat, turret);
      bx(0.5, 0.32, 0.46, -0.5, s * 1.1, 0.26, trimMat, turret);
      for (k = 0; k < 2; k++) {
        cyl(0.034, 0.028, 2.35, 2.2, s * 1.1 + (k ? 0.06 : -0.06), 0.22, steelMat, turret, "x", 8);
        cyl(0.05, 0.05, 0.16, 3.42, s * 1.1 + (k ? 0.06 : -0.06), 0.22, darkMat, turret, "x", 8);
      }
    }
    // Missile banks: 4 canisters each side, 2 wide x 2 high, canted out
    for (s = -1; s <= 1; s += 2) {
      var bank = new THREE.Group();
      bank.position.set(-0.4, s * 1.28, 0.6);
      bank.rotation.y = -0.16;
      bank.rotation.z = s * 0.07;
      turret.add(bank);
      var kx, kz;
      for (kx = 0; kx < 2; kx++) {
        for (kz = 0; kz < 2; kz++) {
          bx(2.25, 0.28, 0.32, 0, s * kx * 0.32, -0.2 + kz * 0.4, bodyMat, bank);
          bx(0.05, 0.24, 0.28, 1.14, s * kx * 0.32, -0.2 + kz * 0.4, darkMat, bank);
          bx(0.05, 0.24, 0.28, -1.14, s * kx * 0.32, -0.2 + kz * 0.4, trimMat, bank);
        }
      }
      bx(0.08, 0.05, 0.85, -0.2, s * 0.16, 0, trimMat, bank);
      bx(0.5, 0.03, 0.26, 0.55, s * 1.01, 0.85, teamMat, turret);
    }
    // Tracking radar dish, front centre
    bx(0.35, 0.4, 0.45, 1.5, 0, 0.95, trimMat, turret);
    var tdg = new THREE.CylinderGeometry(0.4, 0.5, 0.12, 18);
    tdg.rotateZ(Math.PI / 2);
    var tdish = new THREE.Mesh(tdg, trimMat);
    tdish.position.set(1.78, 0, 1.0);
    turret.add(tdish);
    cyl(0.016, 0.016, 0.4, 2.0, 0, 1.0, steelMat, turret, "x", 6);
    bx(0.08, 0.08, 0.08, 2.22, 0, 1.0, darkMat, turret);
    // Search radar antenna on mast, folded back over the rear
    cyl(0.06, 0.08, 0.45, -1.15, 0, 1.45, steelMat, turret, "z", 10);
    var sa = bx(0.1, 1.6, 0.55, -1.25, 0, 1.72, trimMat, turret);
    sa.rotation.y = -0.55;
    bx(0.18, 0.4, 0.2, -1.05, 0, 1.5, darkMat, turret);
    // Commander hatch and periscopes
    cyl(0.27, 0.27, 0.06, 0.45, 0.55, 1.06, trimMat, turret, "z", 16);
    bx(0.12, 0.26, 0.08, 0.75, 0.55, 1.07, darkMat, turret);
    return g;
  }
};
UNIT_MODELS["spaag_c"] = {
  len: 7.4,
  build: function (THREE, M, C) {
    var g = new THREE.Group();
    var seed = 83;
    function rnd() { seed = (seed * 48271) % 2147483647; return seed / 2147483647; }
    function paint(base) {
      var cv = document.createElement("canvas");
      cv.width = 256; cv.height = 256;
      var p = cv.getContext("2d");
      p.fillStyle = base; p.fillRect(0, 0, 256, 256);
      var i;
      for (i = 0; i < 24; i++) {
        p.globalAlpha = 0.04 + rnd() * 0.04;
        p.fillStyle = rnd() < 0.5 ? "#c2cbb0" : "#171c12";
        p.fillRect(rnd() * 256, rnd() * 256, 22 + rnd() * 66, 14 + rnd() * 48);
      }
      p.globalAlpha = 1;
      p.strokeStyle = "rgba(8,10,6,0.32)";
      p.lineWidth = 1;
      var vx = [24, 60, 96, 134, 170, 206, 240], hz = [38, 92, 148, 206];
      for (i = 0; i < vx.length; i++) { p.beginPath(); p.moveTo(vx[i], 0); p.lineTo(vx[i] + rnd() * 5, 256); p.stroke(); }
      for (i = 0; i < hz.length; i++) { p.beginPath(); p.moveTo(0, hz[i]); p.lineTo(256, hz[i] + rnd() * 5); p.stroke(); }
      p.fillStyle = "rgba(0,0,0,0.30)";
      for (i = 0; i < 42; i++) p.fillRect(rnd() * 256, rnd() * 256, 2, 2);
      for (i = 0; i < 12; i++) {
        p.fillStyle = "rgba(14,11,5,0.06)";
        p.fillRect(rnd() * 256, rnd() * 170, 2 + rnd() * 3, 26 + rnd() * 64);
      }
      var t = new THREE.CanvasTexture(cv);
      t.wrapS = THREE.RepeatWrapping; t.wrapT = THREE.RepeatWrapping;
      return t;
    }
    var bodyMat = new THREE.MeshStandardMaterial({ map: paint("#4e5844"), metalness: 0.25, roughness: 0.62 });
    var trimMat = new THREE.MeshStandardMaterial({ color: 0x454f3c, metalness: 0.25, roughness: 0.66 });
    var darkMat = new THREE.MeshStandardMaterial({ color: 0x20231d, metalness: 0.15, roughness: 0.9 });
    var steelMat = new THREE.MeshStandardMaterial({ color: 0x33363a, metalness: 0.85, roughness: 0.3 });
    var lensMat = new THREE.MeshStandardMaterial({ color: 0x10151c, metalness: 0.6, roughness: 0.25 });
    var teamMat = new THREE.MeshStandardMaterial({ color: C.team, metalness: 0.3, roughness: 0.55 });
    function bx(sx, sy, sz, x, y, z, mat, parent) {
      var m = new THREE.Mesh(new THREE.BoxGeometry(sx, sy, sz), mat);
      m.position.set(x, y, z);
      (parent || g).add(m);
      return m;
    }
    function cyl(r1, r2, h, x, y, z, mat, parent, axis, seg) {
      var geo = new THREE.CylinderGeometry(r1, r2, h, seg || 14);
      if (axis === "x") geo.rotateZ(Math.PI / 2);
      else if (axis === "z") geo.rotateX(Math.PI / 2);
      var m = new THREE.Mesh(geo, mat);
      m.position.set(x, y, z);
      (parent || g).add(m);
      return m;
    }
    function track(sy, tl, th, tw, n, wr, span) {
      var t = new THREE.Group();
      var pts = [
        [tl * 0.5, th * 0.55],
        [tl * 0.5 - 0.5, th],
        [-tl * 0.5 + 0.5, th],
        [-tl * 0.5, th * 0.55],
        [-tl * 0.5 + 0.55, 0.01],
        [tl * 0.5 - 0.55, 0.01]
      ];
      var geo = M.slab(THREE, pts, tw, "xz");
      geo.computeBoundingBox();
      geo.translate(0, -(geo.boundingBox.max.y + geo.boundingBox.min.y) / 2, 0);
      t.add(new THREE.Mesh(geo, darkMat));
      var wg = new THREE.CylinderGeometry(wr, wr, tw + 0.1, 16);
      var hg = new THREE.CylinderGeometry(wr * 0.55, wr * 0.55, tw + 0.16, 12);
      for (var i = 0; i < n; i++) {
        var wx = span * 0.5 - span * i / (n - 1);
        var w = new THREE.Mesh(wg, darkMat);
        w.position.set(wx, 0, wr + 0.01);
        t.add(w);
        var h = new THREE.Mesh(hg, trimMat);
        h.position.set(wx, 0, wr + 0.01);
        t.add(h);
      }
      var sg = new THREE.CylinderGeometry(wr * 0.75, wr * 0.75, tw + 0.06, 12);
      var s1 = new THREE.Mesh(sg, darkMat);
      s1.position.set(tl * 0.5 - 0.26, 0, th * 0.58);
      t.add(s1);
      var s2 = new THREE.Mesh(sg, darkMat);
      s2.position.set(-tl * 0.5 + 0.26, 0, th * 0.58);
      t.add(s2);
      t.position.y = sy;
      g.add(t);
      return t;
    }
    // PLZ-05 family chassis. Roof 1.76 m.
    var hull = new THREE.Mesh(M.loft(THREE, [
      { x: -3.65, w: 1.36, h: 0.52, zc: 1.2, sq: 0.88 },
      { x: -2.3, w: 1.46, h: 0.62, zc: 1.14, sq: 0.9 },
      { x: 1.4, w: 1.46, h: 0.62, zc: 1.14, sq: 0.9 },
      { x: 2.6, w: 1.42, h: 0.46, zc: 1.28, sq: 0.88 },
      { x: 3.65, w: 1.1, h: 0.12, zc: 1.4, sq: 0.85 }
    ], 12), bodyMat);
    g.add(hull);
    bx(0.06, 2.35, 0.96, -3.66, 0, 1.2, trimMat);
    track(1.4, 6.8, 0.98, 0.44, 6, 0.34, 5.2);
    track(-1.4, 6.8, 0.98, 0.44, 6, 0.34, 5.2);
    var i, s;
    // Side skirts, headlights, mudflaps, id panels
    for (s = -1; s <= 1; s += 2) {
      bx(6.4, 0.05, 0.34, 0, s * 1.55, 1.12, bodyMat);
      bx(0.14, 0.2, 0.14, 3.25, s * 0.8, 1.58, trimMat);
      bx(0.03, 0.14, 0.1, 3.33, s * 0.8, 1.58, lensMat);
      bx(0.4, 0.44, 0.05, 3.35, s * 1.4, 1.04, trimMat);
      bx(0.55, 0.03, 0.36, 2.6, s * 1.4, 1.3, teamMat);
    }
    // Driver hatch and engine deck grille
    bx(0.55, 0.45, 0.06, 2.45, 0.6, 1.74, trimMat);
    bx(1.1, 1.6, 0.04, -2.75, 0, 1.75, darkMat);
    for (i = 0; i < 4; i++) bx(0.05, 1.5, 0.06, -3.16 + i * 0.27, 0, 1.76, trimMat);
    var ant = cyl(0.012, 0.012, 1.3, -3.35, -0.9, 2.4, steelMat, g, "z", 6);
    ant.rotation.x = -0.13;
    // PGZ-09 turret: twin 35 mm, dome radar aft of guns ----------------
    var turret = new THREE.Group();
    turret.name = "turret";
    turret.position.set(-0.35, 0, 1.76);
    g.add(turret);
    var tur = new THREE.Mesh(M.loft(THREE, [
      { x: -1.45, w: 0.8, h: 0.4, zc: 0.48, sq: 0.82 },
      { x: -0.5, w: 1.05, h: 0.55, zc: 0.55, sq: 0.85 },
      { x: 0.7, w: 0.95, h: 0.5, zc: 0.5, sq: 0.82 },
      { x: 1.35, w: 0.55, h: 0.3, zc: 0.42, sq: 0.8 }
    ], 12), bodyMat);
    turret.add(tur);
    bx(0.16, 0.9, 0.5, 1.38, 0, 0.42, trimMat, turret);
    bx(0.14, 1.4, 0.7, -1.5, 0, 0.5, trimMat, turret);
    // Twin 35 mm guns in armoured sleeves at the front corners
    for (s = -1; s <= 1; s += 2) {
      bx(1.2, 0.3, 0.42, 0.9, s * 0.95, 0.4, bodyMat, turret);
      cyl(0.16, 0.16, 0.34, 0.35, s * 0.95, 0.4, trimMat, turret, null, 12);
      cyl(0.052, 0.058, 2.4, 2.55, s * 0.95, 0.4, steelMat, turret, "x", 10);
      cyl(0.085, 0.07, 0.3, 3.85, s * 0.95, 0.4, darkMat, turret, "x", 10);
      cyl(0.035, 0.035, 0.6, 1.75, s * 0.95, 0.22, steelMat, turret, "x", 8);
      for (i = 0; i < 3; i++) {
        var st = cyl(0.042, 0.042, 0.3, -0.75, s * (1.0 + i * 0.09), 0.75, steelMat, turret, "x", 8);
        st.rotation.z = s * (0.35 + i * 0.22);
        st.rotation.y = -0.45;
      }
      bx(0.6, 0.03, 0.26, 0.1, s * 1.04, 0.6, teamMat, turret);
    }
    // Tracking radar dome aft of the guns
    var domeGeo = new THREE.SphereGeometry(0.46, 20, 12, 0, Math.PI * 2, 0, Math.PI / 2);
    domeGeo.rotateX(Math.PI / 2);
    var dome = new THREE.Mesh(domeGeo, trimMat);
    dome.position.set(-0.15, 0, 1.06);
    turret.add(dome);
    cyl(0.46, 0.5, 0.12, -0.15, 0, 1.02, darkMat, turret, "z", 20);
    // Folding search radar panel at the turret rear
    cyl(0.05, 0.07, 0.4, -1.15, 0, 1.15, steelMat, turret, "z", 10);
    var arr = bx(0.08, 1.35, 0.5, -1.22, 0, 1.42, trimMat, turret);
    arr.rotation.y = -0.5;
    bx(0.14, 0.3, 0.16, -1.05, 0, 1.2, darkMat, turret);
    // EO director and commander hatch
    bx(0.3, 0.34, 0.3, 0.55, 0.4, 1.06, trimMat, turret);
    bx(0.03, 0.24, 0.2, 0.71, 0.4, 1.08, lensMat, turret);
    cyl(0.27, 0.27, 0.06, -0.6, -0.5, 1.14, trimMat, turret, "z", 16);
    return g;
  }
};

/* ---- pack a6c225f6 ---- */
// ============================================================================
// LIGHTARMOR SET - 9 vehicles, all with named "turret" groups
// Space: +X forward, +Y left, +Z up. Metres, real scale. Ground contact z=0.
// ============================================================================

// ---------------------------------------------------------------------------
// M1151 up-armored Humvee. 4.9 x 2.3 x 1.9 m. Boxy 4x4, flat wide bonnet,
// slant windshield, brush guard, roof gunner ring with M2 .50cal.
// ---------------------------------------------------------------------------
UNIT_MODELS["recon_n"] = {
  len: 4.9,
  build: function (THREE, M, C) {
    var g = new THREE.Group();
    function paint(base) {
      var cv = document.createElement("canvas");
      cv.width = 256; cv.height = 256;
      var q = cv.getContext("2d");
      q.fillStyle = base; q.fillRect(0, 0, 256, 256);
      var i, x, y, h, gr;
      for (i = 0; i < 24; i++) {
        q.fillStyle = (i % 2) ? "rgba(20,24,14,0.07)" : "rgba(235,238,218,0.05)";
        q.fillRect(Math.random() * 256, Math.random() * 256, 22 + Math.random() * 58, 14 + Math.random() * 42);
      }
      q.strokeStyle = "rgba(10,12,8,0.35)"; q.lineWidth = 1;
      for (i = 0; i < 6; i++) {
        x = 12 + Math.random() * 232;
        q.beginPath(); q.moveTo(x, 0); q.lineTo(x, 256); q.stroke();
        y = 12 + Math.random() * 232;
        q.beginPath(); q.moveTo(0, y); q.lineTo(256, y); q.stroke();
      }
      q.fillStyle = "rgba(15,15,10,0.45)";
      for (i = 0; i < 36; i++) { q.fillRect(Math.random() * 256, Math.random() * 256, 2, 2); }
      for (i = 0; i < 10; i++) {
        x = Math.random() * 256; y = Math.random() * 180; h = 40 + Math.random() * 60;
        gr = q.createLinearGradient(0, y, 0, y + h);
        gr.addColorStop(0, "rgba(0,0,0,0.12)"); gr.addColorStop(1, "rgba(0,0,0,0)");
        q.fillStyle = gr; q.fillRect(x, y, 2 + Math.random() * 3, h);
      }
      var t = new THREE.CanvasTexture(cv);
      t.wrapS = THREE.RepeatWrapping; t.wrapT = THREE.RepeatWrapping;
      t.anisotropy = 4;
      return t;
    }
    var hullMat = new THREE.MeshStandardMaterial({ map: paint("#4d5443"), metalness: 0.25, roughness: 0.6, side: THREE.DoubleSide });
    var plainMat = new THREE.MeshStandardMaterial({ color: 0x454c3c, metalness: 0.25, roughness: 0.6 });
    var rubberMat = new THREE.MeshStandardMaterial({ color: 0x17191a, metalness: 0.05, roughness: 0.92 });
    var steelMat = new THREE.MeshStandardMaterial({ color: 0x43474a, metalness: 0.85, roughness: 0.3 });
    var glassMat = new THREE.MeshStandardMaterial({ color: 0x131b21, metalness: 0.4, roughness: 0.16 });
    var teamMat = new THREE.MeshStandardMaterial({ color: C.team, metalness: 0.3, roughness: 0.5 });
    function B(a, b, c, m) { return new THREE.Mesh(new THREE.BoxGeometry(a, b, c), m); }
    function P(m, x, y, z, pr) { m.position.set(x, y, z); (pr || g).add(m); return m; }
    function cylX(r, l, m, s) { var o = new THREE.Mesh(new THREE.CylinderGeometry(r, r, l, s || 12), m); o.rotation.z = -Math.PI / 2; return o; }
    function cylZ(rt, rb, h, m, s) { var o = new THREE.Mesh(new THREE.CylinderGeometry(rt, rb, h, s || 14), m); o.rotation.x = Math.PI / 2; return o; }

    // chassis pan
    P(B(3.6, 1.7, 0.14, plainMat), 0, 0, 0.5);
    // flat wide bonnet
    P(B(1.55, 2.16, 0.46, hullMat), 1.5, 0, 0.8);
    P(B(0.06, 1.7, 0.3, rubberMat), 2.28, 0, 0.84);        // grille inset
    P(B(0.18, 2.2, 0.2, plainMat), 2.38, 0, 0.62);          // bumper
    // brush guard
    P(B(0.04, 1.9, 0.05, steelMat), 2.47, 0, 1.1);
    P(B(0.04, 1.9, 0.05, steelMat), 2.47, 0, 0.76);
    [-0.8, 0, 0.8].forEach(function (yy) { P(B(0.04, 0.05, 0.56, steelMat), 2.47, yy, 0.93); });
    // cab (armored doors read through the texture seams)
    P(B(1.8, 2.2, 0.75, hullMat), -0.15, 0, 0.925);
    var ws = P(B(0.05, 1.9, 0.86, glassMat), 0.57, 0, 1.44); ws.rotation.y = -0.42;
    P(B(1.45, 2.2, 0.1, hullMat), -0.33, 0, 1.86);          // roof
    P(B(1.32, 0.05, 0.5, glassMat), -0.32, 1.06, 1.55);     // side glass
    P(B(1.32, 0.05, 0.5, glassMat), -0.32, -1.06, 1.55);
    P(B(0.05, 1.9, 0.5, glassMat), -1.02, 0, 1.55);         // rear glass
    [[0.38, 1.04], [0.38, -1.04], [-1.0, 1.04], [-1.0, -1.04]].forEach(function (p) {
      P(B(0.09, 0.09, 0.62, plainMat), p[0], p[1], 1.56);   // pillars
    });
    // rear cargo bed with tarp roll and jerry cans
    P(B(1.25, 2.2, 0.72, hullMat), -1.7, 0, 0.98);
    P(B(1.0, 1.7, 0.26, plainMat), -1.75, 0, 1.47);
    P(B(0.2, 0.34, 0.42, plainMat), -2.2, 0.8, 1.55);
    P(B(0.2, 0.34, 0.42, plainMat), -2.2, -0.8, 1.55);
    // big black tyres + hubs + fender flares
    [[1.65, 0.95], [1.65, -0.95], [-1.65, 0.95], [-1.65, -0.95]].forEach(function (p) {
      P(new THREE.Mesh(new THREE.CylinderGeometry(0.465, 0.465, 0.33, 18), rubberMat), p[0], p[1], 0.465);
      P(new THREE.Mesh(new THREE.CylinderGeometry(0.2, 0.2, 0.38, 12), plainMat), p[0], p[1], 0.465);
      P(B(1.05, 0.14, 0.06, hullMat), p[0], p[1] > 0 ? 1.12 : -1.12, 1.05);
    });
    // headlights
    P(cylX(0.09, 0.05, glassMat, 10), 2.3, 0.85, 1.0);
    P(cylX(0.09, 0.05, glassMat, 10), 2.3, -0.85, 1.0);
    // mirrors
    [1, -1].forEach(function (s) {
      P(B(0.04, 0.05, 0.4, steelMat), 0.72, s * 1.14, 1.5);
      P(B(0.12, 0.03, 0.18, glassMat), 0.72, s * 1.17, 1.7);
    });
    // whip antennas
    P(cylZ(0.012, 0.012, 1.5, steelMat, 6), -2.15, 0.95, 2.1);
    P(cylZ(0.012, 0.012, 1.2, steelMat, 6), -2.15, -0.95, 1.95);
    // team identification panel on the bonnet
    P(B(0.5, 0.36, 0.02, teamMat), 1.55, 0, 1.04);
    // turret: gunner ring drum + M2 .50cal
    var T = new THREE.Group(); T.name = "turret"; T.position.set(-0.33, 0, 1.91); g.add(T);
    P(cylZ(0.48, 0.5, 0.14, hullMat, 20), 0, 0, 0.05, T);
    P(new THREE.Mesh(new THREE.TorusGeometry(0.48, 0.05, 8, 20), plainMat), 0, 0, 0.13, T);
    P(B(0.05, 0.75, 0.42, hullMat), 0.3, 0, 0.34, T);       // gun shield
    P(cylZ(0.04, 0.04, 0.28, steelMat, 8), 0.1, 0, 0.22, T); // pintle
    P(B(0.5, 0.14, 0.16, steelMat), 0.28, 0, 0.42, T);      // M2 receiver
    P(cylX(0.026, 1.1, steelMat, 8), 1.08, 0, 0.44, T);     // barrel
    P(cylX(0.045, 0.1, steelMat, 8), 1.66, 0, 0.44, T);     // muzzle booster
    P(B(0.16, 0.22, 0.18, plainMat), 0.25, 0.22, 0.44, T);  // ammo box
    return g;
  }
};

// ---------------------------------------------------------------------------
// BRDM-2 amphibious scout car. 5.75 x 2.35 x 2.31 m. Boat-nose hull,
// bow trim vane, small cone turret with 14.5mm KPVT, big 4x4 wheels.
// ---------------------------------------------------------------------------
UNIT_MODELS["recon_p"] = {
  len: 5.75,
  build: function (THREE, M, C) {
    var g = new THREE.Group();
    function paint(base) {
      var cv = document.createElement("canvas");
      cv.width = 256; cv.height = 256;
      var q = cv.getContext("2d");
      q.fillStyle = base; q.fillRect(0, 0, 256, 256);
      var i, x, y, h, gr;
      for (i = 0; i < 24; i++) {
        q.fillStyle = (i % 2) ? "rgba(16,22,12,0.07)" : "rgba(230,236,214,0.05)";
        q.fillRect(Math.random() * 256, Math.random() * 256, 22 + Math.random() * 58, 14 + Math.random() * 42);
      }
      q.strokeStyle = "rgba(8,12,6,0.35)"; q.lineWidth = 1;
      for (i = 0; i < 6; i++) {
        x = 12 + Math.random() * 232;
        q.beginPath(); q.moveTo(x, 0); q.lineTo(x, 256); q.stroke();
        y = 12 + Math.random() * 232;
        q.beginPath(); q.moveTo(0, y); q.lineTo(256, y); q.stroke();
      }
      q.fillStyle = "rgba(12,14,8,0.45)";
      for (i = 0; i < 36; i++) { q.fillRect(Math.random() * 256, Math.random() * 256, 2, 2); }
      for (i = 0; i < 12; i++) {
        x = Math.random() * 256; y = Math.random() * 180; h = 40 + Math.random() * 70;
        gr = q.createLinearGradient(0, y, 0, y + h);
        gr.addColorStop(0, "rgba(0,0,0,0.13)"); gr.addColorStop(1, "rgba(0,0,0,0)");
        q.fillStyle = gr; q.fillRect(x, y, 2 + Math.random() * 3, h);
      }
      var t = new THREE.CanvasTexture(cv);
      t.wrapS = THREE.RepeatWrapping; t.wrapT = THREE.RepeatWrapping;
      t.anisotropy = 4;
      return t;
    }
    var hullMat = new THREE.MeshStandardMaterial({ map: paint("#3e4a35"), metalness: 0.25, roughness: 0.62, side: THREE.DoubleSide });
    var plainMat = new THREE.MeshStandardMaterial({ color: 0x39432f, metalness: 0.25, roughness: 0.62 });
    var rubberMat = new THREE.MeshStandardMaterial({ color: 0x17191a, metalness: 0.05, roughness: 0.92 });
    var steelMat = new THREE.MeshStandardMaterial({ color: 0x43474a, metalness: 0.85, roughness: 0.3 });
    var glassMat = new THREE.MeshStandardMaterial({ color: 0x131b21, metalness: 0.4, roughness: 0.16 });
    var teamMat = new THREE.MeshStandardMaterial({ color: C.team, metalness: 0.3, roughness: 0.5 });
    function B(a, b, c, m) { return new THREE.Mesh(new THREE.BoxGeometry(a, b, c), m); }
    function P(m, x, y, z, pr) { m.position.set(x, y, z); (pr || g).add(m); return m; }
    function cylX(r, l, m, s) { var o = new THREE.Mesh(new THREE.CylinderGeometry(r, r, l, s || 12), m); o.rotation.z = -Math.PI / 2; return o; }
    function cylZ(rt, rb, h, m, s) { var o = new THREE.Mesh(new THREE.CylinderGeometry(rt, rb, h, s || 14), m); o.rotation.x = Math.PI / 2; return o; }

    // boat-nose amphibious hull
    g.add(new THREE.Mesh(M.loft(THREE, [
      { x: 2.87, w: 0.32, h: 0.16, zc: 1.22 },
      { x: 2.45, w: 0.8, h: 0.36, zc: 1.18, sq: 0.75 },
      { x: 1.8, w: 1.1, h: 0.62, zc: 1.16, sq: 0.8 },
      { x: 0.9, w: 1.17, h: 0.75, zc: 1.15, sq: 0.85 },
      { x: -0.8, w: 1.17, h: 0.74, zc: 1.12, sq: 0.85 },
      { x: -2.0, w: 1.15, h: 0.66, zc: 1.1, sq: 0.85 },
      { x: -2.65, w: 0.98, h: 0.5, zc: 1.06, sq: 0.8 },
      { x: -2.87, w: 0.7, h: 0.34, zc: 1.02 }
    ], 16), hullMat));
    // trim vane stowed flat on the bow, with support ribs
    var tv = P(B(0.95, 1.5, 0.05, plainMat), 2.32, 0, 1.62); tv.rotation.y = 0.38;
    [0.5, -0.5].forEach(function (yy) {
      var rb = P(B(0.95, 0.06, 0.04, plainMat), 2.33, yy, 1.67); rb.rotation.y = 0.38;
    });
    // armored windshield ports
    [0.4, -0.4].forEach(function (yy) {
      var wp = P(B(0.05, 0.42, 0.3, glassMat), 2.02, yy, 1.68); wp.rotation.y = -0.6;
    });
    // wheels: 4 big run-flats
    [[1.55, 1.0], [1.55, -1.0], [-1.55, 1.0], [-1.55, -1.0]].forEach(function (p) {
      P(new THREE.Mesh(new THREE.CylinderGeometry(0.57, 0.57, 0.36, 18), rubberMat), p[0], p[1], 0.57);
      P(new THREE.Mesh(new THREE.CylinderGeometry(0.24, 0.24, 0.42, 12), plainMat), p[0], p[1], 0.57);
    });
    // mud flaps behind rear wheels
    P(B(0.05, 0.3, 0.28, rubberMat), -2.2, 1.0, 0.4);
    P(B(0.05, 0.3, 0.28, rubberMat), -2.2, -1.0, 0.4);
    // headlight pods with brush guards
    [0.5, -0.5].forEach(function (yy) {
      P(cylX(0.08, 0.06, glassMat, 10), 2.52, yy, 1.5);
      P(B(0.06, 0.2, 0.03, steelMat), 2.54, yy, 1.62);
    });
    // flank stowage bins and exhaust pipes
    P(B(0.9, 0.1, 0.3, plainMat), -0.9, 1.2, 1.55);
    P(B(0.9, 0.1, 0.3, plainMat), -0.9, -1.2, 1.55);
    P(B(0.55, 0.08, 0.1, steelMat), -2.2, 1.16, 1.55);
    P(B(0.55, 0.08, 0.1, steelMat), -2.2, -1.16, 1.55);
    // water jet outlet at the stern
    P(cylX(0.26, 0.08, rubberMat, 14), -2.88, 0, 0.95);
    // roof periscopes
    P(B(0.14, 0.1, 0.08, plainMat), 1.35, 0.45, 1.93);
    P(B(0.14, 0.1, 0.08, plainMat), 1.35, -0.45, 1.93);
    // whip antenna
    P(cylZ(0.012, 0.012, 1.4, steelMat, 6), -1.6, 0.9, 2.45);
    // team identification plates on the flanks
    P(B(0.55, 0.04, 0.32, teamMat), 0.6, 1.17, 1.5);
    P(B(0.55, 0.04, 0.32, teamMat), 0.6, -1.17, 1.5);
    // small cone turret with KPVT
    var T = new THREE.Group(); T.name = "turret"; T.position.set(0.35, 0, 1.88); g.add(T);
    P(cylZ(0.3, 0.48, 0.4, hullMat, 16), 0, 0, 0.2, T);
    P(cylZ(0.22, 0.3, 0.1, hullMat, 16), 0, 0, 0.45, T);
    P(B(0.28, 0.34, 0.24, plainMat), 0.42, 0, 0.18, T);     // mantlet
    P(cylX(0.035, 1.35, steelMat, 10), 1.22, 0, 0.2, T);    // 14.5mm KPVT
    P(cylX(0.05, 0.1, steelMat, 8), 1.94, 0, 0.2, T);       // flash hider
    P(cylX(0.016, 0.85, steelMat, 8), 0.97, -0.16, 0.16, T); // PKT coax
    return g;
  }
};

// ---------------------------------------------------------------------------
// CSK-131 Mengshi armoured 4x4. 5.4 x 2.5 x ~2.1 m. Angular protected cab,
// sloped V nose, remote weapon station with sensor head.
// ---------------------------------------------------------------------------
UNIT_MODELS["recon_c"] = {
  len: 5.4,
  build: function (THREE, M, C) {
    var g = new THREE.Group();
    function paint(base) {
      var cv = document.createElement("canvas");
      cv.width = 256; cv.height = 256;
      var q = cv.getContext("2d");
      q.fillStyle = base; q.fillRect(0, 0, 256, 256);
      var i, x, y, h, gr;
      for (i = 0; i < 24; i++) {
        q.fillStyle = (i % 2) ? "rgba(18,24,16,0.07)" : "rgba(232,238,222,0.05)";
        q.fillRect(Math.random() * 256, Math.random() * 256, 22 + Math.random() * 58, 14 + Math.random() * 42);
      }
      q.strokeStyle = "rgba(10,14,8,0.35)"; q.lineWidth = 1;
      for (i = 0; i < 6; i++) {
        x = 12 + Math.random() * 232;
        q.beginPath(); q.moveTo(x, 0); q.lineTo(x, 256); q.stroke();
        y = 12 + Math.random() * 232;
        q.beginPath(); q.moveTo(0, y); q.lineTo(256, y); q.stroke();
      }
      q.fillStyle = "rgba(12,14,10,0.45)";
      for (i = 0; i < 36; i++) { q.fillRect(Math.random() * 256, Math.random() * 256, 2, 2); }
      for (i = 0; i < 10; i++) {
        x = Math.random() * 256; y = Math.random() * 180; h = 40 + Math.random() * 60;
        gr = q.createLinearGradient(0, y, 0, y + h);
        gr.addColorStop(0, "rgba(0,0,0,0.12)"); gr.addColorStop(1, "rgba(0,0,0,0)");
        q.fillStyle = gr; q.fillRect(x, y, 2 + Math.random() * 3, h);
      }
      var t = new THREE.CanvasTexture(cv);
      t.wrapS = THREE.RepeatWrapping; t.wrapT = THREE.RepeatWrapping;
      t.anisotropy = 4;
      return t;
    }
    var hullMat = new THREE.MeshStandardMaterial({ map: paint("#4e5844"), metalness: 0.25, roughness: 0.6, side: THREE.DoubleSide });
    var plainMat = new THREE.MeshStandardMaterial({ color: 0x47503e, metalness: 0.25, roughness: 0.6 });
    var rubberMat = new THREE.MeshStandardMaterial({ color: 0x17191a, metalness: 0.05, roughness: 0.92 });
    var steelMat = new THREE.MeshStandardMaterial({ color: 0x43474a, metalness: 0.85, roughness: 0.3 });
    var glassMat = new THREE.MeshStandardMaterial({ color: 0x131b21, metalness: 0.4, roughness: 0.16 });
    var teamMat = new THREE.MeshStandardMaterial({ color: C.team, metalness: 0.3, roughness: 0.5 });
    function B(a, b, c, m) { return new THREE.Mesh(new THREE.BoxGeometry(a, b, c), m); }
    function P(m, x, y, z, pr) { m.position.set(x, y, z); (pr || g).add(m); return m; }
    function cylX(r, l, m, s) { var o = new THREE.Mesh(new THREE.CylinderGeometry(r, r, l, s || 12), m); o.rotation.z = -Math.PI / 2; return o; }
    function cylZ(rt, rb, h, m, s) { var o = new THREE.Mesh(new THREE.CylinderGeometry(rt, rb, h, s || 14), m); o.rotation.x = Math.PI / 2; return o; }

    // chassis pan
    P(B(4.0, 1.8, 0.14, plainMat), 0, 0, 0.56);
    // bonnet and angled nose plate
    P(B(1.4, 2.3, 0.5, hullMat), 1.62, 0, 0.9);
    var np = P(B(0.05, 2.3, 0.56, hullMat), 2.4, 0, 0.9); np.rotation.y = -0.38;
    P(B(0.06, 1.5, 0.26, rubberMat), 2.3, 0, 0.72);         // grille
    P(B(0.2, 2.4, 0.24, plainMat), 2.55, 0, 0.6);           // bumper
    // brush guard
    P(B(0.04, 2.0, 0.05, steelMat), 2.62, 0, 1.1);
    [-0.85, 0, 0.85].forEach(function (yy) { P(B(0.04, 0.05, 0.5, steelMat), 2.62, yy, 0.88); });
    // armored crew cab (4-door)
    P(B(2.5, 2.36, 0.85, hullMat), -0.37, 0, 1.05);
    var ws = P(B(0.05, 2.0, 0.85, glassMat), 0.68, 0, 1.57); ws.rotation.y = -0.49;
    P(B(2.1, 2.36, 0.12, hullMat), -0.55, 0, 2.0);          // roof
    P(B(1.8, 0.05, 0.42, glassMat), -0.5, 1.16, 1.7);       // side glass strips
    P(B(1.8, 0.05, 0.42, glassMat), -0.5, -1.16, 1.7);
    [[0.45, 1.14], [0.45, -1.14], [-1.55, 1.14], [-1.55, -1.14]].forEach(function (p) {
      P(B(0.1, 0.1, 0.55, plainMat), p[0], p[1], 1.7);      // pillars
    });
    // rear cargo box with stowage
    P(B(0.95, 2.36, 0.8, hullMat), -2.15, 0, 1.02);
    P(B(0.75, 1.8, 0.3, plainMat), -2.15, 0, 1.57);
    // wheels
    [[1.7, 1.0], [1.7, -1.0], [-1.7, 1.0], [-1.7, -1.0]].forEach(function (p) {
      P(new THREE.Mesh(new THREE.CylinderGeometry(0.53, 0.53, 0.36, 18), rubberMat), p[0], p[1], 0.53);
      P(new THREE.Mesh(new THREE.CylinderGeometry(0.22, 0.22, 0.42, 12), plainMat), p[0], p[1], 0.53);
      P(B(1.15, 0.14, 0.06, hullMat), p[0], p[1] > 0 ? 1.2 : -1.2, 1.16);
    });
    // running boards
    P(B(2.3, 0.16, 0.05, plainMat), -0.35, 1.22, 0.55);
    P(B(2.3, 0.16, 0.05, plainMat), -0.35, -1.22, 0.55);
    // roof rack rails
    P(B(2.0, 0.05, 0.07, steelMat), -0.55, 1.05, 2.12);
    P(B(2.0, 0.05, 0.07, steelMat), -0.55, -1.05, 2.12);
    // headlights, mirrors, antennas
    P(cylX(0.08, 0.05, glassMat, 10), 2.44, 0.9, 1.12);
    P(cylX(0.08, 0.05, glassMat, 10), 2.44, -0.9, 1.12);
    [1, -1].forEach(function (s) {
      P(B(0.04, 0.05, 0.35, steelMat), 0.7, s * 1.24, 1.6);
      P(B(0.12, 0.03, 0.2, glassMat), 0.7, s * 1.27, 1.78);
    });
    P(cylZ(0.012, 0.012, 1.4, steelMat, 6), -1.5, 0.95, 2.76);
    P(cylZ(0.012, 0.012, 1.1, steelMat, 6), -1.5, -0.95, 2.61);
    // team identification plates on the front doors
    P(B(0.45, 0.03, 0.3, teamMat), 0.1, 1.19, 1.15);
    P(B(0.45, 0.03, 0.3, teamMat), 0.1, -1.19, 1.15);
    // remote weapon station
    var T = new THREE.Group(); T.name = "turret"; T.position.set(-0.55, 0, 2.06); g.add(T);
    P(cylZ(0.22, 0.24, 0.12, hullMat, 16), 0, 0, 0.06, T);  // slewing ring
    P(B(0.26, 0.26, 0.26, plainMat), 0, 0, 0.25, T);        // pedestal
    P(B(0.55, 0.3, 0.28, plainMat), 0.1, 0, 0.46, T);       // weapon cradle
    P(cylX(0.03, 1.25, steelMat, 10), 1.0, 0, 0.48, T);     // 12.7mm barrel
    P(cylX(0.045, 0.09, steelMat, 8), 1.66, 0, 0.48, T);    // muzzle
    P(B(0.3, 0.18, 0.2, plainMat), -0.05, -0.24, 0.46, T);  // ammo box
    P(B(0.18, 0.24, 0.3, plainMat), 0.05, 0.3, 0.42, T);    // sensor head
    P(B(0.02, 0.16, 0.12, glassMat), 0.15, 0.3, 0.44, T);   // sensor window
    return g;
  }
};

// ---------------------------------------------------------------------------
// M2A3 Bradley IFV. 6.55 x 3.6 x 2.98 m. Tall boxy hull, sloped upper sides,
// side skirts, 6 road wheels, 25mm turret offset LEFT with TOW box on its
// left cheek, commander's independent viewer.
// ---------------------------------------------------------------------------
UNIT_MODELS["ifv_n"] = {
  len: 6.55,
  build: function (THREE, M, C) {
    var g = new THREE.Group();
    function paint(base) {
      var cv = document.createElement("canvas");
      cv.width = 256; cv.height = 256;
      var q = cv.getContext("2d");
      q.fillStyle = base; q.fillRect(0, 0, 256, 256);
      var i, x, y, h, gr;
      for (i = 0; i < 26; i++) {
        q.fillStyle = (i % 2) ? "rgba(20,24,14,0.08)" : "rgba(235,238,218,0.05)";
        q.fillRect(Math.random() * 256, Math.random() * 256, 24 + Math.random() * 60, 16 + Math.random() * 44);
      }
      q.strokeStyle = "rgba(10,12,8,0.35)"; q.lineWidth = 1;
      for (i = 0; i < 7; i++) {
        x = 12 + Math.random() * 232;
        q.beginPath(); q.moveTo(x, 0); q.lineTo(x, 256); q.stroke();
        y = 12 + Math.random() * 232;
        q.beginPath(); q.moveTo(0, y); q.lineTo(256, y); q.stroke();
      }
      q.fillStyle = "rgba(15,15,10,0.45)";
      for (i = 0; i < 40; i++) { q.fillRect(Math.random() * 256, Math.random() * 256, 2, 2); }
      for (i = 0; i < 12; i++) {
        x = Math.random() * 256; y = Math.random() * 180; h = 40 + Math.random() * 70;
        gr = q.createLinearGradient(0, y, 0, y + h);
        gr.addColorStop(0, "rgba(0,0,0,0.13)"); gr.addColorStop(1, "rgba(0,0,0,0)");
        q.fillStyle = gr; q.fillRect(x, y, 2 + Math.random() * 3, h);
      }
      var t = new THREE.CanvasTexture(cv);
      t.wrapS = THREE.RepeatWrapping; t.wrapT = THREE.RepeatWrapping;
      t.anisotropy = 4;
      return t;
    }
    var hullMat = new THREE.MeshStandardMaterial({ map: paint("#4a5240"), metalness: 0.25, roughness: 0.62, side: THREE.DoubleSide });
    var plainMat = new THREE.MeshStandardMaterial({ color: 0x434a39, metalness: 0.25, roughness: 0.62 });
    var trackMat = new THREE.MeshStandardMaterial({ color: 0x1e201d, metalness: 0.3, roughness: 0.88 });
    var steelMat = new THREE.MeshStandardMaterial({ color: 0x43474a, metalness: 0.85, roughness: 0.3 });
    var glassMat = new THREE.MeshStandardMaterial({ color: 0x131b21, metalness: 0.4, roughness: 0.16 });
    var teamMat = new THREE.MeshStandardMaterial({ color: C.team, metalness: 0.3, roughness: 0.5 });
    function B(a, b, c, m) { return new THREE.Mesh(new THREE.BoxGeometry(a, b, c), m); }
    function P(m, x, y, z, pr) { m.position.set(x, y, z); (pr || g).add(m); return m; }
    function cylX(r, l, m, s) { var o = new THREE.Mesh(new THREE.CylinderGeometry(r, r, l, s || 12), m); o.rotation.z = -Math.PI / 2; return o; }
    function cylZ(rt, rb, h, m, s) { var o = new THREE.Mesh(new THREE.CylinderGeometry(rt, rb, h, s || 14), m); o.rotation.x = Math.PI / 2; return o; }

    // central hull: tall side profile with the long Bradley glacis
    g.add(new THREE.Mesh(M.slab(THREE, [
      [2.9, 0.45], [3.27, 0.95], [1.5, 2.2], [-3.2, 2.2], [-3.27, 0.5]
    ], 2.5, "xz"), hullMat));
    // sponsons over the tracks + sloped upper side plates
    [1, -1].forEach(function (s) {
      P(B(5.9, 0.55, 0.7, hullMat), -0.3, s * 1.52, 1.65);
      var sp = P(B(5.9, 0.6, 0.05, hullMat), -0.3, s * 1.51, 2.1);
      sp.rotation.x = -s * 0.35;
      // side skirts covering the upper track run
      P(B(5.8, 0.05, 0.75, hullMat), -0.25, s * 1.76, 0.97);
      // track band and running gear
      P(B(6.2, 0.5, 0.6, trackMat), -0.15, s * 1.48, 0.65);
      [2.3, 1.35, 0.4, -0.55, -1.5, -2.45].forEach(function (wx) {
        P(new THREE.Mesh(new THREE.CylinderGeometry(0.33, 0.33, 0.3, 16), trackMat), wx, s * 1.48, 0.33);
        P(new THREE.Mesh(new THREE.CylinderGeometry(0.15, 0.15, 0.34, 10), plainMat), wx, s * 1.48, 0.33);
      });
      P(new THREE.Mesh(new THREE.CylinderGeometry(0.29, 0.29, 0.24, 14), trackMat), 2.95, s * 1.48, 0.55); // drive sprocket
      P(new THREE.Mesh(new THREE.CylinderGeometry(0.27, 0.27, 0.24, 14), trackMat), -2.95, s * 1.48, 0.5); // idler
    });
    // stowed trim vane plate proud of the glacis
    var vane = P(B(1.3, 2.3, 0.05, plainMat), 2.42, 0, 1.66); vane.rotation.y = 0.615;
    // headlight clusters and tow shackles
    [0.85, -0.85].forEach(function (yy) {
      P(B(0.14, 0.3, 0.16, plainMat), 3.02, yy, 1.32);
      P(cylX(0.06, 0.05, glassMat, 10), 3.11, yy, 1.32);
      P(B(0.12, 0.14, 0.1, steelMat), 3.05, yy * 0.6, 0.75);
    });
    // raised engine deck panel front right
    P(B(1.3, 1.0, 0.08, hullMat), 1.9, -0.6, 2.24);
    // rear ramp frame lip
    P(B(0.06, 2.3, 1.5, hullMat), -3.29, 0, 1.35);
    // team identification plates on the skirts
    P(B(0.5, 0.04, 0.35, teamMat), 1.8, 1.79, 1.0);
    P(B(0.5, 0.04, 0.35, teamMat), 1.8, -1.79, 1.0);

    // Headlight clusters and tow shackles: both sit right on the front plate
    // and are the first things you see in a photograph of the vehicle.
    var lampGlass = new THREE.MeshStandardMaterial({ color: 0xd8d2bc, roughness: 0.25,
      metalness: 0.1, emissive: 0x3a3524, emissiveIntensity: 0.35 });
    [1, -1].forEach(function (s2) {
      // rectangular housing with a brush guard, one each side of the glacis
      P(B(0.30, 0.62, 0.52, hullMat), 3.02, s2 * 1.30, 1.28);
      var hl = P(new THREE.Mesh(new THREE.CylinderGeometry(0.20, 0.20, 0.10, 14), lampGlass),
                 3.19, s2 * 1.44, 1.30);
      hl.rotation.z = -Math.PI / 2;
      var bl = P(new THREE.Mesh(new THREE.CylinderGeometry(0.13, 0.13, 0.10, 12), lampGlass),
                 3.19, s2 * 1.13, 1.30);
      bl.rotation.z = -Math.PI / 2;
      // guard bars across the front of the housing
      for (var gb = 0; gb < 2; gb++) {
        P(B(0.05, 0.60, 0.05, steelMat), 3.21, s2 * 1.30, 1.10 + gb * 0.36);
      }
      // towing shackle on the lower front plate
      var sh = P(new THREE.Mesh(new THREE.TorusGeometry(0.20, 0.055, 6, 12), steelMat),
                 3.06, s2 * 0.72, 0.52);
      sh.rotation.y = Math.PI / 2;
      P(B(0.16, 0.14, 0.16, steelMat), 2.96, s2 * 0.72, 0.52);
    });
    // Turret offset to the vehicle's RIGHT of centreline. The M2's driver sits
    // front left with the powerpack beside him, which pushes the turret ring
    // to starboard — clearly visible in any head-on photograph of a Bradley.
    // Model space is +Y left, so that is a negative offset.
    var T = new THREE.Group(); T.name = "turret"; T.position.set(0.3, -0.45, 2.2); g.add(T);
    P(cylZ(0.8, 0.82, 0.12, hullMat, 20), 0, 0, 0.05, T);   // race ring
    P(B(1.7, 1.5, 0.55, hullMat), 0, 0, 0.35, T);           // turret box
    var tf = P(B(0.05, 1.5, 0.65, hullMat), 0.78, 0, 0.36, T); tf.rotation.y = -0.59;
    P(B(0.35, 0.5, 0.4, plainMat), 0.95, 0.1, 0.45, T);     // mantlet
    P(cylX(0.038, 2.2, steelMat, 10), 2.15, 0.1, 0.5, T);   // 25mm M242
    P(cylX(0.055, 0.14, steelMat, 8), 3.28, 0.1, 0.5, T);   // muzzle
    P(cylX(0.02, 0.6, steelMat, 8), 1.35, -0.22, 0.45, T);  // M240 coax
    // TOW launcher box on the LEFT cheek
    var tow = new THREE.Group(); tow.position.set(0.0, 0.95, 0.55); tow.rotation.y = -0.12; T.add(tow);
    P(B(1.15, 0.42, 0.5, plainMat), 0, 0, 0, tow);
    P(cylX(0.13, 0.05, trackMat, 12), 0.58, 0.1, 0, tow);
    P(cylX(0.13, 0.05, trackMat, 12), 0.58, -0.1, 0, tow);
    // commander cupola + independent viewer + gunner sight
    P(cylZ(0.28, 0.3, 0.14, hullMat, 16), -0.35, -0.35, 0.66, T);
    P(cylZ(0.1, 0.1, 0.22, plainMat, 10), -0.5, 0.35, 0.72, T);
    P(B(0.24, 0.22, 0.2, plainMat), -0.5, 0.35, 0.9, T);
    P(B(0.02, 0.14, 0.1, glassMat), -0.37, 0.35, 0.9, T);
    P(B(0.35, 0.4, 0.2, plainMat), 0.4, 0.38, 0.68, T);
    P(B(0.02, 0.26, 0.12, glassMat), 0.59, 0.38, 0.7, T);
    // smoke launchers on the turret front corners
    [1, -1].forEach(function (s) {
      for (var i = 0; i < 3; i++) {
        var sm = new THREE.Mesh(new THREE.CylinderGeometry(0.03, 0.03, 0.28, 8), steelMat);
        sm.rotation.z = -Math.PI / 2; sm.rotation.y = -0.6;
        P(sm, 0.72, s * (0.45 + i * 0.11), 0.62, T);
      }
    });
    // bustle rack rails + antennas
    P(B(0.05, 1.3, 0.05, steelMat), -0.95, 0, 0.62, T);
    P(B(0.6, 0.05, 0.05, steelMat), -0.62, 0.65, 0.62, T);
    P(B(0.6, 0.05, 0.05, steelMat), -0.62, -0.65, 0.62, T);
    P(cylZ(0.012, 0.012, 1.3, steelMat, 6), -0.85, 0.55, 1.3, T);
    P(cylZ(0.012, 0.012, 1.1, steelMat, 6), -0.85, -0.55, 1.2, T);
    return g;
  }
};

// ---------------------------------------------------------------------------
// BMP-3 IFV. 7.14 x 3.2 x ~2.3 m. Low hull with sharply pointed bow, round
// centre turret, stubby 100mm 2A70 with thin 30mm 2A72 coax, 6 road wheels.
// ---------------------------------------------------------------------------
UNIT_MODELS["ifv_p"] = {
  len: 7.14,
  build: function (THREE, M, C) {
    var g = new THREE.Group();
    function paint(base) {
      var cv = document.createElement("canvas");
      cv.width = 256; cv.height = 256;
      var q = cv.getContext("2d");
      q.fillStyle = base; q.fillRect(0, 0, 256, 256);
      var i, x, y, h, gr;
      for (i = 0; i < 26; i++) {
        q.fillStyle = (i % 2) ? "rgba(14,20,10,0.08)" : "rgba(228,234,212,0.05)";
        q.fillRect(Math.random() * 256, Math.random() * 256, 24 + Math.random() * 60, 16 + Math.random() * 44);
      }
      q.strokeStyle = "rgba(8,12,6,0.35)"; q.lineWidth = 1;
      for (i = 0; i < 6; i++) {
        x = 12 + Math.random() * 232;
        q.beginPath(); q.moveTo(x, 0); q.lineTo(x, 256); q.stroke();
        y = 12 + Math.random() * 232;
        q.beginPath(); q.moveTo(0, y); q.lineTo(256, y); q.stroke();
      }
      q.fillStyle = "rgba(12,14,8,0.45)";
      for (i = 0; i < 40; i++) { q.fillRect(Math.random() * 256, Math.random() * 256, 2, 2); }
      for (i = 0; i < 12; i++) {
        x = Math.random() * 256; y = Math.random() * 180; h = 40 + Math.random() * 70;
        gr = q.createLinearGradient(0, y, 0, y + h);
        gr.addColorStop(0, "rgba(0,0,0,0.13)"); gr.addColorStop(1, "rgba(0,0,0,0)");
        q.fillStyle = gr; q.fillRect(x, y, 2 + Math.random() * 3, h);
      }
      var t = new THREE.CanvasTexture(cv);
      t.wrapS = THREE.RepeatWrapping; t.wrapT = THREE.RepeatWrapping;
      t.anisotropy = 4;
      return t;
    }
    var hullMat = new THREE.MeshStandardMaterial({ map: paint("#3e4a35"), metalness: 0.25, roughness: 0.62, side: THREE.DoubleSide });
    var plainMat = new THREE.MeshStandardMaterial({ color: 0x39432f, metalness: 0.25, roughness: 0.62 });
    var trackMat = new THREE.MeshStandardMaterial({ color: 0x1e201d, metalness: 0.3, roughness: 0.88 });
    var steelMat = new THREE.MeshStandardMaterial({ color: 0x43474a, metalness: 0.85, roughness: 0.3 });
    var glassMat = new THREE.MeshStandardMaterial({ color: 0x131b21, metalness: 0.4, roughness: 0.16 });
    var teamMat = new THREE.MeshStandardMaterial({ color: C.team, metalness: 0.3, roughness: 0.5 });
    function B(a, b, c, m) { return new THREE.Mesh(new THREE.BoxGeometry(a, b, c), m); }
    function P(m, x, y, z, pr) { m.position.set(x, y, z); (pr || g).add(m); return m; }
    function cylX(r, l, m, s) { var o = new THREE.Mesh(new THREE.CylinderGeometry(r, r, l, s || 12), m); o.rotation.z = -Math.PI / 2; return o; }
    function cylZ(rt, rb, h, m, s) { var o = new THREE.Mesh(new THREE.CylinderGeometry(rt, rb, h, s || 14), m); o.rotation.x = Math.PI / 2; return o; }

    // low hull with sharply pointed bow
    g.add(new THREE.Mesh(M.loft(THREE, [
      { x: 3.57, w: 0.5, h: 0.1, zc: 1.1 },
      { x: 3.0, w: 1.1, h: 0.22, zc: 1.05 },
      { x: 2.2, w: 1.5, h: 0.38, zc: 1.0, sq: 0.85 },
      { x: 1.0, w: 1.58, h: 0.45, zc: 1.0, sq: 0.9 },
      { x: -2.2, w: 1.58, h: 0.45, zc: 1.0, sq: 0.9 },
      { x: -3.3, w: 1.45, h: 0.42, zc: 1.0, sq: 0.85 },
      { x: -3.57, w: 1.2, h: 0.35, zc: 1.0 }
    ], 16), hullMat));
    // lower hull tub between the tracks
    P(B(6.6, 2.0, 0.5, plainMat), -0.1, 0, 0.6);
    // running gear
    [1, -1].forEach(function (s) {
      P(B(6.4, 0.5, 0.34, trackMat), 0, s * 1.33, 0.46);
      [2.45, 1.5, 0.55, -0.4, -1.35, -2.3].forEach(function (wx) {
        P(new THREE.Mesh(new THREE.CylinderGeometry(0.32, 0.32, 0.3, 16), trackMat), wx, s * 1.33, 0.32);
        P(new THREE.Mesh(new THREE.CylinderGeometry(0.16, 0.16, 0.34, 10), plainMat), wx, s * 1.33, 0.32);
      });
      P(new THREE.Mesh(new THREE.CylinderGeometry(0.26, 0.26, 0.24, 14), trackMat), -2.95, s * 1.33, 0.45); // rear drive
      P(new THREE.Mesh(new THREE.CylinderGeometry(0.24, 0.24, 0.24, 14), trackMat), 2.95, s * 1.33, 0.45);  // idler
      // flat fender strip over the track
      P(B(6.9, 0.46, 0.05, hullMat), -0.1, s * 1.33, 0.68);
      // glacis rib strips
      var rib = P(B(1.6, 0.06, 0.05, plainMat), 2.3, s * 0.45, 1.32); rib.rotation.y = 0.12;
    });
    // driver + bow gunner hatches
    P(cylZ(0.18, 0.18, 0.06, plainMat, 12), 1.9, 0, 1.47);
    P(cylZ(0.16, 0.16, 0.06, plainMat, 12), 1.75, 0.75, 1.44);
    P(cylZ(0.16, 0.16, 0.06, plainMat, 12), 1.75, -0.75, 1.44);
    // rear water-jet housings and roof stowage
    P(B(0.5, 0.55, 0.5, hullMat), -3.4, 0.8, 0.95);
    P(B(0.5, 0.55, 0.5, hullMat), -3.4, -0.8, 0.95);
    P(B(1.4, 1.2, 0.15, plainMat), -2.6, 0, 1.5);
    // headlights on the bow
    P(cylX(0.06, 0.05, glassMat, 10), 3.15, 0.6, 1.28);
    P(cylX(0.06, 0.05, glassMat, 10), 3.15, -0.6, 1.28);
    // whip antenna
    P(cylZ(0.012, 0.012, 1.5, steelMat, 6), -2.7, 0.9, 2.2);
    // team identification plates on the hull flanks
    P(B(0.5, 0.04, 0.3, teamMat), 1.5, 1.56, 1.05);
    P(B(0.5, 0.04, 0.3, teamMat), 1.5, -1.56, 1.05);
    // round centre turret: 100mm 2A70 + 30mm 2A72 coax
    var T = new THREE.Group(); T.name = "turret"; T.position.set(0.15, 0, 1.42); g.add(T);
    P(cylZ(1.0, 1.05, 0.16, hullMat, 20), 0, 0, 0.08, T);   // race ring
    var dome = new THREE.Mesh(new THREE.SphereGeometry(1.0, 20, 12), hullMat);
    dome.scale.set(1, 1, 0.45);
    P(dome, -0.05, 0, 0.16, T);
    P(B(0.4, 0.55, 0.35, plainMat), 0.8, 0, 0.28, T);       // mantlet
    /* the 2A70's muzzle overhangs the bow in every photograph of a BMP-3;
           it used to stop half a metre short and never broke the outline */
        P(cylX(0.09, 2.90, steelMat, 12), 2.45, 0, 0.32, T);
        P(cylX(0.10, 0.14, steelMat, 10), 3.95, 0, 0.32, T);
        P(cylX(0.030, 2.60, steelMat, 8), 2.60, -0.24, 0.30, T);   // stubby 100mm
    P(cylX(0.085, 0.12, steelMat, 10), 2.95, 0, 0.32, T);   // muzzle collar
    P(cylX(0.028, 2.35, steelMat, 8), 2.18, -0.24, 0.3, T); // thin 30mm coax
    P(cylX(0.018, 0.7, steelMat, 8), 1.3, 0.3, 0.3, T);     // PKT
    // sights and commander hatch dome
    P(B(0.3, 0.28, 0.2, plainMat), 0.32, 0.38, 0.52, T);
    P(B(0.02, 0.18, 0.1, glassMat), 0.48, 0.38, 0.54, T);
    var hd = new THREE.Mesh(new THREE.SphereGeometry(0.22, 12, 8), hullMat);
    hd.scale.set(1, 1, 0.5);
    P(hd, -0.45, -0.45, 0.4, T);
    // smoke launchers on the rear quarters of the dome
    [1, -1].forEach(function (s) {
      for (var i = 0; i < 3; i++) {
        var sm = new THREE.Mesh(new THREE.CylinderGeometry(0.03, 0.03, 0.28, 8), steelMat);
        sm.rotation.z = -Math.PI / 2; sm.rotation.y = -0.6;
        P(sm, -0.55, s * (0.5 + i * 0.11), 0.42, T);
      }
    });
    return g;
  }
};

// ---------------------------------------------------------------------------
// ZBD-04A IFV. 7.5 x 3.3 x ~2.5 m. Boxier low hull with applique armour on
// the glacis and flanks, round twin-gun turret (100mm + 30mm), 6 road wheels.
// ---------------------------------------------------------------------------
UNIT_MODELS["ifv_c"] = {
  len: 7.5,
  build: function (THREE, M, C) {
    var g = new THREE.Group();
    function paint(base) {
      var cv = document.createElement("canvas");
      cv.width = 256; cv.height = 256;
      var q = cv.getContext("2d");
      q.fillStyle = base; q.fillRect(0, 0, 256, 256);
      var i, x, y, h, gr;
      for (i = 0; i < 26; i++) {
        q.fillStyle = (i % 2) ? "rgba(18,24,16,0.08)" : "rgba(232,238,222,0.05)";
        q.fillRect(Math.random() * 256, Math.random() * 256, 24 + Math.random() * 60, 16 + Math.random() * 44);
      }
      q.strokeStyle = "rgba(10,14,8,0.35)"; q.lineWidth = 1;
      for (i = 0; i < 7; i++) {
        x = 12 + Math.random() * 232;
        q.beginPath(); q.moveTo(x, 0); q.lineTo(x, 256); q.stroke();
        y = 12 + Math.random() * 232;
        q.beginPath(); q.moveTo(0, y); q.lineTo(256, y); q.stroke();
      }
      q.fillStyle = "rgba(12,14,10,0.45)";
      for (i = 0; i < 40; i++) { q.fillRect(Math.random() * 256, Math.random() * 256, 2, 2); }
      for (i = 0; i < 12; i++) {
        x = Math.random() * 256; y = Math.random() * 180; h = 40 + Math.random() * 70;
        gr = q.createLinearGradient(0, y, 0, y + h);
        gr.addColorStop(0, "rgba(0,0,0,0.13)"); gr.addColorStop(1, "rgba(0,0,0,0)");
        q.fillStyle = gr; q.fillRect(x, y, 2 + Math.random() * 3, h);
      }
      var t = new THREE.CanvasTexture(cv);
      t.wrapS = THREE.RepeatWrapping; t.wrapT = THREE.RepeatWrapping;
      t.anisotropy = 4;
      return t;
    }
    var hullMat = new THREE.MeshStandardMaterial({ map: paint("#4e5844"), metalness: 0.25, roughness: 0.62, side: THREE.DoubleSide });
    var plainMat = new THREE.MeshStandardMaterial({ color: 0x47503e, metalness: 0.25, roughness: 0.62 });
    var trackMat = new THREE.MeshStandardMaterial({ color: 0x1e201d, metalness: 0.3, roughness: 0.88 });
    var steelMat = new THREE.MeshStandardMaterial({ color: 0x43474a, metalness: 0.85, roughness: 0.3 });
    var glassMat = new THREE.MeshStandardMaterial({ color: 0x131b21, metalness: 0.4, roughness: 0.16 });
    var teamMat = new THREE.MeshStandardMaterial({ color: C.team, metalness: 0.3, roughness: 0.5 });
    function B(a, b, c, m) { return new THREE.Mesh(new THREE.BoxGeometry(a, b, c), m); }
    function P(m, x, y, z, pr) { m.position.set(x, y, z); (pr || g).add(m); return m; }
    function cylX(r, l, m, s) { var o = new THREE.Mesh(new THREE.CylinderGeometry(r, r, l, s || 12), m); o.rotation.z = -Math.PI / 2; return o; }
    function cylZ(rt, rb, h, m, s) { var o = new THREE.Mesh(new THREE.CylinderGeometry(rt, rb, h, s || 14), m); o.rotation.x = Math.PI / 2; return o; }

    // boxy central hull, side profile slab
    g.add(new THREE.Mesh(M.slab(THREE, [
      [3.2, 0.4], [3.75, 0.85], [1.7, 1.6], [-3.55, 1.6], [-3.75, 1.3], [-3.7, 0.45]
    ], 2.3, "xz"), hullMat));
    // sponsons over the tracks
    P(B(6.1, 0.5, 0.65, hullMat), -0.65, 1.4, 1.28);
    P(B(6.1, 0.5, 0.65, hullMat), -0.65, -1.4, 1.28);
    // glacis applique with a 2x3 grid of armour blocks
    var ap = new THREE.Group(); ap.position.set(2.7, 0, 1.3); ap.rotation.y = 0.35; g.add(ap);
    P(B(2.0, 2.2, 0.06, hullMat), 0, 0, 0, ap);
    for (var r = 0; r < 2; r++) {
      for (var c2 = 0; c2 < 3; c2++) {
        P(B(0.6, 0.55, 0.07, plainMat), -0.45 + r * 0.9, -0.7 + c2 * 0.7, 0.05, ap);
      }
    }
    // flank applique panels on the crew section
    P(B(2.2, 0.06, 0.5, plainMat), 1.1, 1.68, 1.2);
    P(B(2.2, 0.06, 0.5, plainMat), 1.1, -1.68, 1.2);
    // lower hull tub + running gear
    P(B(6.8, 2.2, 0.5, plainMat), -0.2, 0, 0.62);
    [1, -1].forEach(function (s) {
      P(B(6.5, 0.5, 0.35, trackMat), -0.25, s * 1.4, 0.48);
      [2.55, 1.55, 0.55, -0.55, -1.55, -2.55].forEach(function (wx) {
        P(new THREE.Mesh(new THREE.CylinderGeometry(0.34, 0.34, 0.3, 16), trackMat), wx, s * 1.4, 0.34);
        P(new THREE.Mesh(new THREE.CylinderGeometry(0.17, 0.17, 0.34, 10), plainMat), wx, s * 1.4, 0.34);
      });
      P(new THREE.Mesh(new THREE.CylinderGeometry(0.27, 0.27, 0.24, 14), trackMat), -3.2, s * 1.4, 0.48); // rear drive
      P(new THREE.Mesh(new THREE.CylinderGeometry(0.25, 0.25, 0.24, 14), trackMat), 3.1, s * 1.4, 0.48);  // idler
      P(B(6.9, 0.46, 0.05, hullMat), -0.3, s * 1.4, 0.68);  // fender strip
    });
    // roof hatches, headlights, antenna
    P(cylZ(0.18, 0.18, 0.06, plainMat, 12), 2.3, 0.55, 1.62);
    P(cylZ(0.2, 0.2, 0.06, plainMat, 12), -2.6, 0.6, 1.62);
    P(cylZ(0.2, 0.2, 0.06, plainMat, 12), -2.6, -0.6, 1.62);
    P(cylX(0.06, 0.05, glassMat, 10), 3.6, 0.7, 1.05);
    P(cylX(0.06, 0.05, glassMat, 10), 3.6, -0.7, 1.05);
    P(cylZ(0.012, 0.012, 1.5, steelMat, 6), -3.0, 0.9, 2.35);
    // team identification plates
    P(B(0.5, 0.04, 0.3, teamMat), -0.6, 1.66, 1.3);
    P(B(0.5, 0.04, 0.3, teamMat), -0.6, -1.66, 1.3);
    // round twin-gun turret
    var T = new THREE.Group(); T.name = "turret"; T.position.set(0.4, 0, 1.6); g.add(T);
    P(cylZ(0.95, 1.08, 0.5, hullMat, 20), 0, 0, 0.25, T);   // tapered drum
    P(cylZ(0.9, 0.95, 0.08, hullMat, 20), 0, 0, 0.54, T);   // roof cap
    P(B(0.5, 0.6, 0.4, plainMat), 0.9, 0, 0.3, T);          // mantlet wedge
    P(cylX(0.075, 2.05, steelMat, 12), 2.15, 0, 0.34, T);   // 100mm
    P(cylX(0.085, 0.12, steelMat, 10), 3.2, 0, 0.34, T);    // muzzle collar
    P(cylX(0.028, 2.55, steelMat, 8), 2.4, -0.24, 0.3, T);  // 30mm coax
    // tall gunner sight, commander hatch, smoke tubes
    P(B(0.3, 0.3, 0.35, plainMat), -0.15, 0.38, 0.75, T);
    P(B(0.02, 0.2, 0.14, glassMat), 0.01, 0.38, 0.8, T);
    P(cylZ(0.26, 0.28, 0.1, hullMat, 14), -0.4, -0.42, 0.62, T);
    [1, -1].forEach(function (s) {
      for (var i = 0; i < 3; i++) {
        var sm = new THREE.Mesh(new THREE.CylinderGeometry(0.03, 0.03, 0.28, 8), steelMat);
        sm.rotation.z = -Math.PI / 2; sm.rotation.y = -0.6;
        P(sm, -0.5, s * (0.5 + i * 0.11), 0.5, T);
      }
    });
    return g;
  }
};

// ---------------------------------------------------------------------------
// M1128 Stryker MGS. Hull 6.95 x 2.72 m. 8 big wheels in two clearly
// separated pairs of axles, slab hull, low unmanned turret with a long
// overhanging 105mm and pepperpot muzzle brake.
// ---------------------------------------------------------------------------
UNIT_MODELS["lt_n"] = {
  len: 6.95,
  build: function (THREE, M, C) {
    var g = new THREE.Group();
    function paint(base) {
      var cv = document.createElement("canvas");
      cv.width = 256; cv.height = 256;
      var q = cv.getContext("2d");
      q.fillStyle = base; q.fillRect(0, 0, 256, 256);
      var i, x, y, h, gr;
      for (i = 0; i < 26; i++) {
        q.fillStyle = (i % 2) ? "rgba(20,24,14,0.08)" : "rgba(235,238,218,0.05)";
        q.fillRect(Math.random() * 256, Math.random() * 256, 24 + Math.random() * 60, 16 + Math.random() * 44);
      }
      q.strokeStyle = "rgba(10,12,8,0.35)"; q.lineWidth = 1;
      for (i = 0; i < 7; i++) {
        x = 12 + Math.random() * 232;
        q.beginPath(); q.moveTo(x, 0); q.lineTo(x, 256); q.stroke();
        y = 12 + Math.random() * 232;
        q.beginPath(); q.moveTo(0, y); q.lineTo(256, y); q.stroke();
      }
      q.fillStyle = "rgba(15,15,10,0.45)";
      for (i = 0; i < 40; i++) { q.fillRect(Math.random() * 256, Math.random() * 256, 2, 2); }
      for (i = 0; i < 12; i++) {
        x = Math.random() * 256; y = Math.random() * 180; h = 40 + Math.random() * 70;
        gr = q.createLinearGradient(0, y, 0, y + h);
        gr.addColorStop(0, "rgba(0,0,0,0.13)"); gr.addColorStop(1, "rgba(0,0,0,0)");
        q.fillStyle = gr; q.fillRect(x, y, 2 + Math.random() * 3, h);
      }
      var t = new THREE.CanvasTexture(cv);
      t.wrapS = THREE.RepeatWrapping; t.wrapT = THREE.RepeatWrapping;
      t.anisotropy = 4;
      return t;
    }
    var hullMat = new THREE.MeshStandardMaterial({ map: paint("#4a5240"), metalness: 0.25, roughness: 0.62, side: THREE.DoubleSide });
    var plainMat = new THREE.MeshStandardMaterial({ color: 0x434a39, metalness: 0.25, roughness: 0.62 });
    var rubberMat = new THREE.MeshStandardMaterial({ color: 0x17191a, metalness: 0.05, roughness: 0.92 });
    var steelMat = new THREE.MeshStandardMaterial({ color: 0x43474a, metalness: 0.85, roughness: 0.3 });
    var glassMat = new THREE.MeshStandardMaterial({ color: 0x131b21, metalness: 0.4, roughness: 0.16 });
    var teamMat = new THREE.MeshStandardMaterial({ color: C.team, metalness: 0.3, roughness: 0.5 });
    function B(a, b, c, m) { return new THREE.Mesh(new THREE.BoxGeometry(a, b, c), m); }
    function P(m, x, y, z, pr) { m.position.set(x, y, z); (pr || g).add(m); return m; }
    function cylX(r, l, m, s) { var o = new THREE.Mesh(new THREE.CylinderGeometry(r, r, l, s || 12), m); o.rotation.z = -Math.PI / 2; return o; }
    function cylZ(rt, rb, h, m, s) { var o = new THREE.Mesh(new THREE.CylinderGeometry(rt, rb, h, s || 14), m); o.rotation.x = Math.PI / 2; return o; }

    // slab hull with big sloped glacis
    g.add(new THREE.Mesh(M.loft(THREE, [
      { x: 3.47, w: 0.55, h: 0.22, zc: 1.05 },
      { x: 2.85, w: 1.05, h: 0.5, zc: 1.2, sq: 0.8 },
      { x: 1.7, w: 1.32, h: 0.73, zc: 1.3, sq: 0.85 },
      { x: -2.7, w: 1.32, h: 0.73, zc: 1.3, sq: 0.85 },
      { x: -3.25, w: 1.18, h: 0.65, zc: 1.25, sq: 0.8 },
      { x: -3.47, w: 0.85, h: 0.45, zc: 1.15 }
    ], 16), hullMat));
    // 8 big wheels: two clearly separated axle pairs
    [2.35, 1.15, -0.95, -2.15].forEach(function (wx) {
      [1, -1].forEach(function (s) {
        P(new THREE.Mesh(new THREE.CylinderGeometry(0.52, 0.52, 0.36, 18), rubberMat), wx, s * 1.16, 0.52);
        P(new THREE.Mesh(new THREE.CylinderGeometry(0.21, 0.21, 0.42, 12), plainMat), wx, s * 1.16, 0.52);
      });
    });
    // driver hatch, engine deck lip, mirrors
    P(cylZ(0.24, 0.24, 0.06, plainMat, 14), 2.3, 0.6, 2.02);
    P(B(1.2, 1.1, 0.06, hullMat), 2.1, -0.55, 2.04);
    [1, -1].forEach(function (s) {
      P(B(0.04, 0.05, 0.35, steelMat), 2.85, s * 1.3, 1.95);
      P(B(0.12, 0.03, 0.18, glassMat), 2.85, s * 1.33, 2.12);
    });
    // headlight boxes and tow points on the bow
    [0.75, -0.75].forEach(function (yy) {
      P(B(0.12, 0.28, 0.14, plainMat), 3.35, yy, 1.35);
      P(cylX(0.055, 0.04, glassMat, 10), 3.43, yy, 1.35);
    });
    // flank stowage baskets on the rear half
    P(B(2.0, 0.1, 0.4, plainMat), -1.8, 1.36, 1.75);
    P(B(2.0, 0.1, 0.4, plainMat), -1.8, -1.36, 1.75);
    P(B(0.9, 0.1, 0.35, plainMat), -3.0, 1.3, 1.7);
    P(B(0.9, 0.1, 0.35, plainMat), -3.0, -1.3, 1.7);
    // antennas
    P(cylZ(0.012, 0.012, 1.5, steelMat, 6), -3.1, 0.8, 2.75);
    P(cylZ(0.012, 0.012, 1.2, steelMat, 6), -3.1, -0.8, 2.6);
    // team identification plates on the forward flanks
    P(B(0.5, 0.04, 0.35, teamMat), 1.6, 1.31, 1.5);
    P(B(0.5, 0.04, 0.35, teamMat), 1.6, -1.31, 1.5);
    // low unmanned MGS turret with the long overhanging 105mm
    var T = new THREE.Group(); T.name = "turret"; T.position.set(0.3, 0, 2.03); g.add(T);
    P(cylZ(0.75, 0.78, 0.1, hullMat, 20), 0, 0, 0.05, T);
    P(B(1.9, 1.5, 0.4, hullMat), -0.2, 0, 0.3, T);          // flat crown
    var tf = P(B(0.05, 1.5, 0.48, hullMat), 0.85, 0, 0.28, T); tf.rotation.y = -0.6;
    P(B(0.8, 1.2, 0.35, hullMat), -1.05, 0, 0.35, T);       // autoloader bustle
    P(B(0.4, 0.45, 0.32, plainMat), 0.9, 0, 0.44, T);       // mantlet
    P(cylX(0.058, 3.5, steelMat, 12), 2.85, 0, 0.48, T);    // 105mm M68A1 tube
    P(cylX(0.07, 0.9, steelMat, 12), 1.65, 0, 0.48, T);     // thermal sleeve
    P(cylX(0.085, 0.5, steelMat, 12), 2.6, 0, 0.48, T);     // bore evacuator
    P(cylX(0.09, 0.45, steelMat, 12), 4.75, 0, 0.48, T);    // pepperpot brake
    P(cylX(0.1, 0.05, rubberMat, 12), 4.62, 0, 0.48, T);    // brake ring
    P(cylX(0.1, 0.05, rubberMat, 12), 4.88, 0, 0.48, T);
    // sight head and smoke tubes
    P(B(0.3, 0.35, 0.22, plainMat), 0.2, 0.5, 0.56, T);
    P(B(0.02, 0.22, 0.12, glassMat), 0.36, 0.5, 0.58, T);
    [1, -1].forEach(function (s) {
      for (var i = 0; i < 3; i++) {
        var sm = new THREE.Mesh(new THREE.CylinderGeometry(0.03, 0.03, 0.28, 8), steelMat);
        sm.rotation.z = -Math.PI / 2; sm.rotation.y = -0.6;
        P(sm, 0.55, s * (0.45 + i * 0.11), 0.5, T);
      }
    });
    return g;
  }
};

// ---------------------------------------------------------------------------
// 2S25 Sprut-SD. 9.77 m with gun, hull 7.07 x 3.15, very low thin hull,
// 7 small road wheels, disproportionately long 125mm 2A75 on a small turret.
// ---------------------------------------------------------------------------
UNIT_MODELS["lt_p"] = {
  len: 9.77,
  build: function (THREE, M, C) {
    var g = new THREE.Group();
    function paint(base) {
      var cv = document.createElement("canvas");
      cv.width = 256; cv.height = 256;
      var q = cv.getContext("2d");
      q.fillStyle = base; q.fillRect(0, 0, 256, 256);
      var i, x, y, h, gr;
      for (i = 0; i < 26; i++) {
        q.fillStyle = (i % 2) ? "rgba(14,20,10,0.08)" : "rgba(228,234,212,0.05)";
        q.fillRect(Math.random() * 256, Math.random() * 256, 24 + Math.random() * 60, 16 + Math.random() * 44);
      }
      q.strokeStyle = "rgba(8,12,6,0.35)"; q.lineWidth = 1;
      for (i = 0; i < 6; i++) {
        x = 12 + Math.random() * 232;
        q.beginPath(); q.moveTo(x, 0); q.lineTo(x, 256); q.stroke();
        y = 12 + Math.random() * 232;
        q.beginPath(); q.moveTo(0, y); q.lineTo(256, y); q.stroke();
      }
      q.fillStyle = "rgba(12,14,8,0.45)";
      for (i = 0; i < 40; i++) { q.fillRect(Math.random() * 256, Math.random() * 256, 2, 2); }
      for (i = 0; i < 12; i++) {
        x = Math.random() * 256; y = Math.random() * 180; h = 40 + Math.random() * 70;
        gr = q.createLinearGradient(0, y, 0, y + h);
        gr.addColorStop(0, "rgba(0,0,0,0.13)"); gr.addColorStop(1, "rgba(0,0,0,0)");
        q.fillStyle = gr; q.fillRect(x, y, 2 + Math.random() * 3, h);
      }
      var t = new THREE.CanvasTexture(cv);
      t.wrapS = THREE.RepeatWrapping; t.wrapT = THREE.RepeatWrapping;
      t.anisotropy = 4;
      return t;
    }
    var hullMat = new THREE.MeshStandardMaterial({ map: paint("#3e4a35"), metalness: 0.25, roughness: 0.62, side: THREE.DoubleSide });
    var plainMat = new THREE.MeshStandardMaterial({ color: 0x39432f, metalness: 0.25, roughness: 0.62 });
    var trackMat = new THREE.MeshStandardMaterial({ color: 0x1e201d, metalness: 0.3, roughness: 0.88 });
    var steelMat = new THREE.MeshStandardMaterial({ color: 0x43474a, metalness: 0.85, roughness: 0.3 });
    var glassMat = new THREE.MeshStandardMaterial({ color: 0x131b21, metalness: 0.4, roughness: 0.16 });
    var teamMat = new THREE.MeshStandardMaterial({ color: C.team, metalness: 0.3, roughness: 0.5 });
    function B(a, b, c, m) { return new THREE.Mesh(new THREE.BoxGeometry(a, b, c), m); }
    function P(m, x, y, z, pr) { m.position.set(x, y, z); (pr || g).add(m); return m; }
    function cylX(r, l, m, s) { var o = new THREE.Mesh(new THREE.CylinderGeometry(r, r, l, s || 12), m); o.rotation.z = -Math.PI / 2; return o; }
    function cylZ(rt, rb, h, m, s) { var o = new THREE.Mesh(new THREE.CylinderGeometry(rt, rb, h, s || 14), m); o.rotation.x = Math.PI / 2; return o; }

    // very low thin hull, side profile slab
    g.add(new THREE.Mesh(M.slab(THREE, [
      [3.0, 0.35], [3.47, 0.62], [1.5, 1.28], [-3.35, 1.28], [-3.6, 0.95], [-3.4, 0.35]
    ], 2.1, "xz"), hullMat));
    // sponsons over the tracks
    P(B(6.6, 0.52, 0.5, hullMat), -0.2, 1.31, 1.03);
    P(B(6.6, 0.52, 0.5, hullMat), -0.2, -1.31, 1.03);
    // running gear: 7 small road wheels per side
    [1, -1].forEach(function (s) {
      P(B(6.4, 0.48, 0.36, trackMat), -0.1, s * 1.32, 0.42);
      [2.7, 1.77, 0.84, -0.09, -1.02, -1.95, -2.88].forEach(function (wx) {
        P(new THREE.Mesh(new THREE.CylinderGeometry(0.28, 0.28, 0.28, 14), trackMat), wx, s * 1.32, 0.28);
        P(new THREE.Mesh(new THREE.CylinderGeometry(0.14, 0.14, 0.32, 10), plainMat), wx, s * 1.32, 0.28);
      });
      P(new THREE.Mesh(new THREE.CylinderGeometry(0.24, 0.24, 0.22, 14), trackMat), -3.3, s * 1.32, 0.45); // rear drive
      P(new THREE.Mesh(new THREE.CylinderGeometry(0.22, 0.22, 0.22, 14), trackMat), 3.2, s * 1.32, 0.42);  // idler
      P(B(6.9, 0.46, 0.05, hullMat), -0.15, s * 1.32, 0.63); // fender strip
    });
    // splash board across the glacis, driver hatch and periscopes
    var sb = P(B(0.7, 1.9, 0.05, plainMat), 2.55, 0, 1.0); sb.rotation.y = 0.33;
    P(cylZ(0.2, 0.2, 0.05, plainMat, 12), 1.9, 0, 1.3);
    P(B(0.1, 0.3, 0.06, plainMat), 2.15, 0, 1.28);
    // headlights, antenna
    P(cylX(0.055, 0.05, glassMat, 10), 3.3, 0.65, 0.95);
    P(cylX(0.055, 0.05, glassMat, 10), 3.3, -0.65, 0.95);
    P(cylZ(0.012, 0.012, 1.4, steelMat, 6), -2.9, 0.85, 2.0);
    // team identification plates on the hull flanks
    P(B(0.5, 0.04, 0.26, teamMat), 1.4, 1.55, 1.0);
    P(B(0.5, 0.04, 0.26, teamMat), 1.4, -1.55, 1.0);
    // small turret with the very long 125mm 2A75 (no muzzle brake)
    var T = new THREE.Group(); T.name = "turret"; T.position.set(0, 0, 1.28); g.add(T);
    P(cylZ(0.85, 0.9, 0.22, hullMat, 20), 0, 0, 0.11, T);   // race ring
    var dome = new THREE.Mesh(new THREE.SphereGeometry(0.95, 20, 12), hullMat);
    dome.scale.set(1, 1, 0.55);
    P(dome, -0.1, 0, 0.2, T);
    P(B(0.6, 1.0, 0.32, hullMat), -0.95, 0, 0.28, T);       // small bustle
    P(B(0.4, 0.5, 0.4, plainMat), 0.8, 0, 0.3, T);          // mantlet
    P(cylX(0.062, 5.22, steelMat, 12), 3.56, 0, 0.34, T);   // 125mm tube
    P(cylX(0.074, 1.1, steelMat, 12), 1.75, 0, 0.34, T);    // thermal sleeve fwd
    P(cylX(0.074, 1.2, steelMat, 12), 3.7, 0, 0.34, T);     // thermal sleeve aft
    P(cylX(0.088, 0.55, steelMat, 12), 2.7, 0, 0.34, T);    // bore evacuator
    P(cylX(0.07, 0.1, steelMat, 10), 6.12, 0, 0.34, T);     // muzzle collar
    // commander cupola, gunner sight, smoke tubes
    P(cylZ(0.24, 0.26, 0.12, hullMat, 14), -0.4, -0.42, 0.52, T);
    P(B(0.28, 0.3, 0.2, plainMat), 0.25, 0.42, 0.52, T);
    P(B(0.02, 0.2, 0.12, glassMat), 0.4, 0.42, 0.54, T);
    [1, -1].forEach(function (s) {
      for (var i = 0; i < 3; i++) {
        var sm = new THREE.Mesh(new THREE.CylinderGeometry(0.03, 0.03, 0.28, 8), steelMat);
        sm.rotation.z = -Math.PI / 2; sm.rotation.y = -0.6;
        P(sm, 0.4, s * (0.62 + i * 0.11), 0.42, T);
      }
    });
    P(cylZ(0.012, 0.012, 1.2, steelMat, 6), -1.1, 0.5, 1.05, T);
    return g;
  }
};

// ---------------------------------------------------------------------------
// ZTQ-15 light tank. 9.2 m with gun, hull ~7.5 x 3.3, angular welded turret
// with bustle and stowage basket, 105mm with big muzzle brake, 6 road wheels.
// ---------------------------------------------------------------------------
UNIT_MODELS["lt_c"] = {
  len: 9.2,
  build: function (THREE, M, C) {
    var g = new THREE.Group();
    function paint(base) {
      var cv = document.createElement("canvas");
      cv.width = 256; cv.height = 256;
      var q = cv.getContext("2d");
      q.fillStyle = base; q.fillRect(0, 0, 256, 256);
      var i, x, y, h, gr;
      for (i = 0; i < 26; i++) {
        q.fillStyle = (i % 2) ? "rgba(18,24,16,0.08)" : "rgba(232,238,222,0.05)";
        q.fillRect(Math.random() * 256, Math.random() * 256, 24 + Math.random() * 60, 16 + Math.random() * 44);
      }
      q.strokeStyle = "rgba(10,14,8,0.35)"; q.lineWidth = 1;
      for (i = 0; i < 7; i++) {
        x = 12 + Math.random() * 232;
        q.beginPath(); q.moveTo(x, 0); q.lineTo(x, 256); q.stroke();
        y = 12 + Math.random() * 232;
        q.beginPath(); q.moveTo(0, y); q.lineTo(256, y); q.stroke();
      }
      q.fillStyle = "rgba(12,14,10,0.45)";
      for (i = 0; i < 40; i++) { q.fillRect(Math.random() * 256, Math.random() * 256, 2, 2); }
      for (i = 0; i < 12; i++) {
        x = Math.random() * 256; y = Math.random() * 180; h = 40 + Math.random() * 70;
        gr = q.createLinearGradient(0, y, 0, y + h);
        gr.addColorStop(0, "rgba(0,0,0,0.13)"); gr.addColorStop(1, "rgba(0,0,0,0)");
        q.fillStyle = gr; q.fillRect(x, y, 2 + Math.random() * 3, h);
      }
      var t = new THREE.CanvasTexture(cv);
      t.wrapS = THREE.RepeatWrapping; t.wrapT = THREE.RepeatWrapping;
      t.anisotropy = 4;
      return t;
    }
    var hullMat = new THREE.MeshStandardMaterial({ map: paint("#4e5844"), metalness: 0.25, roughness: 0.62, side: THREE.DoubleSide });
    var plainMat = new THREE.MeshStandardMaterial({ color: 0x47503e, metalness: 0.25, roughness: 0.62 });
    var trackMat = new THREE.MeshStandardMaterial({ color: 0x1e201d, metalness: 0.3, roughness: 0.88 });
    var steelMat = new THREE.MeshStandardMaterial({ color: 0x43474a, metalness: 0.85, roughness: 0.3 });
    var glassMat = new THREE.MeshStandardMaterial({ color: 0x131b21, metalness: 0.4, roughness: 0.16 });
    var teamMat = new THREE.MeshStandardMaterial({ color: C.team, metalness: 0.3, roughness: 0.5 });
    function B(a, b, c, m) { return new THREE.Mesh(new THREE.BoxGeometry(a, b, c), m); }
    function P(m, x, y, z, pr) { m.position.set(x, y, z); (pr || g).add(m); return m; }
    function cylX(r, l, m, s) { var o = new THREE.Mesh(new THREE.CylinderGeometry(r, r, l, s || 12), m); o.rotation.z = -Math.PI / 2; return o; }
    function cylZ(rt, rb, h, m, s) { var o = new THREE.Mesh(new THREE.CylinderGeometry(rt, rb, h, s || 14), m); o.rotation.x = Math.PI / 2; return o; }

    // angular hull, side profile slab
    g.add(new THREE.Mesh(M.slab(THREE, [
      [3.45, 0.45], [3.9, 0.85], [1.9, 1.62], [-3.35, 1.62], [-3.6, 1.15], [-3.45, 0.45]
    ], 2.2, "xz"), hullMat));
    // sponsons and side skirts
    [1, -1].forEach(function (s) {
      P(B(6.05, 0.55, 0.6, hullMat), -0.52, s * 1.375, 1.32);
      P(B(5.8, 0.05, 0.5, hullMat), -0.4, s * 1.66, 0.8);   // skirt
      // running gear: 6 road wheels
      P(B(6.6, 0.5, 0.4, trackMat), -0.1, s * 1.4, 0.5);
      [2.6, 1.5, 0.4, -0.7, -1.8, -2.9].forEach(function (wx) {
        P(new THREE.Mesh(new THREE.CylinderGeometry(0.34, 0.34, 0.3, 16), trackMat), wx, s * 1.4, 0.34);
        P(new THREE.Mesh(new THREE.CylinderGeometry(0.17, 0.17, 0.34, 10), plainMat), wx, s * 1.4, 0.34);
      });
      P(new THREE.Mesh(new THREE.CylinderGeometry(0.28, 0.28, 0.24, 14), trackMat), -3.35, s * 1.4, 0.52); // rear drive
      P(new THREE.Mesh(new THREE.CylinderGeometry(0.26, 0.26, 0.24, 14), trackMat), 3.15, s * 1.4, 0.5);   // idler
      P(B(6.8, 0.5, 0.05, hullMat), -0.35, s * 1.4, 0.98);  // fender strip
      // rear sponson stowage boxes
      P(B(1.1, 0.5, 0.35, plainMat), -2.8, s * 1.37, 1.75);
    });
    // driver hatch front left, headlight boxes, antenna
    P(cylZ(0.22, 0.22, 0.06, plainMat, 14), 2.5, 0.6, 1.64);
    [0.8, -0.8].forEach(function (yy) {
      P(B(0.12, 0.26, 0.14, plainMat), 3.55, yy, 1.15);
      P(cylX(0.05, 0.04, glassMat, 10), 3.63, yy, 1.15);
    });
    // team identification plates on the skirts
    P(B(0.5, 0.04, 0.3, teamMat), 1.6, 1.69, 0.85);
    P(B(0.5, 0.04, 0.3, teamMat), 1.6, -1.69, 0.85);
    // angular welded turret with bustle
    var T = new THREE.Group(); T.name = "turret"; T.position.set(-0.3, 0, 1.62); g.add(T);
    P(cylZ(0.8, 0.85, 0.1, hullMat, 20), 0, 0, 0.05, T);    // race ring
    P(B(1.8, 1.7, 0.62, hullMat), -0.1, 0, 0.4, T);         // turret box
    // arrow-shaped cheek plates
    var ck1 = P(B(1.13, 0.06, 0.6, hullMat), 0.775, 0.425, 0.4, T); ck1.rotation.z = -0.85;
    var ck2 = P(B(1.13, 0.06, 0.6, hullMat), 0.775, -0.425, 0.4, T); ck2.rotation.z = 0.85;
    P(B(0.5, 0.5, 0.45, plainMat), 1.05, 0, 0.4, T);        // mantlet
    // 105mm with thermal sleeve, evacuator and big muzzle brake
    P(cylX(0.056, 4.05, steelMat, 12), 3.32, 0, 0.45, T);   // tube
    P(cylX(0.068, 0.9, steelMat, 12), 1.95, 0, 0.45, T);    // sleeve fwd
    P(cylX(0.068, 0.9, steelMat, 12), 3.35, 0, 0.45, T);    // sleeve aft
    P(cylX(0.082, 0.5, steelMat, 12), 2.65, 0, 0.45, T);    // bore evacuator
    P(cylX(0.085, 0.55, steelMat, 12), 5.62, 0, 0.45, T);   // muzzle brake body
    P(cylX(0.095, 0.05, trackMat, 12), 5.45, 0, 0.45, T);   // brake slots
    P(cylX(0.095, 0.05, trackMat, 12), 5.72, 0, 0.45, T);
    // bustle with stowage basket rails
    P(B(1.0, 1.5, 0.55, hullMat), -1.4, 0, 0.42, T);
    P(B(0.05, 1.6, 0.05, steelMat), -1.98, 0, 0.6, T);
    P(B(1.0, 0.05, 0.05, steelMat), -1.45, 0.82, 0.6, T);
    P(B(1.0, 0.05, 0.05, steelMat), -1.45, -0.82, 0.6, T);
    // roof: RCWS, panoramic sight, gunner sight
    var W = new THREE.Group(); W.position.set(-0.6, -0.45, 0.71); T.add(W);
    P(cylZ(0.11, 0.12, 0.14, plainMat, 10), 0, 0, 0.07, W);
    P(B(0.32, 0.2, 0.16, plainMat), 0, 0, 0.22, W);
    P(cylX(0.02, 0.85, steelMat, 8), 0.55, 0, 0.24, W);
    P(cylZ(0.12, 0.12, 0.26, plainMat, 10), -0.5, 0.45, 0.84, T);  // pano mast
    P(B(0.22, 0.2, 0.18, plainMat), -0.5, 0.45, 1.02, T);
    P(B(0.02, 0.14, 0.1, glassMat), -0.38, 0.45, 1.02, T);
    P(B(0.3, 0.3, 0.2, plainMat), 0.35, 0.35, 0.78, T);     // gunner sight
    P(B(0.02, 0.2, 0.12, glassMat), 0.51, 0.35, 0.8, T);
    // commander hatch
    P(cylZ(0.24, 0.26, 0.08, hullMat, 14), -0.55, -0.45, 0.75, T);
    // smoke launcher banks
    [1, -1].forEach(function (s) {
      for (var i = 0; i < 4; i++) {
        var sm = new THREE.Mesh(new THREE.CylinderGeometry(0.03, 0.03, 0.28, 8), steelMat);
        sm.rotation.z = -Math.PI / 2; sm.rotation.y = -0.55;
        P(sm, 0.5, s * (0.55 + i * 0.1), 0.55, T);
      }
    });
    // antennas on the bustle
    P(cylZ(0.012, 0.012, 1.3, steelMat, 6), -1.8, 0.6, 1.35, T);
    P(cylZ(0.012, 0.012, 1.1, steelMat, 6), -1.8, -0.6, 1.25, T);
    return g;
  }
};

/* ---- pack ab27459c ---- */
UNIT_MODELS["carrier_n"] = {
  len: 333,
  build: function (THREE, M, C) {
    var G = new THREE.Group();
    var DECK = [
      [-166.5, -20], [-60, -24], [-45, -32], [65, -32], [80, -24],
      [120, -22], [150, -16], [166.5, -9], [166.5, 9], [150, 16],
      [120, 18], [60, 18], [45, 40], [-10, 45], [-120, 36], [-166.5, 30]
    ];
    var minX = -166.5, maxX = 166.5, minY = -32, maxY = 45;
    var spanX = maxX - minX, spanY = maxY - minY;
    var deckTop = 19, deckThk = 2.6;
    var i, k;

    // ---------- painted flight deck ----------
    var cv = document.createElement("canvas");
    cv.width = 2048; cv.height = 512;
    var g2 = cv.getContext("2d");
    var W = cv.width, H = cv.height;
    function PX(x) { return (x - minX) / spanX * W; }
    function PY(y) { return (maxY - y) / spanY * H; }
    var seed = 7;
    function rnd() { seed = (seed * 9301 + 49297) % 233280; return seed / 233280; }
    function ln(x1, y1, x2, y2, wd, col, dash) {
      g2.save();
      g2.strokeStyle = col; g2.lineWidth = wd;
      if (dash) g2.setLineDash(dash);
      g2.beginPath(); g2.moveTo(PX(x1), PY(y1)); g2.lineTo(PX(x2), PY(y2)); g2.stroke();
      g2.restore();
    }
    g2.fillStyle = "#3b3f43"; g2.fillRect(0, 0, W, H);
    for (i = 0; i < 120; i++) {
      g2.fillStyle = rnd() < 0.5 ? "rgba(255,255,255,0.05)" : "rgba(0,0,0,0.07)";
      g2.fillRect(rnd() * W, rnd() * H, 30 + rnd() * 140, 14 + rnd() * 60);
    }
    g2.strokeStyle = "rgba(0,0,0,0.22)"; g2.lineWidth = 1;
    for (var gx = minX; gx <= maxX; gx += 12) { g2.beginPath(); g2.moveTo(PX(gx), 0); g2.lineTo(PX(gx), H); g2.stroke(); }
    for (var gy = minY; gy <= maxY; gy += 14) { g2.beginPath(); g2.moveTo(0, PY(gy)); g2.lineTo(W, PY(gy)); g2.stroke(); }
    g2.fillStyle = "rgba(8,10,12,0.35)";
    for (var tx = minX + 4; tx < maxX; tx += 6) {
      for (var ty = minY + 3; ty < maxY; ty += 6) g2.fillRect(PX(tx) - 1, PY(ty) - 1, 2.5, 2.5);
    }
    // landing area wear + rubber skid marks
    ln(-165, 4, 55, 37, 64, "rgba(0,0,0,0.10)");
    for (i = 0; i < 12; i++) {
      var t0 = 15 + rnd() * 70, off = (rnd() - 0.5) * 10, l0 = 8 + rnd() * 14;
      var ax = -165 + 0.989 * t0 - 0.148 * off, ay = 4 + 0.148 * t0 + 0.989 * off;
      ln(ax, ay, ax + 0.989 * l0, ay + 0.148 * l0, 2, "rgba(0,0,0,0.3)");
    }
    // angled landing strip (9 deg to port), borders + dashed centreline
    ln(-163.4, -6.9, 56.6, 26.1, 4, "#cfd3d6");
    ln(-166.6, 14.9, 53.4, 47.9, 4, "#cfd3d6");
    ln(-165, 4, 55, 37, 4, "#cfd3d6", [30, 24]);
    ln(-161.6, -6.3, -158.4, 15.5, 5, "#cfd3d6");
    // arresting wires
    ln(-138.7, -2.7, -141.9, 18.1, 2, "#232629");
    ln(-126.8, -0.9, -130.0, 19.9, 2, "#232629");
    ln(-114.9, 0.9, -118.1, 21.7, 2, "#232629");
    ln(-103.1, 2.6, -106.3, 23.4, 2, "#232629");
    // 4 catapult tracks: 2 bow, 2 waist on the angle
    function cat(x1, y1, x2, y2) {
      ln(x1, y1, x2, y2, 12, "rgba(255,255,255,0.06)");
      ln(x1, y1, x2, y2, 7, "#90969b");
      ln(x1, y1, x2, y2, 2, "#26292c");
    }
    cat(58, -7, 161, -7); cat(58, 9, 161, 9);
    cat(-95, 17, 4, 31.8); cat(-70, 6, 29, 20.8);
    // deck edge safety line
    g2.strokeStyle = "#c9ced2"; g2.lineWidth = 4;
    g2.beginPath(); g2.moveTo(PX(DECK[0][0]), PY(DECK[0][1]));
    for (i = 1; i < DECK.length; i++) g2.lineTo(PX(DECK[i][0]), PY(DECK[i][1]));
    g2.closePath(); g2.stroke();
    // bow number
    g2.save(); g2.translate(PX(150), PY(0)); g2.rotate(Math.PI / 2);
    g2.fillStyle = "#c9ced2"; g2.font = "bold 64px sans-serif"; g2.textAlign = "center";
    g2.fillText("68", 0, 22); g2.restore();

    var tex = new THREE.CanvasTexture(cv); tex.anisotropy = 4;
    var deckMat = new THREE.MeshStandardMaterial({ map: tex, metalness: 0.2, roughness: 0.75 });
    var hullMat = new THREE.MeshStandardMaterial({ color: 0x6d7378, metalness: 0.3, roughness: 0.6 });
    var darkMat = new THREE.MeshStandardMaterial({ color: 0x2f3437, metalness: 0.3, roughness: 0.6 });
    var winMat = new THREE.MeshStandardMaterial({ color: 0x101820, metalness: 0.4, roughness: 0.25 });
    var teamMat = new THREE.MeshStandardMaterial({ color: new THREE.Color(C.team), metalness: 0.3, roughness: 0.55 });
    var lampMat = new THREE.MeshStandardMaterial({ color: 0x661111, emissive: 0xff2222, emissiveIntensity: 0.9, roughness: 0.4 });

    function bx(parent, sx, sy, sz, x, y, z, mt) {
      var m = new THREE.Mesh(new THREE.BoxGeometry(sx, sy, sz), mt);
      m.position.set(x, y, z); parent.add(m); return m;
    }
    function pole(parent, r1, r2, h, x, y, z, mt) {
      var pg = new THREE.CylinderGeometry(r1, r2, h, 8); pg.rotateX(Math.PI / 2);
      var m = new THREE.Mesh(pg, mt); m.position.set(x, y, z); parent.add(m); return m;
    }

    // flight deck slab (top face carries the painted texture)
    var shp = new THREE.Shape(); shp.moveTo(DECK[0][0], DECK[0][1]);
    for (i = 1; i < DECK.length; i++) shp.lineTo(DECK[i][0], DECK[i][1]);
    shp.closePath();
    var dg = new THREE.ExtrudeGeometry(shp, { depth: deckThk, bevelEnabled: false });
    var dpos = dg.attributes.position, duv = dg.attributes.uv;
    for (i = 0; i < dpos.count; i++) duv.setXY(i, (dpos.getX(i) - minX) / spanX, (dpos.getY(i) - minY) / spanY);
    var deck = new THREE.Mesh(dg, [deckMat, hullMat]);
    deck.position.z = deckTop - deckThk; G.add(deck);

    // hull (waterline origin, keel near z=-11.5)
    var hull = new THREE.Mesh(M.loft(THREE, [
      { x: -160, w: 15, h: 12, zc: 5, sq: 0.85 },
      { x: -120, w: 19, h: 14, zc: 3.4, sq: 0.85 },
      { x: -40, w: 20.5, h: 14.5, zc: 3, sq: 0.85 },
      { x: 60, w: 20, h: 14.5, zc: 3, sq: 0.85 },
      { x: 120, w: 15, h: 13.5, zc: 4, sq: 0.8 },
      { x: 150, w: 8, h: 12.5, zc: 5, sq: 0.7 },
      { x: 165, w: 1.2, h: 11, zc: 6.5 }
    ], 20), hullMat);
    G.add(hull);
    bx(G, 2.5, 30, 22, -160.5, 0, 5, hullMat);   // stern transom
    bx(G, 12, 24, 1.2, -161, 0, 8.2, hullMat);   // fantail platform
    pole(G, 0.4, 0.4, 7.5, -157, 9, 12.4, hullMat);
    pole(G, 0.4, 0.4, 7.5, -157, -9, 12.4, hullMat);
    // gallery-deck sponsons carrying the overhangs
    bx(G, 56, 7, 6, 8, -26, 14, hullMat);
    bx(G, 20, 6, 6, -60, -24, 14, hullMat);
    bx(G, 64, 7, 6, -40, 32, 14, hullMat);
    bx(G, 30, 6, 5, -140, 26, 14.5, hullMat);
    bx(G, 18, 6, 5, -135, 30, 15, hullMat);

    // 4 deck-edge elevators (3 stbd, 1 port aft)
    function elevator(x, y, innerSign) {
      bx(G, 16, 13, 1.6, x, y, deckTop - 0.8, hullMat);
      bx(G, 15, 1.4, 9, x, y + innerSign * 6.4, deckTop - 6.4, hullMat);
    }
    elevator(72, -33, 1); elevator(35, -37, 1); elevator(-60, -29, 1); elevator(-135, 38, -1);

    // catwalks along the deck edge
    bx(G, 46, 1.8, 0.6, 100, -24, 18.1, darkMat);
    bx(G, 56, 1.8, 0.6, 0, -33.6, 18.1, darkMat);
    bx(G, 50, 1.8, 0.6, -70, 41.6, 18.1, darkMat);
    bx(G, 40, 1.8, 0.6, 100, 19.4, 18.1, darkMat);

    // jet blast deflectors behind the bow cats
    var jbd1 = bx(G, 5, 12, 0.4, 54, -7, 20.6, hullMat); jbd1.rotation.y = 0.7;
    var jbd2 = bx(G, 5, 12, 0.4, 54, 9, 20.6, hullMat); jbd2.rotation.y = 0.7;

    // island, starboard aft of middle
    bx(G, 22, 9, 7, -15, -25.5, 22.5, hullMat);
    bx(G, 17, 8.4, 6, -16, -25.5, 29, hullMat);
    bx(G, 17.4, 8.8, 1.1, -16, -25.5, 30.6, winMat);   // flag bridge glazing
    bx(G, 12, 8, 4, -17, -25.5, 34, hullMat);
    bx(G, 12.4, 8.4, 1.3, -17, -25.5, 35, winMat);     // nav bridge glazing
    bx(G, 13, 8.6, 0.5, -17, -25.5, 36.4, hullMat);
    bx(G, 4, 4, 2.6, -8.5, -23.5, 30.9, winMat);       // pri-fly overhang
    bx(G, 5, 9.6, 2, -13, -25.5, 24, teamMat);         // team identification band
    pole(G, 0.3, 0.5, 13, -19, -25.5, 42.9, darkMat);
    var yd1 = new THREE.Mesh(new THREE.CylinderGeometry(0.09, 0.09, 9, 6), darkMat);
    yd1.position.set(-19, -25.5, 45.2); G.add(yd1);
    var yd2 = new THREE.Mesh(new THREE.CylinderGeometry(0.09, 0.09, 7, 6), darkMat);
    yd2.rotation.z = Math.PI / 2; yd2.position.set(-19, -25.5, 47.2); G.add(yd2);
    bx(G, 1.2, 1.2, 2, -10, -27, 33, hullMat);
    var r48 = bx(G, 0.5, 3.8, 4.2, -10, -27, 36.2, darkMat); r48.rotation.y = -0.3;
    pole(G, 0.3, 0.3, 1.6, -23, -25.5, 32.8, hullMat);
    var dish = new THREE.Mesh(new THREE.CylinderGeometry(1.3, 1.3, 0.3, 12), darkMat);
    dish.rotation.set(0, -0.45, Math.PI / 2); dish.position.set(-23, -25.5, 34); G.add(dish);
    var dm = new THREE.Mesh(new THREE.SphereGeometry(1.2, 12, 8), hullMat);
    dm.position.set(-20.5, -25.5, 37.6); G.add(dm);
    pole(G, 0.6, 0.7, 3.2, -7, -27, 27.4, darkMat);
    pole(G, 0.6, 0.7, 3.2, -7, -24, 27.4, darkMat);
    var lamp = new THREE.Mesh(new THREE.SphereGeometry(0.3, 8, 6), lampMat);
    lamp.position.set(-19, -25.5, 49.7); G.add(lamp);

    // whip antennas along the deck edge
    var antGeo = new THREE.CylinderGeometry(0.06, 0.06, 7, 5); antGeo.rotateX(Math.PI / 2);
    var ants = [[60, -31.5], [-20, -32.5], [90, 17.5], [-90, 38.5], [-25, -29.5]];
    for (i = 0; i < ants.length; i++) {
      var an = new THREE.Mesh(antGeo, darkMat);
      an.position.set(ants[i][0], ants[i][1], deckTop + 3.5); G.add(an);
    }

    // parked aircraft silhouettes
    function jet(x, y, rot, sc) {
      var ac = new THREE.Group();
      var o = [[9, 0], [6, 1], [3.2, 1.3], [2.2, 1.5], [0.2, 5], [-1.4, 5.2], [-2.6, 1.7],
        [-4.6, 1.4], [-5.2, 3.4], [-6.6, 3.5], [-6.2, 1.2], [-7.5, 0.9]];
      var pp = [];
      for (k = 0; k < o.length; k++) pp.push(o[k]);
      pp.push([-7.9, 0]);
      for (k = o.length - 1; k >= 1; k--) pp.push([o[k][0], -o[k][1]]);
      ac.add(new THREE.Mesh(M.slab(THREE, pp, 0.7), darkMat));
      var cp = new THREE.Mesh(new THREE.BoxGeometry(2.6, 1.1, 0.8), darkMat);
      cp.position.set(4.5, 0, 1); ac.add(cp);
      var f1 = new THREE.Mesh(new THREE.BoxGeometry(2, 0.16, 1.6), darkMat);
      f1.position.set(-5.8, 1, 1.4); f1.rotation.x = 0.35; ac.add(f1);
      var f2 = new THREE.Mesh(new THREE.BoxGeometry(2, 0.16, 1.6), darkMat);
      f2.position.set(-5.8, -1, 1.4); f2.rotation.x = -0.35; ac.add(f2);
      ac.scale.set(sc, sc, sc); ac.position.set(x, y, deckTop + 0.02); ac.rotation.z = rot;
      G.add(ac);
    }
    jet(-148, -10, 2.8, 1); jet(-128, -18, -2.7, 1); jet(98, -16, 0.5, 1); jet(137, 10, 0.2, 1);

    return G;
  }
};

UNIT_MODELS["carrier_p"] = {
  len: 305,
  build: function (THREE, M, C) {
    var G = new THREE.Group();
    var DECK = [
      [-152.5, -22], [-90, -25], [-40, -26], [40, -26], [80, -21], [100, -15],
      [130, -12], [152.5, -10], [152.5, 10], [130, 12], [100, 15], [80, 20],
      [55, 25], [30, 42], [-30, 46], [-100, 39], [-152.5, 26]
    ];
    var minX = -152.5, maxX = 152.5, minY = -26, maxY = 46;
    var spanX = maxX - minX, spanY = maxY - minY;
    var deckTop = 17.5, deckThk = 2.4;
    var i, k;

    // ---------- painted flight deck ----------
    var cv = document.createElement("canvas");
    cv.width = 2048; cv.height = 512;
    var g2 = cv.getContext("2d");
    var W = cv.width, H = cv.height;
    function PX(x) { return (x - minX) / spanX * W; }
    function PY(y) { return (maxY - y) / spanY * H; }
    var seed = 31;
    function rnd() { seed = (seed * 9301 + 49297) % 233280; return seed / 233280; }
    function ln(x1, y1, x2, y2, wd, col, dash) {
      g2.save();
      g2.strokeStyle = col; g2.lineWidth = wd;
      if (dash) g2.setLineDash(dash);
      g2.beginPath(); g2.moveTo(PX(x1), PY(y1)); g2.lineTo(PX(x2), PY(y2)); g2.stroke();
      g2.restore();
    }
    g2.fillStyle = "#3e4246"; g2.fillRect(0, 0, W, H);
    for (i = 0; i < 140; i++) {
      g2.fillStyle = rnd() < 0.45 ? "rgba(255,255,255,0.05)" : "rgba(0,0,0,0.08)";
      g2.fillRect(rnd() * W, rnd() * H, 26 + rnd() * 130, 14 + rnd() * 56);
    }
    g2.strokeStyle = "rgba(0,0,0,0.24)"; g2.lineWidth = 1;
    for (var gx = minX; gx <= maxX; gx += 11) { g2.beginPath(); g2.moveTo(PX(gx), 0); g2.lineTo(PX(gx), H); g2.stroke(); }
    for (var gy = minY; gy <= maxY; gy += 13) { g2.beginPath(); g2.moveTo(0, PY(gy)); g2.lineTo(W, PY(gy)); g2.stroke(); }
    g2.fillStyle = "rgba(8,10,12,0.4)";
    for (var tx = minX + 3; tx < maxX; tx += 5.5) {
      for (var ty = minY + 3; ty < maxY; ty += 5.5) g2.fillRect(PX(tx) - 1, PY(ty) - 1, 2.5, 2.5);
    }
    // landing area wear + skid marks
    ln(-150, 4, 30, 26, 58, "rgba(0,0,0,0.12)");
    for (i = 0; i < 14; i++) {
      var t0 = 12 + rnd() * 62, off = (rnd() - 0.5) * 10, l0 = 7 + rnd() * 13;
      var ax = -150 + 0.993 * t0 - 0.121 * off, ay = 4 + 0.121 * t0 + 0.993 * off;
      ln(ax, ay, ax + 0.993 * l0, ay + 0.121 * l0, 2, "rgba(0,0,0,0.32)");
    }
    // angled landing strip, dashed borders (Kuznetsov style)
    ln(-148.7, -6.9, 31.3, 15.1, 4, "#d5d9db", [26, 18]);
    ln(-151.3, 14.9, 28.7, 36.9, 4, "#d5d9db", [26, 18]);
    ln(-150, 4, 30, 26, 4, "#d5d9db", [30, 24]);
    ln(-145.7, -6.5, -148.3, 15.3, 5, "#d5d9db");
    // arresting wires
    ln(-119.0, -2.3, -121.4, 17.6, 2, "#232629");
    ln(-107.1, -0.8, -109.5, 19.0, 2, "#232629");
    ln(-95.2, 0.6, -97.6, 20.5, 2, "#232629");
    ln(-83.3, 2.1, -85.7, 21.9, 2, "#232629");
    // three take-off run lines converging on the ramp
    ln(18, -5.5, 106, -1.5, 3, "#d5d9db");
    ln(13.5, -8.5, 18, -5.5, 3, "#d5d9db"); ln(13.5, -2.5, 18, -5.5, 3, "#d5d9db");
    ln(18, 5.5, 106, 1.5, 3, "#d5d9db");
    ln(13.5, 8.5, 18, 5.5, 3, "#d5d9db"); ln(13.5, 2.5, 18, 5.5, 3, "#d5d9db");
    ln(-62, 11, 106, 1, 3, "#d5d9db");
    ln(-66.5, 14, -62, 11, 3, "#d5d9db"); ln(-66.5, 8, -62, 11, 3, "#d5d9db");
    // 12 SSM silo hatch outlines at the ramp base
    var sxs = [88, 96, 104], sys = [-7.5, -2.5, 2.5, 7.5];
    for (i = 0; i < sxs.length; i++) {
      for (k = 0; k < sys.length; k++) {
        var hx = PX(sxs[i] - 2.1), hy = PY(sys[k] + 2.1);
        var hw = 4.2 / spanX * W, hh = 4.2 / spanY * H;
        g2.fillStyle = "#585d62"; g2.fillRect(hx, hy, hw, hh);
        g2.strokeStyle = "#26292c"; g2.lineWidth = 2; g2.strokeRect(hx, hy, hw, hh);
      }
    }
    // helo spot, port aft
    g2.strokeStyle = "#d5d9db"; g2.lineWidth = 3; g2.beginPath();
    g2.ellipse(PX(-60), PY(37), 4.5 / spanX * W, 4.5 / spanY * H, 0, 0, Math.PI * 2); g2.stroke();
    // deck edge safety line
    g2.strokeStyle = "#c9ced2"; g2.lineWidth = 4;
    g2.beginPath(); g2.moveTo(PX(DECK[0][0]), PY(DECK[0][1]));
    for (i = 1; i < DECK.length; i++) g2.lineTo(PX(DECK[i][0]), PY(DECK[i][1]));
    g2.closePath(); g2.stroke();

    var tex = new THREE.CanvasTexture(cv); tex.anisotropy = 4;
    var deckMat = new THREE.MeshStandardMaterial({ map: tex, metalness: 0.2, roughness: 0.75 });
    var hullMat = new THREE.MeshStandardMaterial({ color: 0x69707a, metalness: 0.3, roughness: 0.6 });
    var rampMat = new THREE.MeshStandardMaterial({ color: 0x3e4246, metalness: 0.2, roughness: 0.75 });
    var darkMat = new THREE.MeshStandardMaterial({ color: 0x2e3336, metalness: 0.3, roughness: 0.6 });
    var winMat = new THREE.MeshStandardMaterial({ color: 0x101820, metalness: 0.4, roughness: 0.25 });
    var teamMat = new THREE.MeshStandardMaterial({ color: new THREE.Color(C.team), metalness: 0.3, roughness: 0.55 });
    var lineMat = new THREE.MeshStandardMaterial({ color: 0xd5d9db, metalness: 0.2, roughness: 0.6 });
    var lampMat = new THREE.MeshStandardMaterial({ color: 0x661111, emissive: 0xff2222, emissiveIntensity: 0.9, roughness: 0.4 });

    function bx(parent, sx, sy, sz, x, y, z, mt) {
      var m = new THREE.Mesh(new THREE.BoxGeometry(sx, sy, sz), mt);
      m.position.set(x, y, z); parent.add(m); return m;
    }
    function pole(parent, r1, r2, h, x, y, z, mt) {
      var pg = new THREE.CylinderGeometry(r1, r2, h, 8); pg.rotateX(Math.PI / 2);
      var m = new THREE.Mesh(pg, mt); m.position.set(x, y, z); parent.add(m); return m;
    }

    // flight deck slab
    var shp = new THREE.Shape(); shp.moveTo(DECK[0][0], DECK[0][1]);
    for (i = 1; i < DECK.length; i++) shp.lineTo(DECK[i][0], DECK[i][1]);
    shp.closePath();
    var dg = new THREE.ExtrudeGeometry(shp, { depth: deckThk, bevelEnabled: false });
    var dpos = dg.attributes.position, duv = dg.attributes.uv;
    for (i = 0; i < dpos.count; i++) duv.setXY(i, (dpos.getX(i) - minX) / spanX, (dpos.getY(i) - minY) / spanY);
    var deck = new THREE.Mesh(dg, [deckMat, hullMat]);
    deck.position.z = deckTop - deckThk; G.add(deck);

    // hull
    var hull = new THREE.Mesh(M.loft(THREE, [
      { x: -148, w: 14, h: 11, zc: 4.5, sq: 0.85 },
      { x: -100, w: 17.5, h: 13, zc: 2.8, sq: 0.85 },
      { x: -20, w: 18.5, h: 13.5, zc: 2.5, sq: 0.85 },
      { x: 60, w: 17, h: 13.5, zc: 2.8, sq: 0.8 },
      { x: 110, w: 11, h: 12.5, zc: 3.5, sq: 0.7 },
      { x: 140, w: 5, h: 11.5, zc: 4.5 },
      { x: 151, w: 1, h: 10.5, zc: 5.5 }
    ], 20), hullMat);
    G.add(hull);
    bx(G, 2.5, 27, 20, -148.5, 0, 4, hullMat);   // stern transom
    // sponsons
    bx(G, 55, 7, 6, -20, -21.5, 13.5, hullMat);
    bx(G, 16, 6, 5, -75, -21, 13.5, hullMat);
    bx(G, 70, 7, 6, -30, 34, 13.5, hullMat);
    bx(G, 30, 6, 5, -120, 28, 14, hullMat);

    // SKI-JUMP ramp: curved wedge over the bow, 12 deg exit
    var rs = new THREE.Shape();
    rs.moveTo(106, 0);
    rs.quadraticCurveTo(130.4, 0, 152.5, 4.9);
    rs.lineTo(152.5, -2.2); rs.lineTo(106, -2.2); rs.closePath();
    var rg = new THREE.ExtrudeGeometry(rs, { depth: 24, bevelEnabled: false, curveSegments: 10 });
    rg.rotateX(Math.PI / 2);
    var ramp = new THREE.Mesh(rg, [rampMat, hullMat]);
    ramp.position.set(0, 12, deckTop); G.add(ramp);
    // centreline dashes climbing the ramp
    var st = [[112, 0.1, 0.03], [119.9, 0.44, 0.063], [133.8, 1.76, 0.126], [145.4, 3.54, 0.178]];
    for (i = 0; i < st.length; i++) {
      var dsh = bx(G, 5, 1.2, 0.12, st[i][0], 0, deckTop + st[i][1] + 0.1, lineMat);
      dsh.rotation.y = -st[i][2];
    }

    // 12 raised SSM silo hatch covers
    for (i = 0; i < sxs.length; i++) {
      for (k = 0; k < sys.length; k++) bx(G, 4, 4, 0.25, sxs[i], sys[k], deckTop + 0.13, hullMat);
    }

    // 2 starboard deck-edge elevators
    function elevator(x, y) {
      bx(G, 16, 13, 1.6, x, y, deckTop - 0.8, hullMat);
      bx(G, 15, 1.4, 8, x, y + 6.4, deckTop - 5.8, hullMat);
    }
    elevator(5, -30); elevator(-75, -29);

    // small jet blast deflectors behind the forward run spots
    var jb1 = bx(G, 4, 9, 0.35, 13, -5.5, 18.8, hullMat); jb1.rotation.y = 0.7;
    var jb2 = bx(G, 4, 9, 0.35, 13, 5.5, 18.8, hullMat); jb2.rotation.y = 0.7;

    // big island, starboard
    bx(G, 34, 12, 8, -35, -19, 21.5, hullMat);
    bx(G, 28, 10.5, 6, -35, -19, 28.5, hullMat);
    bx(G, 16, 10, 4.5, -40, -19, 33.75, hullMat);
    bx(G, 16.4, 10.4, 1.3, -40, -19, 34.8, winMat);    // bridge glazing
    bx(G, 17, 10.8, 0.5, -40, -19, 36.3, hullMat);
    bx(G, 5, 12.6, 2, -30, -19, 23, teamMat);          // team identification band
    // tapered mast tower with phased arrays
    var twg = new THREE.CylinderGeometry(1.6, 3.6, 9, 4); twg.rotateX(Math.PI / 2);
    var tower = new THREE.Mesh(twg, hullMat);
    tower.rotation.z = Math.PI / 4; tower.position.set(-28, -19, 36); G.add(tower);
    var pa1 = bx(G, 0.4, 3.2, 3.6, -24.7, -19, 35.5, darkMat); pa1.rotation.y = 0.15;
    var pa2 = bx(G, 0.4, 3.2, 3.6, -31.3, -19, 35.5, darkMat); pa2.rotation.y = -0.15;
    pole(G, 0.25, 0.4, 8, -28, -19, 44.5, darkMat);
    var pa3 = bx(G, 0.5, 4, 3.5, -28, -19, 43, darkMat); pa3.rotation.y = -0.25;
    var yd1 = new THREE.Mesh(new THREE.CylinderGeometry(0.09, 0.09, 8, 6), darkMat);
    yd1.position.set(-28, -19, 46.2); G.add(yd1);
    // funnel
    bx(G, 6.5, 7, 5, -46, -19, 28, hullMat);
    bx(G, 6.9, 7.4, 0.6, -46, -19, 30.8, darkMat);
    // radars + dome
    bx(G, 1.2, 1.2, 2, -44, -22, 32.5, hullMat);
    var r1 = bx(G, 0.5, 3.6, 3.8, -44, -22, 35.3, darkMat); r1.rotation.y = -0.3;
    var dome = new THREE.Mesh(new THREE.SphereGeometry(1.3, 12, 8), hullMat);
    dome.position.set(-47, -19, 32.4); G.add(dome);
    var lamp = new THREE.Mesh(new THREE.SphereGeometry(0.3, 8, 6), lampMat);
    lamp.position.set(-28, -19, 48.8); G.add(lamp);

    // whip antennas
    var antGeo = new THREE.CylinderGeometry(0.06, 0.06, 6.5, 5); antGeo.rotateX(Math.PI / 2);
    var ants = [[30, -27], [-60, -26.5], [60, 22], [-70, 40.5]];
    for (i = 0; i < ants.length; i++) {
      var an = new THREE.Mesh(antGeo, darkMat);
      an.position.set(ants[i][0], ants[i][1], deckTop + 3.2); G.add(an);
    }
    // catwalks
    bx(G, 46, 1.8, 0.6, 20, -27.5, 16.6, darkMat);
    bx(G, 40, 1.8, 0.6, -100, -26, 16.6, darkMat);
    bx(G, 44, 1.8, 0.6, 0, 45.5, 16.6, darkMat);
    bx(G, 30, 1.8, 0.6, 95, -18, 16.6, darkMat);
    // deck clutter
    bx(G, 3, 2, 1.5, -25, -10, 18.25, darkMat);
    bx(G, 3, 2, 1.5, -40, -10.5, 18.25, darkMat);
    bx(G, 2.8, 1.6, 1.2, -55, -10, 18.1, darkMat);

    // parked aircraft silhouettes (Su-33 scale)
    function jet(x, y, rot, sc) {
      var ac = new THREE.Group();
      var o = [[9, 0], [6, 1], [3.2, 1.3], [2.2, 1.5], [0.2, 5], [-1.4, 5.2], [-2.6, 1.7],
        [-4.6, 1.4], [-5.2, 3.4], [-6.6, 3.5], [-6.2, 1.2], [-7.5, 0.9]];
      var pp = [];
      for (k = 0; k < o.length; k++) pp.push(o[k]);
      pp.push([-7.9, 0]);
      for (k = o.length - 1; k >= 1; k--) pp.push([o[k][0], -o[k][1]]);
      ac.add(new THREE.Mesh(M.slab(THREE, pp, 0.7), darkMat));
      var cp = new THREE.Mesh(new THREE.BoxGeometry(2.6, 1.1, 0.8), darkMat);
      cp.position.set(4.5, 0, 1); ac.add(cp);
      var f1 = new THREE.Mesh(new THREE.BoxGeometry(2, 0.16, 1.6), darkMat);
      f1.position.set(-5.8, 1, 1.4); f1.rotation.x = 0.35; ac.add(f1);
      var f2 = new THREE.Mesh(new THREE.BoxGeometry(2, 0.16, 1.6), darkMat);
      f2.position.set(-5.8, -1, 1.4); f2.rotation.x = -0.35; ac.add(f2);
      ac.scale.set(sc, sc, sc); ac.position.set(x, y, deckTop + 0.02); ac.rotation.z = rot;
      G.add(ac);
    }
    jet(-118, -14, 2.9, 1.25); jet(-126, 24, -2.6, 1.25); jet(-95, 30, 2.7, 1.25); jet(25, -16, 0.6, 1.25);

    return G;
  }
};

UNIT_MODELS["carrier_c"] = {
  len: 316,
  build: function (THREE, M, C) {
    var G = new THREE.Group();
    var DECK = [
      [-158, -22], [-70, -25], [-20, -30], [70, -30], [95, -24], [140, -19],
      [158, -13], [158, 13], [140, 17], [100, 18], [55, 18], [35, 40],
      [-20, 46], [-120, 36], [-158, 28]
    ];
    var minX = -158, maxX = 158, minY = -30, maxY = 46;
    var spanX = maxX - minX, spanY = maxY - minY;
    var deckTop = 19, deckThk = 2.6;
    var i, k;

    // ---------- painted flight deck ----------
    var cv = document.createElement("canvas");
    cv.width = 2048; cv.height = 512;
    var g2 = cv.getContext("2d");
    var W = cv.width, H = cv.height;
    function PX(x) { return (x - minX) / spanX * W; }
    function PY(y) { return (maxY - y) / spanY * H; }
    var seed = 19;
    function rnd() { seed = (seed * 9301 + 49297) % 233280; return seed / 233280; }
    function ln(x1, y1, x2, y2, wd, col, dash) {
      g2.save();
      g2.strokeStyle = col; g2.lineWidth = wd;
      if (dash) g2.setLineDash(dash);
      g2.beginPath(); g2.moveTo(PX(x1), PY(y1)); g2.lineTo(PX(x2), PY(y2)); g2.stroke();
      g2.restore();
    }
    g2.fillStyle = "#45484c"; g2.fillRect(0, 0, W, H);
    for (i = 0; i < 80; i++) {
      g2.fillStyle = rnd() < 0.5 ? "rgba(255,255,255,0.04)" : "rgba(0,0,0,0.05)";
      g2.fillRect(rnd() * W, rnd() * H, 30 + rnd() * 130, 14 + rnd() * 50);
    }
    g2.strokeStyle = "rgba(0,0,0,0.18)"; g2.lineWidth = 1;
    for (var gx = minX; gx <= maxX; gx += 12) { g2.beginPath(); g2.moveTo(PX(gx), 0); g2.lineTo(PX(gx), H); g2.stroke(); }
    for (var gy = minY; gy <= maxY; gy += 14) { g2.beginPath(); g2.moveTo(0, PY(gy)); g2.lineTo(W, PY(gy)); g2.stroke(); }
    g2.fillStyle = "rgba(8,10,12,0.3)";
    for (var tx = minX + 4; tx < maxX; tx += 6) {
      for (var ty = minY + 3; ty < maxY; ty += 6) g2.fillRect(PX(tx) - 1, PY(ty) - 1, 2.5, 2.5);
    }
    // light landing-area wear
    ln(-155, 5, 45, 36, 60, "rgba(0,0,0,0.07)");
    for (i = 0; i < 8; i++) {
      var t0 = 14 + rnd() * 60, off = (rnd() - 0.5) * 9, l0 = 7 + rnd() * 12;
      var ax = -155 + 0.988 * t0 - 0.153 * off, ay = 5 + 0.153 * t0 + 0.988 * off;
      ln(ax, ay, ax + 0.988 * l0, ay + 0.153 * l0, 2, "rgba(0,0,0,0.25)");
    }
    // angled landing strip
    ln(-153.3, -5.9, 46.7, 25.1, 4, "#d3d7da");
    ln(-156.7, 15.9, 43.3, 46.9, 4, "#d3d7da");
    ln(-155, 5, 45, 36, 4, "#d3d7da", [30, 24]);
    ln(-150.4, -5.4, -153.7, 16.3, 5, "#d3d7da");
    // arresting wires
    ln(-125.8, -0.6, -128.9, 19.2, 2, "#232629");
    ln(-114.0, 1.2, -117.0, 21.0, 2, "#232629");
    ln(-102.1, 3.1, -105.1, 22.9, 2, "#232629");
    // THREE electromagnetic catapults: 2 bow, 1 waist port; yellow guide dashes
    function cat(x1, y1, x2, y2) {
      ln(x1, y1, x2, y2, 7, "#93989d");
      ln(x1, y1, x2, y2, 2, "#26292c");
      ln(x1, y1, x2, y2, 3, "#d7b93f", [16, 26]);
    }
    cat(70, -8, 153, -8); cat(70, 8, 153, 8); cat(-45, 14, 48.9, 28.6);
    ln(64, -11, 70, -8, 3, "#d7b93f"); ln(64, -5, 70, -8, 3, "#d7b93f");
    ln(64, 5, 70, 8, 3, "#d7b93f"); ln(64, 11, 70, 8, 3, "#d7b93f");
    ln(-51.4, 16.1, -45, 14, 3, "#d7b93f"); ln(-50.5, 10.2, -45, 14, 3, "#d7b93f");
    // deck edge safety line
    g2.strokeStyle = "#c9ced2"; g2.lineWidth = 4;
    g2.beginPath(); g2.moveTo(PX(DECK[0][0]), PY(DECK[0][1]));
    for (i = 1; i < DECK.length; i++) g2.lineTo(PX(DECK[i][0]), PY(DECK[i][1]));
    g2.closePath(); g2.stroke();
    // bow number
    g2.save(); g2.translate(PX(146), PY(-2)); g2.rotate(Math.PI / 2);
    g2.fillStyle = "#c9ced2"; g2.font = "bold 60px sans-serif"; g2.textAlign = "center";
    g2.fillText("18", 0, 20); g2.restore();

    var tex = new THREE.CanvasTexture(cv); tex.anisotropy = 4;
    var deckMat = new THREE.MeshStandardMaterial({ map: tex, metalness: 0.2, roughness: 0.72 });
    var hullMat = new THREE.MeshStandardMaterial({ color: 0x6d7378, metalness: 0.3, roughness: 0.6 });
    var darkMat = new THREE.MeshStandardMaterial({ color: 0x2f3437, metalness: 0.3, roughness: 0.6 });
    var winMat = new THREE.MeshStandardMaterial({ color: 0x101820, metalness: 0.4, roughness: 0.25 });
    var teamMat = new THREE.MeshStandardMaterial({ color: new THREE.Color(C.team), metalness: 0.3, roughness: 0.55 });
    var lampMat = new THREE.MeshStandardMaterial({ color: 0x661111, emissive: 0xff2222, emissiveIntensity: 0.9, roughness: 0.4 });

    function bx(parent, sx, sy, sz, x, y, z, mt) {
      var m = new THREE.Mesh(new THREE.BoxGeometry(sx, sy, sz), mt);
      m.position.set(x, y, z); parent.add(m); return m;
    }
    function pole(parent, r1, r2, h, x, y, z, mt) {
      var pg = new THREE.CylinderGeometry(r1, r2, h, 8); pg.rotateX(Math.PI / 2);
      var m = new THREE.Mesh(pg, mt); m.position.set(x, y, z); parent.add(m); return m;
    }

    // flight deck slab
    var shp = new THREE.Shape(); shp.moveTo(DECK[0][0], DECK[0][1]);
    for (i = 1; i < DECK.length; i++) shp.lineTo(DECK[i][0], DECK[i][1]);
    shp.closePath();
    var dg = new THREE.ExtrudeGeometry(shp, { depth: deckThk, bevelEnabled: false });
    var dpos = dg.attributes.position, duv = dg.attributes.uv;
    for (i = 0; i < dpos.count; i++) duv.setXY(i, (dpos.getX(i) - minX) / spanX, (dpos.getY(i) - minY) / spanY);
    var deck = new THREE.Mesh(dg, [deckMat, hullMat]);
    deck.position.z = deckTop - deckThk; G.add(deck);

    // hull, flat wide bow
    var hull = new THREE.Mesh(M.loft(THREE, [
      { x: -152, w: 14, h: 12, zc: 5, sq: 0.85 },
      { x: -100, w: 18, h: 14, zc: 3.2, sq: 0.85 },
      { x: -20, w: 19.5, h: 14.5, zc: 3, sq: 0.85 },
      { x: 70, w: 18.5, h: 14.5, zc: 3, sq: 0.8 },
      { x: 120, w: 13, h: 13.5, zc: 3.8, sq: 0.75 },
      { x: 145, w: 7, h: 12.5, zc: 4.8 },
      { x: 157, w: 1.2, h: 11, zc: 6 }
    ], 20), hullMat);
    G.add(hull);
    bx(G, 2.5, 28, 22, -152.5, 0, 5, hullMat);   // stern transom
    bx(G, 12, 22, 1.2, -154, 0, 8.4, hullMat);   // fantail platform
    // sponsons
    bx(G, 60, 7, 6, -30, -25, 14, hullMat);
    bx(G, 16, 6, 6, -85, -23, 14, hullMat);
    bx(G, 70, 7, 6, -45, 32, 14.5, hullMat);
    bx(G, 26, 6, 5, -135, 25, 15, hullMat);

    // 2 starboard deck-edge elevators
    function elevator(x, y) {
      bx(G, 17, 13, 1.6, x, y, deckTop - 0.8, hullMat);
      bx(G, 16, 1.4, 9, x, y + 6.4, deckTop - 6.4, hullMat);
    }
    elevator(10, -34); elevator(-85, -29);

    // jet blast deflectors behind the bow cats
    var jb1 = bx(G, 5, 12, 0.4, 66, -8, 20.6, hullMat); jb1.rotation.y = 0.7;
    var jb2 = bx(G, 5, 12, 0.4, 66, 8, 20.6, hullMat); jb2.rotation.y = 0.7;

    // compact stealthy island, aft-starboard
    bx(G, 20, 9, 8, -45, -22, 23, hullMat);
    var twg = new THREE.CylinderGeometry(5.0, 6.3, 7, 4); twg.rotateX(Math.PI / 2);
    var tower = new THREE.Mesh(twg, hullMat);
    tower.rotation.z = Math.PI / 4; tower.position.set(-45, -22, 30.5); G.add(tower);
    bx(G, 8.0, 8.0, 1.2, -45, -22, 32.6, winMat);     // wraparound bridge glazing
    bx(G, 7.6, 7.6, 0.5, -45, -22, 34.3, hullMat);
    bx(G, 4, 9.6, 1.8, -41, -22, 25, teamMat);        // team identification band
    // fixed phased-array faces on the tower
    var p1 = bx(G, 0.4, 3.4, 3.8, -40.6, -22, 29, darkMat); p1.rotation.y = 0.12;
    var p2 = bx(G, 0.4, 3.4, 3.8, -49.4, -22, 29, darkMat); p2.rotation.y = -0.12;
    var p3 = bx(G, 3.4, 0.4, 3.8, -45, -26.4, 29, darkMat); p3.rotation.x = 0.12;
    var p4 = bx(G, 3.4, 0.4, 3.8, -45, -17.6, 29, darkMat); p4.rotation.x = -0.12;
    var dome = new THREE.Mesh(new THREE.SphereGeometry(1.3, 12, 8), hullMat);
    dome.position.set(-45, -22, 35.7); G.add(dome);
    var d2 = new THREE.Mesh(new THREE.SphereGeometry(0.8, 10, 7), hullMat);
    d2.position.set(-38, -24, 27.6); G.add(d2);
    var d3 = new THREE.Mesh(new THREE.SphereGeometry(0.8, 10, 7), hullMat);
    d3.position.set(-52, -20, 27.6); G.add(d3);
    pole(G, 0.25, 0.35, 6.5, -42, -22, 37.8, darkMat);
    var yd1 = new THREE.Mesh(new THREE.CylinderGeometry(0.08, 0.08, 6, 6), darkMat);
    yd1.position.set(-42, -22, 39.6); G.add(yd1);
    var lamp = new THREE.Mesh(new THREE.SphereGeometry(0.3, 8, 6), lampMat);
    lamp.position.set(-42, -22, 41.3); G.add(lamp);

    // whip antennas
    var antGeo = new THREE.CylinderGeometry(0.06, 0.06, 7, 5); antGeo.rotateX(Math.PI / 2);
    var ants = [[60, -31], [0, -31.5], [-100, 36.5], [80, 19]];
    for (i = 0; i < ants.length; i++) {
      var an = new THREE.Mesh(antGeo, darkMat);
      an.position.set(ants[i][0], ants[i][1], deckTop + 3.5); G.add(an);
    }
    // catwalks
    bx(G, 70, 1.8, 0.6, 30, -31.5, 18.1, darkMat);
    bx(G, 40, 1.8, 0.6, -120, 37.3, 18.1, darkMat);
    bx(G, 36, 1.8, 0.6, 115, 19.4, 18.1, darkMat);
    bx(G, 30, 1.8, 0.6, 120, -22.4, 18.1, darkMat);

    // parked aircraft silhouettes (J-15 scale)
    function jet(x, y, rot, sc) {
      var ac = new THREE.Group();
      var o = [[9, 0], [6, 1], [3.2, 1.3], [2.2, 1.5], [0.2, 5], [-1.4, 5.2], [-2.6, 1.7],
        [-4.6, 1.4], [-5.2, 3.4], [-6.6, 3.5], [-6.2, 1.2], [-7.5, 0.9]];
      var pp = [];
      for (k = 0; k < o.length; k++) pp.push(o[k]);
      pp.push([-7.9, 0]);
      for (k = o.length - 1; k >= 1; k--) pp.push([o[k][0], -o[k][1]]);
      ac.add(new THREE.Mesh(M.slab(THREE, pp, 0.7), darkMat));
      var cp = new THREE.Mesh(new THREE.BoxGeometry(2.6, 1.1, 0.8), darkMat);
      cp.position.set(4.5, 0, 1); ac.add(cp);
      var f1 = new THREE.Mesh(new THREE.BoxGeometry(2, 0.16, 1.6), darkMat);
      f1.position.set(-5.8, 1, 1.4); f1.rotation.x = 0.35; ac.add(f1);
      var f2 = new THREE.Mesh(new THREE.BoxGeometry(2, 0.16, 1.6), darkMat);
      f2.position.set(-5.8, -1, 1.4); f2.rotation.x = -0.35; ac.add(f2);
      ac.scale.set(sc, sc, sc); ac.position.set(x, y, deckTop + 0.02); ac.rotation.z = rot;
      G.add(ac);
    }
    jet(-140, -12, 3.0, 1.25); jet(115, -14, 0.3, 1.25); jet(35, -20, -0.5, 1.25);

    return G;
  }
};

/* ---- pack afa7248c ---- */
UNIT_MODELS["spg_n"] = {
  len: 9.1,
  build: function (THREE, M, C) {
    var G = new THREE.Group();
    function camo(base) {
      var cv = document.createElement("canvas"); cv.width = cv.height = 256;
      var g = cv.getContext("2d");
      g.fillStyle = base; g.fillRect(0, 0, 256, 256);
      for (var i = 0; i < 26; i++) {
        g.fillStyle = (i % 2 ? "rgba(255,255,255," : "rgba(0,0,0,") + (0.03 + (i % 5) * 0.012) + ")";
        g.fillRect((i * 53) % 240, (i * 97) % 240, 22 + (i * 31) % 60, 14 + (i * 23) % 44);
      }
      g.strokeStyle = "rgba(0,0,0,0.28)"; g.lineWidth = 1;
      for (i = 1; i < 7; i++) {
        g.beginPath(); g.moveTo(i * 37, 0); g.lineTo(i * 37, 256); g.stroke();
        g.beginPath(); g.moveTo(0, i * 41); g.lineTo(256, i * 41); g.stroke();
      }
      g.fillStyle = "rgba(20,18,10,0.12)";
      for (i = 0; i < 9; i++) g.fillRect((i * 89) % 250, (i * 47) % 180, 3, 26 + (i * 17) % 40);
      var t = new THREE.CanvasTexture(cv); t.wrapS = t.wrapT = THREE.RepeatWrapping; t.repeat.set(2, 2); return t;
    }
    var bodyM = new THREE.MeshStandardMaterial({ map: camo("#4a5240"), metalness: 0.3, roughness: 0.62 });
    var darkM = new THREE.MeshStandardMaterial({ color: 0x24261f, metalness: 0.15, roughness: 0.88 });
    var gunM = new THREE.MeshStandardMaterial({ color: 0x2f322c, metalness: 0.4, roughness: 0.5 });
    var steelM = new THREE.MeshStandardMaterial({ color: 0x555a55, metalness: 0.85, roughness: 0.35 });
    var teamM = new THREE.MeshStandardMaterial({ color: C.team, metalness: 0.2, roughness: 0.6 });
    function put(geo, mat, x, y, z, p) { var m = new THREE.Mesh(geo, mat); m.position.set(x, y, z); (p || G).add(m); return m; }
    function xcyl(r1, r2, l, x, p, mat) { var c = put(new THREE.CylinderGeometry(r1, r2, l, 12), mat || gunM, x, 0, 0, p); c.rotation.z = -Math.PI / 2; return c; }

    // hull
    put(M.loft(THREE, [
      { x: -3.30, w: 1.26, h: 0.56, zc: 1.06, sq: 0.85 },
      { x: -2.80, w: 1.35, h: 0.64, zc: 1.10, sq: 0.85 },
      { x: 2.30, w: 1.35, h: 0.64, zc: 1.10, sq: 0.85 },
      { x: 3.05, w: 1.30, h: 0.40, zc: 1.34, sq: 0.85 },
      { x: 3.30, w: 1.08, h: 0.14, zc: 1.60, sq: 0.80 }
    ], 14), bodyM, 0, 0, 0);

    // tracks, road wheels, fenders, travel-lock legs
    [-1, 1].forEach(function (s) {
      put(new THREE.BoxGeometry(6.5, 0.60, 0.56), darkM, -0.05, s * 1.62, 0.52);
      put(new THREE.BoxGeometry(6.8, 0.66, 0.06), bodyM, -0.05, s * 1.62, 0.86);
      for (var i = 0; i < 7; i++) {
        var wx = -2.45 + i * 0.82;
        put(new THREE.CylinderGeometry(0.31, 0.31, 0.66, 14), darkM, wx, s * 1.62, 0.33);
        put(new THREE.CylinderGeometry(0.13, 0.13, 0.70, 10), steelM, wx, s * 1.62, 0.33);
      }
      put(new THREE.CylinderGeometry(0.24, 0.24, 0.58, 12), steelM, 3.0, s * 1.62, 0.55);
      put(new THREE.CylinderGeometry(0.22, 0.22, 0.58, 12), steelM, -3.05, s * 1.62, 0.52);
      var leg = put(new THREE.CylinderGeometry(0.04, 0.04, 0.9, 8), gunM, 3.05, s * 0.32, 1.95);
      leg.rotation.x = Math.PI / 2 + s * 0.32;
    });
    put(new THREE.BoxGeometry(0.16, 0.5, 0.12), gunM, 3.05, 0, 2.38);
    put(new THREE.BoxGeometry(0.12, 0.18, 0.12), steelM, 3.28, 0.9, 1.62);
    put(new THREE.BoxGeometry(0.12, 0.18, 0.12), steelM, 3.28, -0.9, 1.62);

    // turret - big boxy, set aft
    var T = new THREE.Group(); T.name = "turret"; T.position.set(-0.7, 0, 1.72); G.add(T);
    put(new THREE.BoxGeometry(3.3, 2.95, 1.12), bodyM, 0.0, 0, 0.58, T);
    put(new THREE.BoxGeometry(0.8, 2.4, 0.62), bodyM, 1.85, 0, 0.42, T);
    put(new THREE.BoxGeometry(0.55, 2.6, 0.55), bodyM, -1.9, 0, 0.75, T);
    [-1, 1].forEach(function (s) {
      put(new THREE.BoxGeometry(1.5, 0.26, 0.42), darkM, -0.4, s * 1.58, 0.62, T);
      var a = put(new THREE.CylinderGeometry(0.012, 0.012, 1.6, 5), gunM, -1.5, s * 1.25, 1.9, T);
      a.rotation.x = Math.PI / 2;
    });
    put(new THREE.CylinderGeometry(0.34, 0.36, 0.22, 14), bodyM, -0.5, 0.75, 1.22, T);
    put(new THREE.CylinderGeometry(0.28, 0.28, 0.10, 12), bodyM, -0.5, -0.85, 1.18, T);
    var mg = put(new THREE.CylinderGeometry(0.03, 0.03, 1.15, 6), gunM, 0.1, 0.75, 1.42, T); mg.rotation.z = -Math.PI / 2;
    put(new THREE.BoxGeometry(0.7, 0.02, 0.26), teamM, -0.9, 1.49, 0.62, T);
    put(new THREE.BoxGeometry(0.7, 0.02, 0.26), teamM, -0.9, -1.49, 0.62, T);

    // 155mm M284 with double-baffle muzzle brake
    var gun = new THREE.Group(); gun.position.set(1.55, 0, 0.42); gun.rotation.y = -0.06; T.add(gun);
    xcyl(0.100, 0.125, 2.30, 1.15, gun);
    xcyl(0.148, 0.148, 0.70, 2.30, gun);
    xcyl(0.085, 0.100, 1.95, 3.60, gun);
    xcyl(0.100, 0.100, 0.55, 4.75, gun);
    xcyl(0.185, 0.185, 0.10, 4.62, gun);
    xcyl(0.185, 0.185, 0.10, 4.94, gun);
    return G;
  }
};

UNIT_MODELS["spg_p"] = {
  len: 11.9,
  build: function (THREE, M, C) {
    var G = new THREE.Group();
    function camo(base) {
      var cv = document.createElement("canvas"); cv.width = cv.height = 256;
      var g = cv.getContext("2d");
      g.fillStyle = base; g.fillRect(0, 0, 256, 256);
      for (var i = 0; i < 26; i++) {
        g.fillStyle = (i % 2 ? "rgba(255,255,255," : "rgba(0,0,0,") + (0.03 + (i % 5) * 0.012) + ")";
        g.fillRect((i * 53) % 240, (i * 97) % 240, 22 + (i * 31) % 60, 14 + (i * 23) % 44);
      }
      g.strokeStyle = "rgba(0,0,0,0.28)"; g.lineWidth = 1;
      for (i = 1; i < 7; i++) {
        g.beginPath(); g.moveTo(i * 37, 0); g.lineTo(i * 37, 256); g.stroke();
        g.beginPath(); g.moveTo(0, i * 41); g.lineTo(256, i * 41); g.stroke();
      }
      g.fillStyle = "rgba(20,18,10,0.12)";
      for (i = 0; i < 9; i++) g.fillRect((i * 89) % 250, (i * 47) % 180, 3, 26 + (i * 17) % 40);
      var t = new THREE.CanvasTexture(cv); t.wrapS = t.wrapT = THREE.RepeatWrapping; t.repeat.set(2, 2); return t;
    }
    var bodyM = new THREE.MeshStandardMaterial({ map: camo("#3e4a35"), metalness: 0.3, roughness: 0.62 });
    var darkM = new THREE.MeshStandardMaterial({ color: 0x23251e, metalness: 0.15, roughness: 0.88 });
    var gunM = new THREE.MeshStandardMaterial({ color: 0x2e312b, metalness: 0.4, roughness: 0.5 });
    var steelM = new THREE.MeshStandardMaterial({ color: 0x53584f, metalness: 0.85, roughness: 0.35 });
    var teamM = new THREE.MeshStandardMaterial({ color: C.team, metalness: 0.2, roughness: 0.6 });
    function put(geo, mat, x, y, z, p) { var m = new THREE.Mesh(geo, mat); m.position.set(x, y, z); (p || G).add(m); return m; }
    function xcyl(r1, r2, l, x, p, mat) { var c = put(new THREE.CylinderGeometry(r1, r2, l, 12), mat || gunM, x, 0, 0, p); c.rotation.z = -Math.PI / 2; return c; }

    // T-80 hull, low profile
    put(M.loft(THREE, [
      { x: -3.50, w: 1.02, h: 0.48, zc: 0.95, sq: 0.85 },
      { x: -2.90, w: 1.12, h: 0.54, zc: 0.99, sq: 0.85 },
      { x: 2.40, w: 1.12, h: 0.54, zc: 0.99, sq: 0.85 },
      { x: 3.15, w: 1.06, h: 0.34, zc: 1.19, sq: 0.85 },
      { x: 3.50, w: 0.88, h: 0.12, zc: 1.41, sq: 0.80 }
    ], 14), bodyM, 0, 0, 0);

    // tracks: 6 big road wheels, rear drive sprocket, rubber side skirts
    [-1, 1].forEach(function (s) {
      put(new THREE.BoxGeometry(6.6, 0.56, 0.55), darkM, 0, s * 1.41, 0.50);
      put(new THREE.BoxGeometry(6.5, 0.05, 0.55), bodyM, 0, s * 1.68, 1.00);
      for (var i = 0; i < 6; i++) {
        var wx = -2.6 + i * 1.04;
        put(new THREE.CylinderGeometry(0.34, 0.34, 0.62, 14), darkM, wx, s * 1.41, 0.34);
        put(new THREE.CylinderGeometry(0.15, 0.15, 0.66, 10), steelM, wx, s * 1.41, 0.34);
      }
      put(new THREE.CylinderGeometry(0.23, 0.23, 0.54, 12), steelM, -3.15, s * 1.41, 0.48);
      put(new THREE.CylinderGeometry(0.21, 0.21, 0.54, 12), steelM, 3.12, s * 1.41, 0.50);
    });
    put(new THREE.BoxGeometry(0.5, 0.2, 0.12), gunM, -1.0, 1.52, 1.10);
    put(new THREE.BoxGeometry(0.12, 0.16, 0.10), steelM, 3.48, 0.75, 1.35);
    put(new THREE.BoxGeometry(0.12, 0.16, 0.10), steelM, 3.48, -0.75, 1.35);

    // big rounded turret with rear bustle conveyor
    var T = new THREE.Group(); T.name = "turret"; T.position.set(0.2, 0, 1.52); G.add(T);
    put(M.loft(THREE, [
      { x: -2.05, w: 0.85, h: 0.38, zc: 0.50, sq: 0.60 },
      { x: -1.30, w: 1.42, h: 0.55, zc: 0.55, sq: 0.65 },
      { x: 0.70, w: 1.48, h: 0.55, zc: 0.55, sq: 0.65 },
      { x: 1.50, w: 1.05, h: 0.45, zc: 0.52, sq: 0.60 },
      { x: 1.85, w: 0.60, h: 0.28, zc: 0.50, sq: 0.60 }
    ], 14), bodyM, 0, 0, 0, T);
    put(new THREE.BoxGeometry(0.45, 2.2, 0.42), darkM, -2.25, 0, 0.55, T);
    put(new THREE.CylinderGeometry(0.35, 0.38, 0.24, 14), bodyM, -0.4, 0.72, 1.14, T);
    put(new THREE.CylinderGeometry(0.28, 0.28, 0.12, 12), bodyM, -0.3, -0.82, 1.10, T);
    put(new THREE.BoxGeometry(0.35, 0.32, 0.28), bodyM, 0.85, 0.5, 1.10, T);
    var mg = put(new THREE.CylinderGeometry(0.028, 0.028, 1.0, 6), gunM, 0.0, 0.72, 1.36, T); mg.rotation.z = -Math.PI / 2;
    for (var i = 0; i < 5; i++) {
      var sm = put(new THREE.CylinderGeometry(0.035, 0.035, 0.26, 8), gunM, 1.35, -0.35 - i * 0.17, 0.95, T);
      sm.rotation.set(0, -0.6, -Math.PI / 2);
    }
    var an = put(new THREE.CylinderGeometry(0.012, 0.012, 1.7, 5), gunM, -1.8, 0.95, 1.65, T); an.rotation.x = Math.PI / 2;
    put(new THREE.BoxGeometry(0.7, 0.02, 0.26), teamM, -0.3, 1.46, 0.55, T);
    put(new THREE.BoxGeometry(0.7, 0.02, 0.26), teamM, -0.3, -1.46, 0.55, T);

    // very long 152mm 2A64 with fume extractor and double-baffle brake
    var gun = new THREE.Group(); gun.position.set(1.7, 0, 0.40); gun.rotation.y = -0.07; T.add(gun);
    xcyl(0.115, 0.135, 3.20, 1.60, gun);
    xcyl(0.160, 0.160, 0.85, 2.90, gun);
    xcyl(0.090, 0.105, 2.70, 4.55, gun);
    xcyl(0.105, 0.105, 0.60, 6.20, gun);
    xcyl(0.200, 0.200, 0.11, 6.02, gun);
    xcyl(0.200, 0.200, 0.11, 6.38, gun);
    return G;
  }
};

UNIT_MODELS["spg_c"] = {
  len: 11.5,
  build: function (THREE, M, C) {
    var G = new THREE.Group();
    function camo(base) {
      var cv = document.createElement("canvas"); cv.width = cv.height = 256;
      var g = cv.getContext("2d");
      g.fillStyle = base; g.fillRect(0, 0, 256, 256);
      for (var i = 0; i < 26; i++) {
        g.fillStyle = (i % 2 ? "rgba(255,255,255," : "rgba(0,0,0,") + (0.03 + (i % 5) * 0.012) + ")";
        g.fillRect((i * 53) % 240, (i * 97) % 240, 22 + (i * 31) % 60, 14 + (i * 23) % 44);
      }
      g.strokeStyle = "rgba(0,0,0,0.28)"; g.lineWidth = 1;
      for (i = 1; i < 7; i++) {
        g.beginPath(); g.moveTo(i * 37, 0); g.lineTo(i * 37, 256); g.stroke();
        g.beginPath(); g.moveTo(0, i * 41); g.lineTo(256, i * 41); g.stroke();
      }
      g.fillStyle = "rgba(20,18,10,0.12)";
      for (i = 0; i < 9; i++) g.fillRect((i * 89) % 250, (i * 47) % 180, 3, 26 + (i * 17) % 40);
      var t = new THREE.CanvasTexture(cv); t.wrapS = t.wrapT = THREE.RepeatWrapping; t.repeat.set(2, 2); return t;
    }
    var bodyM = new THREE.MeshStandardMaterial({ map: camo("#4e5844"), metalness: 0.3, roughness: 0.62 });
    var darkM = new THREE.MeshStandardMaterial({ color: 0x25271f, metalness: 0.15, roughness: 0.88 });
    var gunM = new THREE.MeshStandardMaterial({ color: 0x2f322c, metalness: 0.4, roughness: 0.5 });
    var steelM = new THREE.MeshStandardMaterial({ color: 0x565b52, metalness: 0.85, roughness: 0.35 });
    var teamM = new THREE.MeshStandardMaterial({ color: C.team, metalness: 0.2, roughness: 0.6 });
    function put(geo, mat, x, y, z, p) { var m = new THREE.Mesh(geo, mat); m.position.set(x, y, z); (p || G).add(m); return m; }
    function xcyl(r1, r2, l, x, p, mat) { var c = put(new THREE.CylinderGeometry(r1, r2, l, 12), mat || gunM, x, 0, 0, p); c.rotation.z = -Math.PI / 2; return c; }

    // hull
    put(M.loft(THREE, [
      { x: -3.60, w: 1.05, h: 0.50, zc: 1.00, sq: 0.85 },
      { x: -3.00, w: 1.15, h: 0.57, zc: 1.05, sq: 0.85 },
      { x: 2.50, w: 1.15, h: 0.57, zc: 1.05, sq: 0.85 },
      { x: 3.25, w: 1.08, h: 0.36, zc: 1.26, sq: 0.85 },
      { x: 3.60, w: 0.90, h: 0.13, zc: 1.49, sq: 0.80 }
    ], 14), bodyM, 0, 0, 0);

    // tracks and 6 road wheels each side
    [-1, 1].forEach(function (s) {
      put(new THREE.BoxGeometry(6.7, 0.56, 0.56), darkM, 0, s * 1.45, 0.52);
      put(new THREE.BoxGeometry(6.9, 0.62, 0.06), bodyM, 0, s * 1.45, 0.88);
      for (var i = 0; i < 6; i++) {
        var wx = -2.7 + i * 1.08;
        put(new THREE.CylinderGeometry(0.35, 0.35, 0.62, 14), darkM, wx, s * 1.45, 0.35);
        put(new THREE.CylinderGeometry(0.15, 0.15, 0.66, 10), steelM, wx, s * 1.45, 0.35);
      }
      put(new THREE.CylinderGeometry(0.23, 0.23, 0.54, 12), steelM, 3.25, s * 1.45, 0.52);
      put(new THREE.CylinderGeometry(0.22, 0.22, 0.54, 12), steelM, -3.3, s * 1.45, 0.50);
    });
    put(new THREE.BoxGeometry(0.12, 0.16, 0.10), steelM, 3.58, 0.8, 1.42);
    put(new THREE.BoxGeometry(0.12, 0.16, 0.10), steelM, 3.58, -0.8, 1.42);

    // angular welded turret
    var T = new THREE.Group(); T.name = "turret"; T.position.set(-0.3, 0, 1.62); G.add(T);
    put(new THREE.BoxGeometry(3.4, 3.0, 1.05), bodyM, -0.1, 0, 0.55, T);
    var fr = put(new THREE.BoxGeometry(1.0, 2.9, 0.92), bodyM, 1.65, 0, 0.45, T); fr.rotation.y = -0.35;
    put(new THREE.BoxGeometry(0.8, 2.8, 0.90), bodyM, -1.95, 0, 0.50, T);
    [-1, 1].forEach(function (s) {
      put(new THREE.BoxGeometry(2.0, 0.24, 0.5), darkM, -0.6, s * 1.6, 0.55, T);
      var a = put(new THREE.CylinderGeometry(0.012, 0.012, 1.7, 5), gunM, -1.9, s * 1.2, 1.75, T);
      a.rotation.x = Math.PI / 2;
    });
    put(new THREE.CylinderGeometry(0.33, 0.35, 0.22, 14), bodyM, -0.5, 0.72, 1.16, T);
    put(new THREE.CylinderGeometry(0.27, 0.27, 0.12, 12), bodyM, -0.5, -0.80, 1.12, T);
    put(new THREE.BoxGeometry(0.32, 0.32, 0.36), bodyM, 0.45, 0.55, 1.24, T);
    var mg = put(new THREE.CylinderGeometry(0.028, 0.028, 1.05, 6), gunM, 0.0, 0.72, 1.38, T); mg.rotation.z = -Math.PI / 2;
    put(new THREE.BoxGeometry(0.7, 0.02, 0.26), teamM, -0.8, 1.51, 0.55, T);
    put(new THREE.BoxGeometry(0.7, 0.02, 0.26), teamM, -0.8, -1.51, 0.55, T);

    // long 155mm L52 with fume extractor and slotted brake
    var gun = new THREE.Group(); gun.position.set(1.8, 0, 0.45); gun.rotation.y = -0.07; T.add(gun);
    xcyl(0.110, 0.130, 3.00, 1.50, gun);
    xcyl(0.150, 0.150, 0.80, 2.75, gun);
    xcyl(0.085, 0.100, 2.50, 4.40, gun);
    xcyl(0.140, 0.140, 0.75, 6.00, gun);
    xcyl(0.170, 0.170, 0.10, 5.80, gun);
    xcyl(0.170, 0.170, 0.10, 6.10, gun);
    return G;
  }
};

UNIT_MODELS["mlrs_n"] = {
  len: 6.85,
  build: function (THREE, M, C) {
    var G = new THREE.Group();
    function camo(base) {
      var cv = document.createElement("canvas"); cv.width = cv.height = 256;
      var g = cv.getContext("2d");
      g.fillStyle = base; g.fillRect(0, 0, 256, 256);
      for (var i = 0; i < 26; i++) {
        g.fillStyle = (i % 2 ? "rgba(255,255,255," : "rgba(0,0,0,") + (0.03 + (i % 5) * 0.012) + ")";
        g.fillRect((i * 53) % 240, (i * 97) % 240, 22 + (i * 31) % 60, 14 + (i * 23) % 44);
      }
      g.strokeStyle = "rgba(0,0,0,0.28)"; g.lineWidth = 1;
      for (i = 1; i < 7; i++) {
        g.beginPath(); g.moveTo(i * 37, 0); g.lineTo(i * 37, 256); g.stroke();
        g.beginPath(); g.moveTo(0, i * 41); g.lineTo(256, i * 41); g.stroke();
      }
      g.fillStyle = "rgba(20,18,10,0.12)";
      for (i = 0; i < 9; i++) g.fillRect((i * 89) % 250, (i * 47) % 180, 3, 26 + (i * 17) % 40);
      var t = new THREE.CanvasTexture(cv); t.wrapS = t.wrapT = THREE.RepeatWrapping; t.repeat.set(2, 2); return t;
    }
    var bodyM = new THREE.MeshStandardMaterial({ map: camo("#4a5240"), metalness: 0.3, roughness: 0.62 });
    var darkM = new THREE.MeshStandardMaterial({ color: 0x24261f, metalness: 0.15, roughness: 0.88 });
    var gunM = new THREE.MeshStandardMaterial({ color: 0x2f322c, metalness: 0.4, roughness: 0.5 });
    var steelM = new THREE.MeshStandardMaterial({ color: 0x555a55, metalness: 0.85, roughness: 0.35 });
    var glassM = new THREE.MeshStandardMaterial({ color: 0x18242c, metalness: 0.5, roughness: 0.25 });
    var teamM = new THREE.MeshStandardMaterial({ color: C.team, metalness: 0.2, roughness: 0.6 });
    function put(geo, mat, x, y, z, p) { var m = new THREE.Mesh(geo, mat); m.position.set(x, y, z); (p || G).add(m); return m; }

    // Bradley-derived hull
    put(M.loft(THREE, [
      { x: -3.42, w: 0.98, h: 0.50, zc: 1.00, sq: 0.88 },
      { x: -2.90, w: 1.00, h: 0.55, zc: 1.02, sq: 0.88 },
      { x: 2.60, w: 1.00, h: 0.55, zc: 1.02, sq: 0.88 },
      { x: 3.42, w: 0.95, h: 0.30, zc: 1.25, sq: 0.85 }
    ], 14), bodyM, 0, 0, 0);

    // tracks with 6 road wheels
    [-1, 1].forEach(function (s) {
      put(new THREE.BoxGeometry(6.6, 0.50, 0.54), darkM, 0, s * 1.23, 0.50);
      put(new THREE.BoxGeometry(6.7, 0.56, 0.06), bodyM, 0, s * 1.23, 0.84);
      for (var i = 0; i < 6; i++) {
        var wx = -2.5 + i * 1.0;
        put(new THREE.CylinderGeometry(0.30, 0.30, 0.56, 14), darkM, wx, s * 1.23, 0.32);
        put(new THREE.CylinderGeometry(0.13, 0.13, 0.60, 10), steelM, wx, s * 1.23, 0.32);
      }
      put(new THREE.CylinderGeometry(0.22, 0.22, 0.50, 12), steelM, 3.05, s * 1.23, 0.50);
      put(new THREE.CylinderGeometry(0.20, 0.20, 0.50, 12), steelM, -3.1, s * 1.23, 0.48);
      put(new THREE.BoxGeometry(0.7, 0.5, 0.06), bodyM, 3.3, s * 1.23, 1.12);
    });

    // armoured cab at bow with 2 windows
    put(new THREE.BoxGeometry(1.5, 2.5, 1.05), bodyM, 2.6, 0, 2.10);
    put(new THREE.BoxGeometry(0.04, 0.95, 0.60), glassM, 3.36, 0.66, 2.30);
    put(new THREE.BoxGeometry(0.04, 0.95, 0.60), glassM, 3.36, -0.66, 2.30);
    put(new THREE.BoxGeometry(1.0, 2.3, 0.10), bodyM, 2.5, 0, 2.68);
    put(new THREE.BoxGeometry(0.12, 0.18, 0.12), steelM, 3.40, 1.0, 1.55);
    put(new THREE.BoxGeometry(0.12, 0.18, 0.12), steelM, 3.40, -1.0, 1.55);
    var an = put(new THREE.CylinderGeometry(0.012, 0.012, 1.5, 5), gunM, 2.1, 1.1, 3.4); an.rotation.x = Math.PI / 2;

    // the whole rear: one huge armoured launcher box, elevated
    var T = new THREE.Group(); T.name = "turret"; T.position.set(-0.75, 0, 1.62); G.add(T);
    put(new THREE.CylinderGeometry(0.85, 0.95, 0.30, 16), bodyM, 0, 0, -0.05, T);
    var E = new THREE.Group(); E.rotation.y = -0.16; T.add(E);
    put(new THREE.BoxGeometry(4.0, 2.7, 1.75), bodyM, 0.85, 0, 0.75, E);
    put(new THREE.BoxGeometry(0.03, 2.5, 1.55), darkM, 2.845, 0, 0.75, E);
    put(new THREE.BoxGeometry(0.05, 1.15, 1.45), bodyM, 2.88, 0.62, 0.75, E);
    put(new THREE.BoxGeometry(0.05, 1.15, 1.45), bodyM, 2.88, -0.62, 0.75, E);
    [-1, 1].forEach(function (s) {
      for (var i = 0; i < 3; i++) put(new THREE.BoxGeometry(0.35, 0.06, 1.8), bodyM, -0.5 + i * 1.3, s * 1.37, 0.75, E);
    });
    put(new THREE.BoxGeometry(3.2, 0.28, 0.22), bodyM, 0.5, 0, 1.72, E);
    put(new THREE.BoxGeometry(0.7, 0.02, 0.5), teamM, 2.1, 1.39, 0.75, E);
    put(new THREE.BoxGeometry(0.7, 0.02, 0.5), teamM, 2.1, -1.39, 0.75, E);
    return G;
  }
};

UNIT_MODELS["mlrs_p"] = {
  len: 12.1,
  build: function (THREE, M, C) {
    var G = new THREE.Group();
    function camo(base) {
      var cv = document.createElement("canvas"); cv.width = cv.height = 256;
      var g = cv.getContext("2d");
      g.fillStyle = base; g.fillRect(0, 0, 256, 256);
      for (var i = 0; i < 26; i++) {
        g.fillStyle = (i % 2 ? "rgba(255,255,255," : "rgba(0,0,0,") + (0.03 + (i % 5) * 0.012) + ")";
        g.fillRect((i * 53) % 240, (i * 97) % 240, 22 + (i * 31) % 60, 14 + (i * 23) % 44);
      }
      g.strokeStyle = "rgba(0,0,0,0.28)"; g.lineWidth = 1;
      for (i = 1; i < 7; i++) {
        g.beginPath(); g.moveTo(i * 37, 0); g.lineTo(i * 37, 256); g.stroke();
        g.beginPath(); g.moveTo(0, i * 41); g.lineTo(256, i * 41); g.stroke();
      }
      g.fillStyle = "rgba(20,18,10,0.12)";
      for (i = 0; i < 9; i++) g.fillRect((i * 89) % 250, (i * 47) % 180, 3, 26 + (i * 17) % 40);
      var t = new THREE.CanvasTexture(cv); t.wrapS = t.wrapT = THREE.RepeatWrapping; t.repeat.set(2, 2); return t;
    }
    var bodyM = new THREE.MeshStandardMaterial({ map: camo("#3e4a35"), metalness: 0.3, roughness: 0.62 });
    var darkM = new THREE.MeshStandardMaterial({ color: 0x1c1e18, metalness: 0.1, roughness: 0.9 });
    var gunM = new THREE.MeshStandardMaterial({ color: 0x2e312b, metalness: 0.4, roughness: 0.5 });
    var steelM = new THREE.MeshStandardMaterial({ color: 0x53584f, metalness: 0.85, roughness: 0.35 });
    var glassM = new THREE.MeshStandardMaterial({ color: 0x18242c, metalness: 0.5, roughness: 0.25 });
    var tubeEndM = new THREE.MeshStandardMaterial({ color: 0x0d0f0c, metalness: 0.1, roughness: 0.9 });
    var teamM = new THREE.MeshStandardMaterial({ color: C.team, metalness: 0.2, roughness: 0.6 });
    function put(geo, mat, x, y, z, p) { var m = new THREE.Mesh(geo, mat); m.position.set(x, y, z); (p || G).add(m); return m; }

    // MAZ-543M 8x8 chassis
    put(new THREE.BoxGeometry(11.9, 1.3, 0.45), bodyM, 0, 0, 0.95);
    put(new THREE.BoxGeometry(6.6, 2.9, 0.30), bodyM, -2.4, 0, 1.42);
    [4.55, 3.15, -2.45, -3.85].forEach(function (ax) {
      [-1, 1].forEach(function (s) {
        put(new THREE.CylinderGeometry(0.63, 0.63, 0.46, 16), darkM, ax, s * 1.27, 0.63);
        put(new THREE.CylinderGeometry(0.22, 0.22, 0.50, 10), steelM, ax, s * 1.27, 0.63);
      });
    });
    [-1, 1].forEach(function (s) {
      put(new THREE.BoxGeometry(3.2, 0.5, 0.08), bodyM, 3.85, s * 1.27, 1.40);
      put(new THREE.BoxGeometry(3.2, 0.5, 0.08), bodyM, -3.15, s * 1.27, 1.40);
      put(new THREE.BoxGeometry(0.06, 0.45, 0.40), darkM, -4.6, s * 1.27, 0.35);
    });

    // cab
    put(new THREE.BoxGeometry(2.0, 2.85, 1.55), bodyM, 4.95, 0, 2.15);
    put(new THREE.BoxGeometry(0.3, 2.9, 0.5), bodyM, 5.9, 0, 0.85);
    put(new THREE.BoxGeometry(0.04, 1.05, 0.62), glassM, 5.96, 0.68, 2.50);
    put(new THREE.BoxGeometry(0.04, 1.05, 0.62), glassM, 5.96, -0.68, 2.50);
    put(new THREE.BoxGeometry(0.8, 0.04, 0.5), glassM, 4.6, 1.44, 2.45);
    put(new THREE.BoxGeometry(0.8, 0.04, 0.5), glassM, 4.6, -1.44, 2.45);
    put(new THREE.BoxGeometry(0.12, 0.2, 0.14), steelM, 5.97, 1.1, 1.35);
    put(new THREE.BoxGeometry(0.12, 0.2, 0.14), steelM, 5.97, -1.1, 1.35);
    put(new THREE.BoxGeometry(0.5, 0.03, 0.4), teamM, 4.4, 1.44, 1.85);
    put(new THREE.BoxGeometry(0.5, 0.03, 0.4), teamM, 4.4, -1.44, 1.85);
    var an = put(new THREE.CylinderGeometry(0.012, 0.012, 1.6, 5), gunM, 4.3, 1.2, 3.7); an.rotation.x = Math.PI / 2;

    // engine deck and equipment bay behind cab
    put(new THREE.BoxGeometry(1.5, 2.6, 0.80), bodyM, 3.2, 0, 1.75);
    put(new THREE.BoxGeometry(1.8, 2.7, 0.70), bodyM, 1.6, 0, 1.65);

    // bank of 12 launch tubes on turntable
    var T = new THREE.Group(); T.name = "turret"; T.position.set(-2.5, 0, 1.75); G.add(T);
    put(new THREE.CylinderGeometry(0.95, 1.05, 0.40, 16), bodyM, 0, 0, -0.12, T);
    [-1, 1].forEach(function (s) {
      var ram = put(new THREE.CylinderGeometry(0.05, 0.05, 1.5, 8), steelM, 0.9, s * 1.0, 0.55, T);
      ram.rotation.set(0, -0.55, -Math.PI / 2);
    });
    var E = new THREE.Group(); E.rotation.y = -0.12; T.add(E);
    put(new THREE.BoxGeometry(2.4, 2.3, 0.30), bodyM, -1.5, 0, 0.15, E);
    put(new THREE.BoxGeometry(7.2, 0.05, 1.05), bodyM, 0.7, 1.1, 0.60, E);
    put(new THREE.BoxGeometry(7.2, 0.05, 1.05), bodyM, 0.7, -1.1, 0.60, E);
    put(new THREE.BoxGeometry(7.2, 2.25, 0.05), bodyM, 0.7, 0, 1.00, E);
    for (var r = 0; r < 2; r++) {
      for (var j = 0; j < 6; j++) {
        var ty = -0.9 + j * 0.36, tz = 0.42 + r * 0.36;
        var tb = put(new THREE.CylinderGeometry(0.16, 0.16, 7.2, 10), bodyM, 0.7, ty, tz, E);
        tb.rotation.z = -Math.PI / 2;
        var d1 = put(new THREE.CircleGeometry(0.135, 12), tubeEndM, -2.905, ty, tz, E);
        d1.rotation.y = -Math.PI / 2;
        var d2 = put(new THREE.CircleGeometry(0.135, 12), tubeEndM, 4.305, ty, tz, E);
        d2.rotation.y = Math.PI / 2;
      }
    }
    return G;
  }
};

UNIT_MODELS["mlrs_c"] = {
  len: 12,
  build: function (THREE, M, C) {
    var G = new THREE.Group();
    function camo(base) {
      var cv = document.createElement("canvas"); cv.width = cv.height = 256;
      var g = cv.getContext("2d");
      g.fillStyle = base; g.fillRect(0, 0, 256, 256);
      for (var i = 0; i < 26; i++) {
        g.fillStyle = (i % 2 ? "rgba(255,255,255," : "rgba(0,0,0,") + (0.03 + (i % 5) * 0.012) + ")";
        g.fillRect((i * 53) % 240, (i * 97) % 240, 22 + (i * 31) % 60, 14 + (i * 23) % 44);
      }
      g.strokeStyle = "rgba(0,0,0,0.28)"; g.lineWidth = 1;
      for (i = 1; i < 7; i++) {
        g.beginPath(); g.moveTo(i * 37, 0); g.lineTo(i * 37, 256); g.stroke();
        g.beginPath(); g.moveTo(0, i * 41); g.lineTo(256, i * 41); g.stroke();
      }
      g.fillStyle = "rgba(20,18,10,0.12)";
      for (i = 0; i < 9; i++) g.fillRect((i * 89) % 250, (i * 47) % 180, 3, 26 + (i * 17) % 40);
      var t = new THREE.CanvasTexture(cv); t.wrapS = t.wrapT = THREE.RepeatWrapping; t.repeat.set(2, 2); return t;
    }
    function tubeFace() {
      var cv = document.createElement("canvas"); cv.width = 256; cv.height = 128;
      var g = cv.getContext("2d");
      g.fillStyle = "#3a4231"; g.fillRect(0, 0, 256, 128);
      g.strokeStyle = "rgba(0,0,0,0.3)"; g.lineWidth = 1;
      g.strokeRect(4, 4, 248, 120);
      for (var r = 0; r < 2; r++) {
        for (var i = 0; i < 6; i++) {
          var cx = 22 + i * 42.5, cy = 40 + r * 52;
          g.beginPath(); g.arc(cx, cy, 17, 0, Math.PI * 2);
          g.fillStyle = "#0e100d"; g.fill();
          g.lineWidth = 3; g.strokeStyle = "#5a6349"; g.stroke(); g.lineWidth = 1;
        }
      }
      var t = new THREE.CanvasTexture(cv); return t;
    }
    var bodyM = new THREE.MeshStandardMaterial({ map: camo("#4e5844"), metalness: 0.3, roughness: 0.62 });
    var darkM = new THREE.MeshStandardMaterial({ color: 0x1d1f19, metalness: 0.1, roughness: 0.9 });
    var gunM = new THREE.MeshStandardMaterial({ color: 0x2f322c, metalness: 0.4, roughness: 0.5 });
    var steelM = new THREE.MeshStandardMaterial({ color: 0x565b52, metalness: 0.85, roughness: 0.35 });
    var glassM = new THREE.MeshStandardMaterial({ color: 0x18242c, metalness: 0.5, roughness: 0.25 });
    var faceM = new THREE.MeshStandardMaterial({ map: tubeFace(), metalness: 0.3, roughness: 0.6 });
    var teamM = new THREE.MeshStandardMaterial({ color: C.team, metalness: 0.2, roughness: 0.6 });
    function put(geo, mat, x, y, z, p) { var m = new THREE.Mesh(geo, mat); m.position.set(x, y, z); (p || G).add(m); return m; }

    // WS2400 8x8 chassis
    put(new THREE.BoxGeometry(11.8, 1.3, 0.45), bodyM, 0, 0, 0.95);
    put(new THREE.BoxGeometry(6.6, 2.9, 0.30), bodyM, -2.3, 0, 1.42);
    [4.5, 3.1, -2.4, -3.8].forEach(function (ax) {
      [-1, 1].forEach(function (s) {
        put(new THREE.CylinderGeometry(0.62, 0.62, 0.46, 16), darkM, ax, s * 1.27, 0.62);
        put(new THREE.CylinderGeometry(0.22, 0.22, 0.50, 10), steelM, ax, s * 1.27, 0.62);
      });
    });
    [-1, 1].forEach(function (s) {
      put(new THREE.BoxGeometry(3.1, 0.5, 0.08), bodyM, 3.8, s * 1.27, 1.38);
      put(new THREE.BoxGeometry(3.1, 0.5, 0.08), bodyM, -3.1, s * 1.27, 1.38);
      put(new THREE.BoxGeometry(0.06, 0.45, 0.40), darkM, -4.55, s * 1.27, 0.34);
    });

    // angular cab with raked windshield
    put(new THREE.BoxGeometry(2.1, 2.9, 1.00), bodyM, 4.85, 0, 1.60);
    put(new THREE.BoxGeometry(1.7, 2.9, 0.90), bodyM, 4.65, 0, 2.55);
    var ws = put(new THREE.BoxGeometry(0.05, 2.5, 0.75), glassM, 5.45, 0, 2.60); ws.rotation.y = -0.25;
    put(new THREE.BoxGeometry(0.8, 0.04, 0.5), glassM, 4.5, 1.46, 2.55);
    put(new THREE.BoxGeometry(0.8, 0.04, 0.5), glassM, 4.5, -1.46, 2.55);
    put(new THREE.BoxGeometry(0.3, 2.9, 0.45), bodyM, 5.75, 0, 0.85);
    put(new THREE.BoxGeometry(0.12, 0.2, 0.14), steelM, 5.91, 1.1, 1.30);
    put(new THREE.BoxGeometry(0.12, 0.2, 0.14), steelM, 5.91, -1.1, 1.30);
    put(new THREE.BoxGeometry(0.5, 0.03, 0.4), teamM, 4.4, 1.46, 1.70);
    put(new THREE.BoxGeometry(0.5, 0.03, 0.4), teamM, 4.4, -1.46, 1.70);
    var an = put(new THREE.CylinderGeometry(0.012, 0.012, 1.6, 5), gunM, 4.1, 1.2, 3.7); an.rotation.x = Math.PI / 2;

    // deck behind cab
    put(new THREE.BoxGeometry(1.5, 2.6, 0.75), bodyM, 3.1, 0, 1.70);
    put(new THREE.BoxGeometry(1.7, 2.7, 0.65), bodyM, 1.5, 0, 1.62);

    // angular armoured tube bank, ends painted as two rows of dark circles
    var T = new THREE.Group(); T.name = "turret"; T.position.set(-2.4, 0, 1.75); G.add(T);
    put(new THREE.CylinderGeometry(0.95, 1.05, 0.40, 16), bodyM, 0, 0, -0.12, T);
    [-1, 1].forEach(function (s) {
      var ram = put(new THREE.CylinderGeometry(0.05, 0.05, 1.5, 8), steelM, 0.9, s * 1.0, 0.55, T);
      ram.rotation.set(0, -0.55, -Math.PI / 2);
    });
    var E = new THREE.Group(); E.rotation.y = -0.12; T.add(E);
    put(new THREE.BoxGeometry(7.2, 2.4, 1.5), bodyM, 0.7, 0, 0.60, E);
    put(new THREE.BoxGeometry(2.4, 2.5, 0.25), bodyM, -1.4, 0, -0.12, E);
    [-1, 1].forEach(function (s) {
      for (var i = 0; i < 3; i++) put(new THREE.BoxGeometry(0.3, 0.05, 1.6), bodyM, -1.6 + i * 2.3, s * 1.22, 0.60, E);
    });
    var f1 = put(new THREE.PlaneGeometry(2.3, 1.35), faceM, -2.91, 0, 0.60, E);
    f1.rotation.set(Math.PI / 2, -Math.PI / 2, 0);
    var f2 = put(new THREE.PlaneGeometry(2.3, 1.35), faceM, 4.31, 0, 0.60, E);
    f2.rotation.set(Math.PI / 2, Math.PI / 2, 0);
    return G;
  }
};

/* ---- pack a3b10485 ---- */
UNIT_MODELS["mbt_n"] = {
  len: 9.8,
  build: function (THREE, M, C) {
    var g = new THREE.Group();
    function tex(base, blotch) {
      var cv = document.createElement("canvas"); cv.width = 256; cv.height = 256;
      var p = cv.getContext("2d"); p.fillStyle = base; p.fillRect(0, 0, 256, 256);
      var i, x, y, gr;
      if (blotch) {
        for (i = 0; i < 6; i++) {
          x = Math.random() * 256; y = Math.random() * 256;
          p.fillStyle = "rgba(28,36,24,0.20)";
          p.beginPath(); p.ellipse(x, y, 30 + Math.random() * 40, 18 + Math.random() * 26, Math.random() * 3, 0, 6.29); p.fill();
        }
      }
      for (i = 0; i < 24; i++) {
        p.fillStyle = "rgba(" + (Math.random() < 0.5 ? "0,0,0" : "255,255,255") + "," + (0.03 + Math.random() * 0.05).toFixed(3) + ")";
        p.fillRect(Math.random() * 236, Math.random() * 236, 18 + Math.random() * 60, 12 + Math.random() * 46);
      }
      p.strokeStyle = "rgba(0,0,0,0.22)"; p.lineWidth = 1;
      for (i = 0; i < 5; i++) { y = 20 + Math.random() * 216; p.beginPath(); p.moveTo(0, y); p.lineTo(256, y); p.stroke(); }
      for (i = 0; i < 7; i++) { x = 16 + Math.random() * 224; p.beginPath(); p.moveTo(x, 0); p.lineTo(x, 256); p.stroke(); }
      for (i = 0; i < 8; i++) {
        x = Math.random() * 256; y = Math.random() * 256;
        gr = p.createRadialGradient(x, y, 2, x, y, 12 + Math.random() * 20);
        gr.addColorStop(0, "rgba(20,18,10,0.12)"); gr.addColorStop(1, "rgba(20,18,10,0)");
        p.fillStyle = gr; p.fillRect(x - 34, y - 34, 68, 68);
      }
      var t = new THREE.CanvasTexture(cv); t.wrapS = t.wrapT = THREE.RepeatWrapping; return t;
    }
    var hullMat = new THREE.MeshStandardMaterial({ map: tex("#6b6c51"), metalness: 0.25, roughness: 0.62 });
    hullMat.map.repeat.set(2, 1);
    var turMat = new THREE.MeshStandardMaterial({ map: tex("#6b6c51"), metalness: 0.25, roughness: 0.62 });
    var darkMat = new THREE.MeshStandardMaterial({ color: 0x25261f, metalness: 0.2, roughness: 0.9 });
    var gunMat = new THREE.MeshStandardMaterial({ color: 0x585a44, metalness: 0.35, roughness: 0.55 });
    var teamMat = new THREE.MeshStandardMaterial({ color: C.team, metalness: 0.2, roughness: 0.55 });
    function bx(parent, sx, sy, sz, px, py, pz, mat) {
      var m = new THREE.Mesh(new THREE.BoxGeometry(sx, sy, sz), mat || hullMat);
      m.position.set(px, py, pz); parent.add(m); return m;
    }
    function cyx(parent, r, len, px, py, pz, mat, seg) {
      var m = new THREE.Mesh(new THREE.CylinderGeometry(r, r, len, seg || 14), mat || gunMat);
      m.rotation.z = Math.PI / 2; m.position.set(px, py, pz); parent.add(m); return m;
    }
    function czu(parent, r, len, px, py, pz, mat, seg) {
      var m = new THREE.Mesh(new THREE.CylinderGeometry(r, r, len, seg || 14), mat || gunMat);
      m.rotation.x = Math.PI / 2; m.position.set(px, py, pz); parent.add(m); return m;
    }
    function gear(o) {
      var s, i, w, hb, sp, id;
      var wgeo = new THREE.CylinderGeometry(o.r, o.r, 0.5, 16);
      var hgeo = new THREE.CylinderGeometry(o.r * 0.4, o.r * 0.4, 0.54, 10);
      var sgeo = new THREE.CylinderGeometry(o.r * 0.85, o.r * 0.85, 0.46, 12);
      for (s = -1; s <= 1; s += 2) {
        bx(g, o.tl, o.tw, 0.78, o.tx, o.ty * s, 0.53, darkMat);
        for (i = 0; i < o.n; i++) {
          w = new THREE.Mesh(wgeo, darkMat); w.position.set(o.x0 + i * o.dx, o.ty * s, o.r + 0.1); g.add(w);
          hb = new THREE.Mesh(hgeo, gunMat); hb.position.set(o.x0 + i * o.dx, o.ty * s, o.r + 0.1); g.add(hb);
        }
        sp = new THREE.Mesh(sgeo, darkMat); sp.position.set(o.sx, o.ty * s, 0.46); g.add(sp);
        id = new THREE.Mesh(sgeo, darkMat); id.position.set(o.ix, o.ty * s, 0.46); g.add(id);
        /* Side skirts are not one continuous plate. Photographs of an Abrams
           or a T-90 show a row of separate armoured panels with clear vertical
           joins, each hanging to about half wheel height — one of the strongest
           cues that a tracked vehicle is armoured rather than a plain box. */
        var segN = o.segs || 6, segW = o.sl / segN;
        for (var sg = 0; sg < segN; sg++) {
          var sgx = o.scx - o.sl / 2 + segW * (sg + 0.5);
          bx(g, segW * 0.92, o.st, o.sh, sgx, o.sy * s, o.sz, hullMat);
          bx(g, segW * 0.92, o.st * 1.7, 0.07, sgx, o.sy * s, o.sz + o.sh / 2, darkMat);
        }
        bx(g, o.sl + 0.3, 0.55, 0.06, o.scx, (o.sy - 0.2) * s, o.sz + o.sh / 2 + 0.03, hullMat);
      }
    }
    g.add(new THREE.Mesh(M.loft(THREE, [
      { x: -3.95, w: 1.5, h: 0.4, zc: 1.0, sq: 0.85 },
      { x: -3.5, w: 1.62, h: 0.55, zc: 1.0, sq: 0.85 },
      { x: 1.7, w: 1.62, h: 0.55, zc: 1.0, sq: 0.85 },
      { x: 3.2, w: 1.62, h: 0.34, zc: 0.92, sq: 0.85 },
      { x: 3.95, w: 1.35, h: 0.14, zc: 0.68, sq: 0.85 }
    ], 10), hullMat));
    gear({ n: 7, r: 0.33, x0: -2.5, dx: 0.82, tx: -0.1, tl: 7.3, tw: 0.66, ty: 1.48, sx: -3.2, ix: 3.2, sl: 6.7, scx: 0.1, sh: 0.6, sz: 0.95, st: 0.05, sy: 1.83 });
    bx(g, 0.08, 2.4, 0.65, -3.97, 0, 1.05, darkMat);
    bx(g, 0.14, 0.2, 0.14, 3.72, 1.05, 1.2, gunMat); bx(g, 0.14, 0.2, 0.14, 3.72, -1.05, 1.2, gunMat);
    bx(g, 0.2, 0.16, 0.2, 3.9, 0.55, 0.75, gunMat); bx(g, 0.2, 0.16, 0.2, 3.9, -0.55, 0.75, gunMat);
    var dh = czu(g, 0.3, 0.05, 2.55, 0, 1.44, hullMat); dh.rotation.y = 0.35;
    var tur = new THREE.Group(); tur.name = "turret"; tur.position.set(0.2, 0, 1.5); g.add(tur);
    tur.add(new THREE.Mesh(M.loft(THREE, [
      { x: -1.9, w: 1.3, h: 0.34, zc: 0.36, sq: 0.9 },
      { x: -0.3, w: 1.62, h: 0.4, zc: 0.4, sq: 0.9 },
      { x: 0.7, w: 1.55, h: 0.4, zc: 0.4, sq: 0.9 },
      { x: 1.6, w: 0.6, h: 0.26, zc: 0.34, sq: 0.9 }
    ], 10), turMat));
    bx(tur, 2.2, 2.4, 0.06, -0.35, 0, 0.72, turMat);
    bx(tur, 0.45, 0.8, 0.45, 1.72, 0, 0.38, turMat);
    cyx(tur, 0.085, 3.9, 3.75, 0, 0.4, gunMat);
    cyx(tur, 0.125, 0.6, 4.4, 0, 0.4, gunMat);
    cyx(tur, 0.105, 0.12, 2.6, 0, 0.4, darkMat); cyx(tur, 0.105, 0.12, 3.35, 0, 0.4, darkMat);
    bx(tur, 0.18, 0.1, 0.1, 5.55, 0, 0.4, darkMat);
    bx(tur, 0.55, 0.5, 0.28, 0.75, -0.5, 0.89, turMat);
    bx(tur, 0.04, 0.34, 0.16, 1.04, -0.5, 0.9, darkMat);
    czu(tur, 0.14, 0.3, 0.5, 0.55, 0.9, turMat); bx(tur, 0.24, 0.24, 0.2, 0.5, 0.55, 1.14, darkMat);
    czu(tur, 0.07, 0.3, -0.1, -0.75, 0.9, gunMat);
    bx(tur, 0.4, 0.32, 0.3, -0.1, -0.75, 1.18, darkMat);
    cyx(tur, 0.03, 1.0, 0.5, -0.75, 1.2, gunMat, 8);
    bx(tur, 0.14, 0.18, 0.18, 0.14, -0.62, 1.2, gunMat);
    czu(tur, 0.32, 0.05, -0.1, -0.75, 0.77, turMat);
    czu(tur, 0.3, 0.05, -0.35, 0.6, 0.77, turMat);
    czu(tur, 0.015, 0.7, -1.5, 0.5, 1.05, gunMat, 6);
    czu(tur, 0.012, 1.3, -1.7, 1.05, 1.3, darkMat, 6);
    czu(tur, 0.012, 1.3, -1.7, -1.05, 1.3, darkMat, 6);
    var sm1 = bx(tur, 0.18, 0.5, 0.22, 1.2, 0.95, 0.5, gunMat); sm1.rotation.z = 0.5;
    var sm2 = bx(tur, 0.18, 0.5, 0.22, 1.2, -0.95, 0.5, gunMat); sm2.rotation.z = -0.5;
    bx(tur, 0.05, 2.6, 0.35, -2.6, 0, 0.55, gunMat);
    bx(tur, 0.75, 0.05, 0.35, -2.25, 1.28, 0.55, gunMat);
    bx(tur, 0.75, 0.05, 0.35, -2.25, -1.28, 0.55, gunMat);
    bx(tur, 0.7, 2.5, 0.04, -2.25, 0, 0.4, gunMat);
    bx(tur, 0.55, 0.8, 0.35, -2.25, -0.55, 0.6, darkMat);
    bx(tur, 0.5, 0.7, 0.3, -2.25, 0.35, 0.57, gunMat);
    var duf = new THREE.Mesh(new THREE.SphereGeometry(0.26, 8, 6), darkMat);
    duf.scale.set(1.4, 1, 0.8); duf.position.set(-2.2, 1.0, 0.6); tur.add(duf);
    bx(tur, 0.7, 0.04, 0.32, -0.6, 1.6, 0.42, teamMat);
    bx(tur, 0.7, 0.04, 0.32, -0.6, -1.6, 0.42, teamMat);
    return g;
  }
};
UNIT_MODELS["hvy_n"] = {
  len: 9.8,
  build: function (THREE, M, C) {
    var g = new THREE.Group();
    function tex(base, blotch) {
      var cv = document.createElement("canvas"); cv.width = 256; cv.height = 256;
      var p = cv.getContext("2d"); p.fillStyle = base; p.fillRect(0, 0, 256, 256);
      var i, x, y, gr;
      if (blotch) {
        for (i = 0; i < 6; i++) {
          x = Math.random() * 256; y = Math.random() * 256;
          p.fillStyle = "rgba(28,36,24,0.20)";
          p.beginPath(); p.ellipse(x, y, 30 + Math.random() * 40, 18 + Math.random() * 26, Math.random() * 3, 0, 6.29); p.fill();
        }
      }
      for (i = 0; i < 24; i++) {
        p.fillStyle = "rgba(" + (Math.random() < 0.5 ? "0,0,0" : "255,255,255") + "," + (0.03 + Math.random() * 0.05).toFixed(3) + ")";
        p.fillRect(Math.random() * 236, Math.random() * 236, 18 + Math.random() * 60, 12 + Math.random() * 46);
      }
      p.strokeStyle = "rgba(0,0,0,0.22)"; p.lineWidth = 1;
      for (i = 0; i < 5; i++) { y = 20 + Math.random() * 216; p.beginPath(); p.moveTo(0, y); p.lineTo(256, y); p.stroke(); }
      for (i = 0; i < 7; i++) { x = 16 + Math.random() * 224; p.beginPath(); p.moveTo(x, 0); p.lineTo(x, 256); p.stroke(); }
      for (i = 0; i < 8; i++) {
        x = Math.random() * 256; y = Math.random() * 256;
        gr = p.createRadialGradient(x, y, 2, x, y, 12 + Math.random() * 20);
        gr.addColorStop(0, "rgba(20,18,10,0.12)"); gr.addColorStop(1, "rgba(20,18,10,0)");
        p.fillStyle = gr; p.fillRect(x - 34, y - 34, 68, 68);
      }
      var t = new THREE.CanvasTexture(cv); t.wrapS = t.wrapT = THREE.RepeatWrapping; return t;
    }
    var hullMat = new THREE.MeshStandardMaterial({ map: tex("#6f6b4f"), metalness: 0.25, roughness: 0.62 });
    hullMat.map.repeat.set(2, 1);
    var turMat = new THREE.MeshStandardMaterial({ map: tex("#6f6b4f"), metalness: 0.25, roughness: 0.62 });
    var darkMat = new THREE.MeshStandardMaterial({ color: 0x25261f, metalness: 0.2, roughness: 0.9 });
    var gunMat = new THREE.MeshStandardMaterial({ color: 0x5a5a45, metalness: 0.35, roughness: 0.55 });
    var teamMat = new THREE.MeshStandardMaterial({ color: C.team, metalness: 0.2, roughness: 0.55 });
    function bx(parent, sx, sy, sz, px, py, pz, mat) {
      var m = new THREE.Mesh(new THREE.BoxGeometry(sx, sy, sz), mat || hullMat);
      m.position.set(px, py, pz); parent.add(m); return m;
    }
    function cyx(parent, r, len, px, py, pz, mat, seg) {
      var m = new THREE.Mesh(new THREE.CylinderGeometry(r, r, len, seg || 14), mat || gunMat);
      m.rotation.z = Math.PI / 2; m.position.set(px, py, pz); parent.add(m); return m;
    }
    function czu(parent, r, len, px, py, pz, mat, seg) {
      var m = new THREE.Mesh(new THREE.CylinderGeometry(r, r, len, seg || 14), mat || gunMat);
      m.rotation.x = Math.PI / 2; m.position.set(px, py, pz); parent.add(m); return m;
    }
    function gear(o) {
      var s, i, w, hb, sp, id;
      var wgeo = new THREE.CylinderGeometry(o.r, o.r, 0.5, 16);
      var hgeo = new THREE.CylinderGeometry(o.r * 0.4, o.r * 0.4, 0.54, 10);
      var sgeo = new THREE.CylinderGeometry(o.r * 0.85, o.r * 0.85, 0.46, 12);
      for (s = -1; s <= 1; s += 2) {
        bx(g, o.tl, o.tw, 0.78, o.tx, o.ty * s, 0.53, darkMat);
        for (i = 0; i < o.n; i++) {
          w = new THREE.Mesh(wgeo, darkMat); w.position.set(o.x0 + i * o.dx, o.ty * s, o.r + 0.1); g.add(w);
          hb = new THREE.Mesh(hgeo, gunMat); hb.position.set(o.x0 + i * o.dx, o.ty * s, o.r + 0.1); g.add(hb);
        }
        sp = new THREE.Mesh(sgeo, darkMat); sp.position.set(o.sx, o.ty * s, 0.46); g.add(sp);
        id = new THREE.Mesh(sgeo, darkMat); id.position.set(o.ix, o.ty * s, 0.46); g.add(id);
        /* Side skirts are not one continuous plate. Photographs of an Abrams
           or a T-90 show a row of separate armoured panels with clear vertical
           joins, each hanging to about half wheel height — one of the strongest
           cues that a tracked vehicle is armoured rather than a plain box. */
        var segN = o.segs || 6, segW = o.sl / segN;
        for (var sg = 0; sg < segN; sg++) {
          var sgx = o.scx - o.sl / 2 + segW * (sg + 0.5);
          bx(g, segW * 0.92, o.st, o.sh, sgx, o.sy * s, o.sz, hullMat);
          bx(g, segW * 0.92, o.st * 1.7, 0.07, sgx, o.sy * s, o.sz + o.sh / 2, darkMat);
        }
        bx(g, o.sl + 0.3, 0.55, 0.06, o.scx, (o.sy - 0.2) * s, o.sz + o.sh / 2 + 0.03, hullMat);
      }
    }
    g.add(new THREE.Mesh(M.loft(THREE, [
      { x: -3.95, w: 1.5, h: 0.4, zc: 1.0, sq: 0.85 },
      { x: -3.5, w: 1.62, h: 0.55, zc: 1.0, sq: 0.85 },
      { x: 1.7, w: 1.62, h: 0.55, zc: 1.0, sq: 0.85 },
      { x: 3.2, w: 1.62, h: 0.34, zc: 0.92, sq: 0.85 },
      { x: 3.95, w: 1.35, h: 0.14, zc: 0.68, sq: 0.85 }
    ], 10), hullMat));
    gear({ n: 7, r: 0.33, x0: -2.5, dx: 0.82, tx: -0.1, tl: 7.3, tw: 0.66, ty: 1.48, sx: -3.2, ix: 3.2, sl: 6.8, scx: 0.1, sh: 0.8, sz: 0.88, st: 0.08, sy: 1.84 });
    bx(g, 0.08, 2.4, 0.65, -3.97, 0, 1.05, darkMat);
    bx(g, 0.5, 2.6, 0.5, -4.05, 0, 1.25, darkMat);
    bx(g, 0.14, 0.2, 0.14, 3.72, 1.05, 1.2, gunMat); bx(g, 0.14, 0.2, 0.14, 3.72, -1.05, 1.2, gunMat);
    bx(g, 0.2, 0.16, 0.2, 3.9, 0.55, 0.75, gunMat); bx(g, 0.2, 0.16, 0.2, 3.9, -0.55, 0.75, gunMat);
    var dh = czu(g, 0.3, 0.05, 2.55, 0, 1.44, hullMat); dh.rotation.y = 0.35;
    var tur = new THREE.Group(); tur.name = "turret"; tur.position.set(0.2, 0, 1.5); g.add(tur);
    tur.add(new THREE.Mesh(M.loft(THREE, [
      { x: -1.9, w: 1.3, h: 0.34, zc: 0.36, sq: 0.9 },
      { x: -0.3, w: 1.62, h: 0.4, zc: 0.4, sq: 0.9 },
      { x: 0.7, w: 1.55, h: 0.4, zc: 0.4, sq: 0.9 },
      { x: 1.6, w: 0.6, h: 0.26, zc: 0.34, sq: 0.9 }
    ], 10), turMat));
    bx(tur, 2.2, 2.4, 0.06, -0.35, 0, 0.72, turMat);
    bx(tur, 0.45, 0.8, 0.45, 1.72, 0, 0.38, turMat);
    cyx(tur, 0.085, 3.9, 3.75, 0, 0.4, gunMat);
    cyx(tur, 0.125, 0.6, 4.4, 0, 0.4, gunMat);
    cyx(tur, 0.105, 0.12, 2.6, 0, 0.4, darkMat); cyx(tur, 0.105, 0.12, 3.35, 0, 0.4, darkMat);
    bx(tur, 0.18, 0.1, 0.1, 5.55, 0, 0.4, darkMat);
    var s2, tro, trp, trl;
    for (s2 = -1; s2 <= 1; s2 += 2) {
      tro = bx(tur, 1.2, 0.1, 0.7, -0.2, 1.62 * s2, 0.42, gunMat);
      trp = bx(tur, 1.05, 0.22, 0.72, -0.15, 1.8 * s2, 0.44, darkMat);
      trp.rotation.z = -0.1 * s2;
      trl = bx(tur, 0.38, 0.3, 0.16, 0.15, 1.72 * s2, 0.88, gunMat);
      trl.rotation.x = 0.4 * s2;
    }
    bx(tur, 0.55, 0.5, 0.28, 0.75, -0.5, 0.89, turMat);
    bx(tur, 0.04, 0.34, 0.16, 1.04, -0.5, 0.9, darkMat);
    czu(tur, 0.14, 0.3, 0.5, 0.55, 0.9, turMat); bx(tur, 0.24, 0.24, 0.2, 0.5, 0.55, 1.14, darkMat);
    czu(tur, 0.07, 0.3, -0.1, -0.75, 0.9, gunMat);
    bx(tur, 0.4, 0.32, 0.3, -0.1, -0.75, 1.18, darkMat);
    cyx(tur, 0.03, 1.0, 0.5, -0.75, 1.2, gunMat, 8);
    bx(tur, 0.14, 0.18, 0.18, 0.14, -0.62, 1.2, gunMat);
    czu(tur, 0.32, 0.05, -0.1, -0.75, 0.77, turMat);
    czu(tur, 0.3, 0.05, -0.35, 0.6, 0.77, turMat);
    czu(tur, 0.015, 0.7, -1.5, 0.5, 1.05, gunMat, 6);
    czu(tur, 0.012, 1.3, -1.7, 1.05, 1.3, darkMat, 6);
    czu(tur, 0.012, 1.3, -1.7, -1.05, 1.3, darkMat, 6);
    var sm1 = bx(tur, 0.18, 0.5, 0.22, 1.2, 0.95, 0.5, gunMat); sm1.rotation.z = 0.5;
    var sm2 = bx(tur, 0.18, 0.5, 0.22, 1.2, -0.95, 0.5, gunMat); sm2.rotation.z = -0.5;
    bx(tur, 0.05, 2.6, 0.35, -2.6, 0, 0.55, gunMat);
    bx(tur, 0.75, 0.05, 0.35, -2.25, 1.28, 0.55, gunMat);
    bx(tur, 0.75, 0.05, 0.35, -2.25, -1.28, 0.55, gunMat);
    bx(tur, 0.7, 2.5, 0.04, -2.25, 0, 0.4, gunMat);
    bx(tur, 0.55, 0.8, 0.35, -2.25, -0.55, 0.62, darkMat);
    bx(tur, 0.5, 0.7, 0.3, -2.25, 0.35, 0.58, gunMat);
    bx(tur, 0.45, 0.6, 0.4, -2.2, 1.0, 0.62, darkMat);
    bx(tur, 0.35, 0.5, 0.3, -1.6, -1.35, 0.55, darkMat);
    var duf = new THREE.Mesh(new THREE.SphereGeometry(0.26, 8, 6), darkMat);
    duf.scale.set(1.4, 1, 0.8); duf.position.set(-1.55, 1.3, 0.55); tur.add(duf);
    bx(tur, 0.7, 0.04, 0.3, -1.15, 1.42, 0.45, teamMat);
    bx(tur, 0.7, 0.04, 0.3, -1.15, -1.42, 0.45, teamMat);
    return g;
  }
};
UNIT_MODELS["mbt_p"] = {
  len: 9.6,
  build: function (THREE, M, C) {
    var g = new THREE.Group();
    function tex(base, blotch) {
      var cv = document.createElement("canvas"); cv.width = 256; cv.height = 256;
      var p = cv.getContext("2d"); p.fillStyle = base; p.fillRect(0, 0, 256, 256);
      var i, x, y, gr;
      if (blotch) {
        for (i = 0; i < 6; i++) {
          x = Math.random() * 256; y = Math.random() * 256;
          p.fillStyle = "rgba(28,36,24,0.20)";
          p.beginPath(); p.ellipse(x, y, 30 + Math.random() * 40, 18 + Math.random() * 26, Math.random() * 3, 0, 6.29); p.fill();
        }
      }
      for (i = 0; i < 24; i++) {
        p.fillStyle = "rgba(" + (Math.random() < 0.5 ? "0,0,0" : "255,255,255") + "," + (0.03 + Math.random() * 0.05).toFixed(3) + ")";
        p.fillRect(Math.random() * 236, Math.random() * 236, 18 + Math.random() * 60, 12 + Math.random() * 46);
      }
      p.strokeStyle = "rgba(0,0,0,0.22)"; p.lineWidth = 1;
      for (i = 0; i < 5; i++) { y = 20 + Math.random() * 216; p.beginPath(); p.moveTo(0, y); p.lineTo(256, y); p.stroke(); }
      for (i = 0; i < 7; i++) { x = 16 + Math.random() * 224; p.beginPath(); p.moveTo(x, 0); p.lineTo(x, 256); p.stroke(); }
      for (i = 0; i < 8; i++) {
        x = Math.random() * 256; y = Math.random() * 256;
        gr = p.createRadialGradient(x, y, 2, x, y, 12 + Math.random() * 20);
        gr.addColorStop(0, "rgba(20,18,10,0.12)"); gr.addColorStop(1, "rgba(20,18,10,0)");
        p.fillStyle = gr; p.fillRect(x - 34, y - 34, 68, 68);
      }
      var t = new THREE.CanvasTexture(cv); t.wrapS = t.wrapT = THREE.RepeatWrapping; return t;
    }
    var hullMat = new THREE.MeshStandardMaterial({ map: tex("#43503a", true), metalness: 0.25, roughness: 0.62 });
    hullMat.map.repeat.set(2, 1);
    var turMat = new THREE.MeshStandardMaterial({ map: tex("#43503a", true), metalness: 0.25, roughness: 0.62 });
    var darkMat = new THREE.MeshStandardMaterial({ color: 0x24261e, metalness: 0.2, roughness: 0.9 });
    var gunMat = new THREE.MeshStandardMaterial({ color: 0x4a5240, metalness: 0.35, roughness: 0.55 });
    var teamMat = new THREE.MeshStandardMaterial({ color: C.team, metalness: 0.2, roughness: 0.55 });
    function bx(parent, sx, sy, sz, px, py, pz, mat) {
      var m = new THREE.Mesh(new THREE.BoxGeometry(sx, sy, sz), mat || hullMat);
      m.position.set(px, py, pz); parent.add(m); return m;
    }
    function cyx(parent, r, len, px, py, pz, mat, seg) {
      var m = new THREE.Mesh(new THREE.CylinderGeometry(r, r, len, seg || 14), mat || gunMat);
      m.rotation.z = Math.PI / 2; m.position.set(px, py, pz); parent.add(m); return m;
    }
    function czu(parent, r, len, px, py, pz, mat, seg) {
      var m = new THREE.Mesh(new THREE.CylinderGeometry(r, r, len, seg || 14), mat || gunMat);
      m.rotation.x = Math.PI / 2; m.position.set(px, py, pz); parent.add(m); return m;
    }
    function gear(o) {
      var s, i, w, hb, sp, id;
      var wgeo = new THREE.CylinderGeometry(o.r, o.r, 0.5, 16);
      var hgeo = new THREE.CylinderGeometry(o.r * 0.4, o.r * 0.4, 0.54, 10);
      var sgeo = new THREE.CylinderGeometry(o.r * 0.85, o.r * 0.85, 0.46, 12);
      for (s = -1; s <= 1; s += 2) {
        bx(g, o.tl, o.tw, 0.72, o.tx, o.ty * s, 0.5, darkMat);
        for (i = 0; i < o.n; i++) {
          w = new THREE.Mesh(wgeo, darkMat); w.position.set(o.x0 + i * o.dx, o.ty * s, o.r + 0.1); g.add(w);
          hb = new THREE.Mesh(hgeo, gunMat); hb.position.set(o.x0 + i * o.dx, o.ty * s, o.r + 0.1); g.add(hb);
        }
        sp = new THREE.Mesh(sgeo, darkMat); sp.position.set(o.sx, o.ty * s, 0.46); g.add(sp);
        id = new THREE.Mesh(sgeo, darkMat); id.position.set(o.ix, o.ty * s, 0.46); g.add(id);
        /* Side skirts are not one continuous plate. Photographs of an Abrams
           or a T-90 show a row of separate armoured panels with clear vertical
           joins, each hanging to about half wheel height — one of the strongest
           cues that a tracked vehicle is armoured rather than a plain box. */
        var segN = o.segs || 6, segW = o.sl / segN;
        for (var sg = 0; sg < segN; sg++) {
          var sgx = o.scx - o.sl / 2 + segW * (sg + 0.5);
          bx(g, segW * 0.92, o.st, o.sh, sgx, o.sy * s, o.sz, hullMat);
          bx(g, segW * 0.92, o.st * 1.7, 0.07, sgx, o.sy * s, o.sz + o.sh / 2, darkMat);
        }
        bx(g, o.sl + 0.3, 0.55, 0.06, o.scx, (o.sy - 0.2) * s, o.sz + o.sh / 2 + 0.03, hullMat);
      }
    }
    g.add(new THREE.Mesh(M.loft(THREE, [
      { x: -3.43, w: 1.5, h: 0.4, zc: 0.92, sq: 0.85 },
      { x: -3.0, w: 1.58, h: 0.48, zc: 0.94, sq: 0.85 },
      { x: 1.6, w: 1.58, h: 0.48, zc: 0.94, sq: 0.85 },
      { x: 3.43, w: 1.32, h: 0.15, zc: 0.62, sq: 0.85 }
    ], 10), hullMat));
    gear({ n: 6, r: 0.37, x0: -2.2, dx: 0.88, tx: 0, tl: 6.5, tw: 0.64, ty: 1.55, sx: -2.95, ix: 2.95, sl: 6.2, scx: 0.1, sh: 0.5, sz: 0.9, st: 0.05, sy: 1.87 });
    var k, sgn, eb;
    for (k = 0; k < 3; k++) {
      for (sgn = -1; sgn <= 1; sgn += 2) {
        eb = bx(g, 0.55, 0.3, 0.1, 1.9 + k * 0.5, 0.55 * sgn, 1.32 - k * 0.17, hullMat);
        eb.rotation.y = 0.34; eb.rotation.z = 0.35 * sgn;
      }
    }
    bx(g, 0.08, 2.2, 0.55, -3.44, 0, 1.0, darkMat);
    var log = new THREE.Mesh(new THREE.CylinderGeometry(0.14, 0.14, 2.6, 10), darkMat);
    log.position.set(-3.35, 0, 1.2); g.add(log);
    cyx(g, 0.3, 1.0, -3.8, 0.85, 1.15, gunMat, 12);
    cyx(g, 0.3, 1.0, -3.8, -0.85, 1.15, gunMat, 12);
    bx(g, 0.14, 0.2, 0.14, 3.3, 1.0, 1.15, gunMat); bx(g, 0.14, 0.2, 0.14, 3.3, -1.0, 1.15, gunMat);
    var dh = czu(g, 0.28, 0.05, 2.35, 0, 1.28, hullMat); dh.rotation.y = 0.34;
    var tur = new THREE.Group(); tur.name = "turret"; tur.position.set(0.3, 0, 1.4); g.add(tur);
    var dome = new THREE.Mesh(new THREE.SphereGeometry(1.15, 24, 14), turMat);
    dome.scale.set(1.15, 1.0, 0.5); dome.position.z = 0.12; tur.add(dome);
    var a, r2;
    for (a = -1.2; a <= 1.21; a += 0.3) {
      eb = new THREE.Mesh(new THREE.BoxGeometry(0.13, 0.34, 0.42), turMat);
      eb.rotation.order = "ZYX"; eb.rotation.y = -0.5; eb.rotation.z = a;
      eb.position.set(Math.cos(a) * 1.22, Math.sin(a) * 1.22, 0.2); tur.add(eb);
    }
    bx(tur, 0.24, 0.22, 0.22, 1.15, 0.5, 0.33, darkMat);
    bx(tur, 0.24, 0.22, 0.22, 1.15, -0.5, 0.33, darkMat);
    bx(tur, 0.06, 0.12, 0.12, 1.28, 0.5, 0.35, gunMat);
    bx(tur, 0.06, 0.12, 0.12, 1.28, -0.5, 0.35, gunMat);
    bx(tur, 0.5, 0.5, 0.4, 1.2, 0, 0.22, turMat);
    cyx(tur, 0.08, 4.7, 3.55, 0, 0.3, gunMat);
    cyx(tur, 0.115, 0.55, 3.5, 0, 0.3, gunMat);
    cyx(tur, 0.1, 0.12, 2.1, 0, 0.3, darkMat);
    bx(tur, 0.3, 0.35, 0.35, 0.9, -0.6, 0.5, darkMat);
    czu(tur, 0.3, 0.18, -0.4, 0.45, 0.7, turMat);
    czu(tur, 0.26, 0.04, -0.4, 0.45, 0.82, gunMat);
    bx(tur, 0.32, 0.28, 0.2, 0.5, 0.35, 0.66, turMat);
    bx(tur, 0.03, 0.2, 0.12, 0.68, 0.35, 0.68, darkMat);
    var sn = new THREE.Mesh(new THREE.CylinderGeometry(0.07, 0.07, 1.1, 10), gunMat);
    sn.position.set(-1.05, 0, 0.62); tur.add(sn);
    bx(tur, 0.5, 1.2, 0.35, -1.25, 0, 0.3, darkMat);
    czu(tur, 0.012, 1.2, -0.9, -0.7, 1.1, darkMat, 6);
    var sm, q;
    for (q = 0; q < 3; q++) {
      sm = czu(tur, 0.04, 0.35, 0.55 + q * 0.12, 0.95 - q * 0.06, 0.35, gunMat, 8);
      sm.rotation.z = 0.6; sm.rotation.x = 1.1;
      sm = czu(tur, 0.04, 0.35, 0.55 + q * 0.12, -0.95 + q * 0.06, 0.35, gunMat, 8);
      sm.rotation.z = -0.6; sm.rotation.x = 1.1;
    }
    bx(tur, 0.6, 0.04, 0.28, -0.2, 1.12, 0.28, teamMat);
    bx(tur, 0.6, 0.04, 0.28, -0.2, -1.12, 0.28, teamMat);
    return g;
  }
};
UNIT_MODELS["hvy_p"] = {
  len: 10.8,
  build: function (THREE, M, C) {
    var g = new THREE.Group();
    function tex(base, blotch) {
      var cv = document.createElement("canvas"); cv.width = 256; cv.height = 256;
      var p = cv.getContext("2d"); p.fillStyle = base; p.fillRect(0, 0, 256, 256);
      var i, x, y, gr;
      if (blotch) {
        for (i = 0; i < 6; i++) {
          x = Math.random() * 256; y = Math.random() * 256;
          p.fillStyle = "rgba(28,36,24,0.20)";
          p.beginPath(); p.ellipse(x, y, 30 + Math.random() * 40, 18 + Math.random() * 26, Math.random() * 3, 0, 6.29); p.fill();
        }
      }
      for (i = 0; i < 24; i++) {
        p.fillStyle = "rgba(" + (Math.random() < 0.5 ? "0,0,0" : "255,255,255") + "," + (0.03 + Math.random() * 0.05).toFixed(3) + ")";
        p.fillRect(Math.random() * 236, Math.random() * 236, 18 + Math.random() * 60, 12 + Math.random() * 46);
      }
      p.strokeStyle = "rgba(0,0,0,0.22)"; p.lineWidth = 1;
      for (i = 0; i < 5; i++) { y = 20 + Math.random() * 216; p.beginPath(); p.moveTo(0, y); p.lineTo(256, y); p.stroke(); }
      for (i = 0; i < 7; i++) { x = 16 + Math.random() * 224; p.beginPath(); p.moveTo(x, 0); p.lineTo(x, 256); p.stroke(); }
      for (i = 0; i < 8; i++) {
        x = Math.random() * 256; y = Math.random() * 256;
        gr = p.createRadialGradient(x, y, 2, x, y, 12 + Math.random() * 20);
        gr.addColorStop(0, "rgba(20,18,10,0.12)"); gr.addColorStop(1, "rgba(20,18,10,0)");
        p.fillStyle = gr; p.fillRect(x - 34, y - 34, 68, 68);
      }
      var t = new THREE.CanvasTexture(cv); t.wrapS = t.wrapT = THREE.RepeatWrapping; return t;
    }
    var hullMat = new THREE.MeshStandardMaterial({ map: tex("#3e4a35"), metalness: 0.25, roughness: 0.62 });
    hullMat.map.repeat.set(2, 1);
    var turMat = new THREE.MeshStandardMaterial({ map: tex("#3e4a35"), metalness: 0.25, roughness: 0.62 });
    var darkMat = new THREE.MeshStandardMaterial({ color: 0x24261e, metalness: 0.2, roughness: 0.9 });
    var gunMat = new THREE.MeshStandardMaterial({ color: 0x46503c, metalness: 0.35, roughness: 0.55 });
    var teamMat = new THREE.MeshStandardMaterial({ color: C.team, metalness: 0.2, roughness: 0.55 });
    function bx(parent, sx, sy, sz, px, py, pz, mat) {
      var m = new THREE.Mesh(new THREE.BoxGeometry(sx, sy, sz), mat || hullMat);
      m.position.set(px, py, pz); parent.add(m); return m;
    }
    function cyx(parent, r, len, px, py, pz, mat, seg) {
      var m = new THREE.Mesh(new THREE.CylinderGeometry(r, r, len, seg || 14), mat || gunMat);
      m.rotation.z = Math.PI / 2; m.position.set(px, py, pz); parent.add(m); return m;
    }
    function czu(parent, r, len, px, py, pz, mat, seg) {
      var m = new THREE.Mesh(new THREE.CylinderGeometry(r, r, len, seg || 14), mat || gunMat);
      m.rotation.x = Math.PI / 2; m.position.set(px, py, pz); parent.add(m); return m;
    }
    function gear(o) {
      var s, i, w, hb, sp, id;
      var wgeo = new THREE.CylinderGeometry(o.r, o.r, 0.5, 16);
      var hgeo = new THREE.CylinderGeometry(o.r * 0.4, o.r * 0.4, 0.54, 10);
      var sgeo = new THREE.CylinderGeometry(o.r * 0.85, o.r * 0.85, 0.46, 12);
      for (s = -1; s <= 1; s += 2) {
        bx(g, o.tl, o.tw, 0.78, o.tx, o.ty * s, 0.53, darkMat);
        for (i = 0; i < o.n; i++) {
          w = new THREE.Mesh(wgeo, darkMat); w.position.set(o.x0 + i * o.dx, o.ty * s, o.r + 0.1); g.add(w);
          hb = new THREE.Mesh(hgeo, gunMat); hb.position.set(o.x0 + i * o.dx, o.ty * s, o.r + 0.1); g.add(hb);
        }
        sp = new THREE.Mesh(sgeo, darkMat); sp.position.set(o.sx, o.ty * s, 0.46); g.add(sp);
        id = new THREE.Mesh(sgeo, darkMat); id.position.set(o.ix, o.ty * s, 0.46); g.add(id);
        /* Side skirts are not one continuous plate. Photographs of an Abrams
           or a T-90 show a row of separate armoured panels with clear vertical
           joins, each hanging to about half wheel height — one of the strongest
           cues that a tracked vehicle is armoured rather than a plain box. */
        var segN = o.segs || 6, segW = o.sl / segN;
        for (var sg = 0; sg < segN; sg++) {
          var sgx = o.scx - o.sl / 2 + segW * (sg + 0.5);
          bx(g, segW * 0.92, o.st, o.sh, sgx, o.sy * s, o.sz, hullMat);
          bx(g, segW * 0.92, o.st * 1.7, 0.07, sgx, o.sy * s, o.sz + o.sh / 2, darkMat);
        }
        bx(g, o.sl + 0.3, 0.55, 0.06, o.scx, (o.sy - 0.2) * s, o.sz + o.sh / 2 + 0.03, hullMat);
      }
    }
    g.add(new THREE.Mesh(M.loft(THREE, [
      { x: -4.35, w: 1.55, h: 0.42, zc: 1.1, sq: 0.85 },
      { x: -3.9, w: 1.66, h: 0.55, zc: 1.12, sq: 0.85 },
      { x: 2.2, w: 1.66, h: 0.55, zc: 1.12, sq: 0.85 },
      { x: 3.3, w: 1.66, h: 0.4, zc: 1.05, sq: 0.85 },
      { x: 4.35, w: 1.35, h: 0.16, zc: 0.78, sq: 0.85 }
    ], 10), hullMat));
    gear({ n: 7, r: 0.35, x0: -2.5, dx: 0.8, tx: -0.05, tl: 7.8, tw: 0.68, ty: 1.6, sx: -3.5, ix: 3.5, sl: 7.4, scx: -0.1, sh: 0.75, sz: 0.88, st: 0.06, sy: 1.92 });
    bx(g, 0.7, 0.07, 0.8, 3.6, 1.9, 0.95, darkMat);
    bx(g, 0.7, 0.07, 0.8, 3.6, -1.9, 0.95, darkMat);
    bx(g, 0.08, 2.4, 0.6, -4.36, 0, 1.15, darkMat);
    czu(g, 0.28, 0.05, 2.9, -0.6, 1.7, hullMat);
    czu(g, 0.28, 0.05, 2.9, 0, 1.7, hullMat);
    czu(g, 0.28, 0.05, 2.9, 0.6, 1.7, hullMat);
    bx(g, 0.18, 0.5, 0.08, 3.45, 0, 1.66, darkMat);
    bx(g, 0.14, 0.2, 0.14, 4.05, 1.1, 1.1, gunMat); bx(g, 0.14, 0.2, 0.14, 4.05, -1.1, 1.1, gunMat);
    var tur = new THREE.Group(); tur.name = "turret"; tur.position.set(-0.2, 0, 1.65); g.add(tur);
    var sh = new THREE.Shape();
    var pts = [[1.55, 0.65], [0.7, 1.3], [-1.6, 1.3], [-2.15, 0.9], [-2.15, -0.9], [-1.6, -1.3], [0.7, -1.3], [1.55, -0.65]];
    sh.moveTo(pts[0][0], pts[0][1]);
    var i2;
    for (i2 = 1; i2 < pts.length; i2++) sh.lineTo(pts[i2][0], pts[i2][1]);
    tur.add(new THREE.Mesh(new THREE.ExtrudeGeometry(sh, { depth: 0.95, bevelEnabled: false }), turMat));
    bx(tur, 3.1, 2.0, 0.12, -0.35, 0, 1.0, turMat);
    var ch1 = bx(tur, 3.4, 0.5, 0.06, -0.45, 1.18, 0.85, turMat); ch1.rotation.x = 0.7;
    var ch2 = bx(tur, 3.4, 0.5, 0.06, -0.45, -1.18, 0.85, turMat); ch2.rotation.x = -0.7;
    var fp = bx(tur, 0.8, 1.6, 0.08, 1.35, 0, 0.85, turMat); fp.rotation.y = 0.55;
    czu(tur, 0.035, 1.0, -1.5, 0.55, 1.45, gunMat, 8);
    bx(tur, 0.26, 0.2, 0.18, -1.5, 0.55, 2.0, darkMat);
    czu(tur, 0.14, 0.35, 0.2, -0.6, 1.2, gunMat);
    bx(tur, 0.22, 0.22, 0.22, 0.2, -0.6, 1.45, darkMat);
    bx(tur, 0.35, 0.45, 0.3, 1.15, 0.45, 0.75, darkMat);
    czu(tur, 0.015, 0.8, -1.9, -0.7, 1.4, gunMat, 6);
    czu(tur, 0.012, 1.2, -2.0, 1.0, 1.6, darkMat, 6);
    czu(tur, 0.012, 1.2, -2.0, -1.0, 1.6, darkMat, 6);
    var s3, i3, tube, fa;
    for (s3 = -1; s3 <= 1; s3 += 2) {
      for (i3 = 0; i3 < 5; i3++) {
        tube = new THREE.Mesh(new THREE.CylinderGeometry(0.055, 0.055, 0.5, 8), gunMat);
        fa = s3 * (0.25 + i3 * 0.28);
        tube.rotation.z = -Math.PI / 2 + fa;
        tube.position.set(Math.cos(fa) * 1.35 + 0.35, Math.sin(fa) * 1.35, 0.1);
        tur.add(tube);
      }
    }
    bx(tur, 0.6, 0.7, 0.6, 1.75, 0, 0.45, turMat);
    cyx(tur, 0.09, 4.6, 4.3, 0, 0.45, gunMat);
    cyx(tur, 0.13, 0.9, 2.4, 0, 0.45, gunMat);
    cyx(tur, 0.1, 0.12, 5.2, 0, 0.45, darkMat);
    bx(tur, 0.6, 0.04, 0.4, -1.8, 1.32, 0.5, teamMat);
    bx(tur, 0.6, 0.04, 0.4, -1.8, -1.32, 0.5, teamMat);
    return g;
  }
};
UNIT_MODELS["mbt_c"] = {
  len: 11.0,
  build: function (THREE, M, C) {
    var g = new THREE.Group();
    function tex(base, blotch) {
      var cv = document.createElement("canvas"); cv.width = 256; cv.height = 256;
      var p = cv.getContext("2d"); p.fillStyle = base; p.fillRect(0, 0, 256, 256);
      var i, x, y, gr;
      if (blotch) {
        for (i = 0; i < 6; i++) {
          x = Math.random() * 256; y = Math.random() * 256;
          p.fillStyle = "rgba(28,36,24,0.20)";
          p.beginPath(); p.ellipse(x, y, 30 + Math.random() * 40, 18 + Math.random() * 26, Math.random() * 3, 0, 6.29); p.fill();
        }
      }
      for (i = 0; i < 24; i++) {
        p.fillStyle = "rgba(" + (Math.random() < 0.5 ? "0,0,0" : "255,255,255") + "," + (0.03 + Math.random() * 0.05).toFixed(3) + ")";
        p.fillRect(Math.random() * 236, Math.random() * 236, 18 + Math.random() * 60, 12 + Math.random() * 46);
      }
      p.strokeStyle = "rgba(0,0,0,0.22)"; p.lineWidth = 1;
      for (i = 0; i < 5; i++) { y = 20 + Math.random() * 216; p.beginPath(); p.moveTo(0, y); p.lineTo(256, y); p.stroke(); }
      for (i = 0; i < 7; i++) { x = 16 + Math.random() * 224; p.beginPath(); p.moveTo(x, 0); p.lineTo(x, 256); p.stroke(); }
      for (i = 0; i < 8; i++) {
        x = Math.random() * 256; y = Math.random() * 256;
        gr = p.createRadialGradient(x, y, 2, x, y, 12 + Math.random() * 20);
        gr.addColorStop(0, "rgba(20,18,10,0.12)"); gr.addColorStop(1, "rgba(20,18,10,0)");
        p.fillStyle = gr; p.fillRect(x - 34, y - 34, 68, 68);
      }
      var t = new THREE.CanvasTexture(cv); t.wrapS = t.wrapT = THREE.RepeatWrapping; return t;
    }
    var hullMat = new THREE.MeshStandardMaterial({ map: tex("#4e5844", true), metalness: 0.25, roughness: 0.62 });
    hullMat.map.repeat.set(2, 1);
    var turMat = new THREE.MeshStandardMaterial({ map: tex("#4e5844", true), metalness: 0.25, roughness: 0.62 });
    var darkMat = new THREE.MeshStandardMaterial({ color: 0x24261e, metalness: 0.2, roughness: 0.9 });
    var gunMat = new THREE.MeshStandardMaterial({ color: 0x525c48, metalness: 0.35, roughness: 0.55 });
    var teamMat = new THREE.MeshStandardMaterial({ color: C.team, metalness: 0.2, roughness: 0.55 });
    function bx(parent, sx, sy, sz, px, py, pz, mat) {
      var m = new THREE.Mesh(new THREE.BoxGeometry(sx, sy, sz), mat || hullMat);
      m.position.set(px, py, pz); parent.add(m); return m;
    }
    function cyx(parent, r, len, px, py, pz, mat, seg) {
      var m = new THREE.Mesh(new THREE.CylinderGeometry(r, r, len, seg || 14), mat || gunMat);
      m.rotation.z = Math.PI / 2; m.position.set(px, py, pz); parent.add(m); return m;
    }
    function czu(parent, r, len, px, py, pz, mat, seg) {
      var m = new THREE.Mesh(new THREE.CylinderGeometry(r, r, len, seg || 14), mat || gunMat);
      m.rotation.x = Math.PI / 2; m.position.set(px, py, pz); parent.add(m); return m;
    }
    function gear(o) {
      var s, i, w, hb, sp, id;
      var wgeo = new THREE.CylinderGeometry(o.r, o.r, 0.5, 16);
      var hgeo = new THREE.CylinderGeometry(o.r * 0.4, o.r * 0.4, 0.54, 10);
      var sgeo = new THREE.CylinderGeometry(o.r * 0.85, o.r * 0.85, 0.46, 12);
      for (s = -1; s <= 1; s += 2) {
        bx(g, o.tl, o.tw, 0.75, o.tx, o.ty * s, 0.52, darkMat);
        for (i = 0; i < o.n; i++) {
          w = new THREE.Mesh(wgeo, darkMat); w.position.set(o.x0 + i * o.dx, o.ty * s, o.r + 0.1); g.add(w);
          hb = new THREE.Mesh(hgeo, gunMat); hb.position.set(o.x0 + i * o.dx, o.ty * s, o.r + 0.1); g.add(hb);
        }
        sp = new THREE.Mesh(sgeo, darkMat); sp.position.set(o.sx, o.ty * s, 0.46); g.add(sp);
        id = new THREE.Mesh(sgeo, darkMat); id.position.set(o.ix, o.ty * s, 0.46); g.add(id);
        /* Side skirts are not one continuous plate. Photographs of an Abrams
           or a T-90 show a row of separate armoured panels with clear vertical
           joins, each hanging to about half wheel height — one of the strongest
           cues that a tracked vehicle is armoured rather than a plain box. */
        var segN = o.segs || 6, segW = o.sl / segN;
        for (var sg = 0; sg < segN; sg++) {
          var sgx = o.scx - o.sl / 2 + segW * (sg + 0.5);
          bx(g, segW * 0.92, o.st, o.sh, sgx, o.sy * s, o.sz, hullMat);
          bx(g, segW * 0.92, o.st * 1.7, 0.07, sgx, o.sy * s, o.sz + o.sh / 2, darkMat);
        }
        bx(g, o.sl + 0.3, 0.55, 0.06, o.scx, (o.sy - 0.2) * s, o.sz + o.sh / 2 + 0.03, hullMat);
      }
    }
    g.add(new THREE.Mesh(M.loft(THREE, [
      { x: -3.8, w: 1.48, h: 0.4, zc: 0.95, sq: 0.85 },
      { x: -3.3, w: 1.56, h: 0.5, zc: 1.0, sq: 0.85 },
      { x: 2.0, w: 1.56, h: 0.5, zc: 1.0, sq: 0.85 },
      { x: 3.8, w: 1.3, h: 0.15, zc: 0.66, sq: 0.85 }
    ], 10), hullMat));
    gear({ n: 6, r: 0.37, x0: -2.2, dx: 0.88, tx: 0, tl: 6.9, tw: 0.64, ty: 1.52, sx: -3.15, ix: 3.05, sl: 6.5, scx: 0, sh: 0.55, sz: 0.92, st: 0.05, sy: 1.84 });
    var k, sgn, eb;
    for (k = 0; k < 2; k++) {
      for (sgn = -1; sgn <= 1; sgn += 2) {
        eb = bx(g, 0.6, 0.32, 0.1, 2.35 + k * 0.6, 0.45 * sgn, 1.42 - k * 0.23, hullMat);
        eb.rotation.y = 0.37; eb.rotation.z = 0.4 * sgn;
        eb = bx(g, 0.6, 0.32, 0.1, 2.35 + k * 0.6, 1.05 * sgn, 1.42 - k * 0.23, hullMat);
        eb.rotation.y = 0.37; eb.rotation.z = 0.4 * sgn;
      }
    }
    bx(g, 0.08, 2.2, 0.55, -3.81, 0, 1.05, darkMat);
    bx(g, 0.14, 0.2, 0.14, 3.55, 1.0, 1.12, gunMat); bx(g, 0.14, 0.2, 0.14, 3.55, -1.0, 1.12, gunMat);
    var dh = czu(g, 0.28, 0.05, 2.6, 0.45, 1.35, hullMat); dh.rotation.y = 0.37;
    var tur = new THREE.Group(); tur.name = "turret"; tur.position.set(0.3, 0, 1.48); g.add(tur);
    var sh = new THREE.Shape();
    var pts = [[1.3, 0.5], [0.2, 1.35], [-1.4, 1.35], [-1.75, 1.0], [-1.75, -1.0], [-1.4, -1.35], [0.2, -1.35], [1.3, -0.5]];
    sh.moveTo(pts[0][0], pts[0][1]);
    var i2;
    for (i2 = 1; i2 < pts.length; i2++) sh.lineTo(pts[i2][0], pts[i2][1]);
    tur.add(new THREE.Mesh(new THREE.ExtrudeGeometry(sh, { depth: 0.7, bevelEnabled: false }), turMat));
    var wg1 = bx(tur, 1.7, 0.12, 0.72, 1.2, 0.6, 0.35, turMat);
    wg1.rotation.order = "ZYX"; wg1.rotation.x = 0.35; wg1.rotation.z = -0.7;
    var wg2 = bx(tur, 1.7, 0.12, 0.72, 1.2, -0.6, 0.35, turMat);
    wg2.rotation.order = "ZYX"; wg2.rotation.x = -0.35; wg2.rotation.z = 0.7;
    bx(tur, 0.28, 0.26, 0.4, -0.5, 0.85, 0.9, darkMat);
    cyx(tur, 0.09, 0.12, -0.32, 0.85, 0.98, darkMat);
    czu(tur, 0.12, 0.4, -0.4, -0.35, 0.9, gunMat);
    bx(tur, 0.2, 0.2, 0.18, -0.4, -0.35, 1.18, darkMat);
    bx(tur, 0.4, 0.45, 0.22, 0.55, -0.55, 0.81, turMat);
    bx(tur, 0.04, 0.3, 0.14, 0.78, -0.55, 0.83, darkMat);
    czu(tur, 0.3, 0.05, -0.9, 0.5, 0.72, turMat);
    czu(tur, 0.28, 0.05, -0.3, -0.6, 0.72, turMat);
    czu(tur, 0.012, 1.2, -1.6, 0.9, 1.2, darkMat, 6);
    var sm1 = bx(tur, 0.2, 0.55, 0.24, 0.35, 1.25, 0.5, gunMat); sm1.rotation.z = 0.55;
    var sm2 = bx(tur, 0.2, 0.55, 0.24, 0.35, -1.25, 0.5, gunMat); sm2.rotation.z = -0.55;
    bx(tur, 0.05, 2.4, 0.35, -2.15, 0, 0.45, gunMat);
    bx(tur, 0.5, 0.05, 0.35, -1.95, 1.2, 0.45, gunMat);
    bx(tur, 0.5, 0.05, 0.35, -1.95, -1.2, 0.45, gunMat);
    bx(tur, 0.45, 2.2, 0.04, -1.95, 0, 0.32, gunMat);
    bx(tur, 0.4, 0.8, 0.3, -1.95, -0.5, 0.5, darkMat);
    bx(tur, 0.35, 0.7, 0.26, -1.95, 0.45, 0.47, gunMat);
    bx(tur, 0.5, 0.7, 0.5, 1.55, 0, 0.35, turMat);
    cyx(tur, 0.085, 5.0, 4.25, 0, 0.38, gunMat);
    cyx(tur, 0.115, 0.55, 4.9, 0, 0.38, gunMat);
    cyx(tur, 0.1, 0.12, 2.4, 0, 0.38, darkMat);
    bx(tur, 0.16, 0.1, 0.1, 6.5, 0, 0.38, darkMat);
    bx(tur, 0.6, 0.04, 0.32, -1.0, 1.37, 0.38, teamMat);
    bx(tur, 0.6, 0.04, 0.32, -1.0, -1.37, 0.38, teamMat);
    return g;
  }
};
UNIT_MODELS["hvy_c"] = {
  len: 11.0,
  build: function (THREE, M, C) {
    var g = new THREE.Group();
    function tex(base, blotch) {
      var cv = document.createElement("canvas"); cv.width = 256; cv.height = 256;
      var p = cv.getContext("2d"); p.fillStyle = base; p.fillRect(0, 0, 256, 256);
      var i, x, y, gr;
      if (blotch) {
        for (i = 0; i < 6; i++) {
          x = Math.random() * 256; y = Math.random() * 256;
          p.fillStyle = "rgba(28,36,24,0.20)";
          p.beginPath(); p.ellipse(x, y, 30 + Math.random() * 40, 18 + Math.random() * 26, Math.random() * 3, 0, 6.29); p.fill();
        }
      }
      for (i = 0; i < 24; i++) {
        p.fillStyle = "rgba(" + (Math.random() < 0.5 ? "0,0,0" : "255,255,255") + "," + (0.03 + Math.random() * 0.05).toFixed(3) + ")";
        p.fillRect(Math.random() * 236, Math.random() * 236, 18 + Math.random() * 60, 12 + Math.random() * 46);
      }
      p.strokeStyle = "rgba(0,0,0,0.22)"; p.lineWidth = 1;
      for (i = 0; i < 5; i++) { y = 20 + Math.random() * 216; p.beginPath(); p.moveTo(0, y); p.lineTo(256, y); p.stroke(); }
      for (i = 0; i < 7; i++) { x = 16 + Math.random() * 224; p.beginPath(); p.moveTo(x, 0); p.lineTo(x, 256); p.stroke(); }
      for (i = 0; i < 8; i++) {
        x = Math.random() * 256; y = Math.random() * 256;
        gr = p.createRadialGradient(x, y, 2, x, y, 12 + Math.random() * 20);
        gr.addColorStop(0, "rgba(20,18,10,0.12)"); gr.addColorStop(1, "rgba(20,18,10,0)");
        p.fillStyle = gr; p.fillRect(x - 34, y - 34, 68, 68);
      }
      var t = new THREE.CanvasTexture(cv); t.wrapS = t.wrapT = THREE.RepeatWrapping; return t;
    }
    var hullMat = new THREE.MeshStandardMaterial({ map: tex("#4e5844", true), metalness: 0.25, roughness: 0.62 });
    hullMat.map.repeat.set(2, 1);
    var turMat = new THREE.MeshStandardMaterial({ map: tex("#4e5844", true), metalness: 0.25, roughness: 0.62 });
    var darkMat = new THREE.MeshStandardMaterial({ color: 0x24261e, metalness: 0.2, roughness: 0.9 });
    var gunMat = new THREE.MeshStandardMaterial({ color: 0x525c48, metalness: 0.35, roughness: 0.55 });
    var teamMat = new THREE.MeshStandardMaterial({ color: C.team, metalness: 0.2, roughness: 0.55 });
    function bx(parent, sx, sy, sz, px, py, pz, mat) {
      var m = new THREE.Mesh(new THREE.BoxGeometry(sx, sy, sz), mat || hullMat);
      m.position.set(px, py, pz); parent.add(m); return m;
    }
    function cyx(parent, r, len, px, py, pz, mat, seg) {
      var m = new THREE.Mesh(new THREE.CylinderGeometry(r, r, len, seg || 14), mat || gunMat);
      m.rotation.z = Math.PI / 2; m.position.set(px, py, pz); parent.add(m); return m;
    }
    function czu(parent, r, len, px, py, pz, mat, seg) {
      var m = new THREE.Mesh(new THREE.CylinderGeometry(r, r, len, seg || 14), mat || gunMat);
      m.rotation.x = Math.PI / 2; m.position.set(px, py, pz); parent.add(m); return m;
    }
    function gear(o) {
      var s, i, w, hb, sp, id;
      var wgeo = new THREE.CylinderGeometry(o.r, o.r, 0.5, 16);
      var hgeo = new THREE.CylinderGeometry(o.r * 0.4, o.r * 0.4, 0.54, 10);
      var sgeo = new THREE.CylinderGeometry(o.r * 0.85, o.r * 0.85, 0.46, 12);
      for (s = -1; s <= 1; s += 2) {
        bx(g, o.tl, o.tw, 0.75, o.tx, o.ty * s, 0.52, darkMat);
        for (i = 0; i < o.n; i++) {
          w = new THREE.Mesh(wgeo, darkMat); w.position.set(o.x0 + i * o.dx, o.ty * s, o.r + 0.1); g.add(w);
          hb = new THREE.Mesh(hgeo, gunMat); hb.position.set(o.x0 + i * o.dx, o.ty * s, o.r + 0.1); g.add(hb);
        }
        sp = new THREE.Mesh(sgeo, darkMat); sp.position.set(o.sx, o.ty * s, 0.46); g.add(sp);
        id = new THREE.Mesh(sgeo, darkMat); id.position.set(o.ix, o.ty * s, 0.46); g.add(id);
        /* Side skirts are not one continuous plate. Photographs of an Abrams
           or a T-90 show a row of separate armoured panels with clear vertical
           joins, each hanging to about half wheel height — one of the strongest
           cues that a tracked vehicle is armoured rather than a plain box. */
        var segN = o.segs || 6, segW = o.sl / segN;
        for (var sg = 0; sg < segN; sg++) {
          var sgx = o.scx - o.sl / 2 + segW * (sg + 0.5);
          bx(g, segW * 0.92, o.st, o.sh, sgx, o.sy * s, o.sz, hullMat);
          bx(g, segW * 0.92, o.st * 1.7, 0.07, sgx, o.sy * s, o.sz + o.sh / 2, darkMat);
        }
        bx(g, o.sl + 0.3, 0.55, 0.06, o.scx, (o.sy - 0.2) * s, o.sz + o.sh / 2 + 0.03, hullMat);
        for (i = 0; i < 5; i++) {
          bx(g, 1.0, 0.06, 0.45, o.scx - 2.6 + i * 1.3, (o.sy + 0.06) * s, o.sz - 0.02, darkMat);
        }
      }
    }
    g.add(new THREE.Mesh(M.loft(THREE, [
      { x: -3.8, w: 1.48, h: 0.4, zc: 0.95, sq: 0.85 },
      { x: -3.3, w: 1.56, h: 0.5, zc: 1.0, sq: 0.85 },
      { x: 2.0, w: 1.56, h: 0.5, zc: 1.0, sq: 0.85 },
      { x: 3.8, w: 1.3, h: 0.15, zc: 0.66, sq: 0.85 }
    ], 10), hullMat));
    gear({ n: 6, r: 0.37, x0: -2.2, dx: 0.88, tx: 0, tl: 6.9, tw: 0.64, ty: 1.52, sx: -3.15, ix: 3.05, sl: 6.5, scx: 0, sh: 0.7, sz: 0.9, st: 0.1, sy: 1.85 });
    var k, sgn, eb;
    for (k = 0; k < 2; k++) {
      for (sgn = -1; sgn <= 1; sgn += 2) {
        eb = bx(g, 0.6, 0.32, 0.1, 2.35 + k * 0.6, 0.45 * sgn, 1.42 - k * 0.23, hullMat);
        eb.rotation.y = 0.37; eb.rotation.z = 0.4 * sgn;
        eb = bx(g, 0.6, 0.32, 0.1, 2.35 + k * 0.6, 1.05 * sgn, 1.42 - k * 0.23, hullMat);
        eb.rotation.y = 0.37; eb.rotation.z = 0.4 * sgn;
      }
    }
    bx(g, 0.08, 2.2, 0.55, -3.81, 0, 1.05, darkMat);
    bx(g, 0.14, 0.2, 0.14, 3.55, 1.0, 1.12, gunMat); bx(g, 0.14, 0.2, 0.14, 3.55, -1.0, 1.12, gunMat);
    var dh = czu(g, 0.28, 0.05, 2.6, 0.45, 1.35, hullMat); dh.rotation.y = 0.37;
    var tur = new THREE.Group(); tur.name = "turret"; tur.position.set(0.3, 0, 1.48); g.add(tur);
    var sh = new THREE.Shape();
    var pts = [[1.3, 0.5], [0.2, 1.35], [-1.4, 1.35], [-1.75, 1.0], [-1.75, -1.0], [-1.4, -1.35], [0.2, -1.35], [1.3, -0.5]];
    sh.moveTo(pts[0][0], pts[0][1]);
    var i2;
    for (i2 = 1; i2 < pts.length; i2++) sh.lineTo(pts[i2][0], pts[i2][1]);
    tur.add(new THREE.Mesh(new THREE.ExtrudeGeometry(sh, { depth: 0.7, bevelEnabled: false }), turMat));
    var wg1 = bx(tur, 1.7, 0.12, 0.72, 1.2, 0.6, 0.35, turMat);
    wg1.rotation.order = "ZYX"; wg1.rotation.x = 0.35; wg1.rotation.z = -0.7;
    var wg2 = bx(tur, 1.7, 0.12, 0.72, 1.2, -0.6, 0.35, turMat);
    wg2.rotation.order = "ZYX"; wg2.rotation.x = -0.35; wg2.rotation.z = 0.7;
    var ap1 = bx(tur, 0.06, 0.34, 0.4, 0.85, 1.15, 0.85, darkMat); ap1.rotation.z = -0.55;
    var ap2 = bx(tur, 0.06, 0.34, 0.4, 0.85, -1.15, 0.85, darkMat); ap2.rotation.z = 0.55;
    var ap3 = bx(tur, 0.06, 0.34, 0.4, -1.65, 1.15, 0.85, darkMat); ap3.rotation.z = 2.6;
    var ap4 = bx(tur, 0.06, 0.34, 0.4, -1.65, -1.15, 0.85, darkMat); ap4.rotation.z = -2.6;
    bx(tur, 0.1, 0.1, 0.3, 0.85, 1.1, 0.7, gunMat);
    bx(tur, 0.1, 0.1, 0.3, 0.85, -1.1, 0.7, gunMat);
    bx(tur, 0.1, 0.1, 0.3, -1.6, 1.1, 0.7, gunMat);
    bx(tur, 0.1, 0.1, 0.3, -1.6, -1.1, 0.7, gunMat);
    bx(tur, 0.28, 0.26, 0.4, -0.5, 0.85, 0.9, darkMat);
    cyx(tur, 0.09, 0.12, -0.32, 0.85, 0.98, darkMat);
    czu(tur, 0.12, 0.4, -0.4, -0.35, 0.9, gunMat);
    bx(tur, 0.2, 0.2, 0.18, -0.4, -0.35, 1.18, darkMat);
    bx(tur, 0.4, 0.45, 0.22, 0.55, -0.55, 0.81, turMat);
    bx(tur, 0.04, 0.3, 0.14, 0.78, -0.55, 0.83, darkMat);
    czu(tur, 0.3, 0.05, -0.9, 0.5, 0.72, turMat);
    czu(tur, 0.28, 0.05, -0.3, -0.6, 0.72, turMat);
    czu(tur, 0.012, 1.2, -1.6, 0.9, 1.2, darkMat, 6);
    var sm1 = bx(tur, 0.2, 0.55, 0.24, 0.35, 1.25, 0.5, gunMat); sm1.rotation.z = 0.55;
    var sm2 = bx(tur, 0.2, 0.55, 0.24, 0.35, -1.25, 0.5, gunMat); sm2.rotation.z = -0.55;
    bx(tur, 0.05, 2.4, 0.35, -2.15, 0, 0.45, gunMat);
    bx(tur, 0.5, 0.05, 0.35, -1.95, 1.2, 0.45, gunMat);
    bx(tur, 0.5, 0.05, 0.35, -1.95, -1.2, 0.45, gunMat);
    bx(tur, 0.45, 2.2, 0.04, -1.95, 0, 0.32, gunMat);
    bx(tur, 0.4, 0.8, 0.3, -1.95, -0.5, 0.5, darkMat);
    bx(tur, 0.35, 0.7, 0.26, -1.95, 0.45, 0.47, gunMat);
    var duf = new THREE.Mesh(new THREE.SphereGeometry(0.24, 8, 6), darkMat);
    duf.scale.set(1.4, 1, 0.8); duf.position.set(-1.9, 0, 0.52); tur.add(duf);
    bx(tur, 0.5, 0.7, 0.5, 1.55, 0, 0.35, turMat);
    cyx(tur, 0.085, 5.0, 4.25, 0, 0.38, gunMat);
    cyx(tur, 0.115, 0.55, 4.9, 0, 0.38, gunMat);
    cyx(tur, 0.1, 0.12, 2.4, 0, 0.38, darkMat);
    bx(tur, 0.16, 0.1, 0.1, 6.5, 0, 0.38, darkMat);
    bx(tur, 0.6, 0.04, 0.32, -1.0, 1.37, 0.38, teamMat);
    bx(tur, 0.6, 0.04, 0.32, -1.0, -1.37, 0.38, teamMat);
    return g;
  }
};
